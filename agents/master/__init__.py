"""Master bootstrap agent package.

Agent: master
Role: fleet lifecycle manager — activates agents, manages the graph-backed registry.
External I/O: Azure Key Vault (secrets), GraphStore backend (PostgreSQL by default).
"""
