# Project State

**Last updated:** 2026-09-23 15:37 AEST · **Version:** **0.108.00 deployed** (`s225`) / **0.109.05 on `main`** — the gap is gate tooling, docs and the dashboard; no runtime code changed · **🎯 The dashboard and its operator chat now agree with themselves: an unfilled run reads "awaiting fills", not "passed", and "open incidents" lists what health counts (0), not 6,439 historical faults. **1 open (75, queued) + 1 parked (71).****

**How to read.** *Now* = active · *Next* = queued · *Recent* = last few shipped (older detail lives in
each `docs/sprints/sprint-NN-*.md` + [`state-archive/`](state-archive/INDEX.md) `STATE-01…08.md` + git). **LAW-02:** an item is "shipped" only when
its success factors are *proven* (tests, `make ci`, the named live check) — never restate intent as outcome.
**Size rules**, after this file reached 574 lines with a **112 KB header line**: keep it **under 200
lines**, and keep the header above to **three clauses — stamp, version, one headline**. Replace that
headline each session; never append a fourth. Oldest *Recent* entries split to the archive.

---

## Current focus

**The bar, set by the operator 2026-08-20.** The etalon is done when **the pack trades unattended
for a sustained stretch and the evidence discipline catches its own defects without the operator in
the loop.** Not "trading is finished" — but far above where it is. 🚨 **Consequence for ranking:**
whatever currently prevents unattended operation *is* etalon work, not a detour from it. **Until
2026-08-22 that was the 73 % veto rate** ([DL-119](design-log.md)). It is now one level up: the
referee cannot run at all until **2026-08-30**, so the pack trades unvetoed behind a gate that is red
nightly for a non-defect ([DL-125](design-log.md)). 🚨 **A discipline that cries wolf for six
straight nights fails the second half of the bar as surely as not trading fails the first** — which
is why work-queue **item 6** (a declared advisory posture) is now ranked first. 🪤 **Generality is a separate
unproven axis:** ADR-0012's platform/pack wall stays *de jure* until a second pack exists, and
clearing this bar does not close that.

Since P14 the project runs as **etalon-first continuous improvement** (DL-19).
**The platform is self-driving in paper mode**: the DEPLOYED, STANDING fleet (16 Container Apps +
`dispatcher-cron` job, KEDA scale-to-zero windows, idle ≈ $0) places a calendar-gated `RunRequest`
at 22:30 UTC daily, runs graph-pull + served-over-Service-Bus on the Neon Postgres spine (ADR-0014),
reconciles holdings against the broker (DL-44), debates vetoes under compiled prompts (DL-42), and
proves `ACCEPTANCE PASS`. Pausing = disable the job + zero the scale windows (`docs/deployment.md`).
Completed arcs live in their sprint docs + archives: fleet (DL-35), credentials (DL-36), Postgres
migration (DL-43), deliberation quality (DL-41/42). Layer-3 acceptance 🟩 at the full S&P-500;
Layer-2 choreography 🟩 on a distributed run (S102).

## Now

🟩 **MERGED — [DL-207](design-log.md) + [DL-208](design-log.md): the dashboard stops contradicting itself. `0.109.05`, fast-forwarded to `c5ece8c4`, tagged `v0.109.05`, 15:37 AEST 2026-09-23.** `GATE PROVEN` for `c5ece8c4` — CI, CodeQL and Security Findings, all success on attempt 1. `make ci` exit 0 by file redirection: **3,102 passed, 6 skipped, 100.00 %**. **Operator asked "can you fix the dashboard", then "Wrong query??".** (1) A headless render showed the verdict card saying *none filled yet* while the header said **✓ RUN PASSED**, the result box *Run result: passed* and the nav *run passed* — `app.js` keyed all three on `passed`, which UNPROVEN keeps true by design (DL-59). Now amber **AWAITING FILLS** / *Run result: not proven yet*. (2) The *Resume from …* buttons rendered as white browser defaults — only visible since the chat binds again (the local `anthropic` repair); now styled like *Acknowledge*. (3) The chat's **Open incidents** returned **6,439** lines — every Fault since 2026-07-23 — while *Status* said health is green and the kernel counted **0**: `open_faults` never adopted `live_fault_incidents`. Now one definition, test-pinned to `system_health`. **Proven live after the merge:** header *AWAITING FILLS*, styled buttons, *Open incidents* → **"No open incidents."** (223 bytes vs 414,863). 🧹 **One operator action taken on "go ahead":** a *Resume from …* click at 15:07 AEST had left a pending broker-consequence confirmation flag (`b0fc97911b038786`); it was **dismissed, not confirmed**, via the audited CLI `approve` (resolves the flag only) — RunRequests stayed **73**, ResumePlacements **0**, dashboard back to 0 pending flags. No deploy needed.

🟩 **MERGED — [DL-206](design-log.md): the dashboard's schedule is read from its sources, not remembered. `0.109.04`, fast-forwarded to `01dc96b6`, tagged `v0.109.04`, 14:51 AEST 2026-09-23.** `GATE PROVEN` for `01dc96b6` — CI, CodeQL and Security Findings, all success on attempt 1. `make ci` exit 0 by file redirection: **3,099 passed, 6 skipped, 100.00 %**. **Found by hunting DL-205's defect class** (a dashboard copy of a fact that lives elsewhere): `aea47810` moved the master to **20:25 UTC** and made the dispatcher a ten-minute ticker on 2026-09-20, and the dashboard noticed neither. Its run windows opened at 22:25, after the master's startup and the 20:25/21:25 fleet preflights (**381** master log rows on `sched-2026-09-22`); from 06:25 to 08:25 Melbourne the "latest window" was yesterday's; *next fire* promised a Saturday run every Friday night (the old test pinned it); and the cron row read **23:50** for a run placed at **22:30:14**. **Now:** the four schedule settings are test-pinned to `deploy-agents.ps1` and the dispatcher's `_ACTION_START`; next fire walks the dispatcher's own session calendar; the cron row shows the placing tick. **Proven live, after the merge:** master window **20:25–00:30 UTC**, cron row **22:30 Succeeded**, next fire today 22:30 UTC (and **Mon 28 Sep** after Friday's run, not Saturday), verdict GREEN with one warning; deploy currency still **current** against this merge's own image build — a second fresh pair for DL-205. No deploy needed.

🟩 **MERGED — [DL-205](design-log.md): a deploy warning names code the fleet runs, and the stage count reads the pack. `0.109.03`, fast-forwarded to `bdb66d79`, tagged `v0.109.03`, 13:40 AEST 2026-09-23.** `GATE PROVEN` for `bdb66d79` — CI, CodeQL and Security Findings, all success on attempt 1 — and the fast-forward makes the merged SHA the proven one. `make ci` exit 0 by file redirection: **3,092 passed, 6 skipped, 100.00 %**. **Why:** on the clean `sched-2026-09-22` the dashboard showed two warnings and neither was true — *"Fleet deploy currency is behind"* (nine `laws.md` edits and a version bump since `s225`, no runtime change) and *"9/7 stages reached — needs attention"* (a hard-coded 7; the pack has 9; all **41** scheduled runs since 2026-07-27 read it, **8** of them no-trade days). **What changed:** a newer main build reads *behind* only when a file the fleet runs differs — two GitHub tree reads over the build workflow's own trigger paths, minus markdown, `tests/` and a version-line-only lock bump; a failed read is *unverified*; three tests pin the rule to the workflow, every Dockerfile `COPY` source, and "no image code names a markdown file". The stage total is the run's own stage list. **Proven live, after the merge:** `s225` → `bdb66d79` (this merge's own image build) reads **current, 0 runtime changes**; `7039bfb` → `s225` reads the **7** execution files S225 really changed, so the tripwire still fires; verdict **GREEN** with one warning (orders queued for the open), pipeline **9/9 good**. **No deploy needed** — the dashboard runs locally and nothing in the images changed. 🪤 **Side find:** the main checkout's `anthropic` package had lost its top-level files (last written 2026-09-19 11:23) and crashed the dashboard at start; reinstalled at the locked `0.120.2` — the local environment only, never the fleet.

🟩 **MERGED — [item 22 Part B](work-queue.md), `0.109.02`, fast-forwarded to `889ea9fe`, tagged `v0.109.02`, 09:05 AEST 2026-09-23 — the item closes.** `GATE PROVEN` for `889ea9fe` — CI, CodeQL and Security Findings, all success on attempt 1 — and the fast-forward makes the merged SHA the proven one. `make ci` exit 0 by file redirection: **3,060 passed, 6 skipped, 100.00 %**; `gate-selftest` **31/31**. 🚨 **The migration as specified would have written false answers:** of the 190 docs S224 classified, **90** declared a state git contradicts (69 `SPEC`, all 21 `BUILT` — every one merged), and the README was wrong the other way (S205/S207/S223 read `SPEC`). Each was settled against `origin/main`; **124** status lines now cite the merge, tag or hand-read commit beside the original text. `check_sprint_status.py` reports **230 docs: 220 `MERGED`, 10 `SPEC`, 0 refusals**, so the synonyms, the 20 + 20 baseline and `status-unmapped.md` are deleted, and the gate fails on any non-canonical token, missing line, missing README row or README disagreement ([DL-204](design-log.md)). **No deploy:** `scripts/` and `docs/` ship in no image.

🟩 **MERGED — [item 33](work-queue.md), `0.109.01`, fast-forwarded to `9d0420b9`, tagged `v0.109.01`, 05:42 AEST 2026-09-23.** `GATE PROVEN` for `9d0420b9` — CI, CodeQL and Security Findings, all success on attempt 1 — and the fast-forward makes the merged SHA the gated SHA. S187 had baselined **57** PARAM/settings divergences as warnings; re-measured first, still exactly 57. **54** settings fields gained rows copied from their own declarations (the seven provider credentials as `NO (secret)`, by name only — the execution law's existing precedent); **2** execution rows were fixtures retired in 0.73.01 and were deleted; `issuer_map` left the PM table as pack data. `LEGACY_BASELINE` is **deleted, not emptied**: a divergence now fails `make ci`, and the check prints only S223's two envelope warnings (it printed 59). `make ci` exit 0 on the committed tree (**3,061 passed, 6 skipped, 100.00 %**), gate self-test **31/31**. Nine laws took `PARAM`-only amendments; no clause count moved. `param_law_sync.py` fell 212 → 197, so the size ratchet lists **15** files, not 16. [DL-203](design-log.md). **No deploy needed** — no runtime code changed; the merge started the usual image build. 🪤 **One slip, corrected within minutes:** a chained command ran past a failed step and pushed `v0.109.01` onto the pre-merge `561e42ca`. The tag was deleted and re-cut on `9d0420b9`; tag pushes trigger no workflow, so nothing was built from it.

🟩 **MERGED — [item 78](work-queue.md), `0.109.00`, merged `1ee5a7d2`, tagged `v0.109.00`, 00:41 AEST 2026-09-23.** `GATE PROVEN` for `3d088bee` — CI, CodeQL and Security Findings, all success on attempt 1. `check_module_size.py` ran on `$(PKGS) tests`, so `scripts/` was never checked although `check_module_header.py` *was* covering it. 📏 **Re-measured and the row had drifted in two days:** **16** files at or over the block (17 counting one exactly on 200, which fails at `>=`), largest **486** not 411, **23** in the warn band not 6, folder grown 83 → 93 files. 🪤 **I had demonstrated it myself hours earlier** — `replay_dataset.py` shipped at **205** lines in the item-64 chore and nothing objected. 🎯 **Built the reverse of the row's plan:** it said split 16 files *then* widen the gate, which leaves the hole open for the length of a large refactor while the list grows — exactly what happened between the row being written and read. The scope widened **first**, with `scripts/module_size_baseline.py` freezing each offender at its measured count: a listed file may shrink, **never grow**, must be **deleted** once under 200, and must still exist. 🎯 **Every line in that baseline can fail the gate** — that is the difference from item 33, whose 57 rows warn and pass, which is why nobody reads them ([DL-202](design-log.md)). 🟩 **Two files split rather than baselined:** the new self-test case pushed `gate_selftest_cases.py` to **503** and the ratchet **refused it**, so it split into cases **346** + invariants **146** + types **37** (the DL-198 shape), and `replay_dataset.py` went **205 → 142** and left the baseline. Proven by `module-size-reads-scripts` + two wiring invariants (**31/31**) and 8 unit tests; `make ci` exit 0, **3060 passed**, **100.00 %**. 🟠 **Nothing to deploy** — gate tooling ships in no image, so the fleet stays on `s225`.

🧪 **MEASURED — [EXP-013](research/experiments/EXP-013-regime-scaled-sizing-replay.md), 2026-09-22, $0 — work-queue item 64 closed without building anything.** 🐛 **Its remaining task named a quantity that no longer exists.** ADR-0031 (2026-09-20) gave the regime the **risk budget** and said it attaches *“when that lands”*; [ADR-0025's Correction](decisions/0025-concentration-measures-the-book-position-risk-measures-the-capital-base.md) had withdrawn risk-based sizing **two days earlier** on [EXP-012](research/experiments/EXP-012-volatility-sizing-ten-year-replay.md) (−$11,619, 100 % of resamples negative) — and **ADR-0031 cites EXP-012 zero times** ([DL-201](design-log.md), ADR-0031 Correction). 🧪 **So EXP-013 measured the only live question** — should the regime scale the *notional* cap, the sizing knob that exists — over **47,533** decisions, 2017-2026, on the rebuilt EXP-011 dataset. 🟩 **The label is not stuck** (409 changes in 2,475 sessions, all five labels used), 🪤 **but its median run is 2 sessions against a 10-session horizon.** 🎯 **Forward return rises with stress** — `neutral` **0.287 %** → `extreme_volatility` **1.880 %** — **and so does return per unit of dispersion** (0.057 → 0.217), with `neutral` the worst bucket on both. Every less-risk-when-stressed arm loses: mild **−1,818**, strong **−3,157** pct-points, bootstrap **95 % CI [−3,699, −2,574], 100 % negative**. It is the same return gradient that killed iso-risk, one level up. 🟠 **The regime label stays as evidence, not as a multiplier**; nothing shipped, and confirming that as policy is capital-risk and remains the operator's. 🪤 **Biggest caveat, and it cuts against the finding:** survivorship — today's 98 names back-projected through every panic they survived. 🧹 **Fixed while here:** EXP-011 and EXP-012 both left their loader in a markdown appendix and their data in a scratch dir, so ~263k bars had to be re-pulled to reproduce a published number. `scripts/replay_dataset.py` now builds and loads the cache, which lives in **OneDrive** — 🚨 never the repo: it is **public** and Alpaca SIP bars are licensed.

🟩 **MERGED + DEPLOYED — [S225](sprints/sprint-225-a-stop-says-which-lineage-protected-it.md), `0.108.00`, merged `fe26574e`, tagged `v0.108.00`, fleet `s225` at 23:44 AEST 2026-09-22 — work-queue item 27 closed.** `GATE PROVEN` for `96ac04ef` on the branch and again for `fe26574e` on `main` — CI, CodeQL, Security Findings and the image build, all success on attempt 1. 🎯 **The item was not blocked on a rare event; it was blocked on a field that could not answer it.** Item 27 owed a live proof that S182's pending-`Fill` stop path had ever fired, and 2026-08-21 was read as *“the fallback never fired”* because the stops carried `stop_pct_source=position`. That field names where the stop **percent** came from; `filled_entry_stops` reports `position` too whenever the `OrderIntent` carries a `stop_pct`. 🐛 **Nothing else discriminated, by design** — the Fill path deliberately computes the *same* `position_ref` the monitor will later write, so a later `Position` cannot double-place ([DL-118](design-log.md)). 📏 **Measured by recovering the `position_ref` preimage** (it is `sha256(sorted Position keys)[:16]`): **55 of 55** stops ever placed hash from **broker-adopted** `Position` keys, **0** from the keys a Fill-path stop would produce, **23** of them placed after S182 deployed. 🚨 **So that path has never fired in production, and the margin is measured, not assumed:** on `sched-2026-08-21` the `broker:*` Positions for INTC/NEE/XOM were adopted at **22:31:52** and their stops placed at **22:40:02–03** — same run, ~8 minutes apart. Adoption only needs the run-start snapshot; execution's stop settling waits behind PM sizing and the deliberation grace window. 🪤 **It is a race, not an enforced ordering** — a degraded snapshot, a slower monitor, a shorter grace window or a fill landing after adoption would each flip it, which is why the S182 path stays rather than being retired as dead code. 🟩 **Fixed by recording:** every placed stop writes `derived_from` (`active_position` \| `pending_fill`) on both the `BrokerStopOrder` fact and the stop `Fill` ([DL-200](design-log.md)); [DRIFT-072](laws/drift-register.md) records that `EXEC-OBS-03`'s *“fully reconstructable”* read 🟩 on nine tests while this limb had none. Four new tests cite the clause; both guards were planted, watched to fail, restored. 🟩 **Deploy verified, not asserted:** full `up` because the vocabulary pack moved `579a40a3` → `63d3a5eb` while the other two injected packs were byte-identical; 16/16 apps on `s225`, 16 `Succeeded`, KEDA rules and the `*/10 22-23 * * 1-5` dispatcher cron intact, and `GRAPH_VOCABULARY_B64` decoded on execution/monitor/master to the repo's `63d3a5eb` — the S202 currency check, passed. 🟠 **Owed:** no production run has written `derived_from` yet; the first is tonight's `sched-2026-09-22` (22:30 UTC = Tue 08:30 Melbourne), and that is the functionality-check row.

🟩 **MERGED — [chore-an-accepted-advisory-re-checks-itself](sprints/chore-an-accepted-advisory-re-checks-itself.md), `0.107.00`, merged `750ac98`, tagged `v0.107.00`, 2026-09-22 — work-queue item 74 closed.** `GATE PROVEN` for `cc314df0a2dd6f9f0af072131e0a696d3aa963ef` on CI, CodeQL and Security Findings, all success on attempt 1, read from the worktree whose `HEAD` is that commit. 🎯 **The item asked for a re-check; the measurement underneath it was the finding.** Item 74 was *“nothing re-checks whether a fix has shipped”*. Measured in a clean worktree: `uv sync --frozen` installs no optional extra, so `diskcache` is not in CI's environment and a bare `pip-audit` there reads `No known vulnerabilities found`, exit 0 — **with or without the ignore**. 🐛 **Two commands kept textually identical were auditing different package sets**, and the one that mattered was auditing less: the extras the fleet images install had never been audited in CI. 🟩 **Fixed by scope first, measured before widening:** the gate audits `uv export --frozen --all-extras --all-groups` — **2,333 pins, exactly one vulnerable package**, the one already accepted — so local and CI now answer the same question and the audited set is a superset of any container's. 🟩 **Then the re-check:** acceptances live in `scripts/dependency_audit_baseline.py` naming package, aliases, deciding record, reason, retire trigger and the sole extra that pulls it in; every premise is re-measured each run and the gate fails when one stops holding — a fix release appears, the advisory stops being reported, it lands on another package, or a Dockerfile installs that extra. The acceptance is **printed on every green run**. 🚨 **No calendar expiry, on purpose** — a date fires when nothing has changed, which is [DL-184](design-log.md)'s rejected always-red gate and item 33's unread-warning shape ([DL-199](design-log.md)). 🟩 **Proven, not asserted:** gate self-test case `accepted-advisory-re-check` plants a report where the advisory carries fix `5.6.4` and requires rejection — **28/28**; ten unit tests cover each premise, alias matching and pip-audit's duplicate records; redirected `make ci` exit **0**, **3048 passed, 6 skipped**, **100.00 %**. 🪰 **Caught locally, worth keeping:** the first gate run on the chore doc itself failed — `detect-secrets` reads the phrase *“detect-secrets: Passed”* as a keyword assignment. The untracked-file sweep caught it before the push this time. 🧹 **Doc drift fixed in passing:** `CLAUDE.md` claimed **14** gate steps and listed 14 while the `ci:` target already ran **15** — it had missed S224's sprint-status step; the sprint template's *“all 12 steps”* is now *“every step of the `ci:` target”* so the number cannot rot again; and S224's missing `README.md` row was added. 🟠 **Nothing deployed and nothing owed** — gate tooling ships in no image, so the fleet stays on `b18014e`.

🟩 **LIVE — `sched-2026-09-21`, fired 22:30 UTC Mon = 08:30 Melbourne Tue, the first scheduled run on the 2026-09-21 full `up`. `trace_run.py` **8/8**, exit 0; **0** `Fault` rows in the window; `accept.py` reads `UNPROVEN — orders submitted but none filled yet (queued for the open)`, which is the expected post-close state, not a miss.** Provider 99 tickers / 19,894 bars / 1,893 headlines, `quality ok returned=98/99`, **no `*_degraded` notes**; scanner 99→17; analyst scored 26, rejected 0; PM approved **2** (META 1 @ $741.29, DOW 36 @ $28.26) and rejected 24 (23 `hold_recommendation`, 1 `sizing`); deliberation `reviewed=2 revised=2 vetoed=0 status=applied`; execution `submitted=2 rejected=0`; monitor checked 23, held 23, closed 0. 🎯 **Three owed proofs discharged in one run.** **(a) Item 68** — both approvals' gate detail carries `correlation_ramp=0.5000..0.9000`, and it is load-bearing: **DOW** reads `correlated_issuers=XOM:0.6235:w0.3087`, an issuer the 0.70 cliff counted as **zero**, now contributing `cluster_weight_total=0.3087` of `cluster_value_usd=1307.12` against `deployed_portfolio_usd=21919.46`; META reads `correlated_issuers=none` with `below_threshold_top=alphabet:0.4208,AMZN:0.3744,KHC:0.1836` — the ADR-0030 floor as designed. **(b) Item 70** — `regime-context:sched-2026-09-21` reads `vix=14.8`, `vix_status=measured`, `vix_as_of=2026-09-21`, `label=risk_on`: the first scheduled `RegimeContext` in production with a VIX at all, and the first not `neutral`. **(c) The S217/S218/S219 notice path fired for the first time and the answer was silence** — `FleetPreflight` at 20:25:36, 21:25:51, **22:26:08** and 23:26:21 UTC, all `passed=True`; **`RunHold` 0**, **`Escalation` 0**; the dispatcher placed the 22:30 run behind a fresh passing check. 🟩 **S214's test 4 — the bar itself — is now proven:** `accept.py --run-id sched-2026-09-18` flipped `UNPROVEN` → **`ACCEPTANCE PASS`**. The four revised-but-not-vetoed buys filled at Monday's open and were adopted at run start (`broker:AMZN:4`, `broker:BAC:17`, `broker:MDLZ:16`, `broker:USB:16`, all stamped `2026-09-21T22:30:38.669226`); `audit_broker_graph.py` A1 shows broker qty = graph qty on every holding and A2 a matching stop on each. **Tripwire, session 2 of 10:** 2 reviewed, 2 `revise`, **0 `overturn`** — test 2 still matches only vacuously. 🟠 **One thing to watch, not a failure:** 5 new divergence flags at run start — four `missing_graph_position` (the fills) and one `extra_graph_position:DOW` at `graph_qty=34` against a broker holding none. Read through the code-owned predicate `is_active_position_node` there are exactly **23 active positions matching the broker one-for-one, no DOW**, so the reconciler recorded what it then adopted (DL-44). But every flag lands `status=pending` and nothing acknowledges them: the backlog is **93** (`audit_broker_graph.py` A4). `scripts/sweep_divergence_flags.py` exists for exactly this and has never been run against it. 🟠 **Still owed:** tonight's open resolves `sched-2026-09-21`'s two orders; `functionality-checks.md` rows appended for S217/S218/S219, S220 and S213.

🟩 **MERGED — [S207](sprints/sprint-207-a-bound-cites-the-evidence-that-set-it.md), `0.104.00`, merged `d0b4cee`, tagged `v0.104.00`, 2026-09-21 — work-queue item 63 closed.** `GATE PROVEN` for `c484aea` on CI, CodeQL and Security Findings, all success on attempt 1, read from the worktree whose HEAD is that commit. `tunable()` carries an optional evidence envelope with a **mandatory** source — declaring a band without provenance raises at declaration — and the PARAM/settings sync step warns, without vetoing, when a value sits outside it. Thirteen risk settings carry their research wording inline; no bound, default or `contracts/` file moved. 🟩 **Observed rather than inferred:** the step exits **0** with **60** warnings — **57** legacy baseline (unchanged) plus **3** envelope breaches. 🩹 **Review repair:** the Closeout claimed `kernel/config.py` = **122** lines; both `wc -l` and the gate's own `splitlines()` read **147**. The conclusion holds — it is under the 150 warning threshold — but the margin is **3 lines, not 28**, so the next edit to that file trips the warning. Corrected in place with the original kept. 🟠 **Three findings recorded as [DL-195](design-log.md) and ranked as work-queue item 79, none of them a code defect:** the check reads the declared **default**, not the live value, so two of the three breaches are about values the fleet overrides (`max_position_pct` 0.01, `MAX_POSITIONS` 60) while the two it trades on are checked by nothing; `correlation_threshold=0.50` is a **live** breach against a band whose evidence predates [ADR-0030](decisions/0030-the-correlation-gate-is-a-ramp-not-a-cliff.md), which redefined that parameter from a cutoff to a ramp floor four days ago; and two envelopes name values their own `ge`/`le` rails reject. 🟠 **Not done:** no deploy — `kernel/` compiles into every image, so reaching the fleet needs a **rebuild + retag**, not a retag, and nothing here changes what the fleet trades.

🟩 **MERGED — [S222](sprints/sprint-222-one-llm-adapter-lives-in-the-kernel.md), `0.103.02`, merged `7d5528e`, tagged `v0.103.02`, 2026-09-21 — work-queue item 7 closed.** `GATE PROVEN` for `7cea9de` on CI, CodeQL and Security Findings, all success on attempt 1, read from the worktree at that SHA. The vendor adapters and provider factory now live in `kernel/`; the four agent-side modules are deleted and every consumer is repointed. The deliberate response-shape split remains: free-text deliberation and operator tool use are thin public clients over one private Anthropic transport. 🎯 The newly proven gray clauses are `DLIB-SEC-02` and `OPR-SEC-01` (sentinel key containment) plus `OPR-DEP-01` (Anthropic-only dynamic import); derived rollups are deliberator **23 / 56** and operator **18 / 50**. 🩹 **Review repair:** A5 now captures DEBUG, not pytest's default WARNING threshold; a temporary DEBUG sentinel leak in the Anthropic request path failed red before restoration passed green. A1 also rejects any agent-side `llm_*.py`. 🟩 **Post-review local proof:** redirected `make ci` exit 0, **2985 passed, 4 skipped, 100.00 %** coverage, pip-audit clean with two documented exclusions, and both secret scans clean. 🔴 **Planner correction — the handback's environment claim was false.** It stated no `.env` was present; `.env` **is** present, because the build ran in the **main checkout**, not a worktree. So `tests/test_bus_azure_config.py` and `tests/test_deliberator_servicebus_peer.py` **ran instead of skipping** — measured **`14 passed`, 0 skipped** — which is precisely why the suite reads **4 skipped** where S220 and S221 read **6**. 🎯 **The skip count is an environment fingerprint** ([DL-193](design-log.md)): the run's own numbers contradicted its prose, one sprint after [DL-191](design-log.md) named that exact failure. No live vendor call ran — the Anthropic SDK is faked in both new tests and no vendor key was read. 🟩 **Done by the planner:** commit `7cea9de`, push, `make gate-ran`, merge `7d5528e`, tag `v0.103.02`. 🎯 **The red-first proof was re-run independently, not taken on trust:** a `DEBUG` config echo of the key planted in the transport constructor failed the test with the sentinel visible in the captured log, and restoring it passed `5 passed`. 🟩 **Remote CI read `2983 passed, 6 skipped`** against the local `2985 / 4` — a difference of exactly the two `.env`-gated tests, which is [DL-193](design-log.md) confirmed. 🟠 **Not done:** image-only retag and the first scheduled-run ledger check. Operator provider selection remains out of scope: it would require a new `OPR-DEP-01` dependency, tunable, PARAM row, and law cycle.

🟩 **MERGED — [S221](sprints/sprint-221-a-rule-the-repo-states-is-a-rule-the-gate-enforces.md), `0.103.01`, merged `863574e`, tagged `v0.103.01`, 2026-09-21 — work-queue items **76** and **77** closed.** `GATE PROVEN` for `39c28c7` on CI, CodeQL and Security Findings, from the worktree at that SHA; gate self-test **23/23**, each new guard rejecting its planted violation. 🔴 **One defect the handback did not catch, found by probing the checker with fixtures rather than reading it:** `_slug()` dropped underscores, so `## reward_risk gate` produced `rewardrisk-gate` where GitHub produces `reward_risk-gate`. 🎯 **A checker that reports a *valid* link as broken is the failure mode the spec called disqualifying** — it passed every test and the whole corpus only because nothing links to an underscored anchor *yet*, and this repo's headings are full of `reward_risk`, `max_position_pct`, `vix_status`. Fixed with a regression test, planted and watched red then green. 🪰 **Two smaller findings:** `docs/STATE.md` lost the blank line after `## Now` (`make ci` was exit 0 — markdownlint is not one of its steps), and the handback left the header `**Status:**` at `SPEC` while updating only the closeout — which is work-queue item **22**'s complaint, inside the sprint that closed the other two unenforced rules. **Filed:** item **78** — `check_module_size.py` never looks at `scripts/`, where **15** files already exceed 200 lines (largest **411**). *Original handback note follows.* New offline checks make the CI gate reject missing relative Markdown targets, dead Markdown fragments, misleading filename-like labels, and non-`MAJOR.MMM.PP` project versions. The exact tracked Markdown corpus from `main` scanned with **0 findings**; focused checks **25 passed**; the self-test observed both new guards reject planted violations; redirected `make ci` exited **0** with **2975 passed, 6 skipped, 100.00 %** coverage, pip-audit clean, and both secret scans passed. No law cycle, deploy, or `.env` access. 🟠 **Owed:** commit, push, and `make gate-ran` for the full branch SHA; no deploy follows.

🟩 **DEPLOYED — `7039bfb` (`v0.106.01`), 2026-09-22 21:55 AEST — four merges reach the fleet at once.** It had been on `b18014e`/`0.104.01`: this carries item **80** (the PM trace stops echoing a gate as its own failure), item **55** (the silent-LLM-night detector), **S224** (the sprint-status checker) and the **import-cycle repair**. 🎯 **Deployed `7039bfb`, not `main`'s tip** — `9f01385` is the DL-198 docs commit and the build workflow's `paths` filter correctly produced no image for it, so the newest *gated* commit carrying runtime code is the target. **Image-only retag, path proven:** all three injected packs byte-identical to the deployed commit (vocabulary `579a40a3…`, credential-tests `97f083bf…`, issuer-map `2ed1f41c…`), so no full `up` was owed; images **15/15**. 🟩 **Verified, not inferred:** 16/16 apps on the new tag, 16/16 `Succeeded`, **all 16** KEDA rules intact (`min=0/max=1`, windows preserved including master's earlier 20:25 start), job on the new tag with cron `*/10 22-23 * * 1-5` unchanged, `DeployRecord` written against re-read GitHub build evidence. 🟩 **Container startup proven on the image:** one `operator` replica forced up came back `Healthy`, `RunningAtMaxScale`, `ready=true`, **`restartCount=0`**, then `minReplicas` restored to 0 with the scale block re-read from `show` and confirmed identical to baseline. 🪰 **The retag's own `EXIT=0` proved nothing** — it was a redirect's exit code, not the deploy's, which is the DL-88 shape; the per-app lines and the independent `show` queries are the evidence. 🟠 **No trading behaviour changed** — item 80 is display-only, item 55 adds a read-only detector, S224 is repo tooling; what changes is what the trace *says* and what can now be detected. First real exercise is the **08:00 AEST** scheduled run. Rollback: same retag command with `b18014e73be3e4ae43e9e6f2961d506face30c8c`.

🟩 **DEPLOYED — `b18014e` (`v0.104.01`), 2026-09-22 18:52 AEST — S207 and S223 reach the fleet together.** The fleet had been two sprints behind (`7d5528e`/`0.103.02`); it is now level with `main`'s runtime code. **Image-only retag, and the path was proven rather than assumed:** all **three** injected packs are byte-identical between `7d5528e` and `b18014e` — vocabulary `579a40a31fc2c004`, credential-tests `97f083bf782348f8`, issuer-map `2ed1f41c9a396618` — so no full `up` was owed. 🪤 **Checking only the vocabulary pack is what nearly shipped S202 inert**, which is why all three were diffed. 🟩 **Verified, not inferred:** 16/16 apps on the new tag and 16/16 `Succeeded`; **all 16** KEDA rules intact at `min=0` with one rule each (checked across every app, not sampled — DL-88 is the case where 15 targets reported success and had all failed); the `dispatcher-cron` job on the new tag with its cron `*/10 22-23 * * 1-5` unchanged; `DeployRecord` written by `record_deploy.py`, which re-read the GitHub build evidence and would have refused a SHA that no successful build produced. Images came from the merge auto-build, **15/15 jobs success**. Deployed `b18014e`, not `main`'s tip — the commits above it are docs, which the build workflow's `paths` filter correctly excludes. 🟩 **Container startup PROVEN on the deployed image, 2026-09-22 19:06 AEST — the owed live proof, discharged without waiting for the scheduled run.** S223 makes `kernel.tunable()` raise **at declaration**, and every agent imports its settings at import time, so a bad band fails *container startup* rather than a test — a failure mode CI can only argue about. Forced one replica of **`portfolio-manager`**, chosen because it owns **both** corrected envelopes and is therefore the only app that could have failed: revision `portfolio-manager--0000149` on `portfolio_manager:b18014e…` came up `healthState=Healthy`, `runningState=RunningAtMaxScale`, container `ready=true` and 🎯 **`restartCount=0`**. Zero restarts is the measurement that matters: an import-time raise cannot produce it. Cost was one container for a few minutes — no pipeline run, no LLM spend. 🟩 **The test left no trace, verified rather than assumed:** KEDA retired the replica at **09:13:56 UTC** (19:13 AEST) once `minReplicas` returned to 0, and the scale block now reads byte-identical to the untouched `analyst` control — `min 0 / max 1 / cooldown 300 / poll 30`, cron `start 30 22 * * *`, `end 30 00 * * *`, `desiredReplicas 1`, `timezone UTC`. 🪰 **A false alarm was raised and retracted in the same session:** `az containerapp update` echoed the cron rule's metadata as four empty strings, which was read as config damage; `show` proved every value intact. The update response does not populate that block. The trap is now written where it is read, beside `Get-AppState` in `infra/deploy-agents.ps1`. Rollback, if ever needed: same retag command with `7d5528ef197aef0b74bd4b31971f9d1dc210a9ca`.

🟩 **MERGED — [S223](sprints/sprint-223-an-envelope-says-what-it-checked.md), `0.104.01`, merged `b18014e`, tagged `v0.104.01`, 2026-09-22 — work-queue item 79 closed.** `GATE PROVEN` for `b18014e` on CI, CodeQL and Security Findings, all success on attempt 1, read from the worktree at that SHA — and the branch was **rebased before pushing**, so the merge was a fast-forward and the merged SHA **is** the gated SHA (the S186 trap — a commit merging above the gated one — avoided by construction, not by luck). 🟩 **Proven, not assumed:** redirected `make ci` exit 0 re-run independently at merge review, **2,997 passed, 6 skipped, 100.00 %** coverage, pip-audit clean and both secret scans clean; the **6 skipped** is the worktree fingerprint ([DL-193](design-log.md)) — no `.env`, so the two Service Bus tests skipped as they should. The checker now emits `declared_default=`, so a warning can no longer be misread as a fleet value; `kernel.tunable()` refuses an out-of-rails band at declaration via `kernel/tunable_envelope.py`, which covers every rail the primitive has (there is no `lt`); both PM bands were corrected **without moving a rail or a default**, and `correlation_threshold` re-sourced to ADR-0030 / EXP-008 at `(0.50, 0.70)`. 🎯 **Envelope breaches 3 → 2, which is the designed end state** — the two honest out-of-band defaults survive; zero would have meant a default moved. Decisions in `DL-196`, rejected routes included. 🔴 **Two spec defects, found by the build and corrected rather than worked around.** **(a)** The spec claimed **16** declared envelopes and stamped it `[measured]`; there are **13**, pinned since S207 by `test_exactly_thirteen_risk_settings_declare_evidence_envelopes`, and the sweep the spec cited cannot produce 16. The build measured it, refused to invent three envelopes to fit the number, and filed the disagreement — **the evidence discipline catching its own spec**, which is the self-catching bar working. Six derived claims in the spec now read 13. **(b)** The closeout listed `tests/test_config.py` as changed and its red-run paste cites that path; the six new tests were moved to `tests/test_tunable_envelopes.py` before the green run and `test_config.py` is **untouched**. The paste is left verbatim with a note rather than rewritten. 🟠 **Not in scope, still unowned:** the two PM defaults the fleet overrides are checked by nothing — aligning them is an operator decision, and a live-reading check needs a runner with `.env`, which CI deliberately is not. **No deploy is owed** (no runtime behaviour changed).

🟩 **MERGED — [S220](sprints/sprint-220-a-correlated-issuer-counts-in-proportion.md), `0.103.00`, merged `f3993d6`, tagged `v0.103.00`, 2026-09-21 — work-queue item 68.** The correlated-cluster gate now applies every examined issuer's rounded four-decimal ramp contribution (0 at ρ = 0.50, 1 at ρ = 0.90) in both its Decimal arithmetic and rendered detail; `PM-NEV-09` remains `NOT_EVALUATED` when every pair is unusable. PM laws are v1.8; `DRIFT-070` and `DL-189` record the guarantee and design decisions. 🟩 **Local proof:** A1 red first; A2/A3/A4/A5/A6/A8/A10 each deliberately red then restored; PM suite **148 passed**; redirected `make ci` exit 0 with **2950 passed, 6 skipped, 100.00 %** coverage, pip-audit clean, and both secret scans passed. 🩹 **The hard-block trap fired and was corrected:** CI measured `correlation_census.py` at 209 and its test at 215 lines, so comparison construction and gate-integration tests were split by responsibility; final production modules are all below 200. 🔴 **One defect the handback missed, fixed before the commit:** the version read `0.103.0`; the scheme's last group is **two** digits, so `0.103.00`. 🎯 **`make ci` was exit 0 over it** — no gate step reads the version. Filed as item **77**, specced as [S221](sprints/sprint-221-a-rule-the-repo-states-is-a-rule-the-gate-enforces.md). 🟩 **ADR-0030's pre-merge replay ran and met its bar, on a larger book than the ADR was decided on:** 22 runs / 45 approvals, membership identical **45/45** and value within 5 % **45/45** before any counterfactual; the ramp rejects **0 of 45** at a worst share of **0.098** against binary 0.70's **0.106**. 🎯 The bar was the cliff's `0.102` — the cliff has since drifted past it while the ramp held, so the ramp reads this book *more* tightly than the shape it replaces. 🟩 `GATE PROVEN` for `01af4c9` on CI, CodeQL and Security Findings, from the worktree at that SHA. 🟩 **DEPLOYED 2026-09-21 14:05 AEST — full `up`, not a retag.** Pinned to the gated SHA `7d5528e`; **all 15 agents + master verified independently** at that image with `provisioningState=Succeeded` and `runningStatus=Running` — checked per app rather than trusting the script's `Fleet deployed` banner, which is the exact line that printed on 2026-08-02 while all 15 had failed (DL-85). 🎯 **Live pack read-back proves the S202 trap was avoided:** `PORTFOLIO_MANAGER_CORRELATION_THRESHOLD` **0.70 -> 0.50** and `PORTFOLIO_MANAGER_CORRELATION_CEILING` **absent -> 0.90** on the live `portfolio-manager` (measured before and after; the ceiling key did not exist in the fleet beforehand). Dispatcher cron updated to `*/10 22-23 * * 1-5 UTC`. Env preservation passed for all 16 targets — no live key dropped (DL-100). 🟩 **Replayed against production read-only, 2026-09-21:** every recorded correlation census in the graph — **24 censuses across 10 `PMRun`s** — re-run through the deployed ramp. **19 of 24 change.** The canonical MDLZ case of **2026-09-14** moves from `correlated_issuers=none` (**0.0000**) to **0.8380** (KO 0.6397->0.3493, KHC 0.6175->0.2938, MO 0.5780->0.1950). 🪰 **What the ramp does NOT fix:** **39** near-miss issuers still contribute zero because they sit below the **0.50 floor** — including **MSFT at 0.4607** in the AMZN case, which was the referee's specific published complaint. That is ADR-0030 as designed, not a defect, but it means AMZN/MSFT will not change. 🟩 **Owed proof landed 2026-09-22:** `sched-2026-09-21`'s gate detail carries `correlation_ramp=0.5000..0.9000` on both approvals, load-bearing on DOW (`XOM:0.6235:w0.3087`, zero under the cliff). **Item 68 closed.**

🟩 **DEPLOYED — `s219`, 2026-09-20 16:29 AEST — S217, S218 and S219 live together as DL-179 required; work-queue item **58** closed.** Full `up` from merge `25bd053` (`v0.101.00`), images built by run `35493217440`, **15/15 jobs success**. 🟩 **Verified against live Azure, not off the script's own output** (which has reported 17 false failures before): **16/16 apps on `s219`**, **16/16 `Succeeded`**, every app `min=0` with exactly one KEDA rule, and **master kept `daily-master-window`** while the 15 agents kept `daily-agent-window` — the S210 flattening did not recur. Master window **moved 22:25 → 20:25**; agents unchanged at 22:30. `dispatcher-cron` on `s219` with cron **`*/10 22-23 * * 1-5`**. `DeployRecord deploy:2026-09-20T06:29:39…:s219:25bd053…`. 🎯 **The check that mattered was the pack read-back (S202's lesson):** *two* injected packs moved — `trading_graph_vocabulary` `579a40a3…` and `trading_credential_tests` `97f083bf…` — both decoded off the running fleet and hash-matched to the repo. An image-only retag would have left the new Telegram credential probe **inert** while every currency check passed. 🪰 **Also fixed first, and it would have failed silently:** `telegram-bot-token` and `telegram-chat-id` were **absent from Key Vault**; the vault seed now declares them, so the dispatcher would have come up with empty values. 🟩 **The live proof landed 2026-09-22, and it was silence — the invariant, not a non-result.** Four `FleetPreflight` records (20:25:36, 21:25:51, **22:26:08**, 23:26:21 UTC) all `passed=True`; **`RunHold` 0**, **`Escalation` 0**; `sched-2026-09-21` was placed behind the 22:26 check and ran 8/8. 🟠 **`functionality-checks.md` rows for S217/S218/S219 still owed.**

🟩 **MERGED, NOT DEPLOYED — [S218](sprints/sprint-218-a-broken-fleet-gets-no-run.md), `0.100.00`, 2026-09-20 11:34 AEST.** The dispatcher now places a scheduled run only after a fresh passing `FleetPreflight`; failing, absent, or stale evidence writes one append-only `RunHold` and places nothing. Recovery adds `released_at` rather than mutating `state`; the dashboard forces RED for active holds or a recent failed fleet check; master preflight emits one aggregate critical fault per failed check; master wake-up moves to `20:25 UTC`. 🟩 **R1 locally proven:** after a release, a new failing or stale check writes `hold:<run_id>:<n>` rather than reusing released evidence; the returned key names that active fact, and unknown readiness reads `Tonight's run is held: no recent fleet check`. Focused S218 suite: **684 passed**; redirected `make ci` exited 0 with **2906 passed, 6 skipped, 100.00 %** coverage, pip-audit clean, and both secrets checks passed. The branch worktree has no `.env`; no live probe ran. 🟩 **Merged** `69f3601`; `GATE PROVEN` for the handback tip `12bb962` (CI, Security Findings); tagged `v0.100.00`. 🪤 **Corrected on re-check — the merge commit itself is NOT gate-proven:** `69f3601`'s **CI run was `cancelled`** (Security Findings and CodeQL both `success`), superseded by the docs push `049ba55` two minutes later. **Main's current tip `049ba55` IS `GATE PROVEN`** on all three, so the deployable state is green — but the merge SHA is not, and this row said it was until re-measured. **This is [work-queue item 31](work-queue.md)'s `ci.yml` cancel group, measured for the first time.** 🎯 **Returned once, and it paid:** planner amendment **R1** found two defects the B1–B15 table missed — a hold placed again after a release reused the released node, so a stale re-hold left the run **held with the dashboard GREEN**; and a held run with no check rendered `0 check(s) failing`, which reads as *nothing is wrong*. Both were **re-verified here independently**, by re-running the spec's own committed reproduction against the fix: `HELD | GREEN` → **`HELD | RED`**, and the summary now reads `Tonight's run is held: no recent fleet check` (B14-clean). 🪰 **The red run is what made the return provable** — `assert 'GREEN' == 'RED'` and `assert 'hold:…' == 'hold:…:1'` — and guards (e)/(f) were planted, watched red and restored. 🟠 **Deploy is a full `up`, not an image-only retag:** `trading_graph_vocabulary.json` gained `RunHold`. S219 (Telegram) still owns the human channel, so nothing deploys until it lands.

🟩 **MERGED, NOT DEPLOYED — [S217](sprints/sprint-217-the-master-checks-the-whole-fleet.md), `0.99.00`, 2026-09-19 22:22 AEST.** Master now runs a daemon fleet preflight across every grant-policy agent type: every pack probe is required, listed 4xx failures are `unrecoverable`, unlisted 4xx failures are `unexpected`, and transport failures are `transient`; each failed check becomes critical fault evidence and one `FleetPreflight` graph record. **Proven locally:** redirected `make ci` exited 0 with **2884 passed, 6 skipped, 100.00 %** coverage; pip-audit and both secret scans passed. The branch worktree has no `.env`; no live probe ran. 🟩 **Merged** `f252dca` 2026-09-19 22:39 AEST; `GATE PROVEN` for the handback `dd20794` and the merge; tagged `v0.99.00`. 🟠 **Planner finding:** one critical fault per failed *probe*, not per failed check as specced (unflagged in the handback); S218 fixes it before anything deploys. S217 must not deploy alone; S218/S219 own downstream action and notification.

🟩 **MERGED — [S216](sprints/sprint-216-every-text-file-is-lf.md), `9e39e40`, 2026-09-19 — work-queue item 69 closed; built by GitHub Copilot.** 🟩 **Proven:** handback gate `b1628de` = branch tip; merge `9e39e40` `GATE PROVEN` (CI, CodeQL, Security Findings). Renormalization commit: 52 files, CR-ignoring diff **0 lines**; `pyproject.toml` and `.secrets.baseline` content unchanged (no bump). Census on `main`: 1,727 `i/lf`, 15 `i/-text`, 3 `i/none`, **0** CRLF/mixed. 🪰 **Three builder stops, all correct, one of them my mis-written expectation**; the other two were a missing final newline, caught by the hooks. 🪤 **Spec lesson:** `git add --renormalize .` stages the *content* of every modified tracked file, so a builder's in-progress edit to the spec itself joins the "content-free" commit unless stashed. 🟠 **Next:** item 73 (markdownlint rules), now unblocked.

🟩 **DEPLOYED — `s215`, 2026-09-19 15:10 AEST — S213 (regime VIX, item 70) and S215 (deploy evidence, item 72) are live.** Built from tag `v0.98.12` = merge `5ef4a4c` (run `35422844229`, 15/15 images). Image-only retag, because no injected pack and nothing under `infra/` changed since `s214`. 🟩 **Proven:** 16/16 apps + `dispatcher-cron` on `:s215`, all `Succeeded`, every app `min=0` with its 1 KEDA rule; `record_deploy.py` **exit 0** → `DeployRecord` `…:s215:5ef4a4c…`, the first for a tag-dispatched build, so **item 72 is closed live**. 🟠 **Owed:** item 70's measured `vix_status` arrives with `sched-2026-09-21`. No weekend test run is possible: the NYSE-session gate has no override.

📦 **SPECCED, for Copilot — [S216](sprints/sprint-216-every-text-file-is-lf.md), 2026-09-19 — work-queue item 69.** 52 of 1,742 tracked files are stored CRLF or mixed. The exact change (`.gitattributes` `* text=auto eol=lf` + `git add --renormalize .`) was dry-run in a throwaway worktree: 52 files, an empty `--ignore-cr-at-eol` diff, 0 images touched, 0 CRLF left. The spec pins every step's expected output and all nine guard-test cases. No bump, no deploy. 🟠 **Next:** work-queue item 73 (markdownlint config + local pre-commit hook), after S216 merges.

🟩 **MERGED — [S215](sprints/sprint-215-a-build-is-evidence-for-its-commit-and-its-tag.md) (`0.98.12`, merge `5ef4a4c`), 2026-09-19 — work-queue item 72 code-fixed; the first sprint built by GitHub Copilot.** The deploy recorder now accepts a build whose commit GitHub's `compare` reports contained in `main`, whichever ref dispatched it, and matches tags whole. 🟩 **Proven:** `GATE PROVEN` for the branch tip `39be3b2` and for the merge `5ef4a4c`; live read-only check, old reader beside new: `s211`/`7d3ff51` `[]` → `[35217747046]` (the tag-dispatched build, item 72); `smoke-test`/`576ee57` `[]` → `[]` (never merged, still refused); `s21` and `s2` on `1a6f342` **false accept** `[35336964246]` → `[]`; `s176`/`74bdd7c` **false accept** of the `s176a` build `[31854512795]` → `[]`; controls `s214` and `s176a` accepted by both. 🪤 **Returned once:** the first handback's tag charset was uppercase-only, so `s176` still matched `s176a` — the spec's edge list had no letter suffix. 🩹 **This spec's own commit turned `main` red** (detect-secrets on a full SHA, local hook passed); fixed by `b6c3cd1`. No deploy (`surfaces/` is in no image). 🟠 **Owed:** S213's deploy, dispatched on its tag per the updated deploy-fleet step 2, is the closing proof — its `DeployRecord` closes item 72.

🟩 **LIVE — first run on `:s214`, [ADR-0029](decisions/0029-a-revise-is-a-finding-an-overturn-is-a-block.md) decision 1 proven in production.** `sched-2026-09-18`, 8/8 stages, `trace_run.py` **exit 0**. The deliberator returned `revise` on **all four** approved buys — `{BAC, USB, AMZN, MDLZ}`, its objections undiminished — and `vetoed_tickers` came back **empty**, so execution recorded `submitted=4 rejected=0 skipped=0`. 🎯 **Under `:s211` this identical night would have submitted 0**, as the four before it did: the zero-fill streak is broken at the submission stage. The trace reconciles as `[deliberation] reviewed=4  revised=4  vetoed=0  status=applied` — the late `revised=` addition is what keeps four live objections visible now that they no longer veto. 🪤 **Half proven, and it matters:** test 2 matches only **vacuously** (zero `overturn` verdicts, so the blocking path never ran) and test 5 (a non-answer routed to loud fail-open) was **not exercised**. 🟩 **Test 4 — the bar itself — is PROVEN, re-measured 2026-09-22:** the four orders filled at Monday's open and `accept.py --run-id sched-2026-09-18` now reads **`ACCEPTANCE PASS`**. All four were adopted into the graph at the next run's start and broker qty = graph qty on every holding. **Tripwire, session 1 of 10:** `overturn` share **6.2 %** vs the 6.3 % baseline — no relabelling, as expected while decision 3 stays unshipped. Recorded in [`laws/functionality-checks.md`](laws/functionality-checks.md).

🟩 **MERGED, NOT DEPLOYED — [S213](sprints/sprint-213-a-regime-says-what-vix-it-measured.md) (`0.98.11`), 2026-09-19 — [ADR-0028](decisions/0028-the-regime-reads-vix-from-fmp-and-says-when-it-cannot.md) shipped, work-queue item **70** closed.** FMP `^VIX` is composed as the provider regime-input source; `RegimeContext` carries **defaulted** `vix_status` and `vix_as_of` so the 70 historical snapshots stay readable; prior-session and missing VIX record **warning** faults with empty `provenance.incident_refs`, so a VIX shortfall does not trip the analyst/PM halt path. Provider laws **v1.1 → v1.2**, `CONTRACT.version` 0.5.0 → 0.6.0, rollup recounted **16/62 → 17/62**, `DRIFT-067` and `DL-176` filed; **no pack changed**, so the deploy is an image-only retag. 🟩 **Proven:** redirected `make ci` exit 0 (**2839 passed, 6 skipped, 100.00 %**), `GATE PROVEN` for `b8d696a` (CI + Security Findings), merged `--no-ff`. 🩹 **Carries a security fix:** `anyio` 4.13.0 → **4.14.2** after pip-audit reported two CVEs. 🪤 **Authorship:** the coding agent wrote this sprint and exhausted its allowance before committing; the commit, push, gate and merge were done here after independent verification. 🟠 **Deploy deliberately held.** It changes what the regime reports, which changes what the deliberator is shown — landing it mid-window would muddy [ADR-0029](decisions/0029-a-revise-is-a-finding-an-overturn-is-a-block.md)'s 10-session tripwire that started 2026-09-18. Deploy after Monday's fill check. **Live VIX freshness check **discharged 2026-09-22**: `sched-2026-09-21` recorded `vix_status=measured`, `vix=14.8`, `vix_as_of=2026-09-21`, `label=risk_on`.**

🟩 **MERGED and DEPLOYED — [S214](sprints/sprint-214-a-revise-is-a-finding-an-overturn-is-a-block.md) (`0.98.10`, merge `1a6f342`, tag `s214`), 2026-09-18 — [ADR-0029](decisions/0029-a-revise-is-a-finding-an-overturn-is-a-block.md) decisions **1** and **5** live; 2, 3 and 4 unshipped by design.** `vetoed_tickers` carries `overturn` only; an unreadable judge answer raises `UnreadableVerdictError` and routes to loud fail-open instead of borrowing `revise` — without which decision 1 would have flipped a fail-*closed* default to a silent fail-*open*. Deliberator laws **v1.7 → v1.8**, rollup **21/56 → 22/56**, `DRIFT-066`; `contracts/` and `agents/execution/` both untouched. **Gate proven on the merged SHA** (CI, CodeQL, Security Findings, image build); deployed by image-only retag after all three injected packs were diffed and re-read from the live fleet — **17/17 on `:s214`**, KEDA **zero drift**, `DeployRecord …:s214:1a6f342…`. Full evidence in [`laws/functionality-checks.md`](laws/functionality-checks.md).

🟩 **PROVEN RESULT — [S212](sprints/sprint-212-a-trace-says-why-nothing-was-submitted.md) MERGED, MAIN-GATED, and LIVE-CHECKED (`0.98.09`, main `b275d87`), 2026-09-18 — work-queue items 66/67 closed as operator-observability repairs.** `scripts/trace_run.py` now treats `complete == trace_stage_total()` as success, and `orchestration/batch_trace.py` prints a conservative `[deliberation]` block between PM and execution with reviewed/vetoed/status/tickers plus `withheld=N by deliberation veto` only when approved buys, vetoed tickers, and submitted count reconcile exactly. Law reading and DL-174 are recorded; A1-A8 were red-first, DL-70 break checks were restored, no `agents/` or `contracts/` diff exists, touched modules are under 200 lines, and redirected local `make ci` exited 0 with `2811 passed, 6 skipped`, 100.00 % coverage, pip-audit clean, detect-secrets clean. 🟩 **Gates:** final branch `make gate-ran` proved `b275d87ce243786bd609de0e67f2471c74794e1f`; after fast-forwarding and pushing `main`, post-merge `make gate-ran` proved the same SHA with CI, CodeQL, Security Findings, Dependency Graph, and image build success. 🟩 **LIVE CHECK DISCHARGED on `sched-2026-09-16`** from the main checkout with `.env`: `trace_run.py --run-id sched-2026-09-16` exited 0 and rendered `[deliberation] reviewed=4 vetoed=4 status=applied`, `tickers=USB,WFC,AMZN,MDLZ`, `withheld=4 by deliberation veto`, followed by `RESULT 8/8 stages complete OK batch processed`. No deploy was required or performed because S212 changes operator tooling only.

🟩 **DECIDED, NOT YET BUILT — [ADR-0029](decisions/0029-a-revise-is-a-finding-an-overturn-is-a-block.md), from [DL-173](design-log.md).** Four consecutive zero-fill sessions (09-14, 09-16 ×2, 09-17) traced to a code-level collapse, not a model fault: `revise` (164 all-time) and `overturn` (22) are reduced to one action at `agents/deliberator/review_batch.py:87`, so the referee's *systemic* critiques are spent rejecting *individual* trades. 🎯 **Three experiments show those critiques are correct and their fixes do not pay** — [EXP-011](research/experiments/EXP-011-regime-markov-and-barrier-calibration.md) (the ratio does not predict returns), [EXP-008](research/experiments/EXP-008-correlation-cutoff-replay.md) (no cutoff changes a trade), [EXP-012](research/experiments/EXP-012-volatility-sizing-ten-year-replay.md) (iso-risk costs $11,619/10 y). **Decision:** `overturn` blocks, `revise` records a deduplicated finding, a block must name an order-specific fact, the judge is told what its verdicts do, and 🚨 **a judge that cannot answer routes to `applied_failed_open`, never to `revise`** — without that the change would flip today's fail-*closed* parse default to a silent fail-*open*. Expected block rate **53 % → ~6 %**. 🪤 **Named tripwire:** if `overturn` share exceeds **20 %** over the first 10 real-debate sessions the judge has swapped words, and the ADR is revisited rather than the prompt tightened. 🟠 **Owed:** the implementing sprint (deliberator + execution law cycle) and tests 1-5 in the ADR.

🟩 **PROVEN RESULT — [S211](sprints/sprint-211-reward-risk-compares-two-measured-quantities.md) MERGED, DEPLOYED and LIVE-PROVEN (`0.98.08`, merge `7d3ff51`, tag `s211`), 2026-09-18 — work-queue item 60 closed.** Analyst targets use median trailing favourable-excursion evidence, `StopTargetEvidence` carries the estimate/window/sample count, PM `min_reward_risk_ratio` is `0` per ADR-0027 Correction 2 / EXP-011, and `reward_risk` records `comparison=DISCLOSURE_ONLY` by default while explicit positive floors still reject below-floor ratios. Analyst laws v1.4 → v1.5 (`ANLZ-OBS-06`), PM laws v1.6 → v1.7 (`PM-OBS-05`); rollups analyst **26 / 49**, PM **31 / 50**. 🟩 **Gate + deploy:** `make gate-ran` printed `GATE PROVEN` for `5dc9d1c`, merged `--no-ff` as `7d3ff51`, post-merge CodeQL success; build `35217747046` 15/15; `deploy-agents.ps1 up -Tag s211` exited 0 with **17/17 targets on `:s211`, all `Succeeded`**, `dispatcher-cron` `30 22 * * 1-5`. 🟩 **LIVE CHECK DISCHARGED on `sched-2026-09-17`** (8/8 stages, `ACCEPTANCE PASS`, provider quality `ok` 98/99, no `*_degraded`), recorded in [`laws/functionality-checks.md`](laws/functionality-checks.md): three **distinct** ratios (`AMZN 0.8136`, `USB 0.8479`, `WFC 0.9484`) where S208 saw a constant `2.0`; `comparison=DISCLOSURE_ONLY` at `threshold=0.0` throughout; **zero** `reward_risk_below_min` rejections; **zero** `STRUCTURALLY_DETERMINED`; evidence named as `target_basis=trailing_favorable_excursion`, horizon 10 d, sample 120. 🚨 **The proof arrived with its own finding:** every measured target landed *below* its stop (all ratios < 1), so the 0 floor admitted inverted payoff geometry and the deliberator vetoed **all three** (`verdicts` all `revise`) citing exactly that — a check *"structurally incapable of failing"*. Zero fills; book unchanged at 21 holdings. Whether the floor stays at 0 is reopened as a question, not a defect. 🟠 **Two gaps, both filed:** no `DeployRecord` for `s211` (`record_deploy.py` accepts only `main`-ref builds; this one was dispatched on `v0.98.08`), and the approved-vs-submitted accounting hole — **four consecutive zero-fill sessions, 20/26 buys vetoed (77 %)** — which [S212](sprints/sprint-212-a-trace-says-why-nothing-was-submitted.md) specs as work-queue **66**/**67**.

🟠 **BUILT and BRANCH-GATED — [S210](sprints/sprint-210-a-concentration-gate-divides-by-the-book-it-measures.md) on branch `sprint-210-concentration-divides-by-the-book`, worktree `C:\Users\yury_\Downloads\project\trading-agents-sprint-210-concentration-divides-by-book`, base `origin/main` @ `d4040f84a9b04bd9cd74f6656893433357468ece`.** `max_sector_pct` and `correlated_cluster_pct` now measure concentration against deployed capital while `sizing` keeps the equity denominator; below the derived deployment floor, those gates report explicit `NOT_EVALUATED`; PM laws/test-plan/rollups are amended to v1.6, `DRIFT-065` is filed, and `contracts/` stayed untouched. 🟩 **Local proof:** red-first S210 guards failed 7/9 before implementation, focused S210 tests **14 passed**, full PM tests **132 passed**, and redirected `make ci` exited 0 (**2780 passed, 6 skipped, 100.00 %**; pip-audit and secrets checks passed). 🟩 **Branch proof:** remote CI `35089424010` and Security Findings `35089424128` succeeded on `540415ff91fc312f05dcce3d79bc4c220d450b85`; `make gate-ran` from the S210 worktree printed `GATE PROVEN` for that same SHA. 🟠 **Not done:** merge, deploy, and next-run live denominator check. The evidence commit that records this note still requires its own final branch reproof.

🟩 **MERGED and DEPLOYED — [S209](sprints/sprint-209-a-stop-that-fired-is-not-a-mismatch.md) (`0.98.06`, deploy SHA `032cd8e`, tag `s209`), 2026-09-16 — work-queue item **42** is code-fixed and live-deployed, but not live-closed.** `_is_stop_order` now compares broker stop identity (`is_broker_stop_order`) with graph stop identity (`broker_stop_orders`) and no longer writes a permanent `BrokerStopIdentityMismatch` merely because a fired stop's graph refresh is 11-19 s behind the broker. `EXEC-OBS-05` stays 🟩 but is amended **v1.5 → v1.6**, rollup remains **35 / 61**, `DRIFT-064` is filed, and `DL-170` records the decision not to add a second liveness observer. 🟩 **Proven:** A1 red first, A4 red on intentional broken graph identity, focused 6 passed, local `make ci` exit 0 (**2768 passed, 4 skipped, 100.00 %**), `contracts/` diff empty, deploy-SHA `make gate-ran` on `032cd8ef6fa34f65099bf4ecb82af7d819888a1a` green for CI, CodeQL, Security Findings and image build. 🟩 **DEPLOYED `s209` by image-only retag:** all three injected packs were unchanged and re-read from live env after the retag — vocabulary `58769995…` on **17/17**, credential tests `f8f03950…`, issuer map `2ed1f41c…`; `35050880879` built `s209`; **17/17 targets on `:s209`**, 17/17 `Succeeded`, dispatcher cron `30 22 * * 1-5`, scale/env/cron shape drift **0**. `DeployRecord deploy:2026-09-16T08:45:36…:s209:032cd8e…`. 🟠 **Not done:** the only live closure that matters — a future scheduled run where a protective stop actually fired and zero same-date `BrokerStopIdentityMismatch` faults appear.

🟩 **MERGED and DEPLOYED — [S208](sprints/sprint-208-a-risk-gate-says-what-it-can-reject.md) (`0.98.05`, merge `d43280e`, tag `s208`), 2026-09-16 — work-queue item **61** closed, **60** narrowed.** `reward_risk` keeps its verdict and threshold but now names `base_take_profit_pct`, `base_stop_loss_pct`, `applied_mode`, `structural_basis` and `comparison=STRUCTURALLY_DETERMINED` when the ratio is fixed by the base pair — and a full gate report proves data-varying gates are **not** labelled structural, which is what stops the clause describing one site. `correlated_cluster_pct` now skips only the unusable pair, attributes it as `skipped_pair_issuers`, and still returns whole-gate `NOT_EVALUATED` when no usable pair remains, so `PM-NEV-09` comes out stronger. `PM-OBS-04` added and green; PM rollup **29 / 48 → 30 / 49** as the gate computed it; `DRIFT-063` filed and corrected. 🟩 **Proven:** T1/T2 red on `main` first; local `make ci` exit 0 (**2763 passed, 6 skipped, 100.00 %**); `contracts/` diff empty; `correlation.py` 153 → **115** lines; `make gate-ran` on the **merged** SHA — `main` @ `773c02e`, with CI, **CodeQL**, Security Findings and the image build all `success`, so no commit rides above a gated one (the S186 hazard). 🚨 **One handback defect no gate could catch, fixed on merge:** the `laws.md` v1.5 changelog cited `test_portfolio_manager_audit.py` for the T4 test, which lives in `test_gate_reachability.py`; the test-plan row the coverage gate reads was right, the prose was not.

🟩 **DEPLOYED `s208` by image-only retag, and the path was proven before it was taken.** All **three** injected packs were diffed against the deployed `ea6a3b4` *and* against what the live fleet decodes — vocabulary `58769995…`, credential tests `f8f03950…`, issuer map `2ed1f41c…`, identical on all three sides — so the retag ships nothing inert (the S202 trap). 🟩 **Verified:** 15/15 image jobs green at `773c02e`, **16/16 apps on `:s208`** plus `dispatcher-cron`, 16/16 `Succeeded`, cron `30 22 * * 1-5` intact, scale and KEDA **diffed byte-identical to the pre-deploy baseline, zero drift**, packs re-checked *after* the retag unchanged. `DeployRecord deploy:2026-09-15T14:55:48…:s208:773c02e…`. 🟩 **PROVEN RESULT — the live check is discharged, a day earlier than planned.** `sched-2026-09-15` dispatched at **22:32 UTC, after** the 14:55 UTC retag, so its `PMRun` `pm-run-ef4b658235c7491888338459fd5190b1` is the first written by `s208` — the run to read was last night's, not `sched-2026-09-16`. Read off the spine: every `reward_risk` detail carries `base_take_profit_pct=0.1000; base_stop_loss_pct=0.0500; applied_mode=scaled; structural_basis=base_take_profit_pct/base_stop_loss_pct; comparison=STRUCTURALLY_DETERMINED` with `outcome=passed value=2.0` unchanged, and `correlated_cluster_pct` renders `skipped_pairs=0; skipped_pair_issuers=none`. Recorded in [`laws/functionality-checks.md`](laws/functionality-checks.md). 🟠 **Half proven emitted, not exercised:** `skipped_pairs=0` on all three approvals, so the skip-only-the-unusable-pair path did **not** run live and still stands on its unit tests.

## Recent (most recent first — detail in each sprint doc)

🗄️ **Older shipped results — item 6b's closure with the `binding` posture's proof, the `s219a` image-only retag, item 70's live input proof, item 73's markdown-rule closure and its link remainder, and the `pip-audit` advisory that turned `main` red are now in [state-archive/STATE-12.md](state-archive/STATE-12.md).**

🗄️ **Older shipped results — S206's rendered-verdict repair, its handback verification and the `s206` deploy are now in [state-archive/STATE-12.md](state-archive/STATE-12.md).**

🗄️ **Older shipped results — S205's type-clause rewrite and the S204 verification block are now in [state-archive/STATE-12.md](state-archive/STATE-12.md).**

🗄️ **Older shipped results — S202, S203 and the work-queue item 47 promotion are now in [state-archive/STATE-11.md](state-archive/STATE-11.md); S199, S200, S201 and the item-6b posture decision in [STATE-10.md](state-archive/STATE-10.md); S172, S173 Part A, S180, S191, S195–S198 and the CodeQL repair in [STATE-09.md](state-archive/STATE-09.md); everything earlier in [state-archive/INDEX.md](state-archive/INDEX.md).**

🗄️ **Older shipped results — the late-August deliberation/sizing residue (ADR-0023's PM half, the posture flip conditions, the 73 % veto figure, S182/S184 and the NFLX residue) are now in [state-archive/STATE-12.md](state-archive/STATE-12.md).** 🪤 The 73 % figure there is superseded by [DL-173](design-log.md) / [ADR-0029](decisions/0029-a-revise-is-a-finding-an-overturn-is-a-block.md).

## Next

**Ranked queue of record: [work-queue.md](work-queue.md)** — this section is the narrative around it, not a second ranking.

🎯 **2026-09-23 — one open item left: [22](work-queue.md) Part B.** Stamp `**Status:** <TOKEN> — <original line>` onto the **190** sprint docs the checker can classify, after a human reads the **40** it refuses (20 unmapped, 20 with no status line). Item **71** stays parked and item **75** is queued for after the debt, both by operator direction. The narrative below is the 2026-09-15 review, kept as written.

🎯 **Re-ranked 2026-09-15 against the live spine, then re-counted after S205 merged.**
**14 items open** against the operator's **Friday 2026-09-18** empty-by date. The review put **item 59**
first and displaced everything else for one reason: the referee **vetoes 100 % of what it reviews**, and every
order that has reached the broker since 2026-09-01 went through on a night it was blind — so the etalon bar
cannot be met while it stands ([DL-167](design-log.md)). It also filed **60, 61, 62** off the nine-gate PM
audit: `reward_risk` is 2.0 on 170 of 170 recommendations against a 1.5 threshold, the correlation gate has
never once failed, and sizing caps dollars rather than risk. S205 then closed **item 30**, so of the rows that
were "mostly not code" a week ago only **33** (57 PARAM/settings divergences), **22** (a sprint's built state
is not machine-checkable), **31** (CodeQL runs only on `main`), **7** (the duplicated LLM adapter) and **11**
(the delegated-agent sandbox default) remain. Item **42** is still the one live-behaviour defect open: one
spurious stop-liveness warning per run, every run. **S206 has since shipped and deployed** — see *Now*.

📦 **[S208](sprints/sprint-208-a-risk-gate-says-what-it-can-reject.md) is specced** (2026-09-15) against items **60** and **61**, from a live-spine audit recorded as [DL-169](design-log.md): two PM risk gates have **never rejected an order** — `reward_risk` has one distinct value across 256 recordings because the ratio cancels to two constants the regime label never touches, and `correlated_cluster_pct` abandons the whole gate on the first thin-overlap holding. Both appear **zero** times in 919 `Rejection` rows. 🪤 **The audit also corrected the queue's own denominators** — only **35** of 273 `OrderIntent` nodes carry gate outcomes, and the correlation abort path has **never fired**, so that half is repair before it bites. 🚨 **One decision is the operator's and is not in the sprint:** item **62**, position sizing by risk rather than dollars, is the referee's most-repeated nightly ground and the largest single lever on the veto rate — it is blocked on a policy call, not on engineering. New item **64** records that the regime label never modulates any risk number.

**Ahead of the numbered list — three questions raised and not yet answered.**

**The latency levers.** `effort` `max` → `high` is **done** (live 2026-08-12); `max_rounds` 2 → 1 is
🚨 **cutting the artefact under test to buy wall clock**, a recorded decision rather than a knob. The two are
**coupled** — more effort lengthens the peer-call tail into a fixed timeout — so measure them together or not
at all. Arithmetic and the full history in [DL-105](design-log.md)'s amendment.

**Undecided, recorded so they are not re-derived** — raised 2026-08-11. **(i)** amend S172 and **(ii)** collapse to one ranked queue are both **DONE 2026-08-19** (detail in the queue's own header). **(iii)** stop pinning version numbers in sprint specs (*"next available PATCH/MINOR at merge"*) — after three renumberings in one day — **still open**.

1. 🪤 **WITHDRAWN 2026-08-22 — retracted twice, on a raw-prop count both times.** `pending_human_flags`
   is **0**; `Flag.status` stays `pending` by design, like `Position.status` staying `open`. What pins
   `healthy=false` is `open_incidents`. The retracted-DL-73 class again — audit on the predicate.
2. 🪤 **Sweep the debate context for the same class — now FOUR instances, packaged as [S177](sprints/sprint-177-every-number-names-its-unit.md)** (2026-08-15). The fourth
   is `max_sector_pct`'s `deployed`: `SectorBook.__init__` seeds `_names` from held positions but
   **never seeds `_deployed`**, so `deployed` counts only *this batch* (GOOGL's `deployed=687.05` is
   exactly GOOG's `order_cost` moments earlier). `deployed=0.00` beside `existing_sector_names=2`
   is correct and unreadable, and it cost the AVGO overturn. 🚨 **Same code is also a latent gate
   defect** — the dollar sector cap never sees held positions, so across days sector exposure can
   pass 30 % unnoticed; masked today only because `max_names_per_sector=3` binds first. Every value rendered into the packet should be
   checkable against what the reader will assume it means: right unit, right period, right scope.
   **Not yet a spec.** This is the highest-value item once the divergence is settled.
3-5. **~~DL-104 (a) the invented ATR fragment · DL-104 (b) batch/portfolio absence · DL-112 sentiment
   units~~ — ALL DONE.** S175 `0.90.08` deployed `s176a` 2026-08-15, and `0.90.11` deployed `s176b`. The
   measured cost of each is in those sprint docs. 🚨 **(a) is the one S206 had to finish** — S175 deleted
   the ATR half of that rendered line and left its sibling, with no clause forbidding the class.
6. **DL-104 (c) — the analyst's hardcoded SMA-200 rationale.** The summary string always names
   SMA-200 while `indicators.sma_distance` returns `None` below its period. 🪤 **The bars gap
   underneath it is CLOSED** — S174 ships 203 bars, so SMA-200 now computes; what survives is the
   rationale asserting an input without checking it exists. Same class as item 5. 🚨 **Cited again 2026-08-19** — NEE vetoed because *"the cited technical support is contradicted by a 0.463 technical score with bearish MACD/EMA/golden-cross inputs and a bottom-tier scanner rank"*: the rationale reads as confirmation while the score underneath it is bearish.
7. **DL-104 (d) — a real advisory/binding switch**, so *advisory* is a declared posture rather than a
   grace that happens to expire. Every run writes a truthful but uninformative `error` fault, which
   trains the operator to read a real fault as noise.
8. **S170 — one LLM adapter in `kernel/`**
   ([sprint-170](sprints/sprint-170-one-llm-adapter-in-the-plumbing.md)). Capability, not repair, so
   it ranks below the fixes — but it gives the operator the same provider switch the deliberator has,
   while the Anthropic key is usage-limited to 2026-09-01.
9. **Remaining hardening rows: N, O, R.** **N** — delegated coding agents default to
   `danger-full-access` with no approval prompt; the protection is the operator remembering a CLI
   flag. **O** — S157's 101 missing law-clause test-plan rows, then flip assertion E in
   `scripts/check_law_coverage.py` to hard fail. **R** — three guards covered only by CI lacking the
   extras. 🪤 **Row P is closed** (S176, deployed); **row Q is closed** (the `s176a` full `up`
   returned `[OK]` on all 16 env-preservation checks after refusing once — see Recent).

## Pointers

Product `docs/PRD.md` · architecture `docs/architecture.md` · phases `docs/build-plan.md` · closed
decisions `docs/decisions/INDEX.md` · open threads `docs/design-log.md` · "does it work"
`docs/laws/{ledger,drift-register,functionality-checks}.md` · per-agent `agents/<name>/mission.md`.
