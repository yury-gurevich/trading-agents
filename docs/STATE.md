# Project State

**Last updated:** 2026-09-25 21:45 AEST · **Version:** **0.112.02 deployed** (`s228b`) = `main` · **🎯 Fleet on `s228b`: tonight's run (Sat 08:30 AEST) is the first for S230's stop replacement and the reporter's day point, and the chat's last run now names real runs; both functionality checks are owed after it.**

**How to read.** *Now* = active · *Next* = queued · *Recent* = last few shipped (older detail lives in
each `docs/sprints/sprint-NN-*.md` + [`state-archive/`](state-archive/INDEX.md) `STATE-01…13.md` + git). **LAW-02:** an item is "shipped" only when
its success factors are *proven* (tests, `make ci`, the named live check) — never restate intent as outcome.
**Size rules**, after this file reached 574 lines with a **112 KB header line**: keep it **under 200
lines**, and keep the header above to **three clauses — stamp, version, one headline**. Replace that
headline each session; never append a fourth. Oldest *Recent* entries split to the archive.

---

## Current focus

**The bar, set by the operator 2026-08-20.** The etalon is done when **the pack trades unattended
for a sustained stretch and the evidence discipline catches its own defects without the operator in
the loop.** Not "trading is finished" — but far above where it is. 🚨 **Consequence for ranking:**
whatever currently prevents unattended operation *is* etalon work, not a detour from it. **Until
2026-08-22 that was the 73 % veto rate** ([DL-119](design-log.md)). It is now one level up: the
referee cannot run at all until **2026-08-30**, so the pack trades unvetoed behind a gate that is red
nightly for a non-defect ([DL-125](design-log.md)). 🚨 **A discipline that cries wolf for six
straight nights fails the second half of the bar as surely as not trading fails the first** — which
is why work-queue **item 6** (a declared advisory posture) is now ranked first. 🪤 **Generality is a separate
unproven axis:** ADR-0012's platform/pack wall stays *de jure* until a second pack exists, and
clearing this bar does not close that.

Since P14 the project runs as **etalon-first continuous improvement** (DL-19).
**The platform is self-driving in paper mode**: the DEPLOYED, STANDING fleet (16 Container Apps +
`dispatcher-cron` job, KEDA scale-to-zero windows, idle ≈ $0) places a calendar-gated `RunRequest`
at 22:30 UTC daily, runs graph-pull + served-over-Service-Bus on the Neon Postgres spine (ADR-0014),
reconciles holdings against the broker (DL-44), debates vetoes under compiled prompts (DL-42), and
proves `ACCEPTANCE PASS`. Pausing = disable the job + zero the scale windows (`docs/deployment.md`).
Completed arcs live in their sprint docs + archives: fleet (DL-35), credentials (DL-36), Postgres
migration (DL-43), deliberation quality (DL-41/42). Layer-3 acceptance 🟩 at the full S&P-500;
Layer-2 choreography 🟩 on a distributed run (S102).

## Now

🟩 **MERGED — [DL-225](design-log.md), `0.112.02`, fast-forwarded to `b78292cc`, tagged `v0.112.02`, 2026-09-25 ~21:05 AEST — not deployed.** Chore `chore-mcp-cli-runs-are-graph-pull-runs` (operator: *"look at the dashboard code"*). 📏 **Measured live first:** the MCP `runs` tool and CLI `runs`/`run` listed **6** supervisor intents (flag approvals) as runs and said *not found* for `sched-2026-09-24`; the supervisor's `last_successful_run` (the chat's `last_run`) named `snapshot:verify-2026-09-01-s192-k4-110200`, because **0 of 80** Snapshots carry the `created_at` it sorted by; and 🐛 **`import surfaces.mcp_server` failed on `main`**, a cycle S228's `performance_tool` import introduced that every in-process test hid. **Now:** one run list (moved to `surfaces/queries/runs.py`, the dashboard re-exports it), CLI `run` prints the chain's headline and asks no agent, the supervisor orders by `PMRun.created_at` and names the `RunRequest` (SUP laws **v1.2**), and fresh-interpreter import tests guard the entry modules. Guards planted: the old ordering returns `snapshot:verify-zzz`; the old import fails `mcp_tools` and `mcp_server`; both restored. `make ci` exit 0 (**3,219 passed, 6 skipped, 100.00 %**); `GATE PROVEN` for `b78292ccf57cb99c5bb7d30b17783a38dbfc9c7f`. **Re-probed live on the branch:** runs `sched-2026-09-24`, `manual-2026-09-24`, …; last run `sched-2026-09-24` from both. 🟩 **DEPLOYED `s228b` 2026-09-25 21:40 AEST** (operator: *"yes"*): build `36129885547` from `v0.112.02`, **15 / 15**; image-only retag, packs unchanged; **16 / 16** apps on `s228b`, `Succeeded`, `Running`, scale blocks JSON-identical, `dispatcher-cron` configuration identical; `DeployRecord deploy:2026-09-25T11:40:00…:s228b:b78292cc…`. Rollback: `s228a`.

🟩 **DEPLOYED — `s228a` = `381ce4f8` (`v0.112.01`), 2026-09-25 19:41 AEST — the work-queue 88 reporter fix (S228 rides along, but `surfaces/` ships in no image). Operator approved (*"yes"*).** Build `36119108502` from the `v0.112.01` tag, **15 / 15** success; image-only retag, the three injected packs unchanged since `s230`. 🟩 **Verified on live Azure:** **16 / 16** apps on `s228a`, `Succeeded`, `Running`; scale blocks JSON-identical to the pre-retag snapshot (0 mismatches); `dispatcher-cron` on `s228a`, configuration identical, cron `*/10 22-23 * * 1-5`. `DeployRecord deploy:2026-09-25T09:41:37…:s228a:381ce4f8…`. 🟠 **Owed on `sched-2026-09-25`:** S230's check (9 stops at their decided price) and item 88's (the Snapshot's series uses each date's latest fresh snapshot). Rollback: retag to `s230`.

🟩 **MERGED — work-queue 88 / [DL-224](design-log.md), `0.112.01`, fast-forwarded to `381ce4f8`, tagged `v0.112.01`, 2026-09-25 ~20:20 AEST — not deployed.** Chore `chore-reporter-day-point-is-the-close` (operator: *"fix in the reporter"*). Each UTC date's point is now its **latest** fresh `BrokerPositionSnapshot` created no later than `PMRun.created_at`, not the earliest. 📏 **Measured on the live spine:** all 33 dates carry two or more fresh snapshots; on **6** (08-18, 08-19, 08-20, 09-04, 09-16, 09-24) the old rule read an intraday or pre-market probe, worst **$230**. A read-only re-report of `sched-2026-09-24`: equity 10,200,072 → **10,196,728**, excess −0.28 → **−0.27** pts, exposure-matched −0.17 → **−0.21 %**. Reporter laws **v1.3** (`RPT-OUT-07`, `RPT-IDM-03`). Red first (the 2026-09-24 case read 10,200,072); guard planted (a date-only bound fails the reproducibility test), restored. `make ci` exit 0 (**3,211 passed, 6 skipped, 100.00 %**); `GATE PROVEN` for `381ce4f818fbcec4a59f062638fb3c6bbd479954` (CI, CodeQL, Security Findings, attempt 1). 🟠 **Owed:** the reporter image retag (operator approval); stored Snapshots are not rewritten, the next report recomputes the series.

🟩 **MERGED — [S228](sprints/sprint-228-the-operator-sees-whether-the-book-beats-the-index.md), `0.112.00`, fast-forwarded to `d07be9e8`, tagged `v0.112.00`, 2026-09-25 ~19:30 AEST — work-queue 81 (P16) closed.** Built by the planner in a sibling worktree (operator: *"go ahead build it"*). `GATE PROVEN` for `d07be9e821ab72bc2870fd4ae3a3a7ebc00ed9da` (CI, CodeQL, Security Findings, attempt 1), read from the worktree at that SHA; the fast-forward makes it the merged SHA. `make ci` exit 0: **3,209 passed, 6 skipped, 100.00 %**; guards A3 (zeros for a missing group) and A5 (lookup by `snapshot:<run_id>`) planted, red, restored. 🩹 **The spec's "no law cycle" was stale:** S229 had locked `SRF-TRG-02` at five tools, so the surfaces book went to **v1.1** (`performance` tool, new `SRF-OUT-07`, a `PARAM` row; 28 / 35). **Also fixed:** "latest run" had two definitions in the dashboard, now one. 🟩 **Live, read-only:** `sched-2026-09-24` reads amber `vs SPY −0.28 pts · 32 sessions · 21 % invested` and the chat gives the same numbers in one sentence; a pre-scoreboard run reads *no scoreboard for this run*. 🪤 **Found:** the Snapshot's equity is the day's *earliest* broker snapshot, which on 2026-09-24 was the intraday manual run's ($33.44 off the scheduled run's post-close figure), filed as work-queue **88**. No deploy: no image ships `surfaces/`.

🟩 **DEPLOYED — `s230` = `b96f7a70` (`v0.111.03`), 2026-09-25 18:22 AEST — S230 plus S229 (docs and gate only). Operator approved (*"deploy, of course"*).** Build `36111780860` from the `v0.111.03` tag (`main` had a docs commit above the merge), **15 / 15** success with every entrypoint smoke green. **Image-only retag, path proven:** the three injected packs are byte-identical to `s227` (vocabulary `63d3a5eb`, credential-tests `97f083bf`, issuer-map `2ed1f41c`). 🟩 **Verified on live Azure, not from the update output:** **16 / 16** apps on `s230`, `Succeeded`, `Running`; scale blocks **JSON-identical** to the pre-retag snapshot (0 mismatches, every app `min=0` with one rule, master on `daily-master-window`); `dispatcher-cron` on `s230`, configuration identical, cron `*/10 22-23 * * 1-5`. `DeployRecord deploy:2026-09-25T08:22:26…:s230:b96f7a70…`. No execution replica was forced up, so `sched-2026-09-25` stays the first run to replace. 🟠 **Owed — the S230 functionality check on that run:** the 9 stops at their decided price (or a `StopReplaceRefused` warning where the price fell through, USB the closest), the old orders `replaced`, no `UnprotectedPosition`, `audit_broker_graph.py` A1/A2 clean, a second run replacing nothing; row in `functionality-checks.md`. Rollback: retag to `s227`.

🟩 **PROVEN — S230's live replace check, 2026-09-25 18:03 AEST (08:03 UTC), planner, through the adapter's own `replace_stop_order`.** A 1-share buy stop on F at $50 read `new` within 5 s; the PATCH returned a **new** order id with `replaces` = the old id and **the caller's `client_order_id`** (the builder's open question: Alpaca honours it); the old order read `replaced` with `replaced_by` set; the adapter mapped it to `pending`/`new`. Cancelled; **0** open F orders. `replaced` maps to `rejected`/reason `replaced`, as the paper broker models, and no consumer misreads it. **No adapter change.** 📏 **Replace list re-measured read-only** (`broker_stop_thresholds` → `_mismatched` + `_realign`'s guards): **9 of 25**, all `new`, none at or above the price — tighten AMZN/BAC/BMY/MDLZ/SCHW/USB, widen CSCO/DOW/META; 🪤 **USB's decided 58.06 sits 0.6 % under its 58.40 last**, so a small dip sells it (or, below 58.06 at run time, the guard refuses and keeps 57.47). No injected pack moved since `s227`; the `b96f7a70` image build is 15 / 15. Detail in the [S230 spec](sprints/sprint-230-a-stop-rests-where-the-pm-decided.md#sequencing-after-merge). 🟩 **Deployed as `s230`** (entry above).

🟩 **MERGED — [S229](sprints/sprint-229-every-component-that-decides-answers-to-a-law-book.md), `0.111.02`, fast-forwarded to `a372b48f`, tagged `v0.111.02`, 2026-09-25 — work-queue 86 closed.** `GATE PROVEN` for Codex's handback `d4fc1bbc`, and again for the merge commit `a372b48f` that `main` now points at (CI, CodeQL, Security Findings, attempt 1). **Planner review:** no product `.py` outside `scripts/` and tests; `check_law_coverage.py` is still 228 and `param_law_sync.py` 197 (both baselined); DRIFT-069's closure is legitimate, since it was the same no-law-book gap for the hold notice. **Merge repair:** `main` had moved three docs commits ahead; conflicts in STATE, the design log and the README were resolved keeping both sides; `make ci` exit 0 (**3,159 passed, 6 skipped, 100.00 %**). **Functionality check:** with `DSP-IDM-01`'s citation removed, the law gate exits 1 naming that row; restored, it exits 0. No deploy. 🪤 **S230 also bumped to `0.111.02`**, so its merge must re-bump to `0.111.03` and rebase over DL-221/222.

🟩 **MERGED — [S230](sprints/sprint-230-a-stop-rests-where-the-pm-decided.md), `0.111.03`, fast-forwarded to `b96f7a70`, tagged `v0.111.03`, 2026-09-25 ~15:55 AEST — not deployed.** `main` (with S229) was merged into the branch: only the README conflicted, DL-220–223 and DRIFT-070–074 are unique, and the version was re-bumped from `0.111.02` (taken by S229) to `0.111.03`. `make ci` exit 0 (**3,183 passed, 6 skipped, 100.00 %**), after rebuilding the worktree's `.venv`, whose launchers still pointed at the pre-move path. `GATE PROVEN` for `b96f7a70` (CI, CodeQL, Security Findings, attempt 1). 🩹 **The operator moved the live replace check from gating the merge to gating the deploy** (*"merge"*): a merge reaches no fleet. 🟩 **Live replace check proven 18:03 AEST** (entry above); 🟠 **owed:** the deploy, with operator approval.

📦 **BUILT — [S230](sprints/sprint-230-a-stop-rests-where-the-pm-decided.md), `0.111.02`, branch `sprint-230-a-stop-rests-where-the-pm-decided` @ `bc9327fb`, 2026-09-25 14:50 AEST — `GATE PROVEN` (CI, CodeQL, Security Findings, attempt 1), read from the builder's worktree at that SHA (moved to `../trading-agents-sprint-230-…` so the markdown lint stops reading its `.venv`); not merged.** Built by a local background agent: the `remote` option ran in a local worktree, so the cloud credit was **not** used. Builder: `make ci` exit 0, **3,178 passed, 6 skipped, 100.00 %**; three guard plants each went red and were restored. **Planner review:** the 34 files are all inside the spec's map; `reconcile.py`, the PM, tunables and S229's files are untouched; the resolver matches exactly (ticker, quantity, broker price, a filled `pm-run-*` buy) or says `fallback`; realign replaces only `new` orders, never cancels, refuses at or above the snapshot price, and `replaced`/`replaced_at` end liveness in the one place it is asked. **Measured live:** the builder's flagged assumption holds, because the account has **288** orders in total, inside the 500-order window, and all 25 open stops appear there as `new`. No declared `Fill` property, so the deploy is a rebuild and retag. 🟠 **Owed before merge:** the live replace check on a `new` throwaway order at or after 08:00 UTC (18:00 AEST); a rebase after S229 (DL-223, DRIFT-073/074 to re-check); then the deploy, with operator approval and the replace list re-measured on the day.

🔴 **FOUND + SPECCED — [DL-222](design-log.md) / [S230](sprints/sprint-230-a-stop-rests-where-the-pm-decided.md), 2026-09-25: every protective stop rests at the 5 % fallback.** The operator asked why losing stocks were not sold: by ADR-0017 only the stop or a thesis collapse exits, and neither fired (all 25 held names scored 0.56–0.69 against a 0.50 floor; worst, USB −3.47 %, 1.59 % above its stop). Measuring the stops found all **25** at **5.00–5.02 %** while the PM decided 3.90–7.29 % for the buys since DL-156's 2026-09-05 flip: broker-adopted Positions carry `default_stop_pct` and every stop is placed from one. **9 of 25** differ (6 tighten, 3 widen); 25/25 holdings equal their most recent buy exactly, so the lineage match is exact. 🪤 **A paper probe measured that Alpaca refuses to replace an `accepted` order (422)**; the order was cancelled, 0 left. Not rushed into the 2026-09-26 run: nothing gets worse, and replacing live stops needs the operator's approval at deploy.

📦 **BUILT — [S229](sprints/sprint-229-every-component-that-decides-answers-to-a-law-book.md), Codex branch worktree, 2026-09-25 12:19 AEST.** Worktree: `C:\Users\yury_\Downloads\project\trading-agents-sprint-229-every-component-that-decides-answers-to-a-law-book` on branch `sprint-229-every-component-that-decides-answers-to-a-law-book`. Dispatcher and surfaces law books are LOCKED v1 and wired into law coverage/PARAM gates (`DSP` 25 / 30, `SRF` 27 / 34); DRIFT-068 and DRIFT-069 are CLOSED; DL-221 recorded the non-agent law-root rule; DL-70 plants proved `make ci` fails on a missing `DSP` citation and mismatched `SRF` PARAM `Tunable` cell. Local `make ci > ci.txt` exit 0: **3159 passed, 6 skipped, 100.00 %**; dependency audit accepted only the existing diskcache advisory; tracked and untracked detect-secrets passed. Pre-evidence remote CI, CodeQL and Security Findings succeeded on attempt 1 for `a9cc00ddd371737d98f7abecdc4017d2310f74ee`; this evidence update must be re-proven after push, with the final exact SHA reported in handback. No merge, deploy, or live proof.

📦 **SPECCED — [S229](sprints/sprint-229-every-component-that-decides-answers-to-a-law-book.md) and [S228](sprints/sprint-228-the-operator-sees-whether-the-book-beats-the-index.md), 2026-09-25 — ready for Codex in that order.** **S229** (work-queue **86**, operator: *"a quick sprint to cover this gap before we go further"*): all **14** agents have LOCKED law books, but the **dispatcher** (DRIFT-068) and the **surfaces** have none, and `law_coverage_docs.py:26` / `param_law_sync_sources.py` look only under `agents/`, so a book elsewhere would sit outside `make ci`. Two books (`DSP`, `SRF`), reconciled against the code, divergences recorded not fixed, the gate taught both roots; PATCH, no deploy. **S228** (E16.2, work-queue 81): a *vs SPY* vital and a deterministic *"vs the market"* chat answer from the Snapshot. 🩹 **Re-cut ([DL-220](design-log.md)):** there is no nightly Telegram notice to add a line to, so the Telegram line and the `RPT-SEC-02` question move to E19.1.

🟩 **FIXED — `main` was red from a test time bomb in S227, 2026-09-25 10:30 AEST.** `test_normal_run_still_waits_then_blocks_buy_after_grace` pinned `PMRun.created_at` to 2026-09-24 22:30 UTC while `find_pending` reads the wall clock, so from 22:45 UTC every CI run failed (first seen on `ed70c18f`), and the sibling degraded-run test passed without exercising the bypass. Chore `chore-s227-test-reads-the-clock`: anchored on real now; **guard proven** (posture flipped to `normal` → `assert [] == ['pm-run-fixture']`, restored); `make ci` exit 0 (**3,154 passed, 6 skipped, 100.00 %**); `GATE PROVEN` for `238ca5c2`; `main` = `238ca5c2` + the S229 spec (`5f6ad11e`), **CI, CodeQL, Security Findings and image build all success**. Test-only, no bump.

🟩 **FLEET DOWN AFTER THE RUN, SCHEDULE KEPT — [DL-219](design-log.md), 2026-09-25 09:30–10:40 AEST.** The operator asked to bring the fleet down after `sched-2026-09-24` finished (**8/8**, PM approved 0 of 26, no orders, monitor held 25). **The documented pause failed three ways:** `az resource update` on the job is rejected (`ContainerAppSecretInvalid`, write-only secrets); `desiredReplicas=0` cannot drain an open KEDA window (**16/16** still up 20 min later); and the runbook listed 13 of 16 apps. **What worked:** the Container Apps `stop` action (0 replicas in ~30 s) and a trigger-only `PATCH` on the job. The operator's correction (*"NEXT RUN is not showing anything. Just put next run date there"*) meant pods down, not runs paused, so the dispatcher went back to `*/10 22-23 * * 1-5` and the apps were started after the window closed. 🟩 **Verified:** 16/16 `Running`, scale blocks **JSON-identical** to the pre-change snapshot (0 mismatches), job trigger `Schedule` with its original cron, timeout and retry. `infra/status.ps1` now dates NEXT RUN from the dispatcher's `_ACTION_START` (**Sat 26 Sep, 08:30 AEST**) and says *paused*/*stopped*; `deployment.md` carries the measured procedure (`ed70c18f`).

🟩 **PROVEN — S226's owed first production `Snapshot` with `performance_metrics`, 2026-09-25.** `manual-2026-09-24` and `sched-2026-09-24` both carry `metrics.performance` (12 keys): excess **−0.28** pts over exposure-matched SPY, portfolio **−0.45 %**, SPY **−0.91 %**, exposure **21 %**, drawdown **−0.94 %**, **32** sessions, 0 gaps; headline *"vs SPY: -0.28 pts over 32 sessions at 21% invested"*. The two are identical because both have the UTC as-of date 2026-09-24 (`RPT-IDM-03`). 🪤 The Snapshot key is `snapshot:pm-run-<hex>`, not `snapshot:<run_id>`, a trap S228 names. 🟠 **Still owed:** the first run with an approval, which proves the debate on the restored key.

🟩 **MERGED — [S227](sprints/sprint-227-an-llm-outage-holds-the-buys-not-the-run.md), `0.111.00`, fast-forwarded to `a4ab16d5`, tagged `v0.111.00`, 2026-09-25 00:40 AEST.** `GATE PROVEN` for `a4ab16d5` — CI, CodeQL and Security Findings, all success on attempt 1, read from the worktree at that SHA. The planner rebased onto `96aad94d` before gating (a STATE header conflict, both sides kept) and reviewed the diff: `held_degraded` drops buys and keeps sells, `unknown` still holds, degradable agents are pack-declared, and the dispatcher image copies the new pack. 🟠 **Not deployed; live proof owed** — the first real LLM-only outage has to place a degraded run. *Builder's entry follows.* 📦 **BUILT — 2026-09-24 14:40 AEST — work-queue 85, [DL-214](design-log.md), [DL-217](design-log.md), Codex worktree `trading-agents-sprint-227-llm-outage-holds-buys`.** The law-reading record was filled before source edits, the four required design decisions were recorded, and local `make ci` is green (3154 passed, 6 skipped, 100.00% coverage). When every failing fleet check belongs to a pack-declared degradable agent, the dispatcher places a degraded run (sync, fills, stops, monitor) and execution drops buys without the 900 s grace; anything else still holds. Execution law `v1.8` adds/proves `EXEC-NEV-07`; DRIFT-068 is appended because the dispatcher still has no law book. Remote gate, merge, deploy, and live proof are not done.

🟩 **DEPLOYED — `s226a`, 2026-09-24 14:16 AEST — S226 (`0.110.00`) + DL-216 (`0.110.01`), image-only retag from `v0.110.01` (`623e645c`).** Operator approved (*"deploy."*). No injected pack moved since `s225` (`fe26574e`): vocabulary `63d3a5eb…` both sides, credential tests and issuer map 0 lines changed. Build run `35954204243`, **15/15 jobs success**. **Verified against live Azure:** **16/16** apps on `s226a`, **16/16** `Succeeded`, every app `min=0` with one KEDA rule, master kept `daily-master-window`, `dispatcher-cron` on `s226a` with cron `*/10 22-23 * * 1-5`. `DeployRecord deploy:2026-09-24T04:16:32…:s226a:623e645c…`. 🟠 **Owed:** the first production `Snapshot` with `performance_metrics`, and the first Telegram notice with *Melbourne* time — both need a run, and runs are held until the key returns.

🟩 **MERGED — [S226](sprints/sprint-226-a-run-says-whether-the-book-beat-the-index.md), `0.110.00`, fast-forwarded to `e8959675`, tagged `v0.110.00`, 13:50 AEST 2026-09-24 — the next leg's first sprint (work-queue 81, E16.1).** The reporter recomputes benchmark-relative performance from `BrokerPositionSnapshot` and as-of-bounded `MarketData`, exposes `RunSnapshot.performance_metrics`, and adds the headline clause; no number reaches a decision or a surface (no consumer outside the reporter, checked). **Planner review:** `GATE PROVEN` for the handback SHA `c433c414`; scope = reporter, its law book (LOCKED **v1.2**: `RPT-OUT-07`, `RPT-IDM-03`, `RPT-FAIL-04`, amended `RPT-ORD-01`), one contract field, docs — no security-baseline edit; MINOR bump correct. The live re-report of `sched-2026-09-22` reproduced the spec's **planner-measured** values to 0.0001 pts — portfolio **−0.464357 %**, SPY **+0.054332 %**, exposure-matched **+0.039868 %**, excess **−0.504225 %**, exposure **21.107334 %**, drawdown **−0.863749 %**, 30 sessions, 0 gaps — an independent check, not the builder grading itself. 🩹 **Merge repair:** `main` had moved three fixes ahead; merged into the branch, version kept `0.110.00`, and the builder's **DL-210 renumbered DL-215** (main had given DL-210 to the drain). Redirected `make ci` on the merge exit 0 — **3,131 passed, 6 skipped, 100.00 %** — and `GATE PROVEN` for the merge commit `e8959675` itself, which is the SHA `main` now points at. 🟠 **Owed:** the image-only retag (operator approval), then the first production `Snapshot` carrying `performance_metrics` — which needs an un-held run, so `sched-2026-09-28` at the earliest.
📌 **FILED — work-queue item 85 / [DL-214](design-log.md), 2026-09-24 12:40 AEST: a degraded run for LLM-only outages, ranked by the operator next after S226 is merged and checked in.** The S218 gate holds the whole run when only LLM agents fail, so fills, broker sync and stop placement stop too — BMY can fill tonight with no stop until `sched-2026-09-28`. It partially reverses item 58, so the spec asks the operator.

🟩 **MERGED — [DL-212](design-log.md) `0.109.07` (`759fe23e`) and [DL-213](design-log.md) `0.109.08` (`918cdd61`), 2026-09-24 — the chat and the banner say what the fleet check says, for as long as it is true.** `GATE PROVEN` for each SHA (CI, CodeQL, Security Findings, attempt 1); `make ci` exit 0 — **3,109** then **3,114 passed, 6 skipped, 100.00 %**. **Operator's screenshot** showed *System status* → *System health is green.* and *Explain this run* → raw SDK JSON. DL-212: status leads with the fleet-check line; vendor errors read *The language model refused the request (HTTP 400): …*. **Verifying it live found DL-213:** the master checks only inside 20:25–00:30 UTC, so the 00:26 failure went stale for status after 70 min and would have left the banner at ~03:26 UTC, ~17 h before the next check. Both now hold the last result for a day, and the banner names *next check 06:25, when the master wakes*. **Proven live after restarting the local dashboard at 12:2x:** *System status* → *Tonight's run will be held unless the next fleet check passes: anthropic answered HTTP 400 — 4 agents can't start: …* / *System health is green.*; banner `failing`, checked 10:26, next check 06:25 Melbourne.

🟩 **MERGED — [DL-211](design-log.md): a failing fleet check is a page alert about tonight's run, not the selected run's verdict. `0.109.06`, fast-forwarded to `34dedfa2`, tagged `v0.109.06`, 11:45 AEST 2026-09-24.** `GATE PROVEN` for `34dedfa2` — CI, CodeQL and Security Findings, all success on attempt 1. `make ci` exit 0 by file redirection: **3,104 passed, 6 skipped, 100.00 %**. **Operator asked to move the message and why nothing else was screaming.** Measured cause: S218 wired the check into the verdict hero only, overwriting `sched-2026-09-23`'s headline with a machine string; the next-fire chip read the cron, not the gate; and a failing preflight is a forecast, so no `Fault` is written and Telegram waits for the 22:30 UTC hold (deliberate). **Now:** a red banner above the hero — *Tonight's run will be held unless the next fleet check passes* / *anthropic answered HTTP 400 — 4 agents can't start: …* — with last-check time and the hold-answer buttons; the hero keeps its run's own verdict; the next-fire chip and hero read *— will be held* in red; **every dashboard time is Melbourne 24-hour**. **Proven on live data before the merge** (worktree dashboard on the live spine, headless render): banner as above, chip *next fire 2026-09-25 08:30 — will be held*, hero *1 order placed, none filled yet*. No deploy — the dashboard runs locally (restarted on the merged code).

🟩 **DEPLOYED — `s227` = `3b960066` (`v0.111.01`), 2026-09-25 01:06 AEST — S227 plus the [DL-218](design-log.md) fix it needed.** 🔴 **S227's merge build failed on 1 of 15 images:** the dispatcher smoke, because the image copied `orchestration/packs/__init__.py` without the `us_equities_sp500.py` it imports. Every dispatcher fire would have died at import and placed no run at all. The S195 closure test skipped parent-package `__init__` files; it now reads their top-level imports, and red-first it named exactly that one file. **Reproduced without Docker:** a slim copy of the Dockerfile's `COPY` set raised the build's `ModuleNotFoundError` on the old Dockerfile and printed `skipped sched-2026-07-04` on the new one. Chore `chore-dispatcher-image-imports-packs-init`: `make ci` exit 0 (**3,154 passed, 6 skipped, 100.00 %**), `GATE PROVEN` for `3b960066` (CI, CodeQL, Security Findings, attempt 1), fast-forward merge, tag `v0.111.01`. **Deploy — image-only retag, path proven:** the three injected packs are unchanged since `623e645c`, and the new `trading_run_postures` pack is baked into the image. Build `36016627425` pushed **15/15** at `s227` with the dispatcher smoke green. 🟩 **Verified, not inferred:** 16/16 apps on `s227`, 16/16 `Succeeded`, scale blocks JSON-identical to the pre-run snapshot, job on `s227` with cron `*/10 22-23 * * 1-5` unchanged, `DeployRecord` written by `record_deploy.py`. **Live image smoke:** a manual job start at 15:06 UTC logged `dispatcher:s227 … skipped sched-2026-09-24` (outside the action window, as designed): the import path that failed in the build now loads in Azure. 🟠 **Owed:** tonight's 08:30 `sched-2026-09-24` places a **normal** run behind a passing check; the degraded path's first live proof waits for a real LLM-only outage. Rollback: retag to `s226a`.

🟩 **LIVE — `manual-2026-09-24`, a planner-fired full run, 2026-09-25 00:14–00:26 AEST (14:14–14:26 UTC, US market open).** The operator asked for a full fleet run after the API top-up. **Path:** baseline scale snapshot → forced `master` to one replica, which wrote `FleetPreflight` **14:08:20 UTC `passed=True`**, the first pass since the drain → forced the 15 agents up → `az containerapp job start dispatcher-cron` returned `skipped`, because the dispatcher only places runs between 22:30 and 23:20 UTC (`scheduled_dispatch_actions._ACTION_START`), so the planner wrote the `RunRequest` directly (`place_run_request`, the same 99 tickers as `sched-2026-09-23`, a separate id so tonight's `sched-2026-09-24` is unaffected). **Result:** `trace_run.py` **8/8**, `accept.py` **`ACCEPTANCE PASS`**, **0** `Fault` rows since 14:05 UTC. PM approved 0 of 26 (25 `hold_recommendation`, 1 `sizing`), so deliberation read `reviewed=0 status=applied`: **the LLM debate was not exercised; only the agents' startup credential check was.** 🎯 **DL-210's BMY exposure is closed:** BMY 16 filled at the open at **$61.40** (13:30:21 UTC, limit 61.82), and this run's execution placed its stop — **sell stop 16 @ $58.33** at 14:24:28 UTC. Broker: **25 positions, 25 open stop orders**. 🟩 **Scale restored:** all 16 apps back to `min=0`, and the scale blocks are identical to the pre-run snapshot (compared as JSON, not eyeballed). 🟠 **Still owed:** the first run with an approval, which proves the debate itself on the restored key.

🟢 **KEY RESTORED — [DL-210](design-log.md), 2026-09-25 00:03 AEST.** The operator topped up the API account (Console balance **USD 19.96**, auto-reload off). **Measured:** a one-token `messages.create` on `ANTHROPIC_API_KEY` returns **HTTP 200**, and Key Vault `anthropic-api-key` is byte-equal to the `.env` key. The last `FleetPreflight` (04:16 UTC) still reads failing on `anthropic http_400`, because it predates the top-up. The master checks again at 20:25 UTC (06:25 AEST). 🟠 **Owed:** that check reads `passed=True`, and `sched-2026-09-24` (08:30 AEST) runs with `real_debate_count > 0`.

🛑 **HARD STOP ON THE LLM — [DL-210](design-log.md), 2026-09-24 11:06 AEST.** The operator reported the run failed on the Anthropic access limit, reset Sunday 2026-09-27. **Measured:** a one-token call on `ANTHROPIC_API_KEY` returns `400 … credit balance is too low`; `sched-2026-09-23` was clean (8/8, `real_debate_count=1`, 0 fail-open, 0 faults), so the key drained after 22:41 UTC. 🚨 **Unlike DL-99 this is not a fail-open — the run is held.** The hourly `FleetPreflight` fails on `anthropic` for `operator` and the three deliberator peers, and the deployed dispatcher (S218) holds any run whose latest preflight did not pass: no buys, no monitor, no position sync; the dashboard's *Fleet check failing (4)* is true. *(Corrected 11:32 — the first entry said buys would submit unvetoed.)* 🪤 **BMY 16 @ 61.82 (day) is already at the broker** and can fill at 23:30 AEST tonight; its stop would wait for `sched-2026-09-28`. 🟠 **Owed:** re-probe the key before `sched-2026-09-28` (the error names no reset date) and confirm that run's `DeliberationRun` has `real_debate_count > 0`.

🟩 **MERGED — [DL-207](design-log.md) + [DL-208](design-log.md): the dashboard stops contradicting itself. `0.109.05`, fast-forwarded to `c5ece8c4`, tagged `v0.109.05`, 15:37 AEST 2026-09-23.** `GATE PROVEN` for `c5ece8c4` — CI, CodeQL and Security Findings, all success on attempt 1. `make ci` exit 0 by file redirection: **3,102 passed, 6 skipped, 100.00 %**. **Operator asked "can you fix the dashboard", then "Wrong query??".** (1) A headless render showed the verdict card saying *none filled yet* while the header said **✓ RUN PASSED**, the result box *Run result: passed* and the nav *run passed* — `app.js` keyed all three on `passed`, which UNPROVEN keeps true by design (DL-59). Now amber **AWAITING FILLS** / *Run result: not proven yet*. (2) The *Resume from …* buttons rendered as white browser defaults — only visible since the chat binds again (the local `anthropic` repair); now styled like *Acknowledge*. (3) The chat's **Open incidents** returned **6,439** lines — every Fault since 2026-07-23 — while *Status* said health is green and the kernel counted **0**: `open_faults` never adopted `live_fault_incidents`. Now one definition, test-pinned to `system_health`. **Proven live after the merge:** header *AWAITING FILLS*, styled buttons, *Open incidents* → **"No open incidents."** (223 bytes vs 414,863). 🧹 **One operator action taken on "go ahead":** a *Resume from …* click at 15:07 AEST had left a pending broker-consequence confirmation flag (`b0fc97911b038786`); it was **dismissed, not confirmed**, via the audited CLI `approve` (resolves the flag only) — RunRequests stayed **73**, ResumePlacements **0**, dashboard back to 0 pending flags. No deploy needed.

🟩 **MERGED — [DL-206](design-log.md): the dashboard's schedule is read from its sources, not remembered. `0.109.04`, fast-forwarded to `01dc96b6`, tagged `v0.109.04`, 14:51 AEST 2026-09-23.** `GATE PROVEN` for `01dc96b6` — CI, CodeQL and Security Findings, all success on attempt 1. `make ci` exit 0 by file redirection: **3,099 passed, 6 skipped, 100.00 %**. **Found by hunting DL-205's defect class** (a dashboard copy of a fact that lives elsewhere): `aea47810` moved the master to **20:25 UTC** and made the dispatcher a ten-minute ticker on 2026-09-20, and the dashboard noticed neither. Its run windows opened at 22:25, after the master's startup and the 20:25/21:25 fleet preflights (**381** master log rows on `sched-2026-09-22`); from 06:25 to 08:25 Melbourne the "latest window" was yesterday's; *next fire* promised a Saturday run every Friday night (the old test pinned it); and the cron row read **23:50** for a run placed at **22:30:14**. **Now:** the four schedule settings are test-pinned to `deploy-agents.ps1` and the dispatcher's `_ACTION_START`; next fire walks the dispatcher's own session calendar; the cron row shows the placing tick. **Proven live, after the merge:** master window **20:25–00:30 UTC**, cron row **22:30 Succeeded**, next fire today 22:30 UTC (and **Mon 28 Sep** after Friday's run, not Saturday), verdict GREEN with one warning; deploy currency still **current** against this merge's own image build — a second fresh pair for DL-205. No deploy needed.

🟩 **MERGED — [DL-205](design-log.md): a deploy warning names code the fleet runs, and the stage count reads the pack. `0.109.03`, fast-forwarded to `bdb66d79`, tagged `v0.109.03`, 13:40 AEST 2026-09-23.** `GATE PROVEN` for `bdb66d79` — CI, CodeQL and Security Findings, all success on attempt 1 — and the fast-forward makes the merged SHA the proven one. `make ci` exit 0 by file redirection: **3,092 passed, 6 skipped, 100.00 %**. **Why:** on the clean `sched-2026-09-22` the dashboard showed two warnings and neither was true — *"Fleet deploy currency is behind"* (nine `laws.md` edits and a version bump since `s225`, no runtime change) and *"9/7 stages reached — needs attention"* (a hard-coded 7; the pack has 9; all **41** scheduled runs since 2026-07-27 read it, **8** of them no-trade days). **What changed:** a newer main build reads *behind* only when a file the fleet runs differs — two GitHub tree reads over the build workflow's own trigger paths, minus markdown, `tests/` and a version-line-only lock bump; a failed read is *unverified*; three tests pin the rule to the workflow, every Dockerfile `COPY` source, and "no image code names a markdown file". The stage total is the run's own stage list. **Proven live, after the merge:** `s225` → `bdb66d79` (this merge's own image build) reads **current, 0 runtime changes**; `7039bfb` → `s225` reads the **7** execution files S225 really changed, so the tripwire still fires; verdict **GREEN** with one warning (orders queued for the open), pipeline **9/9 good**. **No deploy needed** — the dashboard runs locally and nothing in the images changed. 🪤 **Side find:** the main checkout's `anthropic` package had lost its top-level files (last written 2026-09-19 11:23) and crashed the dashboard at start; reinstalled at the locked `0.120.2` — the local environment only, never the fleet.

🟩 **MERGED — [item 22 Part B](work-queue.md), `0.109.02`, fast-forwarded to `889ea9fe`, tagged `v0.109.02`, 09:05 AEST 2026-09-23 — the item closes.** `GATE PROVEN` for `889ea9fe` — CI, CodeQL and Security Findings, all success on attempt 1 — and the fast-forward makes the merged SHA the proven one. `make ci` exit 0 by file redirection: **3,060 passed, 6 skipped, 100.00 %**; `gate-selftest` **31/31**. 🚨 **The migration as specified would have written false answers:** of the 190 docs S224 classified, **90** declared a state git contradicts (69 `SPEC`, all 21 `BUILT` — every one merged), and the README was wrong the other way (S205/S207/S223 read `SPEC`). Each was settled against `origin/main`; **124** status lines now cite the merge, tag or hand-read commit beside the original text. `check_sprint_status.py` reports **230 docs: 220 `MERGED`, 10 `SPEC`, 0 refusals**, so the synonyms, the 20 + 20 baseline and `status-unmapped.md` are deleted, and the gate fails on any non-canonical token, missing line, missing README row or README disagreement ([DL-204](design-log.md)). **No deploy:** `scripts/` and `docs/` ship in no image.

🟩 **MERGED — [item 33](work-queue.md), `0.109.01`, fast-forwarded to `9d0420b9`, tagged `v0.109.01`, 05:42 AEST 2026-09-23.** `GATE PROVEN` for `9d0420b9` — CI, CodeQL and Security Findings, all success on attempt 1 — and the fast-forward makes the merged SHA the gated SHA. S187 had baselined **57** PARAM/settings divergences as warnings; re-measured first, still exactly 57. **54** settings fields gained rows copied from their own declarations (the seven provider credentials as `NO (secret)`, by name only — the execution law's existing precedent); **2** execution rows were fixtures retired in 0.73.01 and were deleted; `issuer_map` left the PM table as pack data. `LEGACY_BASELINE` is **deleted, not emptied**: a divergence now fails `make ci`, and the check prints only S223's two envelope warnings (it printed 59). `make ci` exit 0 on the committed tree (**3,061 passed, 6 skipped, 100.00 %**), gate self-test **31/31**. Nine laws took `PARAM`-only amendments; no clause count moved. `param_law_sync.py` fell 212 → 197, so the size ratchet lists **15** files, not 16. [DL-203](design-log.md). **No deploy needed** — no runtime code changed; the merge started the usual image build. 🪤 **One slip, corrected within minutes:** a chained command ran past a failed step and pushed `v0.109.01` onto the pre-merge `561e42ca`. The tag was deleted and re-cut on `9d0420b9`; tag pushes trigger no workflow, so nothing was built from it.

🟩 **MERGED — [item 78](work-queue.md), `0.109.00`, merged `1ee5a7d2`, tagged `v0.109.00`, 00:41 AEST 2026-09-23.** `GATE PROVEN` for `3d088bee` — CI, CodeQL and Security Findings, all success on attempt 1. `check_module_size.py` ran on `$(PKGS) tests`, so `scripts/` was never checked although `check_module_header.py` *was* covering it. 📏 **Re-measured and the row had drifted in two days:** **16** files at or over the block (17 counting one exactly on 200, which fails at `>=`), largest **486** not 411, **23** in the warn band not 6, folder grown 83 → 93 files. 🪤 **I had demonstrated it myself hours earlier** — `replay_dataset.py` shipped at **205** lines in the item-64 chore and nothing objected. 🎯 **Built the reverse of the row's plan:** it said split 16 files *then* widen the gate, which leaves the hole open for the length of a large refactor while the list grows — exactly what happened between the row being written and read. The scope widened **first**, with `scripts/module_size_baseline.py` freezing each offender at its measured count: a listed file may shrink, **never grow**, must be **deleted** once under 200, and must still exist. 🎯 **Every line in that baseline can fail the gate** — that is the difference from item 33, whose 57 rows warn and pass, which is why nobody reads them ([DL-202](design-log.md)). 🟩 **Two files split rather than baselined:** the new self-test case pushed `gate_selftest_cases.py` to **503** and the ratchet **refused it**, so it split into cases **346** + invariants **146** + types **37** (the DL-198 shape), and `replay_dataset.py` went **205 → 142** and left the baseline. Proven by `module-size-reads-scripts` + two wiring invariants (**31/31**) and 8 unit tests; `make ci` exit 0, **3060 passed**, **100.00 %**. 🟠 **Nothing to deploy** — gate tooling ships in no image, so the fleet stays on `s225`.

🧪 **MEASURED — [EXP-013](research/experiments/EXP-013-regime-scaled-sizing-replay.md), 2026-09-22, $0 — work-queue item 64 closed without building anything.** 🐛 **Its remaining task named a quantity that no longer exists.** ADR-0031 (2026-09-20) gave the regime the **risk budget** and said it attaches *“when that lands”*; [ADR-0025's Correction](decisions/0025-concentration-measures-the-book-position-risk-measures-the-capital-base.md) had withdrawn risk-based sizing **two days earlier** on [EXP-012](research/experiments/EXP-012-volatility-sizing-ten-year-replay.md) (−$11,619, 100 % of resamples negative) — and **ADR-0031 cites EXP-012 zero times** ([DL-201](design-log.md), ADR-0031 Correction). 🧪 **So EXP-013 measured the only live question** — should the regime scale the *notional* cap, the sizing knob that exists — over **47,533** decisions, 2017-2026, on the rebuilt EXP-011 dataset. 🟩 **The label is not stuck** (409 changes in 2,475 sessions, all five labels used), 🪤 **but its median run is 2 sessions against a 10-session horizon.** 🎯 **Forward return rises with stress** — `neutral` **0.287 %** → `extreme_volatility` **1.880 %** — **and so does return per unit of dispersion** (0.057 → 0.217), with `neutral` the worst bucket on both. Every less-risk-when-stressed arm loses: mild **−1,818**, strong **−3,157** pct-points, bootstrap **95 % CI [−3,699, −2,574], 100 % negative**. It is the same return gradient that killed iso-risk, one level up. 🟠 **The regime label stays as evidence, not as a multiplier**; nothing shipped, and confirming that as policy is capital-risk and remains the operator's. 🪤 **Biggest caveat, and it cuts against the finding:** survivorship — today's 98 names back-projected through every panic they survived. 🧹 **Fixed while here:** EXP-011 and EXP-012 both left their loader in a markdown appendix and their data in a scratch dir, so ~263k bars had to be re-pulled to reproduce a published number. `scripts/replay_dataset.py` now builds and loads the cache, which lives in **OneDrive** — 🚨 never the repo: it is **public** and Alpaca SIP bars are licensed.

🟩 **MERGED + DEPLOYED — [S225](sprints/sprint-225-a-stop-says-which-lineage-protected-it.md), `0.108.00`, merged `fe26574e`, tagged `v0.108.00`, fleet `s225` at 23:44 AEST 2026-09-22 — work-queue item 27 closed.** `GATE PROVEN` for `96ac04ef` on the branch and again for `fe26574e` on `main` — CI, CodeQL, Security Findings and the image build, all success on attempt 1. 🎯 **The item was not blocked on a rare event; it was blocked on a field that could not answer it.** Item 27 owed a live proof that S182's pending-`Fill` stop path had ever fired, and 2026-08-21 was read as *“the fallback never fired”* because the stops carried `stop_pct_source=position`. That field names where the stop **percent** came from; `filled_entry_stops` reports `position` too whenever the `OrderIntent` carries a `stop_pct`. 🐛 **Nothing else discriminated, by design** — the Fill path deliberately computes the *same* `position_ref` the monitor will later write, so a later `Position` cannot double-place ([DL-118](design-log.md)). 📏 **Measured by recovering the `position_ref` preimage** (it is `sha256(sorted Position keys)[:16]`): **55 of 55** stops ever placed hash from **broker-adopted** `Position` keys, **0** from the keys a Fill-path stop would produce, **23** of them placed after S182 deployed. 🚨 **So that path has never fired in production, and the margin is measured, not assumed:** on `sched-2026-08-21` the `broker:*` Positions for INTC/NEE/XOM were adopted at **22:31:52** and their stops placed at **22:40:02–03** — same run, ~8 minutes apart. Adoption only needs the run-start snapshot; execution's stop settling waits behind PM sizing and the deliberation grace window. 🪤 **It is a race, not an enforced ordering** — a degraded snapshot, a slower monitor, a shorter grace window or a fill landing after adoption would each flip it, which is why the S182 path stays rather than being retired as dead code. 🟩 **Fixed by recording:** every placed stop writes `derived_from` (`active_position` \| `pending_fill`) on both the `BrokerStopOrder` fact and the stop `Fill` ([DL-200](design-log.md)); [DRIFT-072](laws/drift-register.md) records that `EXEC-OBS-03`'s *“fully reconstructable”* read 🟩 on nine tests while this limb had none. Four new tests cite the clause; both guards were planted, watched to fail, restored. 🟩 **Deploy verified, not asserted:** full `up` because the vocabulary pack moved `579a40a3` → `63d3a5eb` while the other two injected packs were byte-identical; 16/16 apps on `s225`, 16 `Succeeded`, KEDA rules and the `*/10 22-23 * * 1-5` dispatcher cron intact, and `GRAPH_VOCABULARY_B64` decoded on execution/monitor/master to the repo's `63d3a5eb` — the S202 currency check, passed. 🟠 **Owed:** no production run has written `derived_from` yet; the first is tonight's `sched-2026-09-22` (22:30 UTC = Tue 08:30 Melbourne), and that is the functionality-check row.

## Recent (most recent first — detail in each sprint doc)

🗄️ **Older shipped results — S217–S223, S207 and the item-74 advisory chore (the fleet preflight and run hold, the Telegram notice, the correlation ramp, the evidence envelopes, the `s219`/`b18014e`/`7039bfb` deploys and `sched-2026-09-21`'s three proofs) are now in [state-archive/STATE-14.md](state-archive/STATE-14.md).**

🗄️ **Older shipped results — S208–S216 (the reward-risk and revise/overturn fixes, the stop-that-fired repair, the regime VIX input, the LF sweep, the `s208`/`s209`/`s214`/`s215` deploys) are now in [state-archive/STATE-13.md](state-archive/STATE-13.md).**

🗄️ **Older shipped results — item 6b's closure with the `binding` posture's proof, the `s219a` image-only retag, item 70's live input proof, item 73's markdown-rule closure and its link remainder, and the `pip-audit` advisory that turned `main` red are now in [state-archive/STATE-12.md](state-archive/STATE-12.md).**

🗄️ **Older shipped results — S206's rendered-verdict repair, its handback verification and the `s206` deploy are now in [state-archive/STATE-12.md](state-archive/STATE-12.md).**

🗄️ **Older shipped results — S205's type-clause rewrite and the S204 verification block are now in [state-archive/STATE-12.md](state-archive/STATE-12.md).**

🗄️ **Older shipped results — S202, S203 and the work-queue item 47 promotion are now in [state-archive/STATE-11.md](state-archive/STATE-11.md); S199, S200, S201 and the item-6b posture decision in [STATE-10.md](state-archive/STATE-10.md); S172, S173 Part A, S180, S191, S195–S198 and the CodeQL repair in [STATE-09.md](state-archive/STATE-09.md); everything earlier in [state-archive/INDEX.md](state-archive/INDEX.md).**

🗄️ **Older shipped results — the late-August deliberation/sizing residue (ADR-0023's PM half, the posture flip conditions, the 73 % veto figure, S182/S184 and the NFLX residue) are now in [state-archive/STATE-12.md](state-archive/STATE-12.md).** 🪤 The 73 % figure there is superseded by [DL-173](design-log.md) / [ADR-0029](decisions/0029-a-revise-is-a-finding-an-overturn-is-a-block.md).

## Next

**Ranked queue of record: [work-queue.md](work-queue.md)** — this section is the narrative around it, not a second ranking.

🎯 **2026-09-23 16:40 AEST — the next leg is ACCEPTED ([next-leg-plan.md](next-leg-plan.md), [DL-209](design-log.md)); ranked as work-queue rows 81–84.** **[S226](sprints/sprint-226-a-run-says-whether-the-book-beat-the-index.md) is merged and deployed (E16.1); item 85 shipped as S227.** Next is work-queue **item 86** ([S229](sprints/sprint-229-every-component-that-decides-answers-to-a-law-book.md), law books for the dispatcher and the surfaces — operator, 2026-09-25), then E16.2 ([S228](sprints/sprint-228-the-operator-sees-whether-the-book-beats-the-index.md); its Telegram line moved to E19.1, [DL-220](design-log.md)), then P17's E17.1 measurement spike. Item **71** stays parked and item **75** queued, both by operator direction. The narrative below is the 2026-09-15 review, kept as written.

🎯 **Re-ranked 2026-09-15 against the live spine, then re-counted after S205 merged.**
**14 items open** against the operator's **Friday 2026-09-18** empty-by date. The review put **item 59**
first and displaced everything else for one reason: the referee **vetoes 100 % of what it reviews**, and every
order that has reached the broker since 2026-09-01 went through on a night it was blind — so the etalon bar
cannot be met while it stands ([DL-167](design-log.md)). It also filed **60, 61, 62** off the nine-gate PM
audit: `reward_risk` is 2.0 on 170 of 170 recommendations against a 1.5 threshold, the correlation gate has
never once failed, and sizing caps dollars rather than risk. S205 then closed **item 30**, so of the rows that
were "mostly not code" a week ago only **33** (57 PARAM/settings divergences), **22** (a sprint's built state
is not machine-checkable), **31** (CodeQL runs only on `main`), **7** (the duplicated LLM adapter) and **11**
(the delegated-agent sandbox default) remain. Item **42** is still the one live-behaviour defect open: one
spurious stop-liveness warning per run, every run. **S206 has since shipped and deployed** — see *Now*.

📦 **[S208](sprints/sprint-208-a-risk-gate-says-what-it-can-reject.md) is specced** (2026-09-15) against items **60** and **61**, from a live-spine audit recorded as [DL-169](design-log.md): two PM risk gates have **never rejected an order** — `reward_risk` has one distinct value across 256 recordings because the ratio cancels to two constants the regime label never touches, and `correlated_cluster_pct` abandons the whole gate on the first thin-overlap holding. Both appear **zero** times in 919 `Rejection` rows. 🪤 **The audit also corrected the queue's own denominators** — only **35** of 273 `OrderIntent` nodes carry gate outcomes, and the correlation abort path has **never fired**, so that half is repair before it bites. 🚨 **One decision is the operator's and is not in the sprint:** item **62**, position sizing by risk rather than dollars, is the referee's most-repeated nightly ground and the largest single lever on the veto rate — it is blocked on a policy call, not on engineering. New item **64** records that the regime label never modulates any risk number.

**Ahead of the numbered list — three questions raised and not yet answered.**

**The latency levers.** `effort` `max` → `high` is **done** (live 2026-08-12); `max_rounds` 2 → 1 is
🚨 **cutting the artefact under test to buy wall clock**, a recorded decision rather than a knob. The two are
**coupled** — more effort lengthens the peer-call tail into a fixed timeout — so measure them together or not
at all. Arithmetic and the full history in [DL-105](design-log.md)'s amendment.

**Undecided, recorded so they are not re-derived** — raised 2026-08-11. **(i)** amend S172 and **(ii)** collapse to one ranked queue are both **DONE 2026-08-19** (detail in the queue's own header). **(iii)** stop pinning version numbers in sprint specs (*"next available PATCH/MINOR at merge"*) — after three renumberings in one day — **still open**.

1. 🪤 **WITHDRAWN 2026-08-22 — retracted twice, on a raw-prop count both times.** `pending_human_flags`
   is **0**; `Flag.status` stays `pending` by design, like `Position.status` staying `open`. What pins
   `healthy=false` is `open_incidents`. The retracted-DL-73 class again — audit on the predicate.
2. 🪤 **Sweep the debate context for the same class — now FOUR instances, packaged as [S177](sprints/sprint-177-every-number-names-its-unit.md)** (2026-08-15). The fourth
   is `max_sector_pct`'s `deployed`: `SectorBook.__init__` seeds `_names` from held positions but
   **never seeds `_deployed`**, so `deployed` counts only *this batch* (GOOGL's `deployed=687.05` is
   exactly GOOG's `order_cost` moments earlier). `deployed=0.00` beside `existing_sector_names=2`
   is correct and unreadable, and it cost the AVGO overturn. 🚨 **Same code is also a latent gate
   defect** — the dollar sector cap never sees held positions, so across days sector exposure can
   pass 30 % unnoticed; masked today only because `max_names_per_sector=3` binds first. Every value rendered into the packet should be
   checkable against what the reader will assume it means: right unit, right period, right scope.
   **Not yet a spec.** This is the highest-value item once the divergence is settled.
3-5. **~~DL-104 (a) the invented ATR fragment · DL-104 (b) batch/portfolio absence · DL-112 sentiment
   units~~ — ALL DONE.** S175 `0.90.08` deployed `s176a` 2026-08-15, and `0.90.11` deployed `s176b`. The
   measured cost of each is in those sprint docs. 🚨 **(a) is the one S206 had to finish** — S175 deleted
   the ATR half of that rendered line and left its sibling, with no clause forbidding the class.
6. **DL-104 (c) — the analyst's hardcoded SMA-200 rationale.** The summary string always names
   SMA-200 while `indicators.sma_distance` returns `None` below its period. 🪤 **The bars gap
   underneath it is CLOSED** — S174 ships 203 bars, so SMA-200 now computes; what survives is the
   rationale asserting an input without checking it exists. Same class as item 5. 🚨 **Cited again 2026-08-19** — NEE vetoed because *"the cited technical support is contradicted by a 0.463 technical score with bearish MACD/EMA/golden-cross inputs and a bottom-tier scanner rank"*: the rationale reads as confirmation while the score underneath it is bearish.
7. **DL-104 (d) — a real advisory/binding switch**, so *advisory* is a declared posture rather than a
   grace that happens to expire. Every run writes a truthful but uninformative `error` fault, which
   trains the operator to read a real fault as noise.
8. **S170 — one LLM adapter in `kernel/`**
   ([sprint-170](sprints/sprint-170-one-llm-adapter-in-the-plumbing.md)). Capability, not repair, so
   it ranks below the fixes — but it gives the operator the same provider switch the deliberator has,
   while the Anthropic key is usage-limited to 2026-09-01.
9. **Remaining hardening rows: N, O, R.** **N** — delegated coding agents default to
   `danger-full-access` with no approval prompt; the protection is the operator remembering a CLI
   flag. **O** — S157's 101 missing law-clause test-plan rows, then flip assertion E in
   `scripts/check_law_coverage.py` to hard fail. **R** — three guards covered only by CI lacking the
   extras. 🪤 **Row P is closed** (S176, deployed); **row Q is closed** (the `s176a` full `up`
   returned `[OK]` on all 16 env-preservation checks after refusing once — see Recent).

## Pointers

Product `docs/PRD.md` · architecture `docs/architecture.md` · phases `docs/build-plan.md` · closed
decisions `docs/decisions/INDEX.md` · open threads `docs/design-log.md` · "does it work"
`docs/laws/{ledger,drift-register,functionality-checks}.md` · per-agent `agents/<name>/mission.md`.
