# Drift register — the correction worklist

Every place where a **law (intent)** and **reality (PRD / mission / code)** disagree is recorded here
once, with a stable `DRIFT-NN` ID, so we can set them back on course later. Fed by each agent's local
**Divergence Register**. See conventions §9.

**Kinds** — `PRD-fork` (law vs PRD, needs a forced decision) · `stale-doc` (PRD/mission out of date
vs a later decision) · `code-drift` (code diverged from intent) · `gap` (intent not yet built).
**Status** — `OPEN` (awaiting forced decision) · `DECIDED` (resolution chosen) · `CORRECTED`.

## Provider (`PROV`)

| ID | Law | Intent says | Reality says | Kind | Status / decision |
| --- | --- | --- | --- | --- | --- |
| DRIFT-001 | PROV-STA-01..04 | The cache is a transparent perf optimisation; not load-bearing. | Mission/PRD: the provider **owns the price cache** (a first-class fact store). | PRD-fork | **DECIDED D1** — load-bearing store; applied to law |
| DRIFT-002 | PROV-NEV-08 | Provider serves *raw* news; sentiment scoring is downstream. | `mission.md` listed **finbert** as a provider client. | stale-doc (vs ADR-0002) | **CORRECTED D2** — `mission.md` fixed; `NEV-08` added |
| DRIFT-003 | PROV-IN-06 | Fields = price, fundamentals, news, benchmark, regime. | `mission.md` also lists **FRED** (macro) and **EDGAR** (filings). | gap / scope | **DECIDED D3** — in-law, deferred; applied (`IN-06`) |
| DRIFT-004 | PROV-OUT-02 | Regime response = classification + its inputs. | PRD/mission: provider emits the regime-derived **policy inputs** (stop/target/holding defaults). | gap (enrich) | **CORRECTED** — adopted; `OUT-02` sharpened |
| DRIFT-005 | PROV-OUT-06 | Degradation = a quality record on the response (pull). | `mission.md`: provider also **emits** `market_data_degraded` (push). | gap (enrich) | **CORRECTED** — adopted; `OUT-06` added |
| DRIFT-006 | PROV-OUT-01 | Benchmark is just another **requested field** of a market-data request. | Code (S38) fetches the benchmark via a **separate** request to dodge a degraded-quality trip. | code-drift | **CORRECTED (S69)**: `DataRequest.benchmark_ticker` + `MarketData.benchmark` added; `taint=False` so degraded benchmark doesn't set `used_fallback` on the candidate quality; analyst uses `market.benchmark` directly. |
| DRIFT-007 | PROV-SEC-07 | Only capability-matrix-authorised callers may invoke the provider. | Unverified that the matrix actually gates data requests. | code-drift (verify) | **CORRECTED (S69)**: `caller_authorized` predicate + `allowed_callers` param in all three buses (`InProcessBus`, `CeleryBus`, `AzureServiceBusBus`); `AgentBase.bind` passes `capability.allowed_callers`; provider contract gates `get_market_data` to 5 callers and `get_regime` to 2 callers. |
| DRIFT-011 | PROV-IDM / PROV-STA | A same-day re-run **idempotently updates** the MarketData node (`ingest.py` keys it by window-end date). | Neo4j `_append_props` enforces provenance **immutability** — a property written once cannot be overwritten with a *different* value; a 2nd same-day run with a different universe → `ValueError: property 'snapshot' cannot be overwritten`. The in-memory store hid it (it allows overwrite). | code-drift (Neo4j-only; in-memory hid it) | **CORRECTED (0.35.01)** — keyed MarketData/RegimeContext by **run_id** (mirrors the pubsub path's `run_id or uuid` in `agent.py`; downstream follows the `INGESTED_BY`/`DERIVED_FROM` edge + derives the regime key from a new `run_id` prop). Proven: the full S&P-100 → Aura run now completes without collision. |
| DRIFT-012 | PROV-OUT-06 / ANLZ-IN | `used_fallback` flags genuinely-degraded data; the analyst bails on it. | A live S&P-100 run returned **clean OHLCV (99/99, no stale)** but `used_fallback=True` from `daily_move_sigma_anomaly` (a real big-mover trips the too-tight default sigma) **and optional-field faults** (`fundamentals/news/sectors/earnings_degraded` — Finnhub rate-limited at 99 per-ticker calls). The analyst then **rejected all 5 candidates** → zero trades. So optional *enrichment* failure (and one sigma outlier) blocks trading on otherwise-good OHLCV. | code-drift (over-taint, surfaced by live acceptance) | **CORRECTED (0.35.02)** — `_fetch_optional` never taints (only core OHLCV degradation blocks; faults still noted + routed); `max_daily_move_sigma` 4.0→8.0. **Proven: the clean S&P-100 → Aura run now PASSES** (5 positions opened; OBSERVATORY OK; ACCEPTANCE PASS). Layer-3 🟩. |
| DRIFT-014 | PROV-OUT-06 / ANLZ-IN | OHLCV quality is per-batch; a degraded batch blocks trading. | At **S&P-500 scale** the per-batch model **does not scale**: the `daily_move_sigma_anomaly` check is *pooled cross-sectional* and its taint is *batch-level*, so **one** name's >8σ move (or a split/glitch) among 503 sets `used_fallback` → the analyst rejects **every** candidate, including the clean survivors (`returned 503/503`, no stale, yet 0 scored). At larger N the chance of ≥1 outlier → 1, so the batch is ~always degraded → zero trades. | code-drift (per-batch quality doesn't scale; surfaced by the live S&P-500 acceptance) | **CORRECTED in code (0.37.01); live S&P-500 proof pending.** `validate_bars` now attributes the pooled-cross-sectional outlier to its **own ticker** and **excludes** it (new `DataQualityTrace.anomalous_tickers` field), exactly like `stale_tickers` — a *partial* degradation, not a whole-batch fallback. `used_fallback` is set only by a genuine whole-batch failure (a tainting note, or `returned == 0`), so one >σ name no longer rejects the clean survivors; the analyst scores the remainder. The pooled detector is **unchanged** (still the data-integrity gate documented in quant-methods) — only the *consequence* changed. The exclusion is **observable** (DRIFT-013 lesson): the observatory prints an `anomalous <tickers>` line and the batch stays `quality ok`. **Proven by unit tests** (`test_domain.py::test_integrity_excludes_anomalous_ticker_keeps_clean_remainder`; observatory `test_anomalous_ticker_is_excluded_and_shown_not_degraded`). **PROVEN LIVE (2026-06-26):** a full **S&P-500 → Aura** acceptance run (OHLCV-only, 503 names, 9.4s) returned **`ACCEPTANCE PASS`** — the provider flagged `anomalous SMCI  (>sigma excluded, DRIFT-014)`, the batch stayed `quality ok  returned=502/503` (the outlier no longer tainted the batch), and the clean remainder flowed end-to-end (scanner 503→5, analyst scored HPE/MRVL, **2 positions opened**). This also demonstrated the **OHLCV-only fast mode** (9.4s vs ~33 min of 503×4 Finnhub calls), now **shipped as a first-class toggle (0.38.00)** — the `--ohlcv-only` CLI flag + `provider.ingest_ohlcv_only` setting (`PROVIDER_INGEST_OHLCV_ONLY`) narrows the ingest to the `ohlcv` field. The only WARN was the expected DRIFT-013 sector-coverage advisory (enrichment skipped for speed; non-blocking). |
| DRIFT-013 | PM-NEV-04 / PM-NEV-06 | The sector caps (dollar + name-count) bound concentration. | The same clean run opened **5 correlated names (4 semis + 1 bank)** with PM-NEV-06 **silently inactive** — `market.sectors` was empty (Finnhub `sectors_degraded`, now non-tainting per DRIFT-012), so every name's sector is `None` → both caps skip. The concentration guard is **data-dependent**; when sector data is absent it quietly does nothing. | code-drift (silent bypass, surfaced by live acceptance) | **CORRECTED (0.37.00).** *Visibility (0.36.00):* the bypass is now **loud** — a `sectors N/M classified (0 = caps INACTIVE)` line + a **`sector_coverage` WARN** via a new **advisory-severity** check (`severity="warn"` — surfaced, non-blocking; the PM acted correctly given absent data). *Robustness (0.37.00):* `sector_cache.resolve_sectors` treats sectors as **cached reference data** (not market data) — each ticker's sector is persisted once in the graph (`Sector` nodes, first-write-wins) and the universe's gaps are filled from the cache, so after the first successful fetch the caps **always have data even when Finnhub rate-limits**. Proven in-memory (a live-empty run fills 2/2 from a warmed cache). *Operational note:* the cache must warm once per ticker (a paced run where the live fetch succeeds); a cold cache still shows the warn. No PM/law change — the existing PM-NEV-04/06 just stop starving. **PROVEN LIVE (2026-06-25):** a paced S&P-100 run cached all **99/99** sectors in Aura (Finnhub returns granular industries — `Semiconductors` is its own bucket); a subsequent **single-shot** run (live sectors rate-limited again) still showed **`sectors 99/99 classified`** from the cache, `OBSERVATORY OK`, and **PM-NEV-06 fired** — `INTC SKIP sector_name_count` (at cap=2; the shipped cap=3 correctly allows the run's 3 semis). |
| DRIFT-021 | PROV-OUT-06 / PROV-OBS-02 | Enrichment (fundamentals/news/sectors/earnings) feeds the analyst's confidence blend on every scheduled run; boundary faults reach the central channel with provenance. | **Every** scheduled fleet run (sched-2026-07-07 → 07-10) carries all four Finnhub `*_degraded` notes. Cause (2026-07-12 feeds diagnosis): ~99 tickers × 4 Finnhub endpoints ≈ 400 sequential calls vs the 60 req/min free tier, with no per-request pacing (`ingest_chunked` sleeps only between chunks); one 429 inside a feed's single `_fetch_optional` boundary discards the **whole feed**, partial results included. The analyst scores on technicals alone — on sched-2026-07-10 the top candidate hit 0.595 vs the 0.600 floor (NO_TRADE). Ruled out: key valid (local probe 200), vault copy byte-identical, no Escalation. The faults are also **invisible**: the 22:25–01:00 window left one console line in Log Analytics and zero graph Fault nodes. | code-drift (no pacing; whole-feed fault granularity) + gap (fault observability after scale-to-zero) | **CORRECTED (S128, 0.71.01; live-proven 2026-07-19).** Per-request Finnhub pacing (55/min tunable; `0` disables for the unpaced proof), per-ticker attribution in fundamentals/news/sectors/earnings, bounded notes `<feed>_degraded:<count>:<tickers>:<error>`, attributed-note merge through `ingest_chunked`, and dashboard/verdict parser compatibility. Regressions: `agents/provider/tests/test_finnhub_resilience.py`, `agents/provider/tests/test_finnhub_provider_resilience.py`, `agents/provider/tests/test_ingest_chunked.py`, `surfaces/tests/test_dashboard_vitals.py`. Live proof (2026-07-19, provider stage only): paced full-universe ingest 99/99 with **zero** degraded notes in 7 min 09 s (inside the fleet window); unpaced ingest against the live 60/min limit drew real 429s → `<feed>_degraded:39:MET,META,MMM,MO,MRK:429` on all four feeds with 60/99 kept per feed and `used_fallback=False` (DRIFT-012 held); attributed notes read back off Neon over a fresh SQL connection after the writer exited. Evidence: `docs/reports/sprint-128-feed-resilience/live-check.md` (+ `code-and-live-preflight.md`). |

## System-level

| ID | Question | Decision | Status |
| --- | --- | --- | --- |
| DRIFT-008 | Inter-agent hand-off: DB-mediated vs RabbitMQ-payload vs claim-check vs synchronous RPC. | **Event-driven pub/sub over Azure Service Bus, claim-check** (data in Neo4j, `ready: <ref>` events on the bus; logs on Event Hubs). Reversed an initial RPC choice on owner review; Azure-native per the lock-in commitment. | **RESOLVED** — ADR-0005 (supersedes ADR-0004); `PROV-TRG-01`/`OUT-01` updated (law v0.3). Kernel `MessageBus` → publish/subscribe is the system-wide consequence. |

| DRIFT-009 | DEP-FEED-01 / PROV-DEP-01 | The provider's keyless OHLCV feed (Stooq) is reachable and parseable. | **Stooq is anti-bot-blocked** (PoW interstitial → 404); **Finnhub `/candle` is premium** (403); **FMP free is only ~87 curated symbols** (PG/HD → 402), not the full S&P 500. **Full-universe live fix:** **Tiingo free** (500 symbols/month, 30+ yrs, real-time IEX) covers the S&P 500; **Alpaca free** = full-US data **+ broker**. | dep-health / code-drift (real-probe finding) | **CORRECTED** — ADR-0006 + **S44 shipped**: `TiingoDataSource` is the live full-S&P-500 default (`market_source_from_settings` OHLCV→Tiingo; `orchestration/bindings.py` re-pointed off broken Stooq). FMP retained as validation/failover; Alpaca broker + a failover wrapper are the remaining follow-ups. |
| DRIFT-015 | DEP-POSTGRES / DEP-NEO4J / agent DEP clauses | Laws still described Neo4j as a rollback dependency and agent precondition after ADR-0014's S118 end state. | Runtime code no longer ships a Neo4j adapter/driver/probe; `NEO4J_URI` is an ADR-0014 startup error and rollback is `git revert` + redeploy. | stale-doc | **CORRECTED (S118)** — dependencies, stack, ledger, agent laws, deployment, and architecture swept to Postgres-only runtime; Neo4j remains workbench-only. |
| DRIFT-016 | DL-35 / Service Bus served transport | Served control-plane containers should consume Service Bus request topics when the bus connection string is configured. | S102 live fleet found the five served images selected Service Bus but lacked the optional Azure SDK in their Dockerfile sync, crashing on `No module named 'azure'`; request topics also needed to exist before the first poll. | code-drift (container-only; unit gate hid it) | **CORRECTED (S102)** — served-agent Dockerfiles install `--extra azure`; `deploy-agents.ps1` prepares served request topics/subscriptions before containers enter `serve_loop`. Regression: `tests/test_served_agent_images.py::test_served_agent_images_install_azure_extra` + `test_served_agent_request_routes_are_stable`. |
| DRIFT-017 | P15 container split / portfolio_manager activation | Every branch-built agent image should start its actual agent entrypoint and EHLO as the grant-policy agent type. | S102 live fleet found the `portfolio_manager` app Running but its Dockerfile command targeted `agents.portfoliomanager.entrypoint`, so Python exited before EHLO and Postgres had no active `portfolio_manager` instance. | code-drift (container-only; unit gate hid it) | **CORRECTED (S102)** — `agents/portfolio_manager/Dockerfile` now targets `agents.portfolio_manager.entrypoint`. Regression: `tests/test_agent_image_commands.py::test_agent_dockerfile_cmd_modules_are_importable`. |
| DRIFT-018 | DEP-BROKER / execution entrypoint | The distributed fleet run should execute against the broker boundary, selecting Alpaca paper when the credentials are injected. | S102 code review before the run found `agents/execution/entrypoint.py` still hard-coded the in-process `PaperBroker`, even though `broker_from_settings()` already selects `AlpacaBroker` when keyed. A distributed run could have passed while never touching DEP-BROKER. | code-drift (container entrypoint; hidden by local graph demos) | **CORRECTED (S102)** — execution entrypoint now uses `broker_from_settings(ExecutionSettings())`. Regression: `agents/execution/tests/test_execution_entrypoint.py::test_main_uses_settings_broker_factory`. |
| DRIFT-019 | S100 claim-check proof helper / Postgres frozen props | Live proof helpers should print claim-checked replies as ordinary JSON evidence. | S102 Service Bus probes received container replies, but `scripts/servicebus_request.py` failed while printing them because Postgres graph nodes freeze nested payloads as `mappingproxy`, which `json.dumps` cannot serialize. | code-drift (Postgres-only evidence capture) | **CORRECTED (S102)** — `servicebus_request.py` normalizes frozen mappings/lists before printing reply payloads. Regression: `tests/test_servicebus_request.py::test_jsonable_thaws_frozen_graph_payloads`. |
| DRIFT-020 | DL-44 / broker truth for holdings | The graph should mirror standing broker holdings before PM sizing, while lineage remains graph-owned. | S103 teardown removed Position/Fill rows for real Alpaca paper holdings, so the first unattended run started blind and re-bought CSCO (89 sh on top of the paper account's 88 sh); pending after-hours Fill status also stayed stale. | code-drift (production-regime teardown exposed it) | **CORRECTED (S120)** — execution run-start writes `BrokerPositionSnapshot`, refreshes pending broker status evidence, and raises a divergence `Flag`; monitor adopts broker truth into `Position` nodes with `reconciled-from-broker`; PM seeds max-position/sector gates from active graph positions. Regression: `agents/execution/tests/test_reconciliation.py::test_reconcile_run_start_snapshots_flags_and_refreshes_pending_fills`, `agents/monitor/tests/test_broker_reconcile.py::test_reconcile_positions_from_snapshot_creates_broker_positions_once`, `agents/portfolio_manager/tests/test_graph_portfolio.py::test_evaluate_analyst_node_uses_graph_positions_for_max_positions_gate`. |
| DRIFT-023 | DEP-POSTGRES / ADR-0014 | The Neon free-tier Postgres spine serves every graph read/write within the free plan's limits. | **2026-07-16 ~15:00 AEST: the spine is DOWN** — every connection refused with `Your project has exceeded the data transfer quota` (free-tier egress cap). The 2026-07-15T22:30Z dispatcher execution **Succeeded on `:s126`** (RunRequest placed; the rotated GHCR PAT and the s126 image are thereby production-proven), but whether the 7-stage chain completed before the quota tripped is unverifiable while the graph is unreachable. Free-tier transfer resets with the billing month (~2026-08-01) — waiting means ~2 weeks of downtime. Likely egress contributors: nightly graph-pull polling across the 22:25–00:30 window, dashboard projection reads (several local sessions 07-13→07-16), and live-check sessions. | dep-health | **RESOLVED (2026-07-19)** — operator **upgraded the Neon plan** (same Sydney endpoint; the free-tier data-transfer cap no longer applies). Verified live before use: `DEP-POSTGRES-01 GREEN postgres: reachable SELECT 1`; the S128 live check then ran full write/read/teardown cycles against the spine the same day. Post-restore verification (2026-07-20): the in-flight `sched-2026-07-15` chain **had completed 7/7 with `ACCEPTANCE PASS`** (1 AMD paper buy) before the quota tripped; the 07-16/07-17/07-18 22:30Z dispatcher executions `Failed` against the dead spine — **Friday 07-17's session run is the outage's one lost run** (not re-fired: day-keyed, market long closed). Egress reduction shipped in S129 (TTL read cache + self-heal backoff); remaining leaner-polling ideas stay optional. |
| DRIFT-022 | DL-47 req 5 (run selector scopes everything) | Selecting a run switches **all** contextual information to that run — including the per-container log drawer. | The log drawer always queries the **latest** fleet window (`container_logs` uses `latest_window`, `surfaces/dashboard/bundle_azure.py`), so viewing `sched-2026-07-08` shows 2026-07-13 log lines. The run-scoped helper (`run_window`) exists and the `/bundle` path already uses it — only the drawer endpoint ignores the selection. Operator-reported 2026-07-14 (screenshot: run day 07-08 circled against 07-13 log stamps). | code-drift | **CORRECTED (0.69.02)** — `/api/containers/<name>/logs?run=<id>` resolves the run day from its RunRequest and queries that day's window; unscoped/unresolvable falls back to latest with an explicit `scope` field; the drawer sends the selected run and titles the window shown. Regression: `surfaces/tests/test_dashboard_bundle_azure.py::test_container_logs_run_day_scopes_window_and_bad_day_falls_back`, `surfaces/tests/test_dashboard_app.py::test_container_logs_route_scopes_to_selected_run`. |
| DRIFT-030 | Kernel graph-pull loop / central fault channel | Agent `FAIL` and `OBS` clauses require contained, visible failures, but the law surface does not declare a system-level bound on graph-pull retry cadence, poison-item quarantine, or duplicate fault emission for one ongoing failure. | S155 found the missing declaration while generalising DL-79: the code can bound retries and collapse ongoing duplicate fault writes locally, but no locked kernel/system law states the required envelope. | law gap | **OPEN (S155)** — record only in this sprint. Per the S152 convention, this appeared gap stays a drift row and must be handled by a later law-amendment cycle; no `laws.md` file was amended here. |
| DRIFT-031 | Law test-plan schema and row identity drift | ADR-0021 requires `test-plan.md` summaries to mirror `laws.md`, and every row ID must correspond to an immutable law clause. | S156 found four incompatible test-plan schemas, seven agents with no summary column for ADR-0021 to attach to, two law clause marker styles that parsers must handle, and a reporter row mismatch: `RPT-OBS-03` existed in `test-plan.md` while `RPT-TYP-03` exists in `laws.md` with no row. | stale-doc / law-book gap | **OPEN (S156)** — do not rename either ID. S156 removed the orphan row so assertion C can enforce no orphan rows; S157 owns the missing-row/schema-generation question. |
| DRIFT-032 | `DEP-BUS-04` / `DEP-CONFIG-02` / deliberator peer credential boundary | Production Service Bus access must use scoped credentials and secrets must not leak, but the law book does not explicitly declare the test-harness invariant that local pytest must never transact with production Service Bus. | S159 found that `.env` plus `AzureServiceBusSettings()` let unit tests publish `run_id="turn-1"` messages into the production deliberator peer topic, where a live consumer retried and dead-lettered them. Code now guards pytest sends, but the durable law declaration is still absent. | law gap | **OPEN (S159)** — record only. No `laws.md` edit in this sprint; future law-amendment work should decide where the pytest-never-transact invariant lives. |
| DRIFT-033 | `master` CAPABILITY DECLARATION `external_io` | A capability declaration must name the external systems the agent actually touches. | `contracts/master.py:99` declared `external_io=("key_vault", "neo4j")`, but the Neo4j runtime was deleted in S118 and ADR-0014 made Postgres the system of record. Verified 2026-08-13 during S176: `grep -rn neo4j kernel/ agents/ orchestration/ contracts/` returns no rows after `contracts/master.py` was corrected to `external_io=("key_vault",)`. A declaration naming a dependency that no longer exists is the exact shape DL-70 is about: dead declarations read identically to live ones. | stale declaration | **CORRECTED (S176, 0.90.09; merge SHA `b17ff5fd1037052e4ff163d026c91fa72119892b`)** — removed the stale `neo4j` external I/O declaration without replacing it with `postgres`, matching the graph-I/O convention used by analyst, forecaster and monitor. |

> **Live OHLCV — CORRECTED 2026-06-16 (ADR-0006; S44 shipped).** No keyless feed serves the full
> S&P 500 live: Stooq is anti-bot-blocked, Finnhub `/candle` is premium, and **FMP free is a curated
> ~87-symbol subset** (empirically PG, HD → `402`). **Two free keyed tiers do, and both keys are now
> live (in `.env`):** **Tiingo** (`TIINGO_API_KEY`; free = 500 unique symbols/month — covers the
> S&P 500 — 30+ yrs history, real-time IEX) is the **primary full-universe OHLCV feed**; **Alpaca**
> (`ALPACA_API_KEY/SECRET`; free full-US data **and** the broker) is the **broker boundary + secondary/
> failover feed** (one vendor for DEP-FEED + DEP-BROKER). **Built:** `agents/provider/fmp.py`
> **Shipped (S44):** `agents/provider/tiingo.py` `TiingoDataSource` (OHLCV only; Z-suffixed ISO date
> sliced to `YYYY-MM-DD`); `market_source_from_settings` routes OHLCV→Tiingo, fundamentals/news→Finnhub;
> `orchestration/bindings.py` default re-pointed off broken Stooq; `tiingo_*` settings; unit tests (620
> passed, 100.00%). `FMPDataSource` retained as the **validation sub-universe / failover**. **Remaining
> follow-ups:** a `FailoverDataSource` wrapper (Tiingo→FMP→Alpaca) and `AlpacaDataSource` + the Alpaca
> broker adapter. Postgres `price_cache` stays the raw historical backtest fallback. Confirms decision
> **D1**; Stooq retired as default; no scraping.

## Analyst (`ANLZ`)

| ID | Law | Intent says | Reality says | Kind | Status / decision |
| --- | --- | --- | --- | --- | --- |
| DRIFT-028 | `ANLZ-PARAM` / `ANLZ-OUT-02` / `ANLZ-OBS-01` | The analyst law declares optional `suggested_stop_pct` / `suggested_target_pct` outputs and its existing tunables, but does not name a selectable stop-scaling mode, scaled stop/target tunables, or durable applied-vs-counterfactual proposal evidence. | S150 ships an ADR-0013 challenger off by default: `flat` preserves today's proposal, `scaled` proposes bounded ATR-scaled stop and target in lockstep, degrades missing ATR to flat, and records applied plus counterfactual proposal evidence on recommendations for later operator comparison. | law gap | **CORRECTED (S152, analyst laws v1.1, 2026-08-01).** Verdict: **decided** — ADR-0013 decided champion–challenger; S150 shipped the stop challenger off by default. Declared by `ANLZ-OUT-07` (selectable mode, target scaled in lockstep so reward-risk is invariant, degrading to `flat` without a usable ATR), `ANLZ-OUT-08` (applied vs counterfactual proposal evidence), `ANLZ-OBS-03` (reconstructable without re-running the agent) and `PARAM` rows for `stop_target_mode` plus the three `scaled_stop_*` bounds. The ceiling row now cites **ADR-0019** — the risk cap binds position size, not stop distance. New clauses start ⬜. |

## Execution (`EXEC`)

| ID | Law | Intent says | Reality says | Kind | Status / decision |
| --- | --- | --- | --- | --- | --- |
| DRIFT-024 | `EXEC-IDN-02` / `EXEC-DEP-02` / `EXEC-PARAM` | Execution's constitution lists durable execution labels and tunables, but does not name ADR-0015 broker-stop state or the broker-stop fallback parameter. | ADR-0015 §3 and S146 require execution to place/retry broker-native stops, write/read `BrokerStopOrder`, and use a bounded fallback stop percent for broker-adopted positions with no PM `stop_pct`. | law gap | **CORRECTED (S152, execution laws v1.1, 2026-08-01).** Verdict: **decided** — ADR-0015 §3 decided broker-native stops before S146 built them; only the declaration was skipped. Declared by `EXEC-IDN-03` (owns `BrokerStopOrder`), `EXEC-OBS-03` (stop lifecycle reconstructable; `UnprotectedPosition` surfaced and retried), `EXEC-DEP-04` (broker cancel + stop placement) and the `PARAM` row for `broker_stop_fallback_stop_pct` (0.05, > 0.0 ≤ 1.0). New clauses start ⬜ — declaring is not proving. |
| DRIFT-025 | `EXEC-IDN-02` / `EXEC-DEP-02` / execution CAP | Execution's constitution says execution exclusively owns `Fill`, `Reconciliation`, `StageTransition`, and `ExecutionResultEvent`, and its graph dependency/capability lists those durable labels. | S120 code already writes `BrokerPositionSnapshot` from `agents/execution/reconciliation_store.py:82` / `:101` during `reconcile_run_start`, and S147 adds a head-of-run work source that relies on that execution-owned snapshot before the analyst runs. | law gap | **CORRECTED (S152, execution laws v1.1, 2026-08-01).** Verdict: **decided** — DL-44 (2026-07-08) decided broker-truth-for-holdings before S120 built it and S147 gated the analyst on it. Declared by `EXEC-IDN-03` (owns `BrokerPositionSnapshot`), `EXEC-TRG-07` (run-start `position_sync` writes exactly one snapshot before downstream scoring, and a cleanup beside it may never prevent it — DL-79) and `EXEC-DEP-04`. New clauses start ⬜. |
| DRIFT-026 | `EXEC-PARAM` / `EXEC-OUT-01` / `EXEC-IDN-02` / execution CAP | Execution's LOCKED constitution declares its tunables, output shape, and owned durable labels, but does not name a bounded order-price tolerance, a dropped-decision outcome, or any durable drop evidence introduced by ADR-0018. | S148 implements ADR-0018: approved orders become bounded by a tolerance tunable; unfilled prior-session decisions are dropped, recorded visibly, and distinguished from rejected/skipped without editing the LOCKED law. S151 narrows the durable evidence shape after the live drop-sweep outage: `Fill.drop_reason` / `Fill.dropped_at` plus an append-only `BrokerOrderStatus` drop node, with no raw terminal reason written into `Fill.broker_status`. | law gap | **CORRECTED (S152, execution laws v1.1, 2026-08-01).** Verdict: **decided** — ADR-0018 decided same-session validity and the drop; S151/DL-79 settled the evidence *shape* after the collision stalled the fleet. Declared by `EXEC-OUT-07` (dropped distinct from rejected/skipped; `Fill.drop_reason`/`dropped_at` plus an append-only `BrokerOrderStatus` drop fact; **no raw terminal reason in `Fill.broker_status`**) and the `PARAM` row for `order_price_tolerance_bps` (50, 0–500). New clauses start ⬜. |
| DRIFT-027 | `EXEC-PARAM` / `EXEC-OUT-01` / `EXEC-OBS-01` / execution CAP | Execution's LOCKED constitution and capability declaration do not name a selectable order-tolerance mode, volatility-scaled tolerance tunables, or counterfactual tolerance/limit-price evidence on submitted orders. | S149 ships the ADR-0013 challenger off by default: `flat` keeps S148 behaviour, `scaled` resolves from decision-time ATR with bounded tunables, and every submitted order records applied and counterfactual tolerance evidence for later operator comparison. | law gap | **CORRECTED (S152, execution laws v1.1, 2026-08-01).** Verdict: **decided** — ADR-0013 decided champion–challenger; S149 shipped the challenger off by default. Declared by `EXEC-OUT-08` (durable applied + counterfactual tolerance evidence; the counterfactual never reaches the broker) and `PARAM` rows for `order_price_tolerance_mode` (**a mode selector, not a tunable**) and the three `scaled_order_price_tolerance_*` bounds. New clauses start ⬜. |
| DRIFT-029 | `EXEC-IDM-01` / `EXEC-STA-03` / `EXEC-FAIL-03` / `EXEC-OBS-01` | Execution's LOCKED constitution requires idempotent, append-only, reconstructable Fill evidence and visible faults, but does not name when broker-status refresh must terminate or how an unresolvable realized-PnL conclusion becomes durable. | S154 stops `refresh_pending_fills` from re-reading and re-writing fills whose `broker_status` is terminal (`filled`/`rejected`) and records unresolved sell-fill PnL once with `Fill.pnl_unresolved_at`. The remaining `partial` case is deliberately deferred: `broker_status="partial"` cannot upgrade to `filled` under the current write-once Fill property model, so the right end-state is a current-status read model derived from `BrokerOrderStatus` facts rather than a mutable Fill status. | law gap | **CORRECTED (S152, execution laws v1.1, 2026-08-01) — with its warrant stated honestly.** Verdict: **decided, but on a weaker basis than 024–028, and this is recorded rather than smoothed over.** Its first half is *decided*: a terminating refresh is implied by ADR-0014's append-only model (`Fill.status` is write-once, so it can never answer "is this settled?") and was named as an open defect in DL-44 — *"nothing ever refreshes broker order status after run end"* — two years of clauses before S154 closed it. Its second half, the `pnl_unresolved_at` marker, has **no ADR**: it was decided in the S154 sprint spec and DL-81, planning artifacts written before the code, which satisfies *decided-before-built* but on thinner authority than an ADR. Declared by `EXEC-STA-05` (terminal `broker_status` ends refresh; `partial` still refreshes) and `EXEC-STA-06` (unresolvable PnL recorded once). New clauses start ⬜. **Deliberately still open inside this row:** `broker_status="partial"` cannot upgrade to `filled` under the write-once property model, so that one case still refreshes indefinitely; 0 production fills are in it today, and the right fix is a current-status read model derived from `BrokerOrderStatus` facts — a design change, not a law edit. |
| DRIFT-035 | `EXEC-IDN-03` / `EXEC-DEP-03` / `EXEC-DEP-04` / execution CAP | Execution owns `BrokerPositionSnapshot` and the broker boundary, and its CAP block names broker operations for submissions, fills, positions, stops and cancellations. | S161 needs execution to read broker account state (`cash`, `equity`, `buying_power`) at run start and record those integer-cent facts on the existing `BrokerPositionSnapshot`. No new label is needed, but the locked law/CAP text does not explicitly declare the account-read operation or account-state snapshot fields. | law gap | **DECIDED (S161, 2026-08-06)** — use properties on execution-owned `BrokerPositionSnapshot`; a future law-amendment cycle should declare the account-read broker operation and account-state evidence shape. |
| DRIFT-038 | `EXEC-OBS-03` | A held position that ends a run with **no live broker stop** is *“surfaced as an `UnprotectedPosition` fault and retried on the next run — a refusal is never recorded once and then forgotten.”* | The retry cannot land. `_place_stop` returns `None` whenever **any** `BrokerStopOrder` fact exists at `stop:{position_ref}:{ticker}` — including a **cancelled** one — and `place_broker_stops` then records *“existing inactive BrokerStopOrder fact blocks retry”*. So a still-held position whose stop was cancelled faults truthfully every run and is never re-protected. Reachable from S163 (an exit cancels its own stop, then the exit is rejected) and already reachable before it via `reconcile_broker_stops`. | code gap | **CORRECTED (S164, 2026-08-07)** — `next_broker_stop_order_key` now walks `stop:{position_ref}:{ticker}`, `#1`, `#2`... and returns the first free broker key only after every prior fact is cancelled; an active fact still returns `None` and blocks duplicate placement. Proven by the six S164 tests, including the S163 rejected-exit path re-protecting on the next hold run. |

## Portfolio Manager (`PM`)

| ID | Law | Intent says | Reality says | Kind | Status / decision |
| --- | --- | --- | --- | --- | --- |
| DRIFT-010 | PM laws.md changelog | `INDEX.md` + `CLAUDE.md` + memory record PM as **LOCKED v1 (S70)**. | PM `laws.md` changelog footer still read *"v0 — drafted… Not yet locked"* (stale; never updated at the S70 lock). | stale-doc | **CORRECTED** (cage audit, 2026-06-25): footer reconciled to v1 + the PM-NEV-06 amendment (v1.1). |
| DRIFT-036 | PM `PARAM` / `PM-IDN-01` / `PM-OUT-06` | PM laws still describe `starting_cash` as the paper account size from which all position limits are derived, while `PM-IDN-01` and `PM-OUT-06` require the PM to size and snapshot against current portfolio state. | S161 decides that PM sizing must use execution-recorded account state from the run-start snapshot, with `starting_cash` retained only as a no-snapshot bootstrap seed for a genuinely fresh paper graph. The locked PARAM rationale is therefore stale once account-backed sizing ships. | stale-doc / law gap | **DECIDED (S161, 2026-08-06)** — equity-backed portfolio value replaces the constant for normal runs; future law amendment should update the PARAM row and declare the account freshness gate explicitly. |
| DRIFT-037 | PM-OUT-03 | The illustrative rejected-order reasons include `"sector_cap"` and `"reward_risk_below_floor"` while requiring the reason string to name the blocking gate. | Code emits `"sector_concentration"` and `"reward_risk_below_min"`; consumers already rely on those emitted strings, and PM-OUT-03 says "etc.", so the list is illustrative rather than a closed enum. | stale-doc | **OPEN (S162, 2026-08-07)** — future law amendment should either align the examples with the emitted vocabulary or declare the canonical rejection-reason vocabulary without renaming production reason strings in this evidence sprint. |
| DRIFT-041 | PM-NEV-06 (pre-v1.3) | The per-sector name cap operates on **GICS level 1** (11 buckets) and **is** the name-correlation penalty the dollar cap misses. | Measured 2026-08-20: the source returns **30** industry-granularity labels, none of `Semiconductors`/`Banking`/`Pharmaceuticals` a GICS sector, so `max_names_per_sector=3` permits up to 90 names rather than 33; and the mega-cap AI cluster scatters across five labels, admitting 3 × 5 = 15 correlated names before the cap fires once. | code-drift / false clause | **CORRECTED (law v1.3, 2026-08-20)** — both claims withdrawn from `PM-NEV-06`; the correlation duty moved to the new `PM-NEV-08` rather than deleted. ADR-0023. |
| DRIFT-042 | PM-NEV-07 (new, v1.3) | Share classes of one issuer are one name and one exposure; every concentration gate aggregates by issuer key first. | No issuer mapping exists anywhere in the repo (`grep` for `issuer` / `dual_class` returns nothing). GOOG and GOOGL both carry the `Media` label and consume 2 of 3 slots as two independent bets. | gap | **DECIDED (ADR-0023)** — issuer map ships as trading-pack data under ADR-0012, not hard-coded in the agent. |
| DRIFT-043 | PM-NEV-08 (new, v1.3) | Concentration is assessed against the held book by measured pairwise correlation, not by a vendor label. | The PM receives tickers, quantities, sector labels, cash and portfolio value and nothing else; `beta` is computed by the scanner and never forwarded. There is no correlation input, no issuer key and no factor model. | gap | **DECIDED (ADR-0023)** — computed from the ~200 bars each run already carries, so zero API cost; the deliberator's exposure-aggregation objections are the falsifiable test. |
| DRIFT-044 | PM-NEV-09 (new, v1.3) | A concentration gate that cannot evaluate emits an explicit not-evaluated outcome. | `agents/portfolio_manager/domain/concentration.py:55-57` returns `()` when a ticker has no sector label; an empty outcome tuple is indistinguishable from a passed gate. Latent today at 99/99 label coverage. | code-drift | **DECIDED (ADR-0023)** — same absence-as-silence shape S183 is correcting in the scanner's earnings gate; fix both to the same rule. |
| DRIFT-045 | PM `max_sector_pct` / `PM-IDN-01` | The dollar cap limits portfolio weight in a sector label, checked against current portfolio state. | `SectorBook.__init__` seeds `_names` from held positions but never `_deployed`, so `max_sector_pct` counts only the current batch. Masked because the name count binds first. | code-drift | 🪤 **DECIDED (ADR-0023)** — must land **with** the name-count fix; fixing the count alone un-masks this and the dollar cap silently becomes the weak gate. |
| DRIFT-046 | PM-TYP-03 (rewritten, v1.3) | `GateOutcome` expresses passed, failed and **not-evaluated** as three distinct values, so a reader can tell a gate that passed from a gate that never ran. | `contracts/portfolio_manager.py:22` declares `passed: bool` — two states. The clause is unexpressible in the contract that carries its evidence, and **no test fails**: the old clause said "matches the file exactly", making the file both claim and oracle. | code-drift / contract gap | **DECIDED (DL-121)** — contract gains the third state in the same sprint as `PM-NEV-09`. The other 15 "matches the file exactly" clauses are a queued sweep, not this amendment. |

## Reporter (`RPT`)

| ID | Law | Intent says | Reality says | Kind | Status / decision |
| --- | --- | --- | --- | --- | --- |
| DRIFT-034 | `RPT-IDN-01` / `RPT-IDN-02` / `RPT-NEV-02` | Reporter traverses upstream graph facts and projects them into its owned durable report labels; it may only write its own labels, currently enumerated as `Snapshot`, `TradeNarrative`, and `ReportSnapshotResult`. | S160 needs a new append-only `RecommendationOutcome` fact per `(Recommendation, horizon)` to measure selection quality. Reporter is the closest conceptual owner, but no locked agent law enumerates that label, and the sprint explicitly forbids inventing an owner or editing `laws.md`. | law gap | **OPEN (S160 law-first stop, 2026-08-06)** — decide and amend ownership before implementation. Likely amendment shape: reporter owns the derived outcome/scorecard projection, but only after a law-amendment cycle updates `RPT-IDN-02`, capability labels, tests, and vocabulary together. |

## Audit-clause sweep (S169 follow-up, 2026-08-14)

Both rows below came out of the `audit`/`observable` shape sweep that S165 asked for: a green law
row whose cited test proves something adjacent to its clause. These two are the ones where the
clause is **false in code**, not merely untested — the rest were demoted in each agent's test-plan.

| ID | Law | Intent says | Reality says | Kind | Status / decision |
| --- | --- | --- | --- | --- | --- |
| DRIFT-039 | `PM-OBS-01` / `PM-OUT-06` | *"The `portfolio_state_snapshot` captures pre- and post-run state on the `OrderIntentResult` node"* — pre/post cash, open positions and sector weights, reconstructable without a live broker query. | `portfolio_state_snapshot` appears **nowhere in the codebase** (0 non-mutant hits). `OrderIntentSet` has no snapshot field, and the graph-pull path writes `PMRun` with `order_intent_set` alone (`poll.py:75`). The `OrderIntentResult` node the clause names is written only on the pub/sub path, which production does not use. S156 already demoted **two** PM-OBS-01 rows and PM-OUT-06 for insufficient proof; a third row stayed green on a weaker test until this sweep. | gap (intent not built) | **OPEN (2026-08-14)** — forced decision: build the snapshot, or amend `PM-OBS-01`/`PM-OUT-06` to claim only what `PMRun` actually carries. Not a test-citation fix either way. |
| DRIFT-040 | `PROV-OUT-04` | *"Every served fact carries provenance (source, fetch-time, transformation) so any downstream output is reconstructable."* | `Provenance` (`contracts/common.py`) carries `run_id`, `source_agent`, `correlation_id`, `graph_node_id`, `incident_refs` — no vendor source, no transformation. `MarketSnapshot` records `created_at` (so fetch-time holds) and a **boolean** `used_fallback`, never which vendor answered. Under ADR-0006 the OHLCV source is Alpaca *or* Tiingo *or* FMP, so "which feed produced this bar" is unanswerable from the graph. | gap (intent not built) | **OPEN (2026-08-14)** — forced decision: record the serving source (and drop or define "transformation"), or narrow `PROV-OUT-04` to fetch-time plus the fallback flag. Note it also weakens DL-37 lineage claims. |


## Other agents

*Populated as each agent is authored and reconciled.*
