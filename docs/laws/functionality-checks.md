# Functionality-check register — every sprint's real-environment proof

Every sprint ends with a **functionality check**: the delivered capability exercised in a *realistic*
environment (not just unit tests — **LAW-02**, success is proven, never assumed). `make ci` green is
necessary but not sufficient. Each entry names what was run, against which **real** systems, the result
(with evidence), and confirms **teardown** — every test artifact removed, the environment left as found.

**Procedure (per sprint):** run the check → tear down (delete stamped graph rows, cancel paper orders,
delete scratch files) → append a row here. If a sprint's feature has no realistic target yet (a
primitive awaiting later sprints), say so and check the nearest real thing it enables.

Dev graph store for checks: **Neon PostgreSQL via `POSTGRES_DSN`**. Return stamped rows to zero after
each check. Historical Aura rows below remain valid evidence for the pre-S117/S118 Neo4j spine.

**Standing broker state note (DL-44 / S120):** `Position` and `Fill` rows that mirror real Alpaca
paper holdings are production state, not disposable proof artifacts. Checks that trade must run
broker reconciliation after teardown so the graph mirrors the standing account again. Stamped test
extras are still fully torn down to zero.

| Date | Sprint | What was exercised | Real environment | Result | Teardown |
| --- | --- | --- | --- | --- | --- |
| 2026-08-06 | S160 read-only shadow book (feat, 0.87.00) | `scripts/shadow_book.py` joined every persisted `Recommendation` to its own AnalystRun/MarketData evidence and recorded PM disposition, then rendered 1/5/20-trading-day action, confidence and disposition cuts. B4 was also watched failing after a planted `merge_node`, then passing after removal. | Neon PostgreSQL via the gitignored main-checkout `.env`; `PostgresGraphStore` asserted without printing the DSN; read-only operator script, no broker or vendor calls. | ✅ **PASS.** Live graph reconciled to Recommendation=163, AnalystRun=26, PMRun=26, MarketData=26. Scorecard: 163 reference-priced; 489 horizon rows = 263 scored + 5 unpriceable + 221 not yet elapsed; disposition census `taken=46 blocked_capacity=2 blocked_other=115 not_actionable=0`. The 1-day key cut has `taken n=45 mean=-0.18%` vs `blocked_capacity n=1 mean=-1.09%`; this sample is too small for a conclusion. Local `make ci` exit 0: **2124 passed, 4 skipped, 100.00% coverage**, 4 import contracts kept, pip-audit clean, both secret scans passed. | ✅ Zero graph writes by design; fixture node/edge censuses identical before/after. No graph rows, orders, API calls, vocabulary edits or fleet changes to tear down. Temporary output files removed after evidence capture. |
| 2026-07-01 | S97 serve_loop | Primitive — **no realistic target yet** (needs an agent served over it, S98, across containers, S100). Checked the nearest real thing it enables: the delivered pipeline + the graph store. | Aura `bce05bd6`; Tiingo/FMP/Finnhub/Alpha Vantage; Alpaca paper (probe) | `probes`: **12 green / 0 red** (incl. Neo4j reachable + write/read + uniqueness on the recreated instance). Live cascade `run_local.py --real --observe` (run-id `live-20260701-1`): full provider→reporter chain, **OBSERVATORY OK**, 1 position (in-memory `PaperBroker`, not live Alpaca). | ✅ Broker probe order canceled (account flat). Cascade's 31 Aura nodes DETACH DELETEd → **Aura at 0**. |
| 2026-07-01 | S98 serve supervisor/operator | `serve_loop` serves a **real agent capability** against real Aura: supervisor `flag_for_human` dispatched via `serve_once` → handler wrote a `Flag`; durability confirmed from a **separate** connection. | Aura `bce05bd6` (`Neo4jGraphStore`, asserted) | ✅ `store = Neo4jGraphStore`; `SERVED accepted=True`; **DURABLE = 1** (a separate raw connection saw `flag:S98-REAL:warn`). Proves serve_loop runs something real and the write commits durably (no store bug). | ✅ `Flag` DETACH DELETEd → **Aura at 0**; scratch scripts removed. |
| 2026-07-01 | fix: master auth-frenzy | The login-frenzy fix: deploy creds corrected + `kernel.startup.ensure_reachable_or_halt` — a startup guard that **halts instead of crash-looping** on a bad Neo4j connection (a crash-loop was hammering Aura auth → account lockout → instance recreate). Checked *without* hammering Aura: bad connection uses a **fake** graph (0 real auth attempts). | Aura `bce05bd6` (real probe) + fake failing graph | ✅ [1] guard passes on real Aura (`reachable=True`, no halt); [2] bad connection **halts, never raises** (`raised=None`, FATAL logged) — no restart storm; [3] deploy user/db now `bce05bd6/bce05bd6` == `.env` (MATCH). | ✅ Reads only — no Aura writes; scratch script removed. |
| 2026-07-01 | S104 credential-tested activation | DL-36 A+B: master tests each credential before handover; a required failure refuses activation + writes an `Escalation`. Bad case uses a deterministic-fail test (0 auth hammering). | Aura `bce05bd6` (`Neo4jGraphStore`, asserted) | ✅ `[GOOD]` a **real** Neo4j connectivity test passed → `ACTIVATE issued` (config handed over); `[BAD]` failing test → `ActivationRefused` + **`Escalation` durably written** (`agent=scanner, failed=['neo4j'], status=open`). | ✅ `AgentInstance`+`CapabilityGrant`+`Escalation` DETACH DELETEd → **Aura at 0**; scratch removed. |
| 2026-07-01 | S105 master secret cache | KV secret cache: the master caches fetched secrets for repeated references (`CachingSecretStore`, TTL minutes, 0=never). | Real `EnvVarSecretStore` (reads `os.environ`) | ✅ Fetched a secret, **deleted the underlying env var**, fetched again → the cache still returned it (`CACHE SERVED THE REPEATED REFERENCE: YES`) — served from memory, no re-fetch. Live-KV verification awaits a provisioned Key Vault (mechanism is store-agnostic). | ✅ Reads only — no Aura writes; process-local env var + scratch removed. |
| 2026-07-02 | S106 remediation planner | DL-36 C: credential failure → `Escalation` → real GPT-5.5 bounded-catalogue selection → linked `RemediationPlan` with `auto_eligible`. Also checked `auto_remediation_scope=all` makes destructive catalogue choices auto-eligible. | Aura `bce05bd6` (`Neo4jGraphStore`, asserted); OpenAI `gpt-5.5` | ✅ Aura started at 0 nodes. GPT-5.5 selected `refetch-from-key-vault` for the safe-only failure (`auto_eligible=True`, rationale captured) and `rotate-credential` for the compromised/destructive failure under `scope=all` (`auto_eligible=True`). Both plans were durably written and linked via `PLANNED_BY`; choices were validated as catalogue members. | ✅ Test `Escalation` + `RemediationPlan` nodes DETACH DELETEd by test keys/agent types → **Aura at 0**. |
| 2026-07-02 | DL-36 A/B/C + frenzy-guard — **end-to-end integration** | The whole delivered activation stack driven through the master's **real HTTP surface** (EHLO→ACTIVATE, RSA-PSS signing over a socket), across six dimensions in one run: **happy** (cred test → signed ACTIVATE → `AgentInstance`/`CapabilityGrant`), **failure→self-heal** (GPT-5.5 remediation), **boundary** (scope truth-table, KV-cache TTL, PassCache), **wrong input** (malformed body, bad signature, adversarial LLM output), **fast input** (25 rapid + 10 concurrent EHLOs = the frenzy stress), and the **startup frenzy-guard**. 29 assertions. | Aura `bce05bd6` (`Neo4jGraphStore`, asserted); OpenAI `gpt-5.5`; in-process `ThreadingHTTPServer` + RSA over a real socket | ✅ **29/29 passed.** GPT-5.5 chose `refetch-from-key-vault` (enum guardrail held). Malformed body → 400, server survived; ACTIVATE verified vs a wrong pubkey → `InvalidSignature`; LLM garbage/off-menu/raising → fail-open `pause-and-escalate`; 25 sequential + 10 concurrent EHLOs all 200 with unique `instance_id`s (no counter-race collision observed); guard **HALTS** on an unreachable graph (no raise/crash-loop) yet passes on live Aura. **Findings (non-blocking):** (1) sequential activation is Aura-write-bound (~1.9 s each) — expected cloud-write latency, no crash-loop; (2) `MasterAgent._instance_counter` increment is not lock-protected — no collision seen but a latent concurrency risk to close before the fleet boots agents in parallel. | ✅ 151 master-owned test nodes DETACH DELETEd → **Aura at 0** (was 0). |
| 2026-07-02 | S107 remediation execution | DL-36 D: eval-gated GPT-5.5 remediation selector plus bounded safe auto-remediation execution, wired through the orchestration composition root. Covered selector gate, safe refetch execution + re-EHLO, one-shot failure, destructive no-executor path, and lock-protected concurrent activation IDs. | Aura `bce05bd6` (`Neo4jGraphStore`, db/user `bce05bd6`, asserted); OpenAI `gpt-5.5` via `uv run --extra llm python -` | ✅ Live selector exact-match **5/5** (`selector_pass_rate=1.0`); gate trip demonstrated against a stricter frozen baseline (`impossible-weaker-model-case`). Safe blank Key Vault failure selected `refetch-from-key-vault`, recorded `RemediationAttempt.status=succeeded`, appended `Escalation.resolution_status=resolved`, refused the first EHLO, then re-EHLO issued ACTIVATE. Unfixable refetch recorded `failed, skipped` with exactly one auto attempt and manual outcomes. Compromised credential selected `rotate-credential` and recorded `skipped`, `auto=false`, `message=no executor registered`. Concurrent activation IDs: **40/40 unique**. Latest `make ci`: **1228 passed, 5 skipped, 100% coverage** (known optional-DSPy `diskcache 5.6.3` / `CVE-2025-69872` reported then ignored by Makefile). | ✅ Pre-clean deleted 0; live check deleted **94** `s107` Aura nodes; final Aura node count restored to baseline **0**. |
| 2026-07-02 | S108 .env → Key Vault seeder | Fail-closed seeding into Azure Key Vault: live Tiingo key probe passed then wrote/read back equal; deliberately broken Tiingo key rejected and absent; dry-run pass case reported seeded/would-seed and remained absent. Seeder defaults dry-run, uses pack manifest/probes, and prefers explicit `AZURE_SP_*` local credentials after `.env` load. | Azure Key Vault `trading-agents-kv` in resource group `trading-agents`; service principal `677fa1c3-5685-450c-a09f-b13679020d40`; live Tiingo credential; `uv run --extra azure python scripts/seed_key_vault_live_check.py` | ✅ `make ci`: **1255 passed, 5 skipped, 100.00% coverage**; import-linter kept. Required RBAC grant added with retained script `infra/grant-key-vault-seeder.ps1`: `Key Vault Secrets Officer` on `trading-agents-kv` for the seeder SP. Live check output: `pass_status=seeded`, `pass_readback_equal=True`, `bad_status=rejected`, `bad_absent=True`, `dry_status=seeded`, `dry_absent=True`, names stamped `20260702142659`. Known `pip-audit` warning remains `diskcache 5.6.3 / CVE-2025-69872` and is ignored by the Makefile. | ✅ Deleted `seedcheck-tiingo-20260702142659`, `seedcheck-badkey-20260702142659`, `seedcheck-dryrun-20260702142659`; active Key Vault sweep found no `seedcheck-*` secrets. Soft-deleted list contains only the written pass-case secret, as expected after delete. |
| 2026-07-03 | S99 serve forecaster/curator/researcher | The last three control-plane stubs served through `serve_once`: forecaster `forecast`, researcher `propose`, curator `build_dataset`. Each used the delivered `build_served_bus` entrypoint seam; durability was confirmed from a separate raw Neo4j connection. | Aura `bce05bd6` (`Neo4jGraphStore`, db/user `bce05bd6`, asserted); `uv run --extra runtime python -`; no LLM needed | ✅ `make ci`: **1254 passed, 5 skipped, 100.00% coverage**; known `diskcache 5.6.3` / `CVE-2025-69872` audit warning reported then ignored by Makefile. Live check: forecaster wrote **1** `ShadowPrediction` with `shadow=true` and did not change `OrderIntent`/`PMRun`/`CloseDecision`; researcher wrote **1** `Experiment` + **1** `ParamChange` and did not change parameter-setting labels; curator wrote **1** `Dataset` + **6** `TrainingExample` nodes and did not change gate labels during build. | ✅ Stamped live nodes DETACH DELETEd (**35** deleted, including the transient `Model` created by the check); final Aura node count restored to baseline **0**. |
| 2026-07-03 | S109 heterogeneous deliberation (GPT-5.5 debaters, Opus judge) | Split-model wiring: `kernel.deliberate` routes Defender/Challenger to the debate model and the Judge to a separate `judge_llm` (dedicated `DELIBERATION_JUDGE_*` env), threaded through the challenger-veto (now on a grounded proposition). Live functional check used a **temporary gpt-5 judge** because Opus is billing-blocked. | `uv run --extra llm python scripts/deliberate.py --real` — OpenAI gpt-5.5 debaters + **gpt-5 judge** (Opus substitute) | ✅ `make ci`: **1266 passed, 5 skipped, 100% coverage** (unit tests prove `judge_llm` routing with fakes). Live wheels-move: `MODE: real (debate OpenAI gpt-5.5 · judge OpenAI gpt-5)` → real Defender/Challenger turns + a separate-judge `VERDICT: REVISE` — the split executes end-to-end. **⚠ DEFERRED (operator-accepted, 2026-07-03):** the real-Opus judge check + the drift-firewall **golden re-freeze** are pending a funded Anthropic key (`credit balance too low`); the committed `deliberation_golden.json` is the pre-Opus baseline and MUST be re-frozen at the re-run (reminder set). | ✅ Deliberation is stateless — no nodes written, nothing to tear down. |
| 2026-07-04 | S110 forecaster signal evaluation battery | The return-model evaluation battery end to end: Tiingo export → retrain LightGBM booster → run `scripts/evaluate_return_model.py` for horizons `1,5,10,20` against held-out test split only. | Tiingo EOD API with live `.env` credential; in-tree `agents/provider/tiingo.py`; free-tier usage read from `docs/laws/tiingo-usage-limits.md` and respected with a resumable scratch export. | ✅ Tiingo-sourced booster (DL-37), **not** Postgres-sourced: 100 S&P-500 tickers, 87,700 bars, 877 bars/ticker, 85,200 label rows; training `oos_ic=0.0132`. Battery report populated every horizon with complete cases in the tens of thousands: <br>\| horizon \| cases \| ic \| rank_ic \| ic_ir \| hit_rate \| top_bottom_spread \| monotonic_fraction \| stability_mean \|<br>\| --- \| --- \| --- \| --- \| --- \| --- \| --- \| --- \| --- \|<br>\| 1 \| 25681 \| 0.0057 \| 0.0047 \| 0.0729 \| 0.5075 \| 0.0002 \| 0.7500 \| 0.4358 \|<br>\| 5 \| 25561 \| 0.0132 \| 0.0072 \| 0.1869 \| 0.5127 \| 0.0018 \| 0.5000 \| 0.4333 \|<br>\| 10 \| 25411 \| 0.0112 \| 0.0041 \| 0.2030 \| 0.5147 \| 0.0016 \| 0.5000 \| 0.4330 \|<br>\| 20 \| 25111 \| 0.0171 \| 0.0233 \| 0.2714 \| 0.5175 \| 0.0025 \| 0.5000 \| 0.4319 \| | ✅ Scratch Tiingo CSV, report JSONs, and scratch exporter deleted; ignored `models/lgbm-return-v1.txt` booster artifact deleted after evidence capture; no data artifacts staged. |
| 2026-07-04 | S111 rolling retrain + IC-decay trigger | The rolling return-model loop end to end: committed Tiingo exporter (paced, resumable, transient backoff) → fresh incumbent training → `scripts/retrain_return_model.py --force` dry-run evidence → `--apply` promotion proof on a scratch model tree. | Tiingo EOD API with live `.env` credential; S110 100-ticker list; `docs/laws/tiingo-usage-limits.md` preflight; LightGBM via `--extra forecaster`; scratch-only model tree for apply. Tiingo is the DL-37 raw-history/cheap-fallback source here; Alpaca remains the primary runtime/batch OHLCV path. | ✅ Tiingo-sourced (DL-37) export: 100 tickers, 100,400 bars, exactly 1,004 bars/ticker, zero duplicate `(ticker,date)` keys. Resumability proven by interrupting after 2 tickers, cleaning the checkpoint, resuming; HTTP 429 stopped one chunk and the exporter resumed after hourly reset. Fresh incumbent training: 97,900 label rows, train=68,530, test=29,370, `oos_ic=-0.0009`. Dry-run `--force`: `DECISION retrain=False reason=recent 0.2451 >= trigger 0.1344`; verdict `swap=False reason=challenger did not win both`.<br>\| metric \| incumbent \| challenger \| delta \|<br>\| --- \| --- \| --- \| --- \|<br>\| complete_cases \| 6000 \| 6000 \| 0.0000 \|<br>\| rank_ic \| -0.0038 \| 0.0467 \| 0.0505 \|<br>\| ic_ir \| 0.2451 \| 0.1692 \| -0.0759 \|<br>\| ic \| 0.0339 \| 0.0314 \| -0.0025 \|<br>\| hit_rate \| 0.5133 \| 0.5543 \| 0.0410 \|<br>\| top_bottom_spread \| -0.0018 \| 0.0154 \| 0.0171 \|<br>Scratch `--apply`: deliberately weakened valid incumbent produced `DECISION retrain=False reason=metric undefined`; `VERDICT swap=True reason=incumbent metric undefined`; `PROMOTED`; archive SHA-256 matched the original weakened incumbent and active file differed. | ✅ Scratch Tiingo CSV, dry-run/apply JSON reports, scratch model tree, logs, and ignored `models/lgbm-return-v1.txt` booster deleted after evidence capture; no data artifacts staged. |
| 2026-07-04 | S112 researcher backtest evidence | The researcher proposal-evidence path end to end: committed Tiingo export → `scripts/backtest_proposal.py` incumbent-vs-proposed on one bounded analyst catalogue parameter → proposed `BacktestEvidence` JSON round-trip validation. | Tiingo EOD API with live `.env` credential; S110 100-ticker list via `scripts/export_tiingo_bars.py`; `docs/laws/tiingo-usage-limits.md` preflight. Tiingo-sourced DL-37 raw-history evidence; Alpaca remains the primary runtime/batch OHLCV path. | ✅ Tiingo-sourced (DL-37) export: 100 tickers, 100,400 rows, exactly 1,004 rows/ticker, date-ascending per ticker, zero duplicate `(ticker,date)` keys, 2022-07-01 through 2026-07-02. Catalogue parameter: `analyst.rsi_period` current `14` vs proposed `21`. `BacktestEvidence.model_validate` round-tripped the written proposed JSON (`engine=walkforward-v1`, `n_days=981`). Every full and holdout metric populated; result direction was mixed and recorded plainly.<br>\| metric \| incumbent full \| proposed full \| delta \| incumbent holdout \| proposed holdout \| holdout delta \|<br>\| --- \| ---: \| ---: \| ---: \| ---: \| ---: \| ---: \|<br>\| n_days \| 988 \| 981 \| -7 \| 297 \| 295 \| -2 \|<br>\| sharpe \| 0.079165 \| 0.212281 \| 0.133116 \| 0.875607 \| 0.758461 \| -0.117146 \|<br>\| ic_mean \| -0.000944 \| 0.001028 \| 0.001972 \| 0.000706 \| 0.000766 \| 0.000059 \|<br>\| max_drawdown \| -0.285766 \| -0.203705 \| 0.082060 \| -0.111137 \| -0.095386 \| 0.015750 \|<br>\| turnover \| 0.269737 \| 0.224006 \| -0.045731 \| 0.266498 \| 0.214407 \| -0.052092 \| | ✅ Scratch Tiingo CSV, evidence JSON, captured table, and exporter logs deleted after evidence capture; no bars CSVs or report JSONs staged. |
| 2026-07-05 | S109 re-run — real-Opus judge + golden re-freeze (deferred item cleared) | The three deferred S109 proofs, now with a funded Anthropic key: (1) live functional check with the **real Opus judge**; (2) drift-firewall **golden re-freeze** with the Opus judge; (3) **firewall gate** with a weaker debater. Plus a harness bug found and fixed mid-check: `gpt-5` challenger turns returned **empty** — reproduced as `finish_reason=length`, 2000/2000 tokens spent on hidden reasoning (measured need 2560) because the OpenAI adapter shared one `max_completion_tokens` pool for reasoning + output and ignored the budget it was handed. Fixed: adapter honours `max_tokens`; debaters 8000, judge 2000. | `scripts/deliberate.py --real` + `scripts/deliberation_gate.py --freeze/--check --real --runs 3` — OpenAI `gpt-5.5` debaters + Anthropic `claude-opus-4-8` judge; probe confirmed key funded (`opus-live`, HTTP 200). | ✅ Live check: `MODE: real (debate OpenAI gpt-5.5 · judge Anthropic claude-opus-4-8)` → real Defender/Challenger + Opus `VERDICT: REVISE`. **Golden re-frozen** with the Opus judge: robust-passing `{alpha158-weight-zero, calendar-staleness, lightgbm-shadow, pooled-sigma}` (n=3, threshold 0.5). **Firewall** `--check gpt-5.4`: `regressed: none`, `VERDICT: PASS` (gate runs end-to-end, no false trip). Bug-fix verified: fixed-budget re-run gave `finish_reason=stop`, 1197-char challenger, and **`gpt-5` homogeneous verdict flipped UPHOLD→REVISE** — the empty challenger had been changing the verdict. `make ci`: **100% coverage, 1339 passed, 5 skipped**; pip-audit + detect-secrets clean. Version 0.55.01 (fix). | ✅ Deliberation is stateless — no nodes written, nothing to tear down. |
| 2026-07-05 | S114 complete deliberation evidence | The challenger-veto evidence path on the real graph: a stamped PMRun lineage with populated PM gate outcomes was driven through `orchestration.veto.deliberate_pm_node`; captured `Proposition.context` asserted every enforced gate had an explicit `PASSED`/`FAILED` outcome. | Aura `bce05bd6` (`Neo4jGraphStore`, asserted); recording fake LLM so the check exercised the real veto stage without spending model tokens. | ✅ `make ci`: **1346 passed, 5 skipped, 100.00% coverage**; import-linter kept; module hard block kept. Live check stamped `s114-livecheck-20260705T081256`; baseline Aura nodes `0`. Captured context included all required outcome lines:<br>\| gate \| rendered outcome line \|<br>\| --- \| --- \|<br>\| sizing \| PM gate outcome: name=sizing value=0.0812 threshold=0.1 -> PASSED (quantity=7; max_position_pct=0.10) \|<br>\| max_sector_pct \| PM gate outcome: name=max_sector_pct value=0.0812 threshold=0.3 -> PASSED (sector=Technology) \|<br>\| max_names_per_sector \| PM gate outcome: name=max_names_per_sector value=2 threshold=3 -> PASSED (sector=Technology) \|<br>\| reward_risk \| PM gate outcome: name=reward_risk value=2.667 threshold=1.5 -> PASSED (target_pct=0.08; stop_pct=0.03) \|<br>\| confidence_floor \| confidence_floor gate: confidence=0.620 vs base_min_confidence=0.570 -> PASSED \|<br>\| stop_vs_regime_volatility \| stop_vs_regime_volatility gate: stop_pct=3.00% vs base_stop_loss_pct=3.00% -> PASSED; target_pct=8.00% vs base_take_profit_pct=8.00% -> PASSED; stop_pct=3.00% vs ATR%=4.31% -> FAILED \| | ✅ Stamped live nodes DETACH DELETEd (`deleted=6`) → Aura restored to baseline **0**; scratch script `data/s114_live_check.py` deleted; no data/report artifacts staged. |
| 2026-07-05 | S113 governed factor proposal | The governed factor-mining proposal path end to end: Tiingo export → live LLM selects from the bounded researcher catalogue → deterministic S112 walk-forward harness scores it → `FactorProposal.model_validate` round-trip → forced off-menu selection fails open. | Tiingo EOD API with live `.env` credential; S110 100-ticker list via `scripts/export_tiingo_bars.py`; `docs/laws/tiingo-usage-limits.md` preflight; OpenAI `gpt-5.5` via `scripts/mine_factors.py --real`. | ✅ Tiingo-sourced export: **100 tickers**, **25,000 rows**, exactly **250 bars/ticker**, date range **2025-07-07..2026-07-02**, zero duplicate `(ticker,date)` keys. Export respected the 50/hour budget: chunk 1 wrote 50, immediate resume hit HTTP 429 after one more ticker, then the post-reset resume wrote the remaining 49. GPT-5.5 selected in-catalogue `momentum` with `lookback=60`; `FactorProposal` round-trip passed. Backtest evidence populated: `engine=walkforward-v1`, `n_days=188`, `window=2025-09-30..2026-07-01`, `sharpe=1.544717`, `ic_mean=0.016306`, `max_drawdown=-0.076077`, `turnover=0.096011`, `holdout_sharpe=1.308173`, `holdout_ic_mean=0.013771`, `slippage_bps=10.0`. Forced `invented_factor` selection returned `off-catalogue selection - no proposal generated`, exit 0, no output JSON. | ✅ Deleted scratch `data/s113-live-bars.csv` and `data/s113-live-proposal.json`; off-menu run wrote no file; no CSV/JSON artifacts staged. |
| 2026-07-06 | S115 factor shadow signal | Scratch operator settings enabled the approved S113-style factor (`momentum`, `lookback=60`, `factor-s115-live-momentum-60`) and drove `forecast_factor` for real tickers; then unset settings proved the default-off refusal path. | Aura `bce05bd6` (`Neo4jGraphStore`, asserted); Tiingo EOD API via in-tree `TiingoDataSource`; `docs/laws/tiingo-usage-limits.md` preflight; `uv run --extra runtime python -`. | ✅ Aura baseline `0`; real Tiingo bars for `AAPL,MSFT`; `ShadowPrediction` nodes written under `factor-s115-live-momentum-60` with `shadow=true`; generic `scorecard` returned `sample_size=2`, `mean_confidence=1.000000`, `promotion_eligible=False`. Default-off path returned `model_id=factor-disabled`, `value=0.5`, `confidence=0.0`, `graph_node_id=None`, and wrote **0** graph nodes. `make ci`: **1370 passed, 5 skipped, 100.00% coverage**; import-linter and module hard block kept; detect-secrets passed. | ✅ Pre-clean deleted `0`; teardown deleted `3` stamped factor nodes (`Model` + 2 `ShadowPrediction`s) → Aura restored to baseline **0**. Provider scratch graph was in-memory; no CSV/JSON/data artifacts were created or staged. |
| 2026-07-07 | S116 PostgresGraphStore | PostgreSQL graph spine adapter end to end: Alembic-owned schema upgrade, env-gated Postgres backend parity, and a stamped PMRun lineage through `build_graph_from_env` → `build_veto_context`. | Neon free Sydney PostgreSQL (`POSTGRES_DSN` from explicit `.env`, DSN never printed); `PostgresGraphStore` asserted. | ✅ `alembic upgrade head` applied `0001_spine`; Postgres backend suite with `POSTGRES_TEST_DSN` on Neon: **7 passed**; live slice stamped `s116-livecheck-20260707T083106` selected `PostgresGraphStore`, wrote MarketData→ScanRun→AnalystRun→PMRun lineage, and rendered PM gate, analyst, scanner, market, and confidence-floor context fragments. Local `uv run pytest`: **1379 passed, 6 skipped, 100.00% coverage**. | ✅ `uv run python scripts/pg_teardown.py --prefix s116-livecheck-20260707T083106 --env-file .env` deleted **3 edges / 4 nodes**; follow-up query verified **nodes=0, edges=0** for the stamp. |
| 2026-07-07 | S117 Postgres fleet swap | The fleet default path was flipped to PostgreSQL: Key Vault seeding used the S108 tested-before-insert path for `postgres-dsn`, deployment now runs Alembic before start, `DEP-POSTGRES` probes the live store, and an S99-style in-process fleet slice wrote durable forecaster/researcher/curator artifacts through the default composition with `NEO4J_URI` absent. | Azure Key Vault `trading-agents-kv`; Neon free Sydney PostgreSQL (`POSTGRES_DSN` loaded from explicit `.env`, DSN never printed); `PostgresGraphStore` asserted. | ✅ `uv run --extra azure --extra runtime python scripts/seed_key_vault.py --only postgres-dsn` dry-run reported `postgres probe passed`; `--apply` reported `postgres-dsn seeded postgres probe passed`; read-back was present and byte-equal to `.env`. `alembic upgrade head` completed against Neon. `DEP-CONFIG-01` green with Postgres + feed/LLM creds; `DEP-POSTGRES-01` green with `SELECT 1`. Live slice stamped `s117-livecheck-20260707T194720` served forecaster, researcher, and curator; a separate raw connection verified durable nodes (`CloseDecision:6`, `Dataset:1`, `Experiment:1`, `Flag:1`, `Model:1`, `ParamChange:1`, `Position:6`, `ShadowPrediction:1`, `Snapshot:6`, `TradeNarrative:6`, `TrainingExample:6`) and **26** edges. Local `make ci`: **1383 passed, 6 skipped, 100.00% coverage**. | ✅ `uv run python scripts/pg_teardown.py --prefix s117-livecheck-20260707T194720 --contains --env-file .env` deleted **26 edges / 36 nodes**; follow-up raw query verified **post_teardown_nodes=0, post_teardown_edges=0** for the stamp. |
| 2026-07-07 | S118 Neo4j runtime rip-out | ADR-0014 end state: Neo4j adapter/tests/dependency/probes/scripts removed from runtime; `NEO4J_URI`-only env fails loudly instead of falling back; Neo4j remains only as the ADR-0008 workbench compose profile. | Fresh local env synced without the `neo4j` package; Neon free Sydney PostgreSQL (`POSTGRES_DSN` loaded from explicit `.env`, DSN never printed); `PostgresGraphStore` asserted. | ✅ `uv sync --extra runtime --extra postgres` uninstalled `neo4j==6.2.0`; `uv pip show neo4j` reported package not found; `importlib.util.find_spec("neo4j")` returned absent. Runtime import grep for `from neo4j`, `import neo4j`, `graph_neo4j`, `graph_cypher`, and `Neo4jGraphStore` across runtime/tests/deps returned no matches. `make ci`: **1374 passed, 5 skipped, 100.00% coverage**; module hard block kept; detect-secrets passed; pip-audit found no known vulnerabilities. Live Neon check stamped `s118-livecheck-20260707T205942`: forced `POSTGRES_DSN`-only, asserted `PostgresGraphStore`, wrote one `S118LiveCheck` node, and raw SQL verified `durable_count=1`. `NEO4J_URI`-only env raised the ADR-0014 error naming `POSTGRES_DSN` and local in-memory development. | ✅ `uv run python scripts/pg_teardown.py --prefix s118-livecheck-20260707T205942 --env-file .env` deleted **0 edges / 1 node**; follow-up raw query verified **post_teardown_nodes=0, post_teardown_edges=0** for the stamp. |
| 2026-07-07 | S100 Service Bus receiver | Claim-checked served request over Azure Service Bus: a disposable request topic carried only the ready event; `AzureServiceBusRequestConsumer` pulled it from a subscription, resolved the `AgentMessage`, `serve_once` dispatched it, and the response came back as a reply-topic ready event resolved via claim-check. | Azure Service Bus namespace `trading-agents-bus` (RG `trading-agents`); `uv run --extra azure python scripts/servicebus_receiver_live_check.py`; connection string loaded from `.env`/settings and never printed. | ✅ Live smoke output: `run_id=s100-a721bfdb5d8d`, request topic `s100-a721bfdb5d8d-request`, reply topic `s100-a721bfdb5d8d-reply`, `served=1`, `reply_payload={"run_id": "s100-a721bfdb5d8d", "served": true}`, `correlation_id=c8ec066f-8589-4c45-8c3c-133221d7b688`. This exercised the optional Azure SDK receive path, ack-on-reply, and claim-check response publication. | ✅ Script teardown deleted `s100-a721bfdb5d8d-request` and `s100-a721bfdb5d8d-reply`; follow-up namespace sweep reported `remaining_s100_topics=[]`. Earlier failed-run residue (`s100-81c5c8b897b1-*`) was also swept; no data files committed. |
| 2026-07-08 | S102 distributed fleet run-through | The full 13-container fleet on branch images: env-selected Service Bus serve transport, master activation for all 12 agents, one distributed graph-pull `RunRequest`, Alpaca paper broker submission, and five served control-plane round-trips. | Azure Container Apps env `trading-agents-env` (RG `trading-agents`); GHCR images `:s102` built by GitHub Actions run `28877717323` from commit `b50b544`; Neon PostgreSQL via Key Vault `postgres-dsn`; Azure Service Bus `trading-agents-bus`; Alpaca paper broker. | ✅ Deploy preflight passed (`POSTGRES_DSN` probe, Service Bus config, GHCR 13/13) and `alembic upgrade head` ran. Activation proof in Postgres: latest active `AgentInstance` + `CapabilityGrant` for all 12 agent types. Run `s102-dist-20260707T1530Z` completed `RunRequest -> MarketData -> ScanRun -> AnalystRun -> PMRun -> ExecutionRun -> MonitorRun -> Snapshot`; trace: 16/16 OHLCV returned, 4 scanner survivors, 3 analyst recommendations, 3 PM approvals, `ExecutionRun submitted=3 rejected=0`; fills had non-empty UUID-like Alpaca broker order IDs. Observatory: **`OBSERVATORY  OK - all invariants hold`**. Acceptance: **`ACCEPTANCE  PASS - every stage did its job within its boundaries`**. Service Bus proofs: operator `interpret` returned an intent; supervisor `report_fault` wrote `Fault` and `dispatch_intent` tripped confirmation `Flag`; forecaster wrote `ShadowPrediction shadow=true`; curator wrote `Dataset`; researcher served `propose`. Local `make ci`: **1393 passed, 5 skipped, 100.00% coverage**. | ✅ `scripts/pg_teardown.py` sweeps deleted **33 edges / 58 S102 nodes**; follow-up graph query reported `remaining_s102_artifacts={}`. Activation registry/audit rows intentionally stayed as production boot evidence (`Session=5`, `AgentInstance=115`, `CapabilityGrant=257`). Service Bus cleanup deleted 11 disposable `s102-*.reply` topics and verified `remaining_s102_topics=[]`; stable served request topics stayed (`curator.requests`, `forecaster.requests`, `operator.requests`, `researcher.requests`, `supervisor.requests`). `infra/deploy-agents.ps1 down` deleted all 13 Container Apps; final `az containerapp list` returned no app names. |
| 2026-07-08 | S103 dispatcher cron | The hands-off scheduled-run path end to end: Azure Container Apps Job dispatcher, KEDA cron-scaled standing 13-app fleet, provider-calendar trading-day gate, day-keyed `RunRequest`, distributed graph-pull run to `Snapshot`, double-fire idempotency, non-trading skip, and post-window scale-to-zero proof. | Azure Container Apps env `trading-agents-env` (RG `trading-agents`); GHCR images `:s103` built by GitHub Actions run `28885499730`; Neon PostgreSQL via Key Vault `postgres-dsn`; Azure Service Bus `trading-agents-bus`; Alpaca paper broker. | ✅ Deploy preflight passed (`POSTGRES_DSN` probe, Service Bus config, GHCR `14/14`) and `alembic upgrade head` ran. Dispatcher job `dispatcher-cron-k5n6da4` logged `placed sched-2026-07-08 reason=NYSE trading session`; distributed trace completed `RunRequest -> MarketData -> ScanRun -> AnalystRun -> PMRun -> ExecutionRun -> MonitorRun -> Snapshot` with provider `99/99` tickers / `3960` bars, scanner `5` survivors, analyst `1` recommendation, PM `1` approval, execution `submitted=1`. Acceptance: **`ACCEPTANCE  PASS - every stage did its job within its boundaries`**. Second fire `dispatcher-cron-v0fbr3u` logged the same placed line; lineage proof after double-fire showed `run_request_count=1` and exactly one full chain for `sched-2026-07-08`. Injected Saturday `2026-07-04` execution `dispatcher-cron-47cppif` logged `skipped sched-2026-07-04 reason=2026-07-04 is not a NYSE trading session` and wrote `0` RunRequests. Cost proof after restoring the normal cron window: all 13 apps reported `replicas=0`; dispatcher executions were terminal only and the job stayed deployed. Local `make ci`: **1404 passed, 5 skipped, 100.00% coverage**; ruff, format, mypy, import-linter, module hard block, headers, pip-audit, and detect-secrets passed. | ✅ `scripts/pg_teardown.py --run-id sched-2026-07-08` deleted **18 edges / 17 nodes** and `--run-id sched-2026-07-07` deleted **11 edges / 10 nodes** for the earlier incomplete fixture-universe proof. Follow-up graph checks reported `lineage_nodes=0 run_requests=0` for `sched-2026-07-07`, `sched-2026-07-08`, and skipped `sched-2026-07-04`; raw SQL reported `remaining_sched_nodes=0 remaining_sched_edges=0`. The CSCO Alpaca paper order was terminal `filled` (no open order to cancel). Production registry rows, Service Bus topics, secrets, deployed apps, and `dispatcher-cron` intentionally stayed. |
| 2026-07-08 | S119 DSPy deliberation roles | DSPy-compiled opt-in role prompt artifacts for Defender, Challenger, and Judge behind the ADR-0010 `PromptOptimizer` port; runtime default stayed byte-identical unless `DELIBERATION_PROMPT_ARTIFACT_DIR` is set. | OpenAI `gpt-5.5` debaters + Anthropic `claude-opus-4-8` judge via explicit `.env`; DSPy compile via `--extra optimizer`; report/transcripts in `docs/reports/sprint-119-deliberation-roles/`; no local Docker path. | ✅ Compiled artifacts v4: defender/challenger `gpt-5.5`, judge `claude-opus-4-8`. Final report call plan `6 cases x 4 prompt sets x 3 repeats = 72 debate calls (+72 scorer calls)`; per-role table all firewall PASS (defender `78%/78%`, challenger `61%/61%`, judge `94%/94%` challenger kw/judge). Formal `deliberation_gate.py --check gpt-5.5 --real --runs 3` PASS for champion and each loaded artifact; env-load real deliberation printed artifact stamps and `VERDICT: REVISE`; unset env proved `UNSET_PROMPTS_BYTE_IDENTICAL: True`. `make ci`: **1421 passed, 5 skipped, 100.00% coverage**; import-linter kept; module hard block kept; detect-secrets passed; known ignored `diskcache 5.6.3 / CVE-2025-69872` advisory remains. | ✅ No graph store was constructed by `scripts/deliberate.py`; no graph writes occurred and no graph teardown was needed. Live conversations are saved under `docs/reports/sprint-119-deliberation-roles/live-report-transcripts/`. |
| 2026-07-08 | S120 broker reconciliation | Broker truth for holdings, graph truth for lineage: execution run-start `BrokerPositionSnapshot`, pending broker-order status evidence, loud divergence `Flag`, monitor adoption into `reconciled-from-broker` Positions, and PM held-position gates from active graph Positions. | Real Alpaca paper account; Neon PostgreSQL via `POSTGRES_DSN` loaded from `.env` and never printed; branch `sprint-120-broker-reconciliation`. | ✅ `make ci`: **1436 passed, 5 skipped, 100.00% coverage**. Live branch check stamped `s120-livecheck-20260708T054129Z`: broker reported AMD 19 @ 51235, CSCO 88 @ 11277, HPE 229 @ 4355, MRVL 44 @ 22621; raw Neon verified active `Position` nodes `broker-reconciled:{AMD,CSCO,HPE,MRVL}` with `provenance=reconciled-from-broker`. The expected first-pass missing-position Flag already existed from a stale S120 live repair and was retained as production evidence: missing graph Position for AMD/CSCO/HPE/MRVL with broker quantities 19/88/229/44. Current-branch first and second run-start passes both wrote fresh snapshots and produced `new_flag_delta=0`, proving idempotence against the already repaired graph. Pending CSCO Fill `pm-run-b4e1cd7618b14bdbb7ecc98ee4d0a370:CSCO:buy` / broker id `632f0604-d36a-4f82-9c19-d621f19710ad` still reports `pending` from Alpaca, so two `BrokerOrderStatus status=pending` evidence nodes were appended and no terminal status was fabricated. | ✅ No order was placed/canceled/sold/flattened. No account holdings or reconciled `Position` rows were torn down; they are production state under DL-44. No disposable stamped extras were created beyond append-only reconciliation audit evidence (`BrokerPositionSnapshot`, `BrokerOrderStatus`). |
| 2026-07-08 | S121 judge promotion + challenger recompile | First ADR-0010 prompt promotion in the deliberation default path: compiled Judge promoted into `JUDGE_SYSTEM`, golden re-frozen, Challenger recompiled once and adopted after beating the promoted-judge champion; Defender untouched. | OpenAI `gpt-5.5` debaters + Anthropic `claude-opus-4-8` judge via explicit `.env`; role-only compile via `--extra optimizer`; reports/transcripts in `docs/reports/sprint-121-judge-promotion-challenger-recompile/`; no artifact env opt-in for the final default check. | ✅ Golden re-freeze moved robust-passing set from `{alpha158-weight-zero, calendar-staleness, lightgbm-shadow, pooled-sigma}` to `{alpha158-weight-zero, calendar-staleness, fixed-fraction-size, lightgbm-shadow, pooled-sigma}`; re-frozen firewall check PASS. Challenger call plan `6 cases x 4 prompt sets x 3 repeats = 72 debate calls (+72 scorer calls)` under `--max-debate-calls 100`; promoted-judge comparison showed Challenger `100%/100%` vs champion `94%/94%`, stability `100%` vs `100%`, firewall PASS, so `CHALLENGER_SYSTEM` was promoted from artifact `2026-07-08-s121-v5-challenger-gpt-5.5`. Final default deliberation proved `ENV_OPT_IN_SET: False` and returned `VERDICT: REVISE`; final-default firewall PASS with `gained: ['name-correlation']`. `make ci`: **1439 passed, 5 skipped, 100.00% coverage**; module hard block and headers passed; detect-secrets passed; known ignored `diskcache 5.6.3 / CVE-2025-69872` advisory remains. | ✅ Deliberation scripts did not construct a graph store; no graph rows were created and no graph teardown was needed. |

**Production operations (deliberate environment changes, for the audit trail):**

| Date | Operation | What was done | Result | Notes |
| --- | --- | --- | --- | --- |
| 2026-07-03 | Production Key Vault seed | `scripts/seed_key_vault.py --apply` (operator-approved) seeded all 8 manifest secrets into `trading-agents-kv` after live probes. | ✅ **8/8 seeded.** Read-back: every vault value present + byte-equal to `.env`. Master path: `resolve_config` against the live vault resolved **complete** entitlements for provider (5), execution (2), operator (1), all values matching the seeded source. | Fail-closed proven on a **real** bad credential first: a mistyped FMP key (I/l transcription) got 401 → `rejected`, never written; corrected key passed then seeded. The vault now intentionally holds the production secrets (not torn down). |

**Lessons (harness):** a first S98 attempt "passed" but silently ran in-memory — `build_graph_from_env()`
returns `InMemoryGraphStore` unless the process has a real graph env (`POSTGRES_DSN`), and a
`load_dotenv()` called from a script *outside* the repo tree (e.g. the scratchpad) never finds `.env`.
**A real check must load `.env` by explicit path and assert `isinstance(graph, PostgresGraphStore)`**
before trusting the result. This false "durability bug" is the reason functionality checks must verify
the environment, not assume it (LAW-02).
| 2026-07-10 | S122 dashboard run view | The dashboard surface served against the **live production graph** (read-only): `/api/runs` listed all 3 real scheduled runs newest-first; `verdict` for `sched-2026-07-09` = FAIL + `no_trade_day=true` + annotation, `sched-2026-07-08` = PASS — both **matching `scripts/accept.py`**; `flags` surfaced the real pending critical divergence Flag; `positions` joined graph-vs-broker per ticker (6 rows, all `match=true` after the S120 adoption); `bundle` returned all stable keys (`meta.ticker_count=99`, `known_runs`=3); `/` served the frontend. | Neon Postgres (`POSTGRES_DSN`, `PostgresGraphStore` via `build_graph_from_env` from repo `.env`); `uv run python -m surfaces.dashboard` on 127.0.0.1:8300 | ✅ All endpoints correct vs the accept/trace ground truth; `make ci` **1463 passed, 5 skipped, 100.00% coverage**. Screenshots not captured (CLI session) — visual pass pending operator's first open, all rendered data verified at the API layer. | ✅ Reads only — no graph writes; server process stopped. |
| 2026-07-10 | S123 dashboard fleet + infrastructure | Served Sections I/II against **live Neon and Azure** through the new read-only ports. `/api/infra` returned **13 Container Apps + `dispatcher-cron`** (14 table rows), every real image tag `:s121`, all direct replica reads `0` outside the window, and latest dispatcher execution `Succeeded`; the execution drawer returned a real Log Analytics row for the last bounded window. Cost Management returned 4 real MTD service rows totaling **A$0.001062**; the `LLMCall` ledger had no July calls (**A$0.00**) and the dashboard preserved unknown-model=`untracked`. Operator-directed A$ conversion uses the committed Commonwealth Bank **Send IMT** snapshot (`1 AUD=0.7171 USD`; `1 USD=A$1.39450565`, 2026-07-10 18:06:11 AEST). `/api/vitals` matched the graph's **2 pending Flags**, broker↔graph in sync, 4 degraded feeds, spine/bus reachable, `:s121` images, and A$ MTD cost. Live `/bundle` returned **14 image entries + 14 log containers**, 13 with rows, `execution=1`, and `errors=0`. | Neon Sydney (`PostgresGraphStore`, asserted; DSN never printed); Azure Container Apps `trading-agents-env`, job `dispatcher-cron`, Log Analytics, Cost Management; local `DASHBOARD_AZURE_CREDENTIAL_MODE=cli` because the existing `AZURE_SP_*` principal returned ARM 403. | ✅ Visual/API pass. Evidence: `docs/reports/sprint-123-dashboard-fleet-infra/section-i-infrastructure.png`, `execution-log-drawer.png`, `section-ii-fleet-vitals.png`. Degraded no-credential mode is covered at HTTP 200. `make ci`: **1489 passed, 5 skipped, 100.00% coverage**. Finding: grant the service principal ARM Reader (and relevant Logs/Cost readers) before using `auto` without CLI. | ✅ Strictly read-only: local server stopped; no graph, broker, Azure, or CBA writes; no data teardown required. |
| 2026-07-11 | S124 dashboard glance-first verdict | Served the new master verdict against the **live Neon spine and Azure reads**. `PYTHONPATH=. uv run python scripts/accept.py --run-id sched-2026-07-10` exited **0** with `ACCEPTANCE NO_TRADE`; `/api/verdict?run=sched-2026-07-10` returned **GREEN**, no faults, and `Run completed — no trades: 5 candidates below the confidence bar.` The hero followed the selected run, exposed next fire + 2 warning rows, and retained all prior drill-down. All four persisted scheduled runs were complete, so the RED visual used the read-only absent-run deep link `sprint-124-incomplete-proof`: **RED**, `Run stopped before provider — 0/7 stages completed.` No synthetic node was created. | Neon Sydney (`PostgresGraphStore` through repo `.env`, DSN never printed); Azure read port with `DASHBOARD_AZURE_CREDENTIAL_MODE=cli`; local dashboard on 127.0.0.1:8300. | ✅ Gate flip and both visual paths proven. Evidence: `docs/reports/sprint-124-dashboard-verdict/green-no-trade.png` and `red-incomplete.png`. `make ci`: **1511 passed, 5 skipped, 100.00% coverage**. | ✅ Strictly read-only: no graph, broker, bus, or Azure writes; local server stopped, port 8300 released, and temporary browser/server profiles removed. No data teardown was required. |
| 2026-07-13 | S125 dashboard operator chat | Served the dashboard against the **live Neon spine** with the real Anthropic key and selected the latest scheduled run, `sched-2026-07-10`. The exact question **“how did we go last night”** traversed the existing dashboard-channel operator dispatch and returned evidence from the selected run: 99 scanned tickers → 5 candidates → all 5 below the analyst confidence floor → 0 recommendations/orders; monitor checked 4 existing positions; all four enrichment feeds were degraded; and both pending critical broker/graph divergence flags were named with their affected holdings. Repeating the exact question produced distinct fresh audit IDs, proving every POST is append-only even when text repeats. The final intent+explanation exchange used 424 input / 323 output tokens; the committed catalogue priced it at **US$0.006117 / A$0.008530**, `pricing_as_of=2026-07-10`, with **0 untracked**. Unsetting the key returned HTTP 200 and the exact honest state `chat is not connected on this deployment`, with no usable input. | Neon Sydney (`PostgresGraphStore`, asserted; DSN never printed); real Anthropic API through the existing `AnthropicLLMClient`; local threaded dashboard on 127.0.0.1:8314. | ✅ Live answer, repeat-message audit, pricing, and disconnected degradation proven. Evidence: `docs/reports/sprint-125-operator-chat/grounded-answer.png`, `not-connected.png`, and exact cost output in `priced-ledger.json`; the full diagnostic/manual session totals **A$0.047303**, 0 untracked. Browser-control discovery remained unavailable, so the screenshots were captured manually from the live page. `make ci`: **1531 passed, 6 skipped, 100.00% coverage**, all 9 steps green. | ✅ Local server stopped, port 8314 released, and temporary logs removed. Live audit records are deliberately **kept**, not deleted: `CommandAudit` keys `audit:0300103d06eb36e8`, `audit:04771bebcca6d7c5`, `audit:0647f72c6eea6be6`, `audit:25f5219e2008b340`, `audit:42f6e230c50ccc4b`, `audit:4e2af3be0053adfe`, `audit:5af8fd4324c0449f`, `audit:6eb32e9c3e421a89`, `audit:7ff78dec939a864b`, `audit:90eaef0915cffc87`, `audit:ab5f404728ce734c`, `audit:ccf873089899f485`; matching `LLMCall` keys use the same 12 suffixes with the `llmcall:` prefix; `Intent` keys `intent:0300103d06eb36e8`, `intent:25f5219e2008b340`, `intent:7ff78dec939a864b`, `intent:90eaef0915cffc87`, `intent:ab5f404728ce734c`, `intent:ccf873089899f485`. These are production audit history, not disposable test residue. No broker, bus, or Azure writes. |
| 2026-07-14 | S126 resume + deploy tripwire | From live dashboard `/api/chat`, `Resume from monitor` for completed `sched-2026-07-13` carried one request/audit ID across the explicit confirmation and created the single superseding child `sched-2026-07-13-resume-monitor`. Provider through execution were linked with `LINKED_FROM`; only monitor/reporter re-derived; `trace_run` returned **7/7**. Monitor checked four existing positions, held all four, closed none; Alpaca showed **0 orders created** during the check. The original run remained untouched. Currency compared verified historical `DeployRecord(:s121, 15e9688…)`, observed fleet tag `:s121`, and newest successful main image build `29299680862 / 9420b78…`: **behind**, with fleet-record match true and main-record match false. With Azure credentials unset, bus health was **unverified**, not unreachable. | Neon Sydney (`PostgresGraphStore`, asserted; DSN never printed); Anthropic operator path; read-only Azure CLI and GitHub Actions APIs; Alpaca paper read; local dashboard ports 8316/8317. | ✅ Complete. API/graph/broker evidence and node inventory in `docs/reports/sprint-126-resume-and-tripwire/live-proof.md`; screenshots captured 2026-07-15 by the planning agent from the live local dashboard: `resume-child-run.png` (child run GREEN/PASS, 7/7 with resume controls, vitals `deploy behind · :s121`, broker-consequence confirm wording verbatim) and `bus-unverified.png` (`bus unverified` + `deploy unverified`, light stays GREEN). At capture, main had advanced to build `29329555693`/`a5bbb5f…` and the judgement still read **behind** — the tripwire tracked main moving. Planning-agent `make ci` on the exact tree: **1566 passed, 5 skipped, 100.00% coverage**, all 9 steps green (pip-audit: accepted diskcache baseline only). | ✅ All graph writes are deliberately retained audit/provenance/monitor history: the historical `DeployRecord`, child lineage, five linked upstream clones + linked regime, monitor/snapshot/four checks, and operator audit/intent/LLM/flag/resolution/message nodes are named in `live-proof.md`. Two diagnostic warning flags were append-resolved. No graph row/edge was deleted or overwritten; no PM/execution/broker/Azure write occurred. Local server processes are stopped after capture. |
| 2026-07-15 | S127 fixpack | Live row-11 operator interpret for the three 2026-07-14 approve phrasings returned typed `approve` intents with fresh audits `audit:753f6487336dde4c`, `audit:d607b17e502c83f8`, and `audit:dd7ce80835223924`. From the live dashboard, exactly one stale S126 confirm-intent warn flag (`15bb3e29df185949`) was acknowledged through the existing chat/confirm path; the confirmation dialog rendered the typed intent verbatim, the result appended `FlagResolution`, and pending flags dropped `2 -> 1`. The remaining critical broker-divergence flag was left untouched. Deploy currency read the dispatcher job template as `:s126` and judged `current` while still rendering "last execution ran :s121"; the unit truth table proves template-behind and template-unreadable cases. Warning-link click landed on the Flags panel. The Azure receiver integration was also proven in a no-Azure throwaway venv as `1 skipped`, not failed. | Neon Sydney (`PostgresGraphStore`, asserted; DSN never printed); dashboard on 127.0.0.1:8327; Anthropic operator path; read-only Azure CLI/GitHub Actions APIs; no broker or Azure control-plane writes. | Complete. Evidence and node inventory in `docs/reports/sprint-127-fixpack/live-proof.md`; screenshots: `flag-ack-before.png`, `flag-ack-confirm-intent.png`, `flag-ack-after.png`, `currency-template-current.png`, and `warning-link-flags.png`. Final `make ci` result is recorded in the Sprint 127 closeout block. | Retained: row-11 `CommandAudit`/`Intent`/`LLMCall` triples; row-10 `audit:a77b5cf18683b893`, `intent:a77b5cf18683b893`, `llmcall:a77b5cf18683b893`, `a77b5cf18683b893:approve`, `flag:a77b5cf18683b893:warn`, `resolution:flag:a77b5cf18683b893:warn`, and `resolution:flag:15bb3e29df185949:warn`; the `s126` `DeployRecord` remains production deploy provenance. Torn down: local dashboard server stopped and port 8327 released; throwaway no-Azure venv deleted. No graph row was deleted or overwritten. |
| 2026-07-16 | S128 feed resilience — live preflight blocked | Code proof completed for DRIFT-021 feed resilience: Finnhub request pacing, per-ticker attribution in fundamentals/news/sectors/earnings, attributed note merge, dashboard/verdict degraded-feed compatibility, and DRIFT-012 `used_fallback` invariant. The live proof precondition failed before any full-universe Finnhub run: DRIFT-023 still reads OPEN and the read-only Postgres probe returned `DEP-POSTGRES-01 RED postgres: reachable OperationalError`. | Local unit/integration gate slice; Neon Postgres preflight through `probe_postgres(load_creds())` inside `uv` with DSN suppressed. No scanner/PM/execution, no broker call, no `sched-*` run id. | Blocked, not corrected live. Evidence: `docs/reports/sprint-128-feed-resilience/code-and-live-preflight.md`. Focused proof: 18 passed; broader provider/dashboard proof: 155 passed, 2 skipped; scoped ruff and mypy passed. Full `make ci` result is recorded in the Sprint 128 closeout block. | No disposable run id was created; no MarketData or RegimeContext nodes were written; no graph teardown sweep was possible or required for Sprint 128 live artifacts while the spine was unreachable. |
| 2026-07-19 | S128 feed resilience — live check complete | DRIFT-023 verified resolved first (`DEP-POSTGRES-01 GREEN` on `SELECT 1`), then the three-leg provider-stage live check: (1) paced full-universe ingest (99 tickers, 55 req/min) → all four feeds populated, zero degraded notes of any kind, `used_fallback=False`, elapsed 429.0 s — inside the fleet window; (2) unpaced full-universe ingest with the live Finnhub 60/min limit as the fault injector → all four feeds carried bounded attributed notes `<feed>_degraded:39:MET,META,MMM,MO,MRK:429` with the majority kept (60/99 per feed) and zero whole-feed notes; (3) durability — a 40-ticker Neon-wired run persisted `sectors_degraded:20:…:429` + `earnings_degraded:20:…:429`, read back over a fresh psycopg SELECT after the writing process exited. | Live Finnhub free tier (real 429s, no mocks) + live Neon Postgres via `POSTGRES_DSN`; provider stage only — no scanner/PM/execution, no broker, no `sched-*` ids; exactly two full-universe ingests (guardrail). | PASS — DoD 1–3 proven live; DRIFT-021 → CORRECTED. Deviations recorded honestly (runs 1–2 wrote in-memory due to a driver dotenv-path defect; durability came from small Neon-wired runs): `docs/reports/sprint-128-feed-resilience/live-check.md`. | `pg_teardown --prefix s128-live --contains`: deleted 12 nodes + 4 edges; remaining `s128-live` nodes=0, edges=0. |
| 2026-07-19 | S129 fixpack | Quant evidence now persists full bounded `ScoreBreakdown.metrics` on Recommendation nodes and renders in all three deliberation roles; dashboard projection reads use a seconds-scale TTL cache and widened self-heal interval; CI hardening D/E added dependency-review and Trivy gates; hardening backlog C/D/E/F reconciled. | Local opt-in deliberation run with recording LLM; live Neon dashboard served locally with fixed GET script; GitHub PR #50 and manual `build-images.yml` dispatch. | PASS for functionality: transcript cited newly available `composite_score`; live dashboard Postgres calls dropped `18 -> 0` inside the 5 s TTL while verdict stayed `GREEN` (`1 order, 1 candidate`); PR security job dependency-review step succeeded; Trivy executed on `analyst`, `master`, and `forecaster` and failed on existing HIGH/CRITICAL base-image findings as intended. Evidence: `docs/reports/sprint-129-fixpack/live-proof.md`. | `pg_teardown --prefix s129-livecheck --contains`: deleted 0 nodes + 0 edges; deliberation was in-memory; dashboard server stopped; no graph/broker/Azure control-plane writes. |
| 2026-07-19 | S130 base-image chore | Backlog row H is drained: Trivy now keeps HIGH/CRITICAL `exit-code: 1`, adds `ignore-unfixed: true`, scans every image in `build-images.yml`, and all 14 agent/dispatcher images use two-stage `dhi.io/python:3.13-dev` → `dhi.io/python:3.13` builds with direct `python` runtime commands. | GitHub Actions manual `build-images.yml` dispatch with `image_tag=s130-test`; provider probe first, then all 14 GHCR images built/pushed/scanned; provider runtime smoke ran by digest inside the authenticated runner because local Docker/GHCR package access was unavailable. | PASS for functionality: final run `29681635979` succeeded; all 14 Trivy steps were green and the actionable HIGH/CRITICAL gate count dropped from S129's representative `22` to `0` gate-blocking findings. Provider smoke reached `agents.provider.entrypoint` and failed loudly with `urllib.error.URLError: <urlopen error [Errno -2] Name or service not known>` due missing master config. Provider size moved `215286970 -> 149756082` bytes. Evidence: `docs/reports/sprint-130-base-image/live-proof.md`. | No graph store was constructed; no graph/broker/Azure control-plane writes; no fleet retag. State sweep count `0`. `s130-test` GHCR tags are retained as CI artifacts. |
| 2026-07-20 | S131 blast radius | Per-agent Postgres blast-radius reduction: 15 `ta_<agent>` LOGIN roles with identical spine grants/default privileges, Key Vault per-role DSNs, secret-backed `POSTGRES_DSN` delivery to each Container App/job, revocability canary, and dispatcher import-closure image slim. | Neon Sydney via admin `POSTGRES_DSN` loaded into process env only; Azure Key Vault `trading-agents-kv`; Azure Container Apps env `trading-agents-env`; GitHub Actions `build-images.yml` run `29714326960`. No generated password/DSN was printed or written to the repo tree. | PASS with one operator follow-up: role provisioning exited `0`; `postgres-flip` returned `[OK] ... POSTGRES_DSN secretref` for all 14 targets; read-only `az ... show` proved every target uses `POSTGRES_DSN.secretRef=postgres-dsn`; preflight proved `per-target Postgres DSNs: 14/14`. Controlled `pg_stat_activity` audit held 15 live connections and saw all permanent roles (`PG_STAT_ACTIVITY_ROLE_COUNT=15`). `ta_canary` connected before revoke, then `ALTER ROLE ta_canary NOLOGIN` produced `OperationalError` while fleet provisioning stayed `Succeeded`. Dispatcher build run `29714326960` passed Trivy and calendar-skip smoke by digest; size moved `151456849 -> 149552133` bytes. Evidence: `docs/reports/sprint-131-blast-radius/live-proof.md`. Deviation: container-origin role sessions were not captured because the apps were scaled to zero outside the KEDA window; repeat the role query during the next scheduled window. | Canary role and Key Vault secret were deleted/purged to zero. Permanent `ta_<agent>` roles, per-role Key Vault DSNs, and fleet `POSTGRES_DSN` secretRefs are retained intentionally as production state. Shared DSN remains untouched for rollback; no image retag/merge was performed. | <!-- pragma: allowlist secret -->
| 2026-07-22 | S133 Service Bus SAS | Service Bus blast-radius reduction: entity-level topic SAS rules with measured Send/Listen grants, per-target Key Vault connection bundles, production secretRef delivery to 12 agent apps + `dispatcher-cron`, and `master` Service Bus env removal because it has no measured bus grants. | Azure Service Bus namespace `trading-agents-bus`; Azure Key Vault `trading-agents-kv`; Azure Container Apps env `trading-agents-env`; Microsoft Learn quota check confirmed the 12 authorization-rule cap per namespace/queue/topic. No SAS key was printed or written to the repo tree. | PASS with one operator follow-up: planner produced 13 targets, 33 topic rules, and no cap violations; provisioner completed `{"rules": 33, "targets": 13}`; Key Vault held 26 Service Bus secret names (primary + bundle per target); `servicebus-flip` returned `[OK]` for master env removal, all 12 apps, and `dispatcher-cron`; read-only `az ... show` proved secretRef delivery; preflight proved `per-target Service Bus SAS strings: 13/13`. Controlled canary served 1 request with separate request/reply Send/Listen identities, refused wrong-topic Send, and refused the revoked requester rule while all Container Apps/job stayed `Succeeded`. Evidence: `docs/reports/sprint-133-servicebus-sas/live-proof.md`. Follow-up: capture container-origin Service Bus sessions during the next coordinated KEDA window if needed. | Canary topics/rules were deleted to zero (`s133-*` topic list empty). Permanent scoped topic rules, Key Vault Service Bus secrets, and fleet secretRefs are retained intentionally as production state. Shared namespace string remains available for rollback through `servicebus-flip -UseSharedServiceBusDsn`. |
| 2026-07-22 | Scoped-credential audit (S131 + S133 delivered state) | Whether the fleet's *delivered* credentials are actually the scoped ones, as opposed to merely being wired to a `secretRef` of the right name. Env-var wiring cannot prove this: `postgres-flip` rewrites the **value** of the `postgres-dsn` secret while the env var name is identical before and after, so `az ... show` looks the same either way. Prompted by an operator request to apply the S131 flip that STATE listed as pending. | Azure Container Apps env `trading-agents-env` (13 apps + `dispatcher-cron`), Azure Key Vault `trading-agents-kv`, Neon Postgres spine. Each app's live `postgres-dsn` secret value was read into memory, its **role name** parsed (non-secret) and a connection opened with it; no DSN, password or SAS key was printed, and the probe was read-only (no writes to production data). | PASS — **the flip was already applied; STATE was wrong**. All 14 targets (`master`, 12 agent apps, `dispatcher-cron`) carry a *distinct* per-role DSN and every one connected as its own role: `OK as ta_<target> nodes=1852 edges=1495 priv=S1I1U1/edgeI1` — SELECT/INSERT/UPDATE on `nodes` and INSERT on `edges`, the exact spine grants. Key Vault values and delivered values agree (no post-flip rotation drift). Bus side re-confirmed in the same pass: all 13 targets on scoped SAS `secretRef`s, `master` with no bus env. Origin of the false entry: a 07-21 note that *one run* used the shared DSN was generalised into "the flip is not applied" (DL-54). No infra change was made — re-running an idempotent flip would have churned 14 revisions for a no-op. | Nothing to tear down: read-only audit, no canary roles or topics created. Shared admin DSN and namespace SAS string remain untouched for rollback. |
| 2026-07-22 | Credential-delivery audit tool (chore, 0.72.00) | The new standing check `scripts/cred_audit.py` itself: does it read the *delivered* credential (not the env-var name), classify it correctly, and actually fail when delivery is wrong? Built because DL-54 showed no cheap check could contradict a false status claim. | Azure Container Apps env `trading-agents-env` (13 apps + `dispatcher-cron`), Neon Postgres spine, live Azure CLI reads. Positive run against the real fleet; negative run against a non-existent resource group. No credential printed. | PASS — positive: `--check-bus --strict` returned `ok: true`, `targets: 14`, `verdicts: {scoped: 14}`, exit 0; `master` correctly showed `bus secretRef: none` (S133 removed it) and `dispatcher-cron` was read through the Container Apps *job* path. Negative: against `no-such-rg-cred-audit-negative` it returned `verdict: missing`, `ok: false` and **exit 1** — the check can fail, which is what makes the pass meaningful. 13 unit tests pin the verdict logic including `cross-wired` (a target holding another agent's role). `make ci` exit 0, 1715 passed, coverage 100.00%. | Nothing to tear down: read-only audit, no roles, topics or canaries created. |
| 2026-07-23 | S135 unified buy/sell decision run (ADR-0016) | The deployed 13-container fleet deciding **both directions in one run on one evidence set**: the analyst scoring scanner survivors *plus* open held positions against the same `MarketData` snapshot and regime, emitting buy/hold/sell; the PM sizing both into one `OrderIntentSet`; and execution putting a **sell** on the existing buy rail to the live Alpaca paper broker. | Azure Container Apps (RG `trading-agents`), all 13 apps + `dispatcher-cron` on GHCR `:s136` built from commit `1b858e7`; Neon PostgreSQL spine via `POSTGRES_DSN`; live Alpaca paper broker. | ✅ **A sell order reached the broker for the first time in the system's history** — lifetime sell count went 0 → 1: `ABT sell qty=98 type=market status=accepted id=fc7f075f 2026-07-23T07:28:35Z`, queued for the open (submitted 07:28Z, after the US close). Run `check-s136-sell` completed **7/7**; analyst `SCHW buy 0.67 / USB hold 0.66 / PYPL hold 0.66 / BAC hold 0.65 / WFC hold 0.63 / ABT sell 0.62` against a forced `ANALYST_EXIT_CONFIDENCE_FLOOR=0.625`, so the floor discriminated at exactly the intended boundary (0.62 sells, 0.63 holds); PM `approved=2` — `ABT sell qty=98` **and** `SCHW buy qty=99` in one `OrderIntentSet`; `qty=98` matched the real held quantity, not the retired `close_quantity=1` fixture (DL-58). Acceptance: **`UNPROVEN`** — correctly refusing to call a queued order a pass (DL-59). Prior run `check-s136-clean` proved the anti-pyramiding half: BAC/USB/WFC/ABT — the names that had accumulated 171→338→503 by nightly re-buying — all returned `hold` and the PM skipped them with `hold_recommendation`, approving only the genuine new candidate. Conservation held with the analyst scoring more names than the scanner passed (the ADR-0016 bound). A degraded-provider run additionally showed `held_count=7` with **zero** scanner survivors, proving held names bypass the scanner. Local `make ci`: **1748 passed, 100.00% coverage**, verified independently of the agent that wrote the code. | ✅ `ANALYST_EXIT_CONFIDENCE_FLOOR` override removed (analyst env verified back to its 5 original vars, all secretRefs intact); KEDA wake windows restored to `22:30-00:30Z` (master `22:25`) and verified against the pre-change backup; litter run `check-s136-unified` (bad universe parse) torn down via `pg_teardown.py --run-id` — **9 edges / 9 nodes**. `check-s136-clean` and `check-s136-sell` deliberately **retained**: the latter is the lineage for a live broker order, and deleting it would manufacture exactly the broker↔graph divergence DL-44 exists to prevent. |
| 2026-07-24 | S138/S139 evidence-based closure + PnL at fill time (ADR-0015 §1) | The deployed fleet running a full cascade with **four previously stranded positions restored to the book**, reconciling a real broker sale, and the ledger repair applied to production. | Azure Container Apps (RG `trading-agents`), 13 apps + `dispatcher-cron` on GHCR `:s138` then `:s139` (commits `b7cc8d9`, `9a8a88c`); Neon PostgreSQL spine; live Alpaca paper broker. | ✅ **The first sell in the system's history FILLED**: `ABT 98 @ $101.35, 2026-07-23T13:31:36Z` (entry $100.78 → **≈ +$55.81 realized**), and `regt_buying_power` recovered **0 → $32,919.70** — capital recycled for the first time. Run `check-s138-unstrand` 7/7: analyst **`scored=10`** including **AMD, CSCO, HPE, MRVL** — the four positions invisible to every run since 07-20 — all evaluated on the same evidence; PM `approved=1` with all 9 held names skipped `hold_recommendation` (no pyramiding); execution **`submitted=1 rejected=0`** (`SCHW ×98`, first entry to clear the broker since the margin wall). **ABT closed by EVIDENCE, not decision**: reconciliation stamped `broker_absent=True` and the sell Fill `broker_status=filled`, leaving graph holdings `AMD, BAC, CSCO, HPE, MRVL, PYPL, USB, WFC` — an exact match to Alpaca. **Three closes were decided (`HPE time`, `MRVL stop`, `CSCO time`) and NOTHING STRANDED** — all three remain in the book; under the old rule they would have vanished permanently. Acceptance: **`UNPROVEN`** (SCHW queued for the open), correctly refusing to call a queued order a pass. Ledger repair applied to production: **7 of 7** decision-time PnL entries marked `pnl_invalidated_at` (`manual_review=0`), `pnl_cents` **preserved** as evidence; re-run is a no-op (`skipped_invalidated=7`); the reporter now returns `{closed_trades_with_pnl: 0.0}` — omitting `profit_factor`/`expectancy_cents` rather than presenting an uncomputable metric as a measured `0`. `make ci` 1762 passed / 100.00% coverage, verified independently of the agent that wrote the code. | ✅ KEDA wake windows restored to `22:30-00:30Z` (master `22:25`, the 5-minute bootstrap lead) and verified against the pre-change values; no `.env` was ever copied into a worktree (the repair used `--env-file` against the real one). Run artifacts **deliberately retained**: `check-s138-unstrand` is the lineage for a live `SCHW` order and a real reconciliation, and deleting it would manufacture the broker↔graph divergence DL-44 exists to prevent. |
| 2026-07-24 | S137 exit authority (ADR-0017) | The deployed fleet resolving the monitor↔analyst exit conflict on a real scheduled run: the monitor no longer authoring exit decisions, and a **breached stop forced onto the analyst rail regardless of thesis confidence**. First live exercise of "alpha proposes, risk disposes". | Azure Container Apps (RG `trading-agents`), 13 apps + `dispatcher-cron` on GHCR `:s141` (commit `29a36f4`, 0.76.00); Neon PostgreSQL spine; live Alpaca paper broker. Run `sched-2026-07-24`. | ✅ **The forced stop fired for the first time**: `MRVL Recommendation action=sell exit_trigger='stop' conf=0.637` — **above** the `exit_confidence_floor=0.5`, so the thesis said HOLD and the stop overrode it (ADR-0017 §2, alpha cannot veto risk). It is the only `exit_trigger=stop` in the graph. The monitor **stopped deciding**: `checked=9 closes=0 holds=9` (§3) — where it previously re-decided HPE/MRVL/CSCO every run — and raised the visibility Fault `"stop breached on MRVL, still held"` @ 22:40:42Z (DL-57). The forced stop reached the broker on the **one rail**: PM sized full exit `MRVL sell qty=44`; execution `submitted=3 rejected=0` (with `SCHW`+`ABT` buys). All other held names held (USB/AMD/PYPL/WFC/HPE/CSCO/BAC), including AMD (0.66) which is not through its stop — no spurious exits. Run 7/7. Acceptance **`UNPROVEN`** — MRVL sell + 2 buys queued for the open (fired 22:30Z, after the US close), correctly refusing to call a queued order a pass. Local `make ci` 1786 passed / 100.00%, verified independently; all four remote gate jobs green before merge. | Windows intact (scheduled run, no manual change); no `.env` copied into any worktree. Run `sched-2026-07-24` **retained** — it is the lineage for live MRVL/SCHW/ABT broker orders; deleting it would manufacture the broker↔graph divergence DL-44 exists to prevent. **Fill confirmation outstanding**: re-check `accept.py` after today's US open to prove the MRVL exit fills (the first real forced-stop realized PnL). |
| 2026-07-27 | Opus 5 at max effort (feat, 0.78.00, fleet `:s143`) | Whether `output_config={"effort": "max"}` actually reaches the live Anthropic API on `claude-opus-5` — the parameter was never sent before, so the knob was inert. Ran the real deliberation debate end-to-end against the live API. | ✅ **PASS.** `MODE: real (debate Anthropic claude-opus-5 · judge Anthropic claude-opus-4-8)`; no 400 on `output_config`, substantive Defender/Challenger turns and a `REVISE` verdict returned. Chain proven: SDK 0.120.0 accepts the param → API accepts `effort=max` on `claude-opus-5` → response returns. | Proves the **script-side** adapter (`_AnthropicText`). The operator agent's tool-use client (`AnthropicLLMClient`) is deployed on `:s143` but **not yet exercised live** — pending. Also confirmed the fleet reads no `.env` (DL-63): the operator container's env carries no model/effort var, so the code default is what runs. |
| 2026-07-27 | S144 vocabulary guard deployable + pack proven complete (feat, 0.80.00) | Whether the write guard can actually be turned on in the fleet, and whether its pack covers what the code can write. Ran `build_graph_from_env()` against the **live Neon spine** with `GRAPH_VOCABULARY_B64` set and `GRAPH_VOCABULARY_PATH` unset — the deployable form, no file on disk. | ✅ **PASS.** `GuardedGraphStore` wrapping **`PostgresGraphStore`** (asserted, per the S98 in-memory lesson). Guard rejected `merge_node('Postion', ...)` with `VocabularyError` and wrote **0 rows**. Both broker-stop signatures (`Fill -STOPS_WITH-> BrokerStopOrder`, `Position -PROTECTED_BY-> BrokerStopOrder`) accepted. Live state at check time: **BrokerStopOrder = 0**, Position = 21. | **Near-miss recorded (DL-68):** both stop signatures were *undeclared* until this sprint — the pack was derived from observed writes and ADR-0015 §3 had never placed a stop, so enabling the guard would have raised `VocabularyError` on the first real stop and destroyed the pending proof. No artifacts created (the only write attempted had to be refused), so nothing to tear down. **Fleet NOT enabled** — `:s143` predates the code; enablement is a dated action after tonight's run. |
| 2026-07-28 | S145 exit replay append-safe (fix, 0.80.02) | Append-safe exit Fill attempts, completed-exit skip visibility, and per-intent execution fault isolation on the real graph backend. | Neon PostgreSQL via `POSTGRES_DSN` loaded from the gitignored main checkout `.env`; synthetic stamped rows; in-process fake broker only; no live Alpaca submissions, stop placement, or fleet resume. | PASS — live check stamp `s145-livecheck-20260728T052258Z`; backend `PostgresGraphStore`; item 1 wrote base Fill price `19451` and replay Fill key `exit:s145-livecheck-20260728T052258Z-append:MRVL:sell#1` price `18928`; item 2 completed-exit replay returned `submitted=0 rejected=0 skipped=1 broker_orders=0`; item 3 poisoned write returned `broker_calls=3 submitted=2 rejected=0 skipped=0`. | Pre-clean deleted 0 edges / 0 nodes. Post-clean deleted 3 edges / 14 nodes; remaining stamped edges=0 and nodes=0. Actual production AMD/ABT orphan adoption and SCHW post-resume stop healing are not done until the branch is merged, retagged, and `/resume-run` is executed. |
| 2026-07-28 | S146 unprotected position (fix, 0.80.03) | Production broker-protection audit, append-only repair for four orphan broker fills, and broker-native stop retry visibility for the remaining unprotected ABT holding. | Neon PostgreSQL via `POSTGRES_DSN` loaded from gitignored `.env`; live Alpaca paper broker; branch-local code against production graph; no secrets printed. | PASS with verified remaining broker block: before audit had A2 FAIL ABT and A3 four PM-run orphan fills; repair dry-run found 4 would_record rows, apply recorded 4, second apply recorded 0 and already_recorded 48; live ABT retry appended Fill attempt `stop:5244d9de63d93691:ABT#1`, wrote 2 Fault nodes, and preserved the Alpaca 403 body: `potential wash trade detected. use complex orders` with existing opposite-side order `fd1f1c2c-4911-4df5-b7a1-e2e9929a7341`; after audit A3 was clean and the only verified failure was A2 ABT `held=96 stop_qty=0`. | Orphan Fill repairs and ABT refusal Fault are retained intentionally as production evidence. Probe orders remain allowlisted historical broker probes; no scratch graph nodes, files, or secrets were created. |
| 2026-07-28 | S146 post-deploy: retry visibility on the running fleet (fix, 0.80.03, `:s146`) | Whether the S146 behaviour holds on the **deployed** fleet rather than branch-local code — specifically that a refused broker stop is re-attempted every run and re-surfaced, instead of being recorded once and forgotten. Codex's own check ran branch-local; this one ran the retagged fleet. | Azure Container Apps (RG `trading-agents`), 13 apps + `dispatcher-cron` on GHCR `:s146` (commit `7b06662`, `DeployRecord …:s146:7b06662` written after 14/14 tag verification); Neon PostgreSQL spine; live Alpaca paper broker. Run `confirm-s146-20260728` (distinct id, deliberately not `sched-2026-07-28`, so tonight's dispatcher cannot day-key-dedupe onto it). | ✅ **PASS.** Run scored **`RESULT 7/7 stages complete`**; provider 100 tickers, scanner 5 survivors, analyst 9 scored, PM `approved=2 rejected=8`, execution `submitted=2 rejected=0`, monitor `checked=9 closes=0 holds=9`, reporter ran. **Retry proven by two Faults, not one**: `UnprotectedPosition` at `08:05:23Z` (Codex's pre-deploy check) **and `09:45:02Z` (this run, on `:s146`)**, both carrying Alpaca's verbatim refusal — `code 40310000`, `"potential wash trade detected. use complex orders"`, `reject_reason: "opposite side market/stop order exists"`, `existing_order_id fd1f1c2c…`. Before S146 the 403 was recorded **once, days earlier, and nothing retried**. The `RuntimeError` at `09:45:02.007Z` followed by `UnprotectedPosition` at `.028Z` shows the fault boundary catching and re-attributing rather than crashing. **No fabricated `BrokerStopOrder`** — still exactly 7, none for ABT; the other 8 stops untouched at correct quantities. **No duplicate order**: the PM re-approved `AMD sell qty=55` and execution **adopted** the existing broker order — AMD market sells at the broker remain **exactly 1** (`d040c762`). Sole new order was `MDT buy 118`, a genuine new position. Acceptance `UNPROVEN - completed` (orders queued for the open), correctly refusing to call a queued order a pass. | KEDA windows opened to `09:00-12:00Z` for the run and **restored and verified** at `25 22`/`30 22` → `30 00Z` across all 13 apps; replicas returned to 0 on cooldown. Run `confirm-s146-20260728` **retained** — it is the lineage for a live `MDT` order; deleting it would manufacture the broker↔graph divergence DL-44 exists to prevent. **Outstanding, stated as a claim not a result:** ABT is still `held=96 stop_qty=0` and stays so while buy `fd1f1c2c` is open. It should clear once that buy fills at the 13:30Z open and a later run retries — **verify, do not assume**. |
| 2026-07-30 | S149 volatility-scaled tolerance challenger | The read/report side of the shipped experiment: `scripts/compare_order_tolerances.py` loaded the configured graph and rendered flat-vs-scaled rows, while a synthetic two-order graph proved the two modes can produce different would-fill and slippage outcomes. The append-only Fill evidence path, fallback semantics, clamps, and vocabulary guard were exercised under `make ci`. | Neon PostgreSQL through `.env` (`PostgresGraphStore` asserted in command output, DSN not printed); local `InMemoryGraphStore` for the deterministic two-order report; in-process fake broker only; no live broker submission and no fleet retag. | PASS for the nearest real target before production S149 rows exist. Real graph read returned zero rows for `missing-run` under both modes, as expected pre-deploy. Synthetic report: flat `orders=2 would_have_filled=1 drop_rate_pct=50.00 avg_slippage_bps=40.00`; scaled `orders=2 would_have_filled=2 drop_rate_pct=0.00 avg_slippage_bps=80.00`. Local `make ci` exit 0: **1950 passed, 5 skipped, 100.00% coverage**, import-linter kept, pip-audit clean, detect-secrets clean. | Reads only against Neon; no graph rows, broker orders, Azure resources, or files to tear down. The synthetic in-memory graph was process-local and discarded. |
| 2026-07-30 | S150 volatility-scaled stop challenger | The read/report side of the shipped stop-width experiment plus the no-live-stop-mutation guard. `scripts/compare_stop_targets.py` loaded the configured graph and rendered flat-vs-scaled rows; a synthetic two-recommendation graph proved the two modes can produce different would-touch rates while preserving RR pass rate. Existing-position and S148 drop-sweep tests proved live `Position.stop_pct` and resting broker stops are not repriced or disturbed. | Neon PostgreSQL through `.env` (DSN not printed); local `InMemoryGraphStore` for deterministic two-recommendation report; in-process fake broker/graph only; no live broker submission, no settings promotion, no fleet retag. | PASS for the nearest real target before production S150 rows exist. Real graph read: flat `recommendations=0 stop_min_pct=n/a rr_pass_rate_pct=0.00 known_outcomes=0`; scaled same. Synthetic report: flat `recommendations=2 stop_min/median/max=5.00/5.00/5.00 rr_pass_rate_pct=100.00 known_outcomes=2 would_touch_rate_pct=50.00`; scaled `4.20/6.10/8.00 rr_pass_rate_pct=100.00 known_outcomes=2 would_touch_rate_pct=0.00`. Focused/affected tests passed including S148 stop-safety fixture. Local `make ci` exit 0: **1971 passed, 5 skipped, 100.00% coverage**, import-linter kept, pip-audit clean, detect-secrets clean, untracked-secret scan clean. | Reads only against Neon; no graph rows, broker orders, Azure resources, settings writes, or files to tear down. The synthetic in-memory graph was process-local and discarded. |
| 2026-07-31 | S151 drop sweep append-safe (fix, 0.84.01, fleet `:s151`) | Whether the 2026-07-30 outage is actually fixed **in production**: that the drop sweep completes against the ten real `Fill` nodes carrying `broker_status="rejected"` that stalled it, that a `BrokerPositionSnapshot` is written and linked so `position_sync` can complete, and that the nine resting `gtc` broker stops survive a sweep that now actually runs. **Not a synthetic check** - the stalled `sched-2026-07-30` was resumed on the live spine against the live broker. | Azure Container Apps (RG `trading-agents`), 13 apps + `dispatcher-cron` on GHCR **`:s151`** (commit `410830f`, merge `8f57f5f`, `v0.84.01`; images built 14/14 by run `30607835286`; `DeployRecord deploy:2026-07-31T05:56:51Z:s151:410830f` written **after** 14/14 tag verification); Neon PostgreSQL spine; live Alpaca paper broker. Manual daytime scale window (operator authorised), no new `RunRequest` minted. | ✅ **PASS - the outage is closed.** The stalled run went **2/8 → `RESULT 8/8 stages complete`** and **`ACCEPTANCE PASS - every stage did its job within its boundaries`**. `position_sync` shows `status=fresh snapshot=broker-position-snapshot:sched-2026-07-30:2026-07-31T05:53:16Z` - exactly one snapshot for the run, the artifact whose absence stalled everything. **The collision is gone**: **11** `Fill` nodes now carry `drop_reason`/`dropped_at` with **11** matching append-only `BrokerOrderStatus` drop facts, and every one of them still reads **`broker_status=rejected`** - the reconciler's account was preserved, not overwritten, which is the whole point of the fix. **Zero** `property 'broker_status' cannot be overwritten` faults, against 5,762 the night before. The 13 faults raised were 11 `DroppedDecision` warnings (the visible evidence ADR-0018 asks for) plus 2 pre-existing `realized PnL skipped for ABT sell fill` unrelated to this sprint. 🚨 **All nine stops survived**: 9 positions / 9 open orders / **9 resting `gtc` stops** at Alpaca (ABT 100.24, BAC 56.65, CSCO 106.78, HPE 41.37, MDT 82.26, PYPL 53.81, SCHW 97.10, USB 59.47, WFC 82.07), submit timestamps unchanged from 07-28/07-30 - not re-placed, simply never touched - and **0 non-stop open orders** remaining, so the sweep dropped exactly what it should and nothing it should not. Cascade: provider 100/100 tickers, scanner 5 survivors, analyst `scored=9 rejected=0` (all hold), PM `approved=0 rejected=9` all `hold_recommendation`, execution `submitted=0 rejected=0`, monitor `checked=9 closes=0 holds=9`. **No orders were placed**, so this is a clean structural proof uncontaminated by new broker state. Local `make ci` verified **independently** of the coding agent on the reviewed tip `7e5c66b`: 1983 passed, 5 skipped, 100.00% coverage. | ✅ Manual scale window **closed and verified**: all 13 apps back to `minReplicas=0` with cron rules intact (12 `daily-agent-window` + 1 `daily-master-window`), all 13 still on `:s151`. No `.env` copied into any worktree. Run `sched-2026-07-30` **retained** - it is real production lineage carrying 11 drop facts and a broker snapshot; deleting it would manufacture the broker↔graph divergence DL-44 exists to prevent. **Stated as a claim, not a result:** the ~11 drops are the legacy 07-22/07-23 backlog clearing at once and are **not** the ADR-0018 per-session drop rate - that becomes measurable only from the next clean run onward. |
| 2026-07-31 | S144 vocabulary guard ENABLED (fix, 0.84.04, fleet `:s152`) | **Pre-flight found the enablement was not safe as scoped.** A sweep of the signature dimension found **46 `add_edge` sites whose endpoint the static scan cannot resolve** (function parameters, or a label read off a descriptor table), so the pack's declared signatures were only ever what past runs produced. Three shapes were undeclared and would have raised `VocabularyError` under the guard: `MonitorRun -LINKED_FROM-> MonitorRun` and `Snapshot -LINKED_FROM-> Snapshot` (both reachable only on a **late-stage resume**, i.e. the recovery path) and `PMRun -DELIBERATED_BY-> DeliberationRun`. All three declared in 0.84.04; `resume_edge_signatures()` derives the resume requirements from the same `ARTIFACTS` table `resume.py` walks, and the new test was **observed failing** on a planted removal (DL-70). Fleet then retagged 14/14 to `:s152` and `GRAPH_VOCABULARY_B64` set on all 14; the value was **read back off a deployed app and decoded byte-identical to the pack** (71 labels, 43 edge types, 43 signatures, 2 property lists). **Guard proof:** the vocabulary taken from the deployed `execution` app accepts **37/37 live node labels, 36/36 live edge signatures and both property-enforced labels' live property sets** through kernel's own `check_node`/`check_edge` — every shape production has ever written passes. | Live Neon Sydney spine (read-only queries; DSN never printed); Azure Container Apps control plane (image retag + env-var set on 13 apps and the `dispatcher-cron` job); GHCR image build at tag `s152` from `d00904b`. No broker calls, no graph writes except the `DeployRecord`. | **PARTIAL — the structural proof is complete, the behavioural one is pending.** Everything above is verified. What is *not* yet proven is a real run under the guard: the functionality check completes only when tonight's 22:30 UTC `sched-2026-07-31` reaches its stages without a `VocabularyError`. Named residual risk: the guard is fail-closed and raises inside the caller's fault boundary (the S148 stall pattern), and the 46 blind `add_edge` sites mean a **never-executed** path can still trip it. | Retained: `DeployRecord …:s152:d00904b` (production deploy provenance). Torn down: nothing created to tear down — the pre-flight and guard proofs were read-only. Rollback remains one command each way: retag to `:s151`, or unset `GRAPH_VOCABULARY_B64`. |
| 2026-08-04 | Fleet retag `:s155` → **`:s158`** (deploy; no version bump — carries S158's loud fail-open plus `v0.86.02` cryptography 50.0.0 / CVE-2026-69247 and aiohttp 3.14.3) | **Deployed via `deploy-agents.ps1 up -Tag s158`, not an image-only retag** — S158 added three properties to `DeliberationRun`, a property-enforced label, so image and vocabulary pack had to move as one unit or the first write would raise `VocabularyError` fail-closed (the S148 stall). Images built at the tag by `build-images` run `30873559222` on `d80e438`: **15/15 jobs success, 0 failed**, Trivy HIGH/CRITICAL and entrypoint smoke included. 🚨 **The deploy script reported `Deploy FAILED for 17 target(s)` and exited 1 — and it was wrong.** Verified independently against live Azure: **16/16 apps on `:s158`**, **16/16 `provisioningState=Succeeded`**, dispatcher job on `:s158` with cron `30 22 * * *` intact. **The pack was read back off every deployed target and decoded — 17/17 byte-identical to the repo pack** (`sha256 dab0623bb37c…`), checked per target, never sampled. Config intact: all 16 apps `minReplicas=0`, one KEDA rule each (`daily-master-window` on master, `daily-agent-window` on the 15 agents), 3 `secretRef`s per agent and 2 on master. **Row Q reproduced exactly as documented:** the 12 `[OK]` marks are all *pre-deploy* steps (10 preflight, `alembic upgrade head`, served topics) and **all 17 deploy targets printed `[XX]`** — the misreport is confined to the per-target result parse, consistent with `az` exiting 0 while the state read out of a merged stdout/stderr stream is not the literal string `Succeeded`. **Preflight was clean: 10/10**, per-target Postgres DSNs **17/17**, Service Bus SAS **16/16**, GHCR images **15/15**; `alembic upgrade head` a no-op and Service Bus routes `created_topics: []` / `created_subscriptions: []`, confirming no re-provisioning occurred. | Azure Container Apps RG `trading-agents` (16 apps + `dispatcher-cron` job), GHCR at tag `s158`, Neon Postgres spine (`PostgresGraphStore`) for the `DeployRecord` write. | ✅ **Structural deploy PROVEN.** `DeployRecord deploy:2026-08-04T03:24:15…:s158:d80e438` written **after** verification and read back. 🟠 **Scope it honestly — this proves the fleet is on the code and pack it should be, NOT that the deliberation works.** Agents are `minReplicas=0` and activate only in the 22:25–00:30 UTC window, so activation, bus round trip and a real debate are **unproven until tonight's 22:30 UTC run**. DL-80 remains open. Watch for: a `DeliberationRun` carrying `real_debate_count`/`failed_open_count`, acceptance's two deliberation checks leaving `MISSING`, and — only on a day the PM approves an order — a non-empty transcript plus a `claude-opus-5` `LLMCall` after 2026-08-02 against a ledger frozen at 25. | ✅ No test artifacts created. The only write was the `DeployRecord`, which is the intended durable evidence, not scratch. No paper orders placed, no graph rows stamped, scale windows and cron left exactly as found. |
| 2026-08-06 | Fleet retag `:s158` → **`:s161`** (deploy; no version bump — carries S159, S160, S161 `v0.88.00` and the chores merged since 08-04) | **S161 was merged but not deployed, so the proof it was waiting for could not have happened.** The fleet had been on `:s158` (built from `d80e438`, 08-04) — three sprints behind — and the scheduled run executes the *deployed* PM, which has no account-backed sizing. With the live book at ~$104k equity against ~$209k deployed, the fix that stops new buys was sitting in `main` while an unfixed PM sized against an over-exposed book. **Deployed via `deploy-agents.ps1 up -Tag s161`, not an image-only retag:** S161 adds five properties to `BrokerPositionSnapshot` — `account_cash_cents`, `account_equity_cents`, `account_buying_power_cents`, `account_status`, `account_stale_reason` — and that label is one of five **property-enforced** ones, so a stale pack would raise `VocabularyError` fail-closed on the first `position_sync` write and stall the run at 2/8 (S148/DL-79 shape). Repo pack had moved `dab0623b…` → **`b8d1a30f…`**. Images built by `build-images` run `31087442801` on `7f9eb5b`: **15/15 success, 0 failed**. 🟢 **Hardening row Q appears fixed:** `up` printed **29 `[OK]`, 0 `[XX]` and exited 0**, where the `:s158` deploy falsely reported all 17 targets failed — consistent with the `chore-deploy-state-from-show` branch reading state from `az containerapp show` rather than parsing the update stream. **Verified independently anyway, and deliberately so: row Q was tolerated because it failed *safe*, so a tool that has just started reporting success has not yet earned the trust its old version was denied.** | Azure Container Apps RG `trading-agents` (16 apps + `dispatcher-cron`), GHCR at tag `s161`, Neon Postgres spine for the `DeployRecord` write. | ✅ **Structural deploy PROVEN.** **16/16 apps on `:s161`**, **16/16 `Succeeded`**, job on `:s161` with cron `30 22 * * *`; **pack read back and decoded on all 17 targets — 17/17 byte-identical to `b8d1a30f…`**, per target, never sampled; all 16 apps `minReplicas=0` with exactly one KEDA rule. Preflight 10/10, `alembic upgrade head` a no-op. Handback verified independently before deploying: `make ci` **2142 passed / 4 skipped / 100.00%** and pip-audit clean on my own tree (matching the reported numbers exactly), `make gate-ran` exit 0 for `7f9eb5b` across CI, Security Findings, CodeQL, Build and push agent images and Configured Graph Update, and the sprint closeout filled with zero placeholders. `DeployRecord deploy:2026-08-06T09:26:44…:s161:7f9eb5b` written **after** verification and read back. 🟠 **Scope it honestly: this proves the fleet runs S161's code and pack, NOT that the sizing fix behaves.** Apps are `minReplicas=0` until the 22:25–00:30 UTC window. **The operator proof is tonight's 22:30 UTC run: zero new buy approvals, sells still permitted, and a `BrokerPositionSnapshot` carrying non-null `account_equity_cents`** — the last being what distinguishes the fix working from the account read silently degrading to stale. | ✅ No test artifacts created. The only write was the `DeployRecord`, the intended durable evidence. No paper orders placed, no graph rows stamped, scale windows and cron left as found. |
| 2026-08-07 | S162 rejections carry every evaluated gate (feat, 0.89.00, fleet `:s162`) | **The question S162 exists to answer, asked against real production data.** On 2026-08-07 the `sched-2026-08-06` run went 8/8 with `ACCEPTANCE PASS` and still could not prove S161's sizing gate had ever fired: MSFT was the only `buy`, `position_rejection` returns on the first mapped failure, and `max_positions` is checked before `cash_available` — so the account gate's outcome was computed, failed, and discarded, because `RejectedOrder` carried only `ticker` and `reason`. The check re-ran the **shipped S162 code** against the **real graph state** of that run: `portfolio_from_graph` rebuilt from the live Neon spine, the 11 real `Recommendation`s off the stored `AnalystRun`, and decision-time closes off the run's own `MarketData` snapshot. Not a fixture — the same inputs production used. | Live Neon PostgreSQL spine (`PostgresGraphStore`, read-only — no writes except the `DeployRecord`); Azure Container Apps RG `trading-agents`, **16 apps + `dispatcher-cron`** on GHCR **`:s162`** (main `9655f4b`, merge `ecde86d`, `v0.89.00`; images built 15/15 by run `31140993266` on `9655f4b`). **Image-only retag, deliberately:** the vocabulary pack is **byte-identical** to `:s161` (`b8d1a30f…` both sides, verified by hashing the file at each commit) and `Rejection` is **not** one of the five property-enforced labels, so there is no pack/image coupling and the S148 fail-closed stall cannot apply. No broker calls, no paper orders. 🚨 **The deploy skill's own app list is stale and would have under-deployed:** it names 13 apps, the fleet has **16** — the three deliberators are missing from it. Caught by listing the live inventory instead of trusting the list; all 16 were retagged. | ✅ **PASS — and it settles this morning's open question.** The shipped code on real data printed `MSFT   SKIP  max_positions (also failed: cash_available)` with the full report: `sizing passed=True 0.0970/0.1000`, `min_order_quantity passed=True 20/1`, **`max_positions passed=False 11.0000/10.0000`**, **`cash_available passed=False 10000.7000/-105748.4955`**. **S161's sizing gate would have fired** — a $10,000.70 order against **−$105,748.50** available. That was a source-reading derivation this morning; it is now measured through the shipped path. 🚨 **No approval decision moved:** `approved=0 rejected=11` with all 11 reason strings identical to production (MSFT `max_positions`, ten × `hold_recommendation`), and the ten pre-gate rejections carry **empty** reports and render with no suffix — the honest emptiness the spec required. Structural deploy proven independently: **16/16 apps on `:s162`, 16/16 `Succeeded`**, job on `:s162` with cron `30 22 * * *` intact, all 16 `minReplicas=0` with exactly one `daily-agent-window` KEDA rule (22:30–00:30 UTC), `portfolio-manager` active revision `Healthy` on the new image. Handback verified before merging, not taken on report: `make ci` on my own worktree at `376fb6b` exit 0 with **2151 passed / 6 skipped / 100.00%**, matching the reported numbers exactly, and `assert_gate_ran.py --sha 376fb6b…` returned `GATE PROVEN` for CI and Security Findings — run unpiped to a file (row S). `DeployRecord deploy:2026-08-07T02:33:53…:s162:9655f4b…` written **after** verification. 🟠 **Scope it honestly — this is the logic proven on real data, NOT an end-to-end run of the deployed image.** Agents are `minReplicas=0` outside the 22:30–00:30 UTC window and the check ran at 02:33 UTC, so no pipeline run was possible without changing scale config, which was deliberately not done. **The end-to-end proof is the 22:30 UTC `sched-2026-08-07` run: the MSFT-shaped rejection carrying its `(also failed: …)` suffix in the trace.** | ✅ No test artifacts created — the check was read-only against existing lineage; nothing stamped, no graph rows written, no paper orders placed. The only write was the `DeployRecord`, which is the intended durable evidence, not scratch. Scale windows and cron left exactly as found (re-verified after the retag, not assumed). Verification worktree removed. Rollback is one command: retag to `:s161`. |
| 2026-08-07 | S162 end-to-end on the deployed fleet (feat, 0.89.00, fleet `:s162`) | **The real end-to-end run, not the logic check.** The earlier S162 row proved the shipped code on real data with the fleet scaled to zero; this exercises the **deployed `:s162` images** through the full graph-pull cascade. Operator directive (2026-08-07): the fleet is a **development** environment until soaking, so widening the scale window to test is expected, not a production intervention. All 16 apps raised to `minReplicas=1`, a `RunRequest` placed via `scripts/dispatch_scheduled_run.py --as-of 2026-07-17`, the cascade watched to completion, then torn down and the window restored. 🪤 **Deliberately not today's date:** `scheduled_run_id` is **day-keyed** and RunRequests merge-dedupe, so testing on `sched-2026-08-07` would have left tonight's 22:30 UTC scheduled run with its artifacts already consumed — a no-op run, destroying the very proof it was meant to give. `2026-07-17` was chosen as a NYSE session with no existing run. | Azure Container Apps RG `trading-agents`, all **16 apps + `dispatcher-cron` on `:s162`** (main `9655f4b`); live Neon PostgreSQL spine; live Alpaca paper account (read-only — the run submitted **0** orders); real provider feeds (Alpaca bars, news, sentiment) — 100/100 tickers returned, 4,200 bars, 1,836 headlines, `quality ok`. | ✅ **PASS — 8/8 stages, `ACCEPTANCE PASS - every stage did its job within its boundaries`.** The deployed PM produced the sprint's whole reason for existing, in the real trace: <br>`MSFT   SKIP  max_positions (also failed: cash_available)` <br>with the durable evidence on the graph — the new `Rejection` node carried all four outcomes (`sizing passed=True 0.0967/0.1`, `min_order_quantity passed=True 20/1`, **`max_positions passed=False 11.0/10.0`**, **`cash_available passed=False 10000.7/-105407.246`**), while the three MSFT `Rejection` nodes from earlier runs read **`gate_report` entries: 0** — additive and defaulted exactly as B1 required, with **no backfill**. 🚨 **No approval decision moved:** `approved=0 rejected=11`, the same eleven reason strings as production (MSFT `max_positions`, ten × `hold_recommendation`), and the ten pre-gate rejections rendered with **no suffix** — the honest emptiness the spec demanded, observed in the live renderer rather than a fixture. Cascade: position_sync `status=fresh`, provider 100/100, scanner 5 survivors, analyst `scored=11 rejected=0` (one `buy`: MSFT conf 0.64), execution `submitted=0 rejected=0`, monitor `checked=10 closes=0 holds=10`, reporter clean. | ✅ **Torn down to zero, verified by stamp rather than inferred from an exit code.** `pg_teardown.py --run-id` reported 50 edges / 21 nodes and **was not complete** — 41 stamped nodes survived (11 `Rejection`, 11 `SentimentReading`, 10 `PositionCheck`, 6 `Recommendation`, 1 `DeliberationRun`, 1 PM-keyed `BrokerPositionSnapshot`), cleared by three further `--prefix … --contains` sweeps; root cause filed as **[DL-94](../design-log.md)**. Final residue across all five of the run's identifiers: **0**. **Standing broker state untouched** — `Position` **27** and `Fill` **59** before and after (the teardown dry-run was checked for exactly this before deleting, per DL-44 / S120). Scale window restored and **re-verified, not assumed**: all 16 back to `minReplicas=0`, one rule each (`daily-master-window` on master, `daily-agent-window` on the other 15), all 16 still on `:s162`, job cron `30 22 * * *` intact. **Tonight's `sched-2026-08-07` is untouched and will run clean.** |
| 2026-08-07 | Flatten the book on `:s164` — S163 + S164 proven in production (chore, no bump; carries `v0.89.01` + `v0.89.02`) | **The exit path exercised against the live broker for the first time since the stops went on (2026-07-28).** Sequence: hash the vocabulary pack at the deployed commit vs `HEAD` → **byte-identical `b8d1a30f…`**, so image-only retag; build 15/15 images at `s164` from `f3b2fcf`; retag 16 apps + `dispatcher-cron`; raise `ANALYST_EXIT_CONFIDENCE_FLOOR` **0.50 → 0.99** so every held name clears the ADR-0017 §1 discretionary-exit test; widen the window; fire `sched-2026-08-07`; restore the floor; apply the DL-93 resize; close the window. **The flatten used the designed rail** — analyst → PM → execution, full lineage — not a hand-fired order (`EXEC-IDN-01`, the mechanism DL-93 ruled out). | Azure Container Apps RG `trading-agents`, **16 apps + `dispatcher-cron` on `:s164`**; live Neon PostgreSQL spine; **live Alpaca paper broker** (10 real sell orders placed); real provider feeds. Pre-flatten baseline: equity **$103,278.47**, cash **−$104,966.77**, deployed **$208,245.24** (2.02×), 10 positions, **10 resting `gtc` stops**, `qty_available = 0` on all ten. | ✅ **PASS — and it settles the half of [DL-95](../design-log.md) that was inferred rather than observed.** The analyst emitted **`sell` for all 10** held names (USB 0.71 → PYPL 0.59, every one under the 0.99 floor); the PM approved **all 10 full exits**; execution reported **`submitted=10 rejected=0`**. 🚨 **Before S163 every one of those would have been rejected for insufficient quantity.** Confirmed at the broker immediately after: **10 open orders, all `limit sell`, and ZERO stop orders** — the ten resting `gtc` stops are gone, cancelled by S163 before submission, and `qty_available = 0` is now caused by the *exits* reserving the shares rather than the stops. That is the causal chain DL-95 predicted, observed end to end. Run went **8/8 stages**. The one `buy` (MSFT, conf 0.63) was rejected **`insufficient_cash`** — correct, and named precisely thanks to S162: the cash gate still binds until the exits settle. Cascade otherwise normal: provider 100/100, scanner 5 survivors, monitor `checked=10 closes=0 holds=10`, reporter stitched 10 recommendations. **Resize applied and measured:** `max_position_pct` **0.10 → 0.01**, `max_positions` **10 → 60** (60 × ~$1,031 = $61.9k max deployed against ~$98k allowed at full equity). 🪤 **The price-bias check the spec demanded, measured on the run's own `MarketData`: at DL-93's suggested $515 parcel, 10 of 100 universe names floor to 0 shares and become unbuyable; at $1,031 only 3 do (BLK, GS, LLY).** Choosing 0.01 over 0.005 cut the selection bias from 10 % to 3 %. 🟠 **Scope it honestly — three things are NOT proven.** (1) **The exits have not filled.** `time_in_force: "day"` submitted pre-market, so all ten sit `status=new` queued for the **13:30 UTC** open; deployed value, equity and `available_for_buys` are unchanged until they settle. (2) **S164's replacement path was never exercised** — no exit was rejected, so no stop needed re-placing; S164 remains proven by tests only, not in production. (3) **Tonight's 22:30 UTC scheduled run will be a no-op**: `sched-2026-08-07` is day-keyed and now consumed, a deliberate trade to keep the lineage correctly dated rather than fire a mis-dated past run. **The first resized buying run is Monday `sched-2026-08-10`.** | ✅ **Nothing torn down — this is real production lineage, deliberately retained** (contrast the S162 synthetic check, which was deleted): ten genuine broker orders and their `Fill`/`PMRun`/`AnalystRun` evidence are the account's actual history, and deleting them would manufacture the DL-44 broker↔graph divergence reconciliation exists to prevent. **Config restored and re-verified, not assumed:** `ANALYST_EXIT_CONFIDENCE_FLOOR` read back at **0.50**; all 16 apps back to **`minReplicas=0`** with exactly one KEDA rule each and all on **`:s164`**; job on `:s164` with cron `30 22 * * *` intact. `DeployRecord deploy:2026-08-07T07:49:17…:s164:f3b2fcf…` written after verification. The resize env vars are intended to persist — they are the DL-93 decision, not scaffolding. |
| 2026-08-07 | S165 a ScanRun keeps its verdicts (fix, 0.89.03, fleet `:s165`) + the funnel opened (`SCANNER_CANDIDATE_CAP` 5 → 25) | **The scanner built a `FilterVerdict` for every evaluated ticker and then threw it away.** `SCAN-OBS-01` already required every `ScanRun` to be reconstructable into the `CandidateSet` *including the `FilterTrace`*, and `FilterTrace.verdicts` has been in the contract since S88 — but `store.py` persisted five scalars and discarded the rest, so every drop vanished at process exit and [DL-09](../design-log.md) had nothing to score. 🚨 **The law-first read produced a third option the spec had not considered:** a `FilterVerdict` **label is unlawful** (`SCAN-IDN-02` closes the enumeration at `ScanRun`/`Candidate`, `SCAN-NEV-04` forbids the rest — the S160 wall, confirmed), *and* a read-only derivation would have been **wrong**, because a verdict depends on the thresholds in force at scan time and those are not persisted per run, so recomputing with today's settings would silently rewrite history. The verdicts belong on a label the scanner **already owns**. One line in `store.py`. | Azure Container Apps RG `trading-agents`, **16 apps + `dispatcher-cron` on `:s165`** (main `34ca6ae`, `v0.89.03`; images 15/15 by run `31166820629`); live Neon spine for the `DeployRecord`. **Image-only retag:** the vocabulary pack hashed **byte-identical `b8d1a30f…`** at the deployed commit and at `HEAD`, and `ScanRun` is **not** one of the five property-enforced labels, so nested props are unguarded and the S148 fail-closed stall cannot apply. | ✅ **PASS (structural).** `make ci` exit 0 — **2168 passed, 6 skipped, 100.00 %**, 4 import contracts kept, pip-audit clean; `assert_gate_ran --sha 34c087f…` **GATE PROVEN** for CI and Security Findings, unpiped. Planted violation: removing the `filter_trace` write gave **3 failed, 1 passed** — the survivor is the ownership test, which correctly does not depend on the trace. Fleet verified: **16/16 on `:s165`**, all `minReplicas=0`, **16/16 weekday-only** windows, and **every tunable survived the retag** — `SCANNER_CANDIDATE_CAP=25`, `ANALYST_EXIT_CONFIDENCE_FLOOR=0.50`, `PORTFOLIO_MANAGER_MAX_POSITION_PCT=0.01`, `PORTFOLIO_MANAGER_MAX_POSITIONS=60`. `DeployRecord deploy:2026-08-07T09:50:22…:s165:34ca6ae` written after verification. 🟠 **Scope it honestly — no behavioural proof yet.** No run has executed on `:s165`, so a persisted `filter_trace` has never been observed in production; that lands on Monday's `sched-2026-08-10`. **No backfill:** the 28 existing `ScanRun` nodes have no trace, so every drop before today stays unattributable and filter-quality evidence starts from the next run. 🟠 **A green law row was hiding a false clause:** `SCAN-OBS-01` was 🟩 citing a test that proves provenance links, not `FilterTrace` reconstructability — the S156/ADR-0021 shape. Row now cites both tests; the clause is finally true. | ✅ Nothing to tear down — no test artifacts, no graph rows stamped, no broker calls. The only write was the `DeployRecord`. Scale windows, KEDA rules, cron and all four tunables re-verified **after** the retag rather than assumed. |
| 2026-08-07 | S165 filter-trace persistence + the widened cap, proven in production (fix, 0.89.03, fleet `:s165`) | **The behavioural proof S165's deploy row said was still owed.** 🚨 **A full pipeline run was unsafe and was not used:** `sweep_unfilled_orders` runs head-of-run in execution's **RunRequest** path, and the ten pending flatten exits (`exit:…:sell`, `limit`, `status=new`, from a *different* run) match every sweep condition — any ordinary run would have **cancelled all ten and undone the flatten** three hours before the open. Instead only **master, provider and scanner** were woken (execution and the PM held at `minReplicas=0`, verified **0 replicas**), so the cascade stopped at `ScanRun` and nothing could reach the broker. The scanner polls `MarketData` directly, so it needs neither `position_sync` nor execution. | Azure Container Apps RG `trading-agents` on **`:s165`**, 3 apps woken of 16; live Neon spine; live provider feeds (100/100 tickers). Synthetic run `sched-2026-07-16` — a NYSE session with no existing run, chosen so the day-keyed `sched-2026-08-07` lineage stayed untouched. No broker calls. | ✅ **PASS — both halves observed.** The `ScanRun` carried a persisted `filter_trace`: **100 verdicts for 100 evaluated** (`universe_size=100`, accounting exact), **22 survived / 78 dropped**, `dropped_by_filter` `{min_average_volume: 60, min_relative_strength: 18}` matching the per-ticker `filter_fired` census exactly, and every verdict carrying its features — e.g. `ABBV dropped min_average_volume avg_volume=289,005`. **A drop now names the filter that bound it and the number it was judged on.** 🟢 **The cap change is live:** `candidate_count=22`, not 5 — every name that passes the filters now reaches the analyst, under the 25 cap. `bypassed={False: 100}`, confirming bypass is still deliberately off so the cap change stays attributable. | ✅ **Torn down to zero, verified by stamp:** `pg_teardown --run-id` removed 25 edges / 26 nodes; residual across both of the run's identifiers **0**; standing counts back to baseline (`ScanRun` 28, `RunRequest` 28, `MarketData` 28, `Candidate` 135). 🚨 **The ten pending exits verified intact after the run — 10/10 still open** — which was the whole risk. All three apps returned to `minReplicas=0`; fleet **16/16 on `:s165`**. |
| 2026-08-08 | DL-94 a run-scoped teardown leaves no orphans (fix, 0.89.04, not deployed — tooling only) | `scripts/pg_teardown.py`'s collection path run **read-only** against the live Neon spine for `sched-2026-08-07`, comparing the old label walk with the fixed collector. Never called the delete path. | Neon Sydney via `POSTGRES_DSN` on a `read_only` connection; `.env` loaded by absolute path from outside the worktree so no credential ever existed as a file in the tree. | ✅ **23 rows that were orphans are now collected** — 11 `SentimentReading`, **10 `PositionCheck`**, 1 `DeliberationRun`, 1 `Rejection`. 🚨 **The 10 `PositionCheck` rows prove DL-94's own proposed fix was insufficient:** widening the walk's label list reaches **0** of them, measured, because `PositionCheck -> Position` is their sole edge type and `Position` is production state no walk may traverse. Only the new container-key prefix pass collects them. 🚨 **10 `Fill` rows are no longer deleted** — `exit:…:sell`, the ten live flatten exit orders: the old code would have deleted the graph's record of ten orders open at the broker, the exact DL-44 divergence. `protected_kept` now reports **20** (10 `Fill` + 10 `Position` reached via the newly-collected `PositionCheck`s), where a stamp-based count reported a misleading **0**. 🪤 **The live check earned its place:** it caught a `psycopg.errors.SyntaxError` — `ESCAPE` is invalid on the `LIKE ANY(...)` form — that all 12 unit tests passed straight through, since a fake cursor does not parse SQL. `make ci` **2180 passed / 6 skipped / 100.00 %**, exit 0, unpiped to a file. Three planted defects observed failing first (DL-70): silent exit-0-with-orphans, the dropped prefix pass, and `BrokerStopOrder` added to the artifact list. | ✅ Nothing to tear down — the check only read. Zero writes, zero deletes, zero broker calls; node counts unchanged. The `.env` copied into the worktree during setup was deleted before any `git add` and never reached the index (verified with `git status --porcelain`). |
| 2026-08-08 | The flatten filled and Monday can buy — proven through the shipped code on real state (no version bump; nothing deployed) | The **live Alpaca paper account** and the **live Neon spine** replayed through the actual `write_snapshot` → `reconcile_positions_from_snapshot` → `portfolio_from_graph` → `evaluate_recommendations` chain in an `InMemoryGraphStore`, with the real 99-name `MarketData` closes. **No graph write, no broker order, no run fired.** | Alpaca paper REST (GET account/positions/orders only); Neon Sydney on a `read_only` connection; live `az containerapp show` for the deployed tunables. | ✅ **All ten exits FILLED, 10/10, `filled_qty == qty`** — eight within 13 min of the 13:30 UTC open, SCHW 14:24, **WFC 18:55**; broker now **0 positions, 0 open orders, equity $102,469.03, cash $102,469.03** (the −$104,966.77 margin repaid). Flatten cost **−$680.88** vs the 08-06 equity, ~0.66 %. 🚨 **The graph did NOT know**: 10 active `Position` nodes survived, because the last run (`sched-2026-08-07`, 07:56 UTC) preceded the fills. **Proven that the run self-heals**: the shipped reconcile marked **all 10 `broker_absent`** from a zero-holding fresh snapshot — there is no empty-snapshot guard, only `status == "fresh"` — leaving **0 active**, `deployed_value` **$0.00**, `account_status=fresh`, and **`available_for_buys` $97,345.58** where it was **−$105,748.50**. **PM decision on all 99 real-priced names: `approved=60 rejected=39`, `insufficient_cash` **0**, `max_positions` 36** (the slot cap binding again, as designed) and **3 `below_min_quantity` — BLK $1,030.90, GS $1,046.18, LLY $1,183.95**, exactly the three names the resize analysis predicted at a ~$1,031 parcel. At the realistic scanner-capped **25** candidates: `approved=24`, `insufficient_cash` **0**. Deployed tunables verified live, not assumed: `MAX_POSITION_PCT=0.01`, `MAX_POSITIONS=60`, `EXIT_CONFIDENCE_FLOOR=0.50`, `SCANNER_CANDIDATE_CAP=25`. | ✅ Nothing to tear down — the replay ran entirely in an in-memory graph; the live spine and broker were read only. Zero writes, zero orders, zero runs fired; `sched-2026-08-10` left unconsumed deliberately. 🟠 **NOT proven: no pipeline run has executed on the deployed images.** This proves the logic on real state, not the fleet. |
| 2026-08-08 | Full pipeline run on the deployed fleet after the flatten — `check-s166-flat-book` (no version bump; nothing deployed) | A **real end-to-end run on the `:s165` fleet**: 16 apps scaled to `minReplicas=1`, one manual `RunRequest` placed via the shipped `place_run_request` with a **`check-*` id** so the day-keyed `sched-2026-08-10` stayed unconsumed, as_of `2026-08-07` (most recent session) for current prices. | Deployed Azure Container Apps fleet on `:s165`; live Neon spine; live Alpaca paper broker. | 🟢 **THE PIPELINE BOUGHT.** 8/8 stages, **zero Faults**. position_sync wrote a snapshot with `holdings=0 account_status=fresh equity=10246903` ($102,469.03, matching the broker to the cent) and **reconciled all 10 stale `Position` nodes to `broker_absent`** — 27 total, **0 active** — proving on the **deployed image** what had only been replayed offline. Scanner **evaluated=100 → candidate_count=23** with a `filter_trace` carrying **100 verdicts for 100 evaluated** (S165 live). Analyst 18 recommendations; **PM approved 18, `Rejection` nodes 0 — zero `insufficient_cash`**, against 8 consecutive days of `approved=0`. Execution **`submitted=18 rejected=0`**; **18 `buy limit` orders `accepted` at Alpaca**, `tif=day`, queued for Monday's open. 🟢 **DL-80 CLOSED:** `LLMCall` **25 → 164**, the 139 new all **`claude-opus-5`**, `DeliberationRun` `real_debate_count=18 failed_open_count=0` with a real narrative. 🚨 **NEW DEFECT — [DL-98](../design-log.md): the veto ran 11 minutes too late.** Orders hit the broker 05:36:22; the `DeliberationRun` was written 05:47:32 and its last `LLMCall` 05:51:53. `_drop_vetoed` is **fail-open on absence**, so execution could not distinguish *not yet* from *not deployed* and submitted the full set. The deadlock had been hiding this: with `approved=0` there was never anything to race. | ✅ Fleet returned to `minReplicas=0` — **verified 16/16**, all still on `:s165`. 🟠 **Deliberately NOT torn down:** the run's lineage (110 nodes) and the **18 open broker orders** are kept — they are the proof, and cancelling them would undo the very state Monday needs. The 18 fill at Monday's open, so `sched-2026-08-10` becomes a manage-run rather than a first-buy run. Reversible via `DELETE /v2/orders/{id}` until the open. |
