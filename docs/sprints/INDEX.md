# Sprints index — phases, goals, and what shipped

**How to use:** find the phase you care about in the table below. For the full
chronological sprint list with goal summaries, see [README.md](README.md). For
the overall progress bar, see [../build-plan.md](../build-plan.md).

---

## Phase map

| Phase | Sprints | Theme | Status |
| --- | --- | --- | --- |
| **P1** | S01–S07, S22 | Kernel runtime, first 3 agents (provider/scanner/analyst), Celery bus, MCP tool binding | **complete** |
| **P2** | S08 | Observability adapter (metrics SDK) | **complete** |
| **P3** | S09, S11–S13 | PM, execution, monitor, reporter (full pipeline slice) | **complete** |
| **P4** | S14–S15 | Dispatcher daily loop + supervisor message lineage | **complete** |
| **P5** | S16–S17 | Operator intent parsing + supervisor capability gate | **complete** |
| **P6** | S18–S21 | Surfaces CLI (resolve_flag, lifecycle queries, approve, incidents, explain) | **complete** |
| **P7** | S23 | Researcher agent: bounded parameter proposals | **complete** |
| **P8** | S24–S25 | Stage gate: evidence-based promotion, StageTransition nodes | **complete** |
| **P9** | S26 | MeteredFaultSink wiring + observability proof | **complete** |
| **P10** | S27–S29 | Curator: dataset assembly, training trigger, predictor registry | **complete** |
| **P11** | S30–S35, S38–S42, S44–S45, S50–S55 | Analyst deterministic port: 15 technical + fundamental + relative-strength pillars; scanner beta/earnings; PM risk/sector gates; reporter %-metrics + $ repoint; provider feeds (Tiingo, Alpaca, Finnhub, FMP, sectors, earnings) | **complete** |
| **P12** | S36–S37, S46–S49, S56–S57 | Sentiment champion–challenger trinity: LM lexicon (champion) + provider/AV (challenger) + FinBERT (advisory); sentiment scorecard harness | **complete** |
| **P14** | S60–S67 | Kernel pub/sub + claim-check + Azure Service Bus backend (all 8 agents dual-mode) | **complete** |
| **Law cycle** | S69 | Provider law cycle: DRIFT-006/007 corrected; 23/43 clauses green; laws LOCKED v1; template locked for agent backfill | **complete** |
| **Q1** | S58–S59 | Qlib Phase Q1: LightGBM price/return shadow signal + training + IC scorecard | **complete** |
| **Q2** | S68 | Qlib Phase Q2: Alpha158 22-field pillar (off by default, weight=0.00) | **complete** |
| **Q1b** | S110 | Qlib workflow adoption (R001 addendum 2026-07-04): signal evaluation battery — rank IC, per-date IC mean/std/IR, quantile spread + monotonicity, multi-horizon decay, stability; OOS-only CLI. Live Tiingo check (DL-37); baseline best at h=20 (IC-IR 0.27) | **complete** — shipped 0.53.00 (2026-07-04, merged `0818679`) |
| **Q1c** | S111 | Qlib workflow adoption: rolling retrain + IC-decay trigger — decay measured against the S110 baseline, challenger must beat incumbent on the same window, operator holds `--apply`; + committed resumable Tiingo exporter | **complete** — shipped 0.54.00 (2026-07-04, merged `45f6c34`) |
| **Q3** | S112 | Qlib workflow adoption: researcher backtest evidence — self-built no-lookahead walk-forward harness (pyqlib 3.13-incompatible), `BacktestEvidence` optional contract field (researcher 0.2.0), bounded signal-catalogue CLI; the Q5 prerequisite | **complete** — shipped 0.55.00 (2026-07-05, merged `feb7f87`); Q5 governed factor mining unblocked |
| **Deliberation quality (DL-41)** | S114 | 🔴 **Priority** — complete the challenger-veto evidence: render every enforced gate as value **+ explicit pass/fail outcome**, and thread the **PM risk gates** (`max_sector_pct` concentration, sizing, held positions) that are computed but currently unrendered; PM emits gate outcomes (additive contract), `veto_context.py` split, completeness test. Deterministic evidence fix, not a prompt change (DSPy = DL-42, later). | **complete** — shipped 0.56.00 (2026-07-05, merged `6d9e9d0`); live Aura check proved every gate rendered with explicit outcome |
| **Q5 (part A)** | S113 | Qlib workflow adoption: governed factor proposal — bounded factor catalogue + LLM proposes an in-catalogue factor (enum-guarded, fail-open, LLM only in composition root) → S112 walk-forward scores it → `FactorProposal` with `BacktestEvidence` into the review queue. LLM never drives; researcher `external_io=()` intact. Shadow→promote is S115 (part B). | **complete** — shipped 0.57.00 (2026-07-06, merged `3ec2d9e`); live check: GPT-5.5 selected in-catalogue `momentum lookback=60`, off-menu selection failed open |
| **Q5 (part B)** | S115 | Qlib workflow adoption: factor shadow signal — approved factor → live forecaster shadow emitter (duplicated catalogue math, parity test, OFF by default per Q2 precedent), additive `forecast_factor` capability, generic scorecard by `model_id`, operator run-book for promote/kill on existing P10/stage rails. Closes the Q5 governed factor-mining loop. | **complete** — shipped 0.58.00 (2026-07-07, merged `ab66caf`); live check: 2 factor ShadowPredictions on Aura, scorecard populated, default-off wrote 0 nodes |
| **DL-43 (step 1)** | S116 | Postgres migration: `PostgresGraphStore` adapter (psycopg 3) over the 6-method port, **alembic-managed schema** (`infra/migrations/`), backend parity suite across InMemory/Neo4j/Postgres, temporary dual env selector (`POSTGRES_DSN` wins; Neo4j fallback retired by S118), FK-ordered teardown script. Live check on the provisioned Neon free (Sydney) instance. | **complete** — shipped 0.59.00 (2026-07-07, merged `5f11b93`); Neon live: alembic applied, suite 7 passed, slice + teardown to 0 |
| **DL-43 (step 2)** | S117 | Postgres fleet swap: `POSTGRES_DSN` into Key Vault via the S108 tested-before-insert seeder, composition defaults flipped to Postgres (temporary `NEO4J_URI` rollback removed by S118), `alembic upgrade head` as deploy step, `DEP-POSTGRES` probe, **ADR-0001 superseded** (Postgres system of record; Neo4j analysis workbench; ADR-0008 amended). Live: seeder read-back + in-process fleet slice on Neon + teardown. | **complete** — shipped 0.60.00 (2026-07-07, merged `d6776ec`); Key Vault read-back equal, Neon served-slice durable, teardown to 0, `make ci` 1383 passed / 6 skipped / 100% |
| **DL-43 (step 3)** | S118 | Neo4j runtime rip-out: kernel adapter + tests + `neo4j` dep deleted, `NEO4J_URI`-only env → clear ADR-0014 startup error, `aura.ps1`/`compare_aura.py`/`neo4j_crud.py` retired, workbench-only compose profile kept (ADR-0008 scope), docs/laws sweep, Aura retirement runbook (operator deletes after 7-day grace). Rollback becomes git revert + redeploy. | **complete** — shipped 0.60.01 (2026-07-07, merged `ce53230`); fresh sync removed `neo4j`, no runtime imports, Neon durable write + teardown to 0, `make ci` 1374 passed / 5 skipped / 100%; **Aura `bce05bd6` deleted by operator 2026-07-07 — DL-43 fully closed** |
| **Deliberation quality (DL-42)** | S119 | DSPy-compile the deliberation role prompts (defender/challenger/judge) behind the ADR-0010 `PromptOptimizer` port — second real instance after S107. Artifact-file opt-in via env (byte-identical default), metric = Class-1 grounded pass-rate + `score_understanding` + n=3 verdict stability, golden firewall as hard veto; per-role gating; promotion operator-held. | **complete** — shipped 0.64.00 (2026-07-08, merged `353d983`); judge artifact 94%/94% + stability 100% (champion 78%/83% + 75%), challenger artifact regressed 61%, defender flat; firewall PASS ×4; no default flipped — resolution packaged as S121 |
| **Deliberation quality (DL-42, resolution)** | S121 | First ADR-0010 prompt **promotion**: compiled judge (`2026-07-08-s119-v4`) becomes `JUDGE_SYSTEM`, golden re-freeze mandatory; one bounded challenger recompile gated against the promoted-judge baseline; defender untouched. Operator-directed 2026-07-08. | **complete** — shipped 0.65.01 (2026-07-08, merged `5c5dd1c`); judge promoted, golden re-frozen to 5 robust cases, challenger v5 adopted after `100%/100%` vs champion `94%/94%`, final-default firewall PASS |
| **Operations dashboard (DL-47)** | S122–S129 shipped/fixed — ARC COMPLETE + fixpacks | The PRD's product surface #1: `surfaces/dashboard/` read-model service + static frontend. S122 = run selector + Section III trading-run logical verdicts (observatory-sourced) + flags/positions/recovery ladder; S123 = fleet lifecycle + infra + per-container logs (Log Analytics) + A$ cost meters; S124 = glance-first master verdict + `NO_TRADE` acceptance verdict + operator-language sweep (redline r2, reqs 13–15); S125 = tier-1 operator-agent chat through the existing bounded dispatch, with run grounding, explicit confirmation, honest disconnection, and priced audit records; S126 = resume-from-stage (supersession, never deletion — child RunRequest + `RESUMES` lineage) + DL-46 deploy-currency judgement (`DeployRecord` + current/behind/unverified) + tri-state bus health; S127 = fixpack for flag acknowledgement, approve routing, template-based deploy currency, warning links, and optional Azure skip; S129 = quant-evidence persistence into Recommendation/veto context plus dashboard read-cache egress reduction. Design spec: `docs/design/dashboard-mockup.html`. | **shipped** — S122 merged `820b8c9`, S123 merged `2ad656e`, S124 merged `b9ed20e` (0.68.00); S125 merged `9420b78` (0.69.00); S126 merged `297354b` (0.70.00); S127 merged `32c73cc` (0.71.00); S129 merged `3be1ee8` (0.71.02, 2026-07-19) |
| **Production hardening (DL-44)** | S120 | Broker reconciliation after the first unattended run exposed graph↔broker divergence (CSCO double-buy): additive `positions()` on the Broker port, execution-owned run-start `BrokerPositionSnapshot` + loud divergence Flag, monitor adopts broker truth (`reconciled-from-broker` provenance), pending-Fill status refresh, teardown discipline amended. Live check = the repair. | **complete** — shipped 0.65.00 (2026-07-08, merged `6c0c0e9`); live check read-only/idempotent (prior in-development repair had already written the production Positions; divergence Flag retained as record); DRIFT-020 |
| **Feed resilience (DRIFT-021)** | S128 | Per-request Finnhub pacing (55/min tunable budget), per-ticker fault attribution in all four enrichment feeds (DRIFT-014 pattern — one 429 costs one ticker, not the feed), durable attributed notes on the graph quality trace; live proof used the real rate limit as fault injector. | **complete** — shipped 0.71.01 (2026-07-19, merged `09120b3`); live check PASSED (paced 99/99 zero degraded notes in 7 min; unpaced per-ticker `:429` attribution with majority kept; Neon durability read-back); DRIFT-021 CORRECTED; fleet deployed at `:s128` same day |
| **Supply-chain hardening (R005 / backlog H)** | S130 | Base-image CVE remediation: Trivy keeps HIGH/CRITICAL enforcement with `ignore-unfixed: true`, `.trivyignore` stays empty, and all 14 images moved to two-stage Docker Hardened Images (`dhi.io/python:3.13-dev` build, `dhi.io/python:3.13` runtime) with direct Python entrypoints. | **complete** — shipped 0.71.03 (2026-07-19, merged `8aefe2a`); manual run `29681635979` built/pushed all 14 `s130-test` GHCR images and passed every Trivy gate; post-merge `build-images` on main GREEN; fleet deployed at `:s130` same day |
| **Blast-radius hardening (DEP-POSTGRES / DEP-BUS / backlog I+J)** | S131, S133 | Per-agent Postgres runtime identities: 15 `ta_<agent>` roles, per-role Key Vault DSNs, secret-backed Container Apps delivery, revocation canary, plus dispatcher import-closure image slim. Service Bus part 2 adds measured entity-level topic SAS rules, per-target Key Vault bundles, scoped Container Apps secretRefs, master bus-env removal, and Send/Listen least-authority + revocation proof. | **handoff** — S131 shipped 0.71.04; S133 handoff 0.71.07 on `sprint-133-servicebus-sas`, with 33 Service Bus rules / 13 targets / 26 Key Vault secret names and live canary proof |
| **Test-quality hardening (backlog G)** | S132 | Manual `mutmut` over deterministic decision engines: analyst/PM/scanner/execution gates, acceptance, and veto context; survivor/no-test rows dispositioned instead of becoming a CI gate. | **handoff** — 0.71.05 branch `sprint-132-mutation-testing` pushed unmerged; kill rate improved from 78.47% to 79.87%, report bundle committed under `docs/reports/sprint-132-mutation-testing/` |
| **Law cycle** | S70 | Per-agent law backfill: scanner/analyst/PM/execution laws authored → cited → LOCKED v1 | **complete** |
| **Law cycle** | S71 | Per-agent law backfill cont.: monitor/reporter/forecaster/operator/supervisor/curator/researcher | **complete** |
| **ADR-0010** | S72 | `system_prompt` tunable on operator + forecaster (ADR-0010 immediate close) | **complete** |
| **ADR-0010 (LLM quality)** | S109 | Heterogeneous deliberation: Defender/Challenger on GPT-5.5, the debate Judge on Opus (`claude-opus-4-8`); dedicated `DELIBERATION_JUDGE_*` env (operator model untouched); veto now debates a grounded proposition (fixes S96). Not the EXP-004 scorer judge. | **complete** — shipped 0.52.00 (2026-07-03); **re-run 0.55.01 (2026-07-05)** cleared the deferred proofs: live-Opus check + golden re-freeze + firewall PASS, plus a reasoning-budget harness fix (empty `gpt-5` challenger) |
| **P15** | S73–S83 | Multi-agent container split: master bootstrap + Dockerfiles + RSA signing + Key Vault + GHCR build pipeline + credential naming + provider ingestor + graph-pull work loops (S79 provider→scanner; S80 scanner→analyst; S81 analyst→PM; S82 execution+monitor+reporter closes DL-08; S83 dispatcher RunRequest trigger makes provider graph-pull + end-to-end demonstrator) | **in progress** — paused under etalon-first (DL-34) |
| **Platform/pack + etalon** (post-P14) | S84–S89, S96 | ADR-0012 substrate/pack extraction — grants + secrets out of the master image (S84–S86, DL-12); staleness gate counts trading sessions (S87, DL-10); DL-09 filter verdicts + quality scorecard (S88–S89); deliberation understanding + challenger-veto (S96, DL-31). Continuous etalon-first work — live status in `../STATE.md`, not a numbered phase. | **shipped** |
| **P12/P13** | tbd | DSPy `PromptOptimizer` port + golden eval set + per-(task×model) compiled prompt artifact | **queued** |
| **P16** | S90–S95 | Continuous-improvement system (ADR-0013): CI-1 parameter catalogue · CI-2 RunMetrics on graph · CI-3 ParameterSet (configurable-not-settable) · CI-4 experiment+compare · CI-5 gate+promote (absorbs ADR-0010) · CI-6 optimiser (sweep; ingest target first) | **queued** — specs written (S90–S95), deferred behind a perfect etalon (DL-19) |
| **Fleet Activation / Credential Validation** (DL-30/DL-35/DL-36) | S97–S108 | Close the distributed-platform gap: `serve_loop` primitive (S97) → control-plane agents served in-process (S98–S99, retires every `idle_loop()`) → Azure Service Bus receiver + parity (S100) → permanent graph spine (S101) → 13-container run-through + distributed acceptance (S102) → dispatcher cron (S103). Then the DL-36 credential arc **(A/B/C/D complete)**: tested activation + escalation on failure (S104) → master secret cache (S105) → LLM bounded-catalogue remediation planner (S106) → **eval-gated auto-remediation execution (S107, Piece D)** — DSPy behind ADR-0010's `PromptOptimizer` port gates the selector (its first harness instance), then safe executors run the `test→execute→production→documentation` loop with one automatic shot, plus the `_instance_counter` concurrency fix + composition-root wiring. Then **`.env`→Key Vault seeder, tested-before-insert (S108)** — only working credentials ever enter the vault (DL-36, one step upstream; fail-closed). In-process before distributed; reverses the etalon-first pause for the fleet workstream (DL-35), then hardens activation handoff (DL-36). | **COMPLETE (2026-07-08, through 0.63.00)** — S97–100, S102, S103 + S104–108 shipped; S101 absorbed by S116–S118 (permanent spine = Neon Postgres, ADR-0014). S102 proved the fleet distributed (13 Container Apps, 12-agent activation, distributed `ACCEPTANCE PASS`, five control-plane round-trips, ledger Layer 2 🟩, DRIFT-016..019); **S103 made it self-driving**: `dispatcher-cron` Job (22:30 UTC) + KEDA scale-to-zero windows, calendar-gated day-keyed `RunRequest`, double-fire deduped, non-session skip proven, fleet standing at 0 replicas. The DL-35 end state is live in paper mode |

---

## Queued / parked sprints

| Sprint | Goal | Blocked by |
| --- | --- | --- |
| S127 (fixpack) | Batch confirmed rows 4/9/10/11/12 in [s127-fixpack-backlog.md](s127-fixpack-backlog.md) | **Shipped** (0.71.00, merged `32c73cc`) |
| S43 (monitor realized PnL) | Monitor real `pnl_cents` → reporter re-point to $ | S55 already re-pointed; revisit when live broker wired |
| P12/P13 DSPy harness | `PromptOptimizer` port + golden eval set + DSPy first impl | Plumbing complete first (build-plan P11/P14 exit) |
| [sprint-156](sprint-156-law-coverage-is-checkable.md) | Make the law book's own proof checkable: a `make ci` gate for conventions §3 (green = a test cites the ID), the 29 unbacked greens adjudicated, roll-ups derived not hand-kept | **SHIPPED** `23ae35f` / `v0.86.00` — roll-ups now derived clause-denominated (252 / 653); row-per-clause warn-only at 101 until S157 |
| [sprint-158](sprint-158-fail-open-must-be-loud.md) | Make the deliberator's fail-open loud: a missing SAS-bundle topic raises instead of falling back, the manager preflights its peers before consuming work, fail-open becomes a queryable fact, and acceptance stops passing an inert veto | **SHIPPED** `8be1570` / `v0.86.01` — **deployed** 2026-08-04 via [chore-deploy-s158](chore-deploy-s158.md) (fleet `:s155` → `:s158`; now on `:s165`). No Key Vault repair is owed: the bundle carries all three topics and the transport was verified live (the 08-02 trigger was the half-failed deploy). Retag packaged as [chore-deploy-s158](chore-deploy-s158.md) |
| [chore-deploy-s158](chore-deploy-s158.md) | Retag the fleet `:s155` → `:s158` so the deliberation acceptance checks can see their own facts — **`up`, not an image-only retag**: S158 added three properties to a property-enforced label, so image and vocabulary pack must move together | **DONE** — executed 2026-08-04, fleet `:s155` → `:s158` via `up` (not an image-only retag). Verified per target: 16/16 apps + job on tag, 16/16 `Succeeded`, pack read back byte-identical `dab0623b…` on all 17. 🚨 `up` printed `Deploy FAILED for 17 target(s)` and every word of it was wrong — hardening row Q, since closed. Did not close DL-80 |
| [chore-wsl2-dev-env](chore-wsl2-dev-env.md) | Move the dev loop to WSL2 (native-ext4 `mutmut`/`pytest`, CI/prod parity); keep `.ps1` under `pwsh`, no rewrite; `.gitattributes` LF normalisation | Sprint queue empty (after S134 0.71.06 + S133 0.71.07) — renormalisation must not overlap an in-flight branch |
| [sprint-159](sprint-159-tests-never-transact.md) | Stop the unit suite transacting with production Service Bus: offline settings by construction, an autouse send-boundary guard proven able to reject, and the clean-checkout half of row T (`uv sync` omits the `azure` extra) | **SHIPPED** `56a157b` / `0.86.03`, residue closed in `a8cb973` / `0.86.04`. Row T's open question answered: **40 dead-lettered test messages** on `deliberator-proponent.requests`, plus topic DDL on the production namespace |
| [sprint-160](sprint-160-shadow-book.md) | The shadow book: price every recommendation from its own run's snapshot, write one append-only outcome fact per (recommendation, horizon) with a **disposition**, backfill the 26 runs already on disk, and score `taken` vs `blocked_capacity` | **SHIPPED** `2865c5e` / `0.87.00`, direction fix `c560549` / `0.87.01` (a sell is a hit when the price *falls*) — deployed on `:s161`. Rev 1 stopped at the law-first gate (no locked constitution owns a `RecommendationOutcome` label; `RPT-IDN-02` enumerates three, `RPT-NEV-02` forbids the rest). Rev 2 dropped the label: the scorecard is a **read-only derivation that persists nothing**, joining `accept.py`/`trace_run.py`. DRIFT-034 stays open for the next law-amendment cycle. ⚠️ Its `blocked_capacity` cut is contaminated from **2026-08-06 onward** — see [S162](sprint-162-rejections-name-every-gate.md) |
| [sprint-161](sprint-161-pm-knows-what-it-paid.md) | Teach the PM what it paid: execution records account cash/equity on its run-start snapshot, PM sizes against that instead of a `starting_cash` constant, stale account blocks buys but never sells | **SHIPPED** `802b1c6` / `0.88.00`, deployed `:s161` (`f7c3cd6`, verified 16/16 apps + pack). First scheduled run `sched-2026-08-06` (22:30 UTC): **8/8, ACCEPTANCE PASS**, `account_equity_cents` **proven** fresh at `$103,149.91`. 🟠 **The sizing gate itself is still unproven in production** — the one buy (MSFT) was rejected on `max_positions`, which is checked first, so `cash_available` never reached the record. [S162](sprint-162-rejections-name-every-gate.md) makes it provable |
| [sprint-162](sprint-162-rejections-name-every-gate.md) | Make a rejection carry the gates it evaluated, not just the first one that blocked: `RejectedOrder` gains the `gate_report` that `OrderIntent` already has, every rejection site attaches what it computed, and both read-only renderers show it | **SHIPPED** `ecde86d` / `v0.89.00` — deployed `:s162` (image-only; pack byte-identical). **Proven end-to-end on the deployed fleet**: 8/8 stages, `ACCEPTANCE PASS`, and the live trace printed `MSFT   SKIP  max_positions (also failed: cash_available)` with all four gate outcomes. No approval decision moved (`approved=0 rejected=11`, same reason strings). 🟠 Left three modules trimmed to 198/198/193 against the 200-line hard block |
| [chore-flatten-and-resize](chore-flatten-and-resize.md) | Clear the deadlock: raise the analyst's `exit_confidence_floor` above the highest held confidence so all 10 positions exit through the **designed ADR-0017 rail**, then resize to small parcels across many slots | **DONE** — executed 2026-08-07 on `:s164`, config-only, after S163 + S164 unblocked it. `exit_confidence_floor` 0.50 → 0.99 → all 10 held names emitted `sell`, PM approved all 10 full exits, execution `submitted=10 rejected=0` on the designed ADR-0017 §1 rail. Resize applied: `max_position_pct` 0.10 → **0.01**, `max_positions` 10 → **60**; floor verified back at 0.50. 🟠 **The fills are not proven** — `tif=day` submitted pre-market, all ten queued for the 13:30 UTC open |
| [chore-teardown-leaves-no-orphans](chore-teardown-leaves-no-orphans.md) | 🔴 **A teardown that under-deletes exits 0.** `pg_teardown.py --run-id` reported `deleted_nodes=21` and left 41 rows carrying the same run's stamps ([DL-94](../design-log.md)). Splits the one tuple that was answering two questions — *disposable artifact?* and *may the walk traverse it?* — adds the `<owning-run-key>:<suffix>` enumerator, and makes `--run-id` **exit 1** naming every survivor | **SHIPPED** — `v0.89.04`, 2026-08-08. Tooling only, nothing deployed. 🚨 **DL-94's own proposed fix was measured insufficient**: widening the label list reaches **0** `PositionCheck` rows, since their sole edge is to `Position`. 🚨 The list was an undeclared traversal barrier — adding `BrokerStopOrder`, which reads exactly like a run artifact, would have deleted the 27 live `Position` rows |
| [chore-split-modules-before-the-block](chore-split-modules-before-the-block.md) | S162 trimmed three modules to fit the 200-line hard block instead of splitting them (`gate_report.py` 198, `trading_observatory_views.py` 198, `risk.py` 193), so the next change to any of them was blocked at the gate — including the PM files Monday's run exercises | **SHIPPED** — `v0.89.05`, 2026-08-08. All three split, every module now under the **150-line warn** line; test count unchanged at the split (2180 → 2180). 🚨 **The split found a real gap**: a planted reorder evaluating `reward_risk` before the sizing gates **passed all 86 PM tests**, silently changing the reason string on every `SKIP` line — [DL-96](../design-log.md), closed by `test_rejection_precedence.py` |
| [chore-one-sector-rejection](chore-one-sector-rejection.md) | `SectorBook.rejection` was an exact duplicate of the sector gate-to-reason mapping and was **called only by tests** — dead production code the 100 % coverage floor could not flag, and a second copy of operator-visible reason strings that could drift from the live one | **SHIPPED** — `v0.89.06`, 2026-08-08. Deleted; the mapping now exists once, in `position_gates.sector_rejection`, and `SectorBook` reports outcomes only. 🚨 **The coverage gate could not see it**: the method was 100 % covered by the test that was its only caller. Test re-pointed (not deleted) at the live composition; a planted reason-string swap now fails **5** tests. `concentration.py` **174 → 144**, out of the warn band. [DL-97](../design-log.md) |
| [sprint-166](sprint-166-the-veto-gates-buys.md) | 🚨 **The LLM veto has never blocked an order — because execution always won the race.** Measured: 18 orders reached the broker at 05:36:22 and the `DeliberationRun` saying *revise* landed 05:47:32. `_drop_vetoed` read an absent veto as *execute everything*, so *not finished yet* and *not deployed* were indistinguishable | **SHIPPED** — `v0.89.07`, 2026-08-08. A buy-carrying PMRun is held unconsumed for a bounded grace (`deliberation_grace_seconds`, default 900); **exits never wait** (S147 / ADR-0017). Fail-open survives but is loud: `ExecutionRun.deliberation_status` + a `DeliberationGraceExpired` fault. [ADR-0022](../decisions/0022-the-veto-gates-buys-never-exits.md); closes [DL-98](../design-log.md) |
| [sprint-167](sprint-167-a-fault-count-cannot-lie.md) | 🚨 **An audit reported “Faults today = 0” while 18 were being written.** `Fault` nodes stamp `occurred_at`; the query read `created_at`, which is `NULL` on every Fault node — measured both ways at the same moment: **18** vs **0**. Plus a fail-open asserting a cause the code never established (`llm unavailable` while 58 `LLMCall`s were logged) | **SHIPPED** — `v0.89.08`, 2026-08-08. Added `kernel.fault_query` for Fault-window reads that raise on a missing timestamp property and count `FaultSuppression` volume; `DeliberationRun.failed_open_reason` now records the captured exception cause and is declared in the property-enforced vocabulary pack. Fail-open policy and the S166 buy gate unchanged. 🟠 **Not deployed/live-proven:** pack moved `b8d1a30f…` → `13c0e3a0…`, so deploy must be full `up`, not image-only retag. Closes the code-shaped halves of [DL-99](../design-log.md) |
| [sprint-169](sprint-169-one-switch-and-a-deploy-that-keeps-it.md) | 🚨 **A provider switch that is four switches, and a deploy that silently unsets them.** Measured 2026-08-08: `DELIBERATOR_LLM_PROVIDER=openai` alone sends an *Anthropic* model name to OpenAI, because the three `*_model` tunables default to `claude-opus-5` and `entrypoint.py:76` passes them explicitly — and `role_models` would then record a vendor that never saw the order. Separately, a full `up` **replaces** each app's env set, wiping `SCANNER_CANDIDATE_CAP=25`, `MAX_POSITION_PCT=0.01`, `MAX_POSITIONS=60` and reverting the weekday-only dispatcher cron — all with a green `[OK]` | **SHIPPED `0.90.10`** (2026-08-14) — an unset model resolves its default from the provider and `role_models` records the **resolved** name; operator tunables and the dispatcher cron move into `orchestration/packs/trading_tunables.json`, and `up` refuses before its first create if it would drop a live env key, naming it (`-DropEnv` to drop one deliberately). `make ci` 2299 passed / 100.00 %. 🪤 **Not deployed** — the live half (a real `up`, tunables re-read off the app) is owed at the next full deploy. Closes [DL-100](../design-log.md) |
| [sprint-170](sprint-170-one-llm-adapter-in-the-plumbing.md) | 🚨 **The LLM layer is half in the substrate.** The port (`kernel/llm.py`) and the `LLMCall` ledger (`kernel/llm_ledger.py`) are kernel and both agents route through them — but the vendor adapters and provider selection are not, and are duplicated: two `AnthropicLLMClient` classes differing in **one method** (free-text vs tool-use). So S168's OpenAI fallback exists **only for the deliberator**; the operator can call Anthropic and nothing else while that key is usage-limited until 2026-09-01, and `surfaces/dashboard/chat_binding.py` imports the agent's adapter directly | **SPEC** — target: next available PATCH at merge, deliberately unpinned. One adapter set + factory in `kernel/`, the operator gains the same provider switch and an `openai-api-key` grant, the surface stops reaching into an agent. Opens [DL-101](../design-log.md) |
| [sprint-171](sprint-171-a-reply-must-answer-its-own-request.md) | 🚨 **The veto pairs a request with whatever reply is at the head of the queue.** Measured 2026-08-08 on `:s169`: the proponent made **18 real `gpt-5.5` completions**, yet the `DeliberationRun` read `real_debate_count=0, failed_open_count=18` with an *Anthropic* usage-limit reason — the manager drained **84** stale replies (oldest 5.5 h old) instead of reading its own. `peer_client.py:143` takes `messages[0]` with **no `request_id` correlation**. The error case fails open loudly; a stale **success** reply would be accepted as a verdict **about a different ticker**, with no fault and a green gate | **SHIPPED** `9a22102` / `v0.90.01` — deployed `:s171`, **proven in production 2026-08-08** from a cold start: `real_debate_count=18`, `failed_open_count=0`, reply subscription **0 active / 0 dead-letter**. 🚨 Fixing it exposed [DL-103](../design-log.md) — the true debate duration (943 s) overran the 900 s grace, mitigated 900 → 1800 |
| [chore-openai-cutover](chore-openai-cutover.md) | **S168's provider switch cannot take effect yet.** Measured 2026-08-08: Anthropic **400** (limit to 2026-09-01), OpenAI **200**; but `trading_secrets.json` grants the three deliberators **Anthropic only** — `OPENAI_API_KEY` appears **0** times. Setting the provider today fails closed at startup: works on a laptop, dies on the fleet | **SHIPPED** `8b30624` — packaged 2026-08-08, granted and deployed the same day. Grant + Key Vault seed + env var + deploy + test. 🪤 **Full `up`, not an image-only retag** — S167 moved the vocabulary pack (`failed_open_reason` on a property-enforced label), so a stale pack stalls the run fail-closed (S148/DL-79) |
| [sprint-163](sprint-163-an-exit-cancels-its-own-stop.md) | 🔴 **The book cannot be sold.** All ten positions show `qty_available = 0` — each is fully reserved by its own resting `gtc` stop, and `execute_pm_node` never cancels the stop of a ticker it is about to sell (`place_broker_stops` computes `sold_tickers` and uses it only to *decline placing* one). Ships the cancel-before-submit, the decision for what happens if the exit then fails, and idempotency | **SHIPPED** `a46449e` / `v0.89.01` — deployed `:s164`. **Proven in production 2026-08-07**: all ten exits reached the broker, `submitted=10 rejected=0`, and the ten resting `gtc` stops are gone — confirmed at the broker as 10 open `limit sell` orders and **zero** stop orders. Before S163 every one would have been rejected for insufficient quantity. Unblocked [chore-flatten-and-resize](chore-flatten-and-resize.md) |
| [sprint-164](sprint-164-a-cancelled-stop-can-be-replaced.md) | 🔴 **`EXEC-OBS-03` promises a retry the code cannot perform.** `_place_stop` returns `None` whenever *any* `BrokerStopOrder` fact exists at `stop:{ref}:{ticker}` — including a **cancelled** one — so a still-held position whose stop was cancelled faults truthfully every run and is never re-protected. Ships an attempt-chained stop key, the pattern the stop **Fill** side already uses | **SHIPPED** `f3b2fcf` / `v0.89.02` — deployed `:s164`. Closes **DRIFT-038**: cancelled `BrokerStopOrder` facts retry under fresh `#N` broker keys while active stops still block duplicates. 🟠 **Proven by tests only, never exercised in production** — no exit was rejected during the flatten, so no stop needed re-placing |
| [sprint-165](sprint-165-score-every-name-not-five.md) | 🔴 **The system produces 1.67 selection decisions per run, and the throttle is a cap of 5 — not the filters.** Measured: 100 evaluated → 22 pass the filters → **5** reach the analyst, because `candidate_cap=5` (bound `le=50`) whose `why` still says *“keep the first vertical slice small”*. `bypass_scanner_filter` already exists and is off; `FilterVerdict` is a live contract that is **never persisted** (0 nodes, not in the vocabulary), so DL-09 cannot work today | **SHIPPED** `34ca6ae` / `v0.89.03` — deployed `:s165`. **Proven in production 2026-08-07**: a scanner-only run persisted a `filter_trace` with **100 verdicts for 100 evaluated**, 22 survived / 78 dropped, each drop naming its filter; `candidate_count=22` not 5, so the widened cap (`SCANNER_CANDIDATE_CAP` 5 → 25, env-only) is live. The label-ownership wall was resolved by keeping the verdicts the `ScanRun` already decided — a `FilterVerdict` label is unlawful and a derivation would have been wrong. **No backfill**: the 28 prior `ScanRun` nodes stay unattributable |
| [sprint-172](sprint-172-independent-debates-run-independently.md) | 🚨 **The debate is serial end to end — measured, not inferred.** On `sched-2026-08-10` the sum of per-call latency ÷ the span from first call to last is **0.90**: for ninety per cent of the wall clock exactly one of 90 calls is in flight. Three serializations — `poll.py:64` is a plain synchronous `for` loop, rounds within an order are inherently sequential (and stay so), and all three deliberator apps run `maxReplicas=1`. At 100 orders the serial cost is **~4,740 s against a `le=3600` grace ceiling — no grace value fixes it**. Ships a bounded `debate_concurrency` tunable, deterministic result reassembly, and peer replicas | **SPEC — AMENDED 2026-08-19, ready to build.** Re-measured on the deployed `s181` fleet: sum ÷ span is **0.95** (worse than the original 0.90) and **124 s per order**, because `effort` reaching the wire in `0.90.02` roughly doubled the per-order cost. 🚨 **The build trigger is now measured, not judged: 15 orders breaches the 1800 s grace** (1,854 s) and the last two runs had 9 and 7. When the grace expires, execution submits **unvetoed** — that happened on `sched-2026-08-19`. Codex handover block written. Target next available MINOR at merge, deliberately unpinned |
| [sprint-173](sprint-173-a-verdict-must-be-reproducible.md) | 🚨 **The veto does not agree with itself: same model, same prompt, same eighteen tickers, 3.5 h apart — 9 of 16, 56 %, on a binary verdict.** Cross-vendor agreement is *higher* (12 of 17). Until reproducibility is a number the gate reads, "is the veto right" is answered by hand. Ships a read-only replay harness on the **Message Batches API** (thousands of independent replays, no latency budget, 50 % off), self- and cross-vendor agreement metrics with the fail-open exclusion applied, and a warn-only quality gate | [DL-104](../design-log.md) **(a)+(b) must land first** — six of fifteen vetoes were manufactured by the deliberator's own context builder, so measuring before that is removed measures the fragment. Also the **2026-09-01** Anthropic key limit ([DL-99](../design-log.md)), since Batches is Anthropic-only |
| [sprint-174](sprint-174-an-indicator-gets-the-history-it-declares.md) | 🚨 **Two of the five momentum indicators have never once computed in production, and every recommendation names both.** Two market-data paths disagree: the analyst's `DataRequest` carries `lookback_days=260` and the provider honours it — on the request path we do **not** use; the graph-pull path we **do** use calls `_today_window()` with no argument, taking `_DEFAULT_LOOKBACK_DAYS = 60` ≈ **41 bars** (measured, `sched-2026-08-11`, all 99 tickers). SMA-200 (needs 200) and the EMA crossover (needs 50) return `None` and are dropped by `if x is not None` with no fault, no note and no confidence penalty, while two hardcoded strings assert both by name. Ships the supply fix, a visible short-series signal, and a rationale built from what actually scored | **SHIPPED** `0.90.07` — declared indicator history now rides on the RunRequest |
| [sprint-175](sprint-175-the-veto-says-only-what-it-can-prove.md) | 🚨 **The veto states things it has not established, and one of them reached the broker.** Three bundled defects sharing a blast radius. (a) `context_pm._atr_pct` averages **every bar supplied** and renders `PASSED`/`FAILED` inside the `stop_vs_regime_volatility gate:` line for a comparison **no gate performs** — DL-104 traced **6 of 15** objections to it, and S174's 41→202 bar window flipped **AMD from `FAILED` to `PASSED` on an unchanged order** (measured 2026-08-13). (b) `fail_open_review()` records verdict `uphold`, so a fail-open never enters `vetoed_tickers` — on `sched-2026-08-13` **AMD and DOW reached the paper broker with no review at all**. (c) The packet carries no portfolio state and the model reasons about the book regardless | **SHIPPED** `0.90.08` — every route to an unreviewed order is a distinct queryable `deliberation_status` |
| [sprint-176](sprint-176-a-partial-fill-must-be-able-to-finish.md) | 🚨 **Hardening row P is filed as a labelling defect; it is a money defect.** `reconciliation_store.py:72` writes `broker_status` only when absent, so a fill first seen `partial` can never become `filled` — and `broker_price_cents` is write-once by the **same guard**, while line 53 reads it back as the basis for realized PnL. A partial therefore freezes the price PnL is computed from, permanently. Measured 2026-08-13: 188 Fills — 52 `filled`, 106 `rejected`, 30 absent, **0 `partial`**, which is the only reason it has never cost anything. Rides with [DRIFT-033](../laws/drift-register.md), the last `neo4j` string in the repo | **SHIPPED** `0.90.09` — a partial fill can complete via a narrow monotonic transition allowlist |
| [sprint-177](sprint-177-every-number-names-its-unit.md) | 🚨 **The veto has now killed four real orders over values it could only misread.** None was a wrong number — each was correctly computed under a name carrying neither its unit nor its scope. Three are fixed (S175 ×2, DL-112). The fourth is open: `SectorBook.__init__` seeds `_names` from held positions but **never `_deployed`**, so `max_sector_pct` renders `deployed=0.00` meaning *nothing from this batch yet* while reading as *no portfolio exposure* — it overturned a real AVGO buy on `sched-2026-08-14` while `existing_sector_names=2` in the same gate report correctly saw AMD and NVDA. Sweeps all ~20 rendered value sites. 🪤 Label only — seeding `_deployed` changes which orders get approved and needs its own ADR | **SHIPPED** `0.90.12`, 2026-08-15 — producer-owned values name unit+scope; vendor dicts render under an explicit unknown-units boundary |
| [sprint-178](sprint-178-the-health-signal-must-be-able-to-go-green.md) | 🚨 **`healthy` has been `false` every day since 2026-07-08 and nothing can turn it green.** Measured 2026-08-15: 52 Flags, 7 resolutions, **45 critical unresolved**. The machinery works — 2 criticals *have* been resolved and all 7 join correctly. The backlog can only grow: `subject_ref` embeds `snapshot.key` (run id + ISO timestamp), so every run mints a unique flag and the dedupe guard never fires; and `resolve_flag` is reachable only via the human `approve` capability. 🪤 Severity is chosen at run **start** — on `sched-2026-08-14` the flag was stale 3 minutes later, describing normal successful adoption. 🟢 Reporting-only: acceptance reads neither field | **SHIPPED** `0.90.13`, 2026-08-18 — [DL-111](../design-log.md). Severity follows **persistence** (warn → critical only if the divergence survives a run); `subject_ref` is now `{kind}:{ticker}`. Backlog swept: `pending_human_flags` **46 → 0**. 🪤 The spec's own recommendation was unimplementable — adoption happens in the *monitor* |
| [sprint-179](sprint-179-a-fault-must-be-able-to-stop-being-an-incident.md) | 🚨 **S178 opened one of `healthy`'s two locks; the other cannot open at all.** Measured 2026-08-18: **6119** `Fault` nodes, **every one** `status=pending`, **0** `FaultResolution`, **0** `FaultSuppression`. `health.py:32` counts faults whose status `!= "resolved"` and **nothing ever writes that value** — so `open_incidents` only grows and `healthy` can never return to true. 🪤 **94 % (5762) is one closed July incident**, not a live fire; the real rate is ~12–14/run. 🪤 Recurrence is **deliberate** (`fault_node_key` is keyed by `occurred_at` on purpose) — do not "fix" it. 🚨 **Two independent copies of the predicate** must move together. 🟢 Reporting-only: acceptance reads neither field | **SHIPPED** `0.90.14`, 2026-08-18 — `open_incidents` is a live incident count, not an all-time tally; `healthy` reached `true` on the spine |
| [sprint-180](sprint-180-a-deploy-record-must-name-the-commit-that-was-built.md) | **A `DeployRecord` can name a commit that was never built.** `record_deploy` validates only that tag/sha/actor are non-empty — no shape check, no cross-check. Measured 2026-08-18: the `s179` record captured `git rev-parse HEAD`, one **docs-only** commit past `4c8eeb0`, the commit the images came from. `projections_currency.py:61` compares that field to the newest main image build, so the dashboard would read the fleet **"behind" while current** — the DL-46 error the record exists to prevent. Caught only by reading the printed SHA. 🪤 40-hex validation would **not** have caught it. 🟢 Nothing in the trading path reads `DeployRecord` | **SPEC** — packaged 2026-08-18. Target next available PATCH at merge, deliberately unpinned |
| [sprint-181](sprint-181-an-untracked-order-is-reported-once.md) | 🚨 **`healthy` went green on 2026-08-18 and was red again at the next run, over one canceled test order from 2026-08-07.** The head-of-run drop sweep re-raises an identical `error` Fault every run for `stop:probe-s164:T#1` — a 1-share S164 probe with no `Fill` node. `_already_dropped` reads `fill.props["drop_reason"]`, so **the only memory the sweep has lives on a node that does not exist**; nothing on the path writes anything, and the next run repeats it exactly. 12 identical Faults since 2026-08-08, one per run. Measured: of 197 broker orders, 103 are terminal and pipeline-owned and exactly **1** has no Fill chain. 🪤 **The artifact cannot be torn down** — the order is already canceled, terminal and immutable at Alpaca; and 🪤 it self-heals once 500 newer orders push it out of the query window, which would flip health green on **order volume**. 🟢 Reporting-only; acceptance reads neither field | **SPEC** — packaged 2026-08-19 from the `sched-2026-08-18` diagnosis. Target next available PATCH at merge, deliberately unpinned |
| [sprint-182](sprint-182-a-new-position-is-protected-on-the-run-that-opens-it.md) | 🚨 **A position that fills between two runs is unprotected until the run *after* the one that notices it.** Measured 2026-08-19: MO, CSCO and NFLX filled at the open, the 22:30 run raised three `no active graph position` errors from `place_broker_stops`, and the stops landed only on the next run — **15½ hours and $2,147.76 exposed**, 22 positions against 19 stops. **Structural:** `Position` is created by the **monitor at stage 7**; stops are placed by **execution at stage 6**, so the producer runs after the consumer. 🪤 `reconcile_run_start` does **not** close the gap — it never creates a `Position`. 🪤 A second blocker: Alpaca returns `403 potential wash trade` for a sell-stop while a buy rests on the same symbol. 🚨 Ownership is a **law question** — the monitor's `laws.md` declares `Position` in `labels_owned`. Same class as S146 | **SHIPPED** `0.90.16`, 2026-08-20 — merged `2fc0672`, gate proven, **deployed `s182`**. Stops are derived from `Fill` + `OrderIntent` lineage with monitor keeping `Position` ownership; the shared `contracts/position_refs.py` makes the no-double-place guarantee structural. `poll.py` 197 → 136. 🪤 Live proof owed — needs a between-runs fill |
| [sprint-183](sprint-183-a-gate-that-did-not-run-says-so.md) | 🚨 **A filter that never ran looks exactly like one that passed.** `if "days_to_earnings" in features:` skips the earnings gate in silence, and `_days_to_earnings` returns `None` for *unknown* and *already past* alike; `max_beta` is the same. **Measured: 10 of 98 tickers had an earnings date**, so ~88 per run pass unevaluated — and `quality.notes` was empty, so it reads as healthy. Second complaint carried: the **debate packet cannot see which mode produced the stop**, hiding S150's fully-built volatility-scaled stop. 🪤 **Corrected mid-build** (`d859746`) — an earlier version told Codex to register `stop_target_mode` as a `tunable()`; the locked analyst law declares it `NO (mode selector)` on purpose ([DL-120](../design-log.md)). 🪤 **Promotion of scaled stops is deliberately out of scope** — that is an experiment, not a fix. 🟢 Attestation is additive: the same tickers should survive | **SHIPPED** `0.91.02`, 2026-08-22 — merged `4187770`, `GATE PROVEN` for `948f1bd`. `Candidate`/`FilterVerdict` carry `skipped_filters`; no-earnings-date is attested, a **known past** date is an evaluated pass, thin beta history is attested, and the packet renders the stop's basis. 🪤 The mid-build correction held — `stop_target_mode` is still a bare default. 🚨 **The spec omitted the law cycle**; closed at merge — `SCAN-OUT-06`/`SCAN-OUT-07`, scanner laws **v1.1**, rollup **18 / 41**, `DRIFT-047` filed |
| [sprint-184](sprint-184-one-issuer-is-one-bet.md) | 🚨 **The PM's risk gates cannot see the whole book** — they count vendor industry labels, so GOOG passes while GOOGL is held and AMZN passes at 1 name in `Retail` beside a book of correlated mega-cap tech. **The deliberator rejected 19 of 26 PM-approved orders (73 %) on four consecutive nights** and `verify-2026-08-20-s182-a` approved four and traded **none**. Implements PM `laws.md` **v1.3**: `PM-NEV-07` issuer aggregation, `PM-NEV-08` a measured correlated-cluster cap, `PM-NEV-09` no silent pass. 🟢 **Measured: the input is free** — exactly 202 bars × 98 tickers sit in the run's `MarketData.snapshot`, and all 22 held names are in it. 🪤 `Bar` has **zero** nodes. 🚨 DRIFT-045 must land with it — fixing the name count un-masks a dollar cap that never sees held positions. Unlike S183 this **will** change which orders are approved | **SHIPPED** `0.91.00`, 2026-08-20 — merged `18c41b1`, `GATE PROVEN` for `8613d72`, deployed `s184` 16/16. 🟩 **PM half proven live unattended on `sched-2026-08-21`**: GOOG's approved $1025.19 counted against GOOGL as `issuer=alphabet`, GOOGL's sizing gate read **1.67 % > 1 %, `outcome=failed`** — pre-S184 both passed and opened two positions in one company. 🚨 **ADR-0023's veto-rate prediction is NOT proven** and cannot be measured until 2026-08-30 ([DL-125](../design-log.md)) |
| [sprint-185](sprint-185-the-veto-posture-is-declared-not-arithmetic.md) | 🚨 **Whether the LLM veto is advisory or binding is not declared anywhere — it is an accident of two numbers.** [DL-104](../design-log.md) set `deliberation_grace_seconds` to **900** *deliberately*, as the only no-code way to keep the veto advisory; [DL-116](../design-log.md) raised it to **1800** so debates could finish, and the veto became **binding by arithmetic**, with no declared mode anywhere. 🚨 **The outage made it top-ranked:** with no API credit until **2026-08-30** ([DL-125](../design-log.md)), every nightly run fails open, submits **unvetoed**, raises an `error` fault and turns acceptance **red for a non-defect** — ~6 sessions of exactly the *"trains the operator to read a real fault as noise"* failure DL-104 (d) named. **Measured: execution's `laws.md` contains ZERO clauses about deliberation**, and `deliberation_grace_seconds` has **no PARAM row** — both filed as drift, both are why the posture could be set by arithmetic. 🪤 **Not a softening of the veto** — DL-119 rejected that; declaring the posture is the opposite move. 🪤 **Exits never wait in either posture** (S147 / ADR-0017). 🪤 Every proof must be a fixture — no real debate is obtainable before 2026-08-30 | 🟢 **BUILT locally** `0.92.00`, 2026-08-22 — `deliberation_posture` is a declared `advisory | binding` mode selector recorded on `ExecutionRun`; advisory fail-open evidence is attributed warning/green, explicit binding blocks unreviewed buys, and exits never wait. Local `make ci` passed: **2378 passed / 4 skipped / 100.00 %**, pip-audit and detect-secrets clean. 🟠 Branch gate, merge, full `up`, and live scheduled-run proof remain pending |
| [sprint-186](sprint-186-a-headline-about-twenty-companies-is-not-news-about-one.md) | 🚨 **A headline the vendor filed under twenty tickers counts once, in full, for each of them.** Finnhub `/company-news?symbol=X` returns market-wide content, `_parse_news` applies no relevance test, and `score_sentiment` is an **unweighted mean** — so contamination moves the number the scanner ranks on. Measured on `sched-2026-08-21`: **50.9 %** of headline slots are filed under ≥2 tickers, **23.4 %** under ≥5, **14.8 %** under ≥10. ✅ **Shape already decided — [DL-127](../design-log.md) measured drop-at-N against down-weight through the real pipeline and chose `1/n`:** it silences **0** tickers where dropping silences **4**, discards **0 %** of slots where dropping discards 23.4 %, and its worst downstream confidence shift is **0.034 vs 0.065**. 🪤 **Not a safety fix** — DL-117's *"mis-attributed news is buying stocks"* was retracted; this is bidirectional noise, so ship it for accuracy. 🪤 The count belongs to the **batch**, not the headline — it cannot live in `_parse_news`. 🪤 DL-112 is the precedent for the metric-naming hazard | **SPEC** — packaged 2026-08-22 from work-queue item 26. Target next available PATCH at merge, deliberately unpinned |
| [sprint-187](sprint-187-a-parameter-is-declared-once.md) | 🚨 **The PARAM tables and the code have drifted apart in BOTH directions, across three agents, with nothing checking.** Verified 2026-08-22: `scanner.benchmark_ticker` is declared `YES` in the scanner law and left a bare default in code — while the **analyst carries the identical law row and honours it**, so one law row has two agents and only one obeys; `provider.alpaca_data_feed` and `ingest_ohlcv_only` appear in **no law at all** (provider `laws.md` is LOCKED v1, so post-lock additions → a law cycle, not a code edit); and `execution.deliberation_grace_seconds` is a registered `tunable()` with **no PARAM row**. 🚨 **`execution.stage` RETRACTED — it is already declared** (`laws.md:310`, `NO (config)`). 🪤 **`stop_target_mode` / `order_price_tolerance_mode` / `curator.predictor_strategy` are `NO (mode selector)` ON PURPOSE** — DL-120's headline called them defects and was retracted; registering any is a law violation. 🎯 **The durable half is a `make ci` check** reconciling every PARAM row against its settings field both ways — this is the second audit to find this class | **BUILT locally** `0.92.02`, 2026-08-30 — branch `sprint-187-a-parameter-is-declared-once`, `make ci` exit 0 (`2390 passed, 4 skipped`, 100.00% coverage). Branch push/remote gate proof happens after final closeout edits; unmerged, not deployed |
| [sprint-188](sprint-188-a-credential-is-tested-before-it-is-handed-over.md) | 🚨 **The master hands out credentials it has never tested, so activation succeeding proves nothing.** `MasterAgent` declares `credential_tests: tuple[CredentialTest, ...] = ()` (`agents/master/agent.py:57`) and `entrypoint.py:90-96` builds it **without that argument** — no pack supplies any, so DL-36 Piece A runs **zero** tests in the deployed fleet. 🚨 **The remediation chain is unreachable for the same reason**: `handle_activation_remediation` fires only when `failures` is non-empty, and `failures` can only be non-empty if there are tests — so DL-36's whole self-healing arc is dead code downstream of one empty tuple. This is why a dead LLM key went unnoticed for **six scheduled nights** ([DL-125](../design-log.md)), and why [DL-134](../design-log.md) could find no cheap probe of the master→Key Vault path. 🪤 **The obvious fix is the rejected one** — `orchestration/master_serve.py` already accepts the tests and 8 working probes already exist in the pack, but the master image copies only `kernel/ contracts/ agents/master/` and import-linter forbids `agents → orchestration`; the image stays pack-agnostic (S86 / DL-12). Tests reach master as **data**, exactly like the grant policy and secret map already do. 🪤 **The subtle decision is transport-vs-credential**: a probe that reads a DNS blip as a bad key halts the whole fleet — worse than the outage it exists to prevent | **BUILT + BRANCH-GATE PROVEN** `0.93.00`, 2026-08-30 — credential tests load as pack data, required failures refuse activation, transport failures fault without blocking/caching, `AgentInstance` records tested credential evidence, master laws v1.2 move to **15 / 44**, local `make ci` exits 0 (`2410 passed, 4 skipped`, 100.00% coverage), and `make gate-ran` proves `33b0cd5571e5ea5a1c7b307744b4f6560e9559be` (CI + Security Findings). Merge, deploy, and live proof remain pending |
| [sprint-189](sprint-189-an-empty-answer-says-why-it-is-empty.md) | 🚨 **Neither LLM adapter inspected why the model stopped, so a truncated answer, a refusal and a genuinely empty answer were one recorded value.** `llm_openai.py` never read `finish_reason`, and `llm_anthropic.py` had the identical defect on `stop_reason`. **Measured on the live spine: 135 of 1,037 `LLMCall` rows had an empty completion** — two agreeing signals, `response_hash == sha256("")` and `tokens_out == 0`. 🚨 **Work-queue item 35's framing was wrong:** it filed this as OpenAI-fallback-only and "not currently biting", but **112 of the 135 were `claude-opus-5`, the primary**, and only 22 were `gpt-5.5`. 🪤 **98 of the 135 fell on 2026-08-08 alone** (a known outage day), so the honest steady-state figure is the **~3 %** on days with no known outage (3 of 100 across 08-17/08-18) — quoting 13 % would repeat DL-119's diluted-denominator mistake. 🟩 **S189 settled the DL-119 contamination question:** 1 of the 11 empty judge calls fell inside the four binding runs (AMZN under `verify-2026-08-19-clean`). | **BUILT locally** `0.94.00`, 2026-08-31 — adapter stop/refusal errors are named, empty debate turns cannot enter transcripts, judge `revise` reasons stop lying, `LLMCall.stop_reason` is vocabulary-backed, `max_tokens` `le=8192`, deliberator laws **v1.1** rollup **9 / 51**. First push exposed S188 CodeQL #189-#191; branch also moves master's `HttpProbeRequest` to support to break that cycle. Local `make ci`: **2420 passed, 4 skipped, 100.00 %**, pip-audit/detect-secrets clean. Branch push/gate, merge, full `up`, and live minimum-`max_tokens` proof remain pending |
| [sprint-190](sprint-190-one-liveness-question-one-answer.md) | 🚨 **Execution's graph facts each denormalise lifecycle differently, and only `Position` has a predicate that hides it** (`contracts/positions.py:126`). One shape, three defects: a `BrokerStopOrder` is active iff `cancelled_at is None`, so a stop that **fired** stays active forever ([DL-131](../design-log.md)); the stale-order sweep compares order **type** against graph **lifecycle**, so every dead stop mismatches forever ([DL-130](../design-log.md)); and `Fill.status` is an immutable submit-time fact with no predicate ([DL-129](../design-log.md)). It has already cost **two retracted work-queue claims**. 🎯 **Re-measured 2026-08-31 and the design fell out of it: the graph already holds the answer** — **all 46** `BrokerStopOrder` nodes have a `Fill` at the *identical* key, and today `stop:87403939105c0a24:PYPL` reads live while its own Fill has said `broker_status=filled` since 08-28 (`realized_pnl_cents=-9758`). So the fix is a **predicate, not a new property** — no graph write, no vocabulary change. 🪤 **7 more stops carry `cancelled_at` written 1-3 days *after* their fill was recorded**, so a stop-out is eventually logged as *cancelled by us*. 🪤 **28 of the 29 "open" Fills are resting stops** — pending by design, forever — which is why item 12 was misread twice. 🪤 **`partial` must stay non-terminal** or S176 is re-broken | **SPEC** — packaged 2026-08-31 from work-queue items 32 + 20 + 12, after re-measuring all three. [DL-139](../design-log.md) + [DRIFT-055](../laws/drift-register.md) filed with it. Target next available PATCH at merge, deliberately unpinned |
| [sprint-191](sprint-191-a-quiet-night-gets-the-same-verdict-twice.md) | 🚨 **The advisory acceptance gate returns opposite verdicts for identical inputs, and a race decides which.** Measured 2026-09-01: `sched-2026-08-31` (zero approved orders) returned `ACCEPTANCE FAIL  deliberation.advisory_attribution: missing`, while `verify-2026-09-01-s190-stops` — **same as-of, same 99 tickers, same zero orders** — returned `ACCEPTANCE PASS`. The only difference is *when* the deliberator writes its empty `DeliberationRun`: land it **before** execution polls and the status is `applied` (green); **after**, and it is `not_required` (red), because `ADVISORY_STATUSES` omits a status the execution gate legitimately declares (`orchestration/packs/trading_deliberation_view.py:20` vs `agents/execution/deliberation_gate.py:38`). 🚨 **A non-deterministic red is worse than a consistent one** — it cannot be reproduced on demand, so it trains the reader to discount the gate on exactly the quiet nights DL-125's cries-wolf shape already threatens. 🪤 **The one-line fix destroys the invariant the red was accidentally protecting:** `not_required` *also* means **sells-only** per [ADR-0022](../decisions/0022-the-veto-gates-buys-never-exits.md), so the condition is *no **buy** intent* — not `submitted == 0`, not `approved_count == 0` — and a `not_required` beside an approved **buy** must **still** breach, because that is a veto skipped on exposure-opening orders. 🟢 No `contracts/` change and no vocabulary move, so the deploy stays an **image-only retag** | **SPEC** — packaged 2026-09-01 from work-queue item 38, after the live `s190` run re-measured it as a race rather than a no-trade rule. `DL-141` + `DRIFT-056` to be filed with it. Target next available PATCH at merge, deliberately unpinned |
| [sprint-192](sprint-192-a-reply-that-arrives-late-is-still-an-answer.md) | 🚨 **S172's concurrency loses peer replies and two-thirds of its speedup.** Measured live 2026-09-01 ([DL-140](../design-log.md)) at `debate_concurrency=4` over 15 orders: **13 of 15** debated, **2 failed open** (`no deliberator peer reply received`), **6 peer replies dead-lettered**, and concurrency came out **~1.5x, not 4x**. 🟩 The run was clean — 69 `LLMCall` rows all `claude-opus-5`, all `stop_reason=end_turn`, mean latency **16.4 s** against a 120 s timeout — so no call was slow and the fault is in reply routing or peer capacity. 🚨 **The sprint's job is diagnosis, not a fix:** three explanations are live and need different remedies — late replies dead-lettered after `expecting()` drops the id on timeout; peer KEDA lag queueing the first burst; or manager-side serialisation on a shared receiver pulling one message per slice. 🪤 **Do not start by rewriting the inbox** — its sibling-stashing test passes. 🪤 **A quiet run is not a fixed run:** assert `real_debate_count == 15`, never the absence of warnings, or `DLIB-NEV-06` is violated by the fix itself | **SPEC** — packaged 2026-09-01 straight off the measurement, branched from `sprint-172-k4-on-s190` rather than `main`. `DL-142` to be filed with it. Target next available PATCH at merge, deliberately unpinned |
| [sprint-193](sprint-193-a-shim-that-never-runs-is-not-a-shim.md) | 🚨 **S184's back-compat shim has never once executed in production.** Found while proving S191 over the whole back catalogue ([DL-143](../design-log.md), work-queue item 40): the acceptance gate could read only **17 of 55** runs on the spine — **38 `ERROR`, 16 `FAIL`, 1 `PASS`**, every error the same `rejected.N.gate_report.M.outcome  Field required ... input_type=mappingproxy`. `_accept_historical_passed` (`contracts/portfolio_manager.py:38`) opens with `isinstance(data, dict)`, but `kernel/graph_support.py:55` wraps every nested Mapping **recursively** in `MappingProxyType` — and `isinstance(mappingproxy, dict)` is `False`, so the shim takes its early return on every payload it exists to convert. 🪤 **It passed its own suite throughout**, because its tests hand it plain dict literals — which is why the deliverable is not the one-word `dict` → `Mapping` fix but a test that round-trips through a real `GraphStore` and fails on `main` today. 🟩 Not a live trading defect (fresh runs write `outcome`); it breaks **anything that re-reads history** — the dashboard raises rather than renders on 69 % of runs. Closes on the sweep returning zero `ERROR`s | SPEC |
| [sprint-194](sprint-194-a-number-nobody-records-is-a-number-nobody-has.md) | 🎯 **S172's acceptance criterion cannot currently be falsified, so this ships the instrument before anyone attempts the fix.** `orphaned_reply_count` is computed every run and thrown away ([DL-145](../design-log.md)): it lives only in `ServiceBusPeerClient` memory (`agents/deliberator/servicebus_peer_client.py:60-66`), is emitted **only as a log warning** (`:170`), is never written to the graph, and `deliberator-manager` contributes **zero** rows to Log Analytics while every other agent logs. DL-140's “6 orphaned replies” can never be re-derived and the 2026-09-01 re-run's count is **UNKNOWN, not zero** — two expensive live runs have failed to settle what one integer would settle. 🟩 **Buildable on `main` today** (only S172's reply *inbox* is branch-only), so it starts recording on **every nightly run**, serial path included. 🪤 **Two traps decide whether it works:** the counter is **cumulative per client instance** and the poll loop serves several runs from one client, so the recorded value must be a **per-run delta** proven by a two-runs-one-client test; and `DeliberationRun` is **property-enforced**, so the pack hash moves and the deploy is a **full `up`, never a retag** | **SHIPPED** — `0.94.05`, merged `e0a144f`, 2026-09-02. Per-run `orphaned_reply_count` is now persisted on `DeliberationRun`, declared in the property-enforced vocabulary, rendered as `n/a` for old rows, and proven against the cumulative-counter trap by a two-runs-one-client test. `make ci` passed (`2452 passed, 4 skipped`, 100.00 %); branch and main `GATE PROVEN`; full `up` required and main workflows completed. |

---

## Adding a sprint

1. **Start from [`_TEMPLATE.md`](_TEMPLATE.md)** — copy it to `sprint-NN-<slug>.md` and fill it
   top-down. It merges S164's law rigour (the 🔴 MUST RULE, the element→law map, the law reading
   record, the handback contract) with the S177–S184 additions (measured-vs-assumed evidence,
   design decisions recorded before code, blast radius, traps, a paste-to-Codex handover block).
   🚨 **It also carries the law-cycle question**, which exists because S183's spec omitted it:
   a sprint that changes `contracts/` or adds a new guarantee owes a clause in the same unit of work.
   🪤 **Do not pin a version number** — name the *kind* (`fix` → PATCH, `feat` → MINOR) and let the
   merge pick the digits.

   *Next number:* **S189**. 🪤 The rest of this item is a **history note frozen at 2026-07-29** and
   its "next number" was stale by 24 sprints — read it for what shipped, never for what is next.

1. ~~Next number: **S160**~~. **S157 is reserved** for the law-clause row backfill (the 101 clauses with
   no test-plan row, hardening row O) and is not yet written; S158 SHIPPED and S159 is packaged.
   The rest of this item is a **history note frozen at 2026-07-29** — read it for what shipped, not
   for what is next; live direction is [../STATE.md](../STATE.md).
   (S143 SHIPPED `798bfd4`/0.79.00 — graph vocabulary guard, DL-66;
   S144 / 0.80.00 — the guard made deployable and its pack proven complete, DL-68; S145 SHIPPED
   `2c49f88`/0.80.02 — exit replay append-safe, DL-71; **S146 SHIPPED** `7b06662`/0.80.03 — the
   unprotected ABT position and the DL-73 retraction; **S147 SHIPPED** `2989acb`/0.81.00 —
   head-of-run position sync, DRIFT-025; **S148 SHIPPED** `314fb8b`/0.82.00 — ADR-0018 fill-it-or-
   drop-it, DRIFT-026, **deployed `:s148` 2026-07-30 and its drop sweep stalled the next run**;
   **S149 SHIPPED** `eee68d1`/0.83.00 — volatility-scaled tolerance challenger, off by default,
   DRIFT-027; **S150 SHIPPED** `ca97797`/0.84.00 — volatility-scaled stop challenger, off by
   default, DRIFT-028, DL-78; **S151 SHIPPED** `8f57f5f`/0.84.01 — append-safe drop evidence,
   **deployed `:s151` 2026-07-31, stalled run resumed 2/8 -> 8/8 `ACCEPTANCE PASS`**;
   **`chore-vocabulary-property-completeness` SHIPPED** `7f05829`/0.84.03 — the vocabulary
   guard's property dimension proven a superset for the 2 enforced labels, an S144
   prerequisite). P12 scorecard-run stays queued until ~2 weeks of clean-news nights
   accumulate. **Next named work: the one law-amendment cycle**
   ([sprint-152](sprint-152-law-amendment-cycle.md)) covering DRIFT-024/025/026/027/028 rather
   than a sixth register row, and **[S153](sprint-153-deliberator-agent.md) — the deliberator
   agent**, because the LLM veto has never run in production ([DL-80](../design-log.md))
   and every order so far reached the broker unvetoed. **S144 is CLOSED** — the write guard
   was enabled on the fleet 2026-07-31 (`:s152`, 0.84.04) after three undeclared edge
   signatures were found and fixed first. Still open: whether to declare properties for the
   other 51 labels, the 46 `add_edge` sites the static scan cannot resolve, and reconciling
   the 8 `labels_owned` declarations to enable `owners` enforcement.
   **[S154](sprint-154-fill-refresh-terminal.md) is queued ahead of both** (fix outranks
   capability): `refresh_pending_fills` selects on the write-once `Fill.status`, so 39
   broker-settled fills are re-refreshed every run — 2,742 `BrokerOrderStatus` nodes across 59
   fills, up to 73 for one — and one unresolvable ABT sell fill re-emits the same fault nightly.
   Found by tracing the clean `sched-2026-07-31` run; invisible to the acceptance gate.
2. Create `sprint-NN-<slug>.md` from [`_TEMPLATE.md`](_TEMPLATE.md) (not by copying another sprint —
   the recent ones dropped the law sections).
3. Add a row to the `README.md` index table immediately.
4. Update the phase map above when the sprint belongs to a defined phase.
5. Set STATUS in [../../docs/STATE.md](../STATE.md) to the active sprint.
