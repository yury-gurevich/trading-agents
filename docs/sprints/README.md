# Sprints — handover to the coding agent

This folder holds **self-contained sprint plans**. Each is a handover a cold-start
coding agent can execute end to end without further context.

## Roles

- **Planning agent (owner of this folder, the PRD, the build plan, and STATE).**
  Decides *what* to build next and *why*, writes the sprint handover, reviews the
  result, and updates `docs/STATE.md` + `docs/build-plan.md` progress. Does not
  write production code.
- **Coding agent.** Executes exactly one active sprint plan: writes the code and
  tests, keeps the quality gate green, and hands back a short report. Stays within
  the sprint's scope; anything out of scope goes back to the planning agent.

## How a sprint runs

1. Planning agent writes `sprint-NN-<slug>.md` here and marks it active in STATE.
2. Coding agent branches `sprint-NN-<slug>`, implements the steps, gets the gate
   green, pushes, and reports back (files changed, coverage %, decisions/notes).
3. Planning agent reviews, merges to `main`, and updates STATE + build-plan status.

## Non-negotiable guardrails (every sprint)

These hold regardless of the sprint's specifics:

- **The one rule.** No agent imports another agent. The kernel imports nothing from
  `contracts`/`agents`. Agents talk only via typed messages. `import-linter` enforces.
- **Small files.** Every module < 200 lines (warn at 150). Split, don't grow.
- **Coding-agent header.** Every module's docstring declares `Agent:` and `Role:`
  (and `External I/O:` where relevant). Enforced by `scripts/check_module_header.py`.
- **No magic numbers.** Any value influencing processing or a forecast is declared
  with `kernel.tunable(..., why="...")` — justified and bounded — never a bare literal.
- **Faults, not silent failure.** Wrap fallible work in `kernel.fault_boundary`;
  errors are redirected with provenance, never swallowed.
- **Green before handback.** `make ci` must pass (ruff, format, mypy, import-linter,
  size + header guards, pytest at/above the coverage floor). Never lower the floor;
  raise it if measured coverage climbs.
- **Stay in scope.** Build only what the active sprint plan lists. Flag anything else.

## Validation (the gate)

```bash
make ci          # full local gate, mirrors GitHub CI
# or individually:
uv run ruff check . && uv run ruff format --check .
uv run mypy kernel contracts agents orchestration surfaces
uv run lint-imports
uv run python scripts/check_module_size.py kernel contracts agents orchestration surfaces tests
uv run python scripts/check_module_header.py kernel contracts agents scripts
uv run pytest
```

## Index

| Sprint | Goal | Status |
| --- | --- | --- |
| [sprint-01](sprint-01-kernel-runtime.md) | Kernel runtime: in-process bus + AgentBase | **shipped** |
| [sprint-02](sprint-02-persistence.md) | Relational persistence adapter + migrations | **shipped** (superseded by ADR-0001) |
| [sprint-03](sprint-03-neo4j-store.md) | Neo4j GraphStore: retire relational adapter, graph spine | **shipped** |
| [sprint-04](sprint-04-provider-agent.md) | Provider agent: first real agent (data boundary + provenance) | **shipped** |
| [sprint-05](sprint-05-scanner-agent.md) | Scanner agent: first agent-to-agent call (universe → ranked candidates) | **shipped** |
| [sprint-06](sprint-06-analyst-agent.md) | Analyst agent: scored recommendations (closes the P2 slice) | **shipped** |
| [sprint-07](sprint-07-distributed-bus.md) | Distributed (Celery) bus: second MessageBus backend (P1 exit) | **shipped** |
| [sprint-08](sprint-08-observability-adapter.md) | Observability: kernel metrics adapter (throughput/latency/fault-rate) | **shipped** |
| [sprint-09](sprint-09-portfolio-manager.md) | Portfolio manager: sized, risk-checked order intents (starts P3) | **shipped** |
| [sprint-10](sprint-10-hardening.md) | Audit-truth & rigor hardening (rejection evidence, contract validators, graph/Neo4j rigor) | **shipped** |
| [sprint-11](sprint-11-execution.md) | Execution agent: idempotent broker boundary (paper stage, fills) | **shipped** |
| [sprint-12](sprint-12-monitor.md) | Monitor agent: open positions from fills, stop/target/time exits | **shipped** |
| [sprint-13](sprint-13-reporter.md) | Reporter agent: run snapshot + per-trade narrative (P3 exit) | **shipped** |
| [sprint-14](sprint-14-dispatcher.md) | Dispatcher: event-driven daily loop on both bus backends (P4 begins) | **shipped** |
| [sprint-15](sprint-15-supervisor.md) | Scheduler + supervisor message lineage (P4 exit) | **shipped** |
| [sprint-16](sprint-16-operator.md) | Operator agent: intent parsing + model-call ledger (P5 begins) | **shipped** |
| [sprint-17](sprint-17-supervisor-gate.md) | Supervisor capability gate + hard-NO surface (P5 exit) | **shipped** |
| [sprint-18](sprint-18-surfaces-cli.md) | Surfaces foundation + CLI: resolve_flag fix, query projections, terminal interface (P6 begins) | **shipped** |
| [sprint-19](sprint-19-list-nodes-lifecycle.md) | GraphStore list_nodes (Neo4j gap fix) + position lifecycle + pending-flags view (P6 continues) | **shipped** |
| [sprint-20](sprint-20-narrative-approve.md) | Trade narrative display + approve command (cli narrative + cli approve, P6 continues) | **shipped** |
| [sprint-21](sprint-21-incidents-explain-p6-exit.md) | Incident view + explain on demand + P6 exit test (P6 closes) | **shipped** |
| [sprint-22](sprint-22-mcp-tool-binding.md) | MCP tool-binding: operator + supervisor tools over Model Control Protocol (P1 closes) | **shipped** |
| [sprint-23](sprint-23-p7-researcher.md) | Researcher agent: propose bounded parameter changes + proposals surface + P7 exit test (P7 begins) | **shipped** |
| [sprint-24](sprint-24-p8-stage-gate.md) | Stage gate machinery: evidence-based promotion, StageTransition nodes, read-only cli stage (P8 begins) | **shipped** |
| [sprint-25](sprint-25-p8-market-pack.md) | Stage command wiring (cli stage promote → supervisor gate) + MarketPack abstraction + P8 exit test (P8 closes) | **shipped** |
| [sprint-26](sprint-26-p9-observability.md) | MeteredFaultSink wiring + P9 exit test + observability.md docs (P9 closes) | **shipped** |
| [sprint-27](sprint-27-p10-curator-datasets.md) | Curator agent: dataset assembly by provenance traversal, versioned splits, cli datasets (P10 begins) | **shipped** |
| [sprint-28](sprint-28-p10-training-trigger.md) | Curator training trigger: deterministic baseline predictor + frozen evidence, advisory-only, cli predictors (P10 training half) | **shipped** |
| [sprint-29](sprint-29-p10-predictor-registry.md) | Curator predictor registry: promote_predictor evidence gate + operator approval + PredictorPromotion audit (P10 exit) | **shipped** |
| [sprint-30](sprint-30-p11-analyst-technical-core.md) | Analyst technical scoring core: pure-Python RSI/MACD/Bollinger/SMA/EMA + band rules + composite (P11 begins) | **shipped** |
| [sprint-31](sprint-31-p11-analyst-oscillators.md) | Analyst oscillators + volatility: ATR/Stochastic/Williams %R/Choppiness folded into the composite (P11 cont.) | **shipped** |
| [sprint-32](sprint-32-p11-analyst-volume-event.md) | Analyst volume/event: OBV + golden cross + RSI-2 folded into the composite (P11 cont.) | **shipped** |
| [sprint-33](sprint-33-p11-analyst-patterns.md) | Analyst patterns/smoothing/calendar: Nadaraya-Watson kernel + geometric patterns + turnaround → composite up to 15 indicators (P11 cont.) | **shipped** |
| [sprint-34](sprint-34-provider-fundamentals.md) | Provider fundamentals feed: Finnhub /stock/metric → MarketData.fundamentals (unblocks analyst fundamental scoring) (P11 cont.) | **shipped** |
| [sprint-35](sprint-35-analyst-fundamental-scoring.md) | Analyst fundamental scoring: 8-metric pillar blended with technical into the confidence gate (P11 cont.) | **shipped** |
| [sprint-36](sprint-36-provider-news-feed.md) | Provider news feed: Finnhub /company-news → MarketData.news per-ticker headlines (feeds the analyst sentiment pillar) (P12) | **shipped** |
| [sprint-37](sprint-37-analyst-sentiment-pillar.md) | Analyst sentiment pillar: deterministic Loughran–McDonald lexicon as the binding third pillar in the renormalised blend (P12 champion) | **shipped** |
| [sprint-38](sprint-38-analyst-relative-strength.md) | Analyst relative strength: benchmark-relative momentum blended into the technical pillar (0.8/0.2); separate fault-tolerant benchmark fetch (P11) | **shipped** |
| [sprint-39](sprint-39-analyst-signal-diversity.md) | Analyst signal-diversity selection: surface the top pillar-diverse signals in the recommendation rationale (explanatory, no score change) (P11) | **shipped** |
| [sprint-40](sprint-40-pm-reward-risk.md) | Portfolio manager reward/risk gate: reject orders whose target_pct/stop_pct is below min_reward_risk_ratio (P11) | **shipped** |
| [sprint-41](sprint-41-reporter-trade-outcomes.md) | Reporter: profit-factor and expectancy over stop/target-closed positions; new `domain/trade_outcomes.py` (P11) | **shipped** |
| [sprint-43](sprint-43-monitor-realized-pnl.md) | Monitor realized PnL on close (`pnl_cents` on CloseDecision) — accuracy upgrade so the reporter can use real $ PnL across all triggers (P11) | **queued** (after S41) |
| [sprint-44](sprint-44-provider-tiingo-feed.md) | Provider Tiingo OHLCV feed: `TiingoDataSource` (full-S&P-500 live, ADR-0006) → re-point `market_source_from_settings` + `bindings.py` default off broken Stooq (closes DRIFT-009) | **shipped** |
| [sprint-45](sprint-45-execution-alpaca-broker.md) | Execution Alpaca paper broker: `AlpacaBroker` behind the `Broker` port (real fills, client_order_id idempotency) + `broker_from_settings` default swap (ADR-0006, DEP-BROKER) | **shipped** |
| [sprint-46](sprint-46-analyst-sentiment-reading.md) | Persisted `SentimentReading` node: champion lexicon reading per scored ticker (incl. rejected) for scorecard alignment; analyst `owns_graph` += SentimentReading (P12 item 2) | **shipped** |
| [sprint-47](sprint-47-provider-sentiment-feed.md) | Provider serves Alpha Vantage vendor sentiment into `MarketData.sentiment` via `DataSource.fetch_sentiment` (provider CONTRACT 0.2.0; the analyst shadow-reading is next) (P12) | **shipped** |
| [sprint-48](sprint-48-analyst-provider-sentiment-reading.md) | Analyst persists the provider-sentiment shadow reading (`scorer="provider"` `SentimentReading`, aligned to lexicon, never gates) — completes the provider-sentiment challenger (P12) | **shipped** |
| [sprint-49](sprint-49-forecaster-finbert-runtime.md) | Forecaster agent's first runtime: FinBERT sentiment shadow scorer behind a model Protocol (torch optional + lazily imported); persists `ShadowPrediction` (shadow, 0-1 aligned) + `Model`; `scorecard` never promotion-eligible — the trinity's 3rd leg (P12) | **shipped** |
| [sprint-50](sprint-50-scanner-beta-cap.md) | Scanner beta computation + beta-cap filter: fault-tolerant benchmark fetch, pure `compute_beta` (cov/var of aligned returns), drop candidates with `beta > max_beta`; additive + dormant on thin history (P11) | **shipped** |
| [sprint-51](sprint-51-provider-sector-feed.md) | Provider sector feed: `DataSource.fetch_sectors` (Finnhub `/stock/profile2` → `finnhubIndustry`), field-gated into `MarketData.sectors` (CONTRACT 0.3.0); the substrate the PM sector cap consumes next (P11) | **shipped** |
| [sprint-52](sprint-52-pm-sector-cap.md) | PM sector-concentration cap: reject orders that would push their sector over `max_sector_pct` of portfolio value (reason `sector_concentration`); consumes `MarketData.sectors`, additive + dormant on unknown sectors — the PM risk-gate pair (twin of S40) (P11) | **shipped** |
| [sprint-42](sprint-42-provider-earnings-feed.md) | Provider earnings-calendar feed: `DataSource.fetch_earnings` (Finnhub `/calendar/earnings` → next upcoming date), field-gated into `MarketData.earnings` (CONTRACT 0.4.0); field-gates refactored into `market_fields.py`; the substrate the scanner earnings-window exclusion consumes next (P11) | **shipped** |
| sprint-53 (no doc; see [ADR-0007](../decisions/0007-container-per-agent-master-bootstrap.md)) | Provider laws: `CAP` + `PARAM` sections (ADR-0007 backfill) added to `agents/provider/laws/laws.md` — runtime capability declaration + 20-entry parameter table; the pattern for the 11 remaining agent law backfills | **shipped** |
| [sprint-54](sprint-54-scanner-earnings-exclusion.md) | Scanner earnings-window exclusion: requests `"earnings_calendar"`, drops candidates whose next earnings is within `earnings_exclusion_days` of the scan as-of (`earnings_window`); consumes `MarketData.earnings`, additive + dormant — completes the scanner deterministic port; version 0.1.0→0.2.0 (P11) | **shipped** |
| [sprint-43](sprint-43-monitor-realized-pnl.md) | Monitor realized PnL on close: pure `realized_pnl_cents=(exit−entry)×qty`; `CloseDecision.pnl_cents` (contract 0.2.0 + node prop), holds `None`; decision logic extracted to `decide.py`; version 0.2.0→0.3.0 — the realized-outcome substrate for the reporter $-PnL re-point (P11) | **shipped** |
| [sprint-55](sprint-55-reporter-realized-pnl-repoint.md) | Reporter re-point to real $ PnL: `collect_trade_outcomes` reads `pnl_cents` off close decisions → dollar `profit_factor`/`expectancy_cents` across all triggers incl. time (`expectancy_pct→expectancy_cents`, no contract change); version 0.3.0→0.4.0 — **closes P11** | **shipped** |
| [sprint-56](sprint-56-analyst-lm-master-dictionary.md) | Analyst champion upgrade: load the full Loughran–McDonald master dictionary (Positive 354, Negative 2355; vendored under `agents/analyst/domain/data/`) unioned with the curated headline terms LM omits (beat/surge/plunge/...); polarity-disjoint, `score_sentiment` unchanged; version 0.4.0→0.5.0 (P12) | **shipped** |
| [sprint-57](sprint-57-forecaster-sentiment-scorecard.md) | Forecaster sentiment scorecard harness: `sentiment_scorecard` capability comparing lexicon/provider/FinBERT readings vs injected forward returns (Pearson + 2-regressor OLS, per-scorer IC + FinBERT incremental IC); reads readings from the graph, returns injected; advisory (never promotion-eligible); forecaster CONTRACT 0.2.0; version 0.5.0→0.6.0 (P12) | **shipped** |
| [sprint-58](sprint-58-forecaster-lightgbm-shadow.md) | Forecaster LightGBM price/return shadow signal (qlib Phase Q1): `ReturnModel` port + lazy `lightgbm` adapter + pure feature builder + provider OHLCV request → `ShadowPrediction` (shadow, never gates); `lightgbm`-direct (pyqlib is 3.13-incompatible on Python 3.13); CONTRACT 0.2.0→0.3.0; version 0.6.0→0.7.0 (Q1) | **shipped** |
| [sprint-59](sprint-59-forecaster-lgbm-training-scorecard.md) | Forecaster LightGBM training pipeline + return IC scorecard (qlib Phase Q1 follow-on): `build_label_rows` + walk-forward `split_rows` + `train_and_save` offline script; `return_scorecard` capability (IC + hit_rate + directional breakdown vs injected forward returns); CONTRACT 0.3.0→0.4.0; version 0.7.0→0.8.0 | **shipped** |
| [sprint-60](sprint-60-p14-kernel-pubsub.md) | P14 step 1 — Kernel pub/sub: `MessageBus.publish/subscribe` + `InProcessBus` fan-out; `ReadyEvent` model; both-mode bus green | **shipped** |
| [sprint-61](sprint-61-p14-claim-check.md) | P14 step 2 — Kernel claim-check primitive: `claim_check_write/read` over the graph; `ReadyEvent` published after each write | **shipped** |
| [sprint-62](sprint-62-p14-provider-pubsub.md) | P14 step 3 — Provider pub/sub dual-mode: `bind()` subscribes to `run.trigger`, publishes `MarketData` via claim-check | **shipped** |
| [sprint-63](sprint-63-p14-scanner-analyst-pubsub.md) | P14 step 4 — Scanner + analyst pub/sub dual-mode: `scan.candidates.ready` → `analysis.recommendations.ready` chain | **shipped** |
| [sprint-64](sprint-64-p14-pm-execution-pubsub.md) | P14 step 5 — PM + execution pub/sub dual-mode: `analysis.recommendations.ready` → `portfolio.orders.ready` → `execution.fills.ready`; `pm_run_id` threading begins | **shipped** |
| [sprint-65](sprint-65-p14-monitor-reporter-pubsub.md) | P14 step 6 — Monitor + reporter pub/sub dual-mode: `execution.fills.ready` → `monitor.decisions.ready` → `report.snapshot.ready`; whole pipeline event-driven in-process | **shipped** |
| [sprint-66](sprint-66-p14-dispatcher-trigger-emitter.md) | P14 step 7 — Dispatcher → trigger-emitter: `execute_run` publishes `run.trigger`, subscribes `report.snapshot.ready`; step sequencing removed; dispatcher unit + daily-loop tests rewritten | **shipped** |
| [sprint-67](sprint-67-p14-azure-servicebus-backend.md) | P14 step 8 — Azure Service Bus backend: `AzureServiceBusBus` + `AzureServiceBusSettings`; in-process shim for RPC; `# pragma: no cover` on I/O path; parity test skips without creds; version 0.8.0→0.9.0; P14 complete | **shipped** |
| [sprint-68](sprint-68-analyst-alpha158-pillar.md) | Analyst Alpha158 pillar (qlib Phase Q2): 22-field time-series subset (ROC/STD/MAX/MIN/IMAX/IMIN) → cross-sectional z-score → logistic 0-100 fifth pillar; `alpha158_pillar_weight=0.00` (off by default); pyqlib-free (3.13 constraint); version 0.9.0→0.10.0 | **shipped** |
| [sprint-69](sprint-69-provider-law-cycle.md) | Provider law cycle — lock the template: DRIFT-006 corrected (benchmark as first-class `DataRequest` field + `taint=False` isolation); DRIFT-007 corrected (caller-authz gate in all 3 buses + provider capability matrix); law-ID citation pass (7 test files); test-plan 23/43 green; provider laws LOCKED v1; template locked for copying to 11 agents; version 0.10.0→0.11.0 | **shipped** |
| [sprint-70](sprint-70-per-agent-law-backfill.md) | Per-agent law backfill (4 of 11): scanner/analyst/PM/execution laws authored from first principles → citation pass (12 test files) → laws LOCKED v1; `test_scanner_explain.py` split; 95 green clauses added | **shipped** |
| [sprint-71](sprint-71-per-agent-law-backfill.md) | Per-agent law backfill (remaining 7 of 11): monitor/reporter/forecaster/operator/supervisor/curator/researcher laws authored from first principles → citation pass → 7 test-plan.md files → all 7 laws LOCKED v1; 124 green clauses added | **shipped** |
| [sprint-72](sprint-72-system-prompt-tunable.md) | ADR-0010 immediate close: `system_prompt` tunable wired into `OperatorSettings` + `_interpret_command`; pre-declared on `ForecasterSettings`; law PARAM sections updated | **shipped** |
| [sprint-73](sprint-73-p15-master-agent.md) | P15 foundation: `MasterAgent` (start/activate/drain) + `DEFAULT_GRANTS` privilege table + `contracts/master.py` (EHLOMessage/ACTIVATEMessage/DRAINMessage) + 11 tests (100%); per-agent Dockerfiles (13 images); multi-service docker-compose.yml; master laws LOCKED v1 (MST, 10 clauses green); RSA + Key Vault deferred to S74 | **shipped** |
| [sprint-74](sprint-74-p15-rsa-entrypoints.md) | P15 RSA signing + agent entrypoints: `kernel/crypto.py` (RSA-PSS sign/verify/generate), `kernel/bootstrap.py` (activate\_agent + injectable \_send), `agents/master/http_server.py` (handle\_health/handle\_ehlo pure), `agents/master/entrypoint.py` (build\_app testable + main), 12 trading-agent entrypoints; 951 tests 100% coverage; version 0.11.0→0.12.0; Key Vault deferred to S75 | **shipped** |
| [sprint-75](sprint-75-p15-key-vault.md) | P15 Key Vault secret distribution: `SecretStore` Protocol + `NullSecretStore` + `EnvVarSecretStore` + `AzureKeyVaultSecretStore`; `AGENT_SECRETS` entitlement table + `resolve_config`; `MasterAgent.activate()` populates `config` with per-agent minimum-privilege secrets; DRIFT-002 resolved; 971 tests 100% coverage; version 0.12.0→0.13.0 | **shipped** |
| [sprint-76](sprint-76-p15-ghcr-deploy.md) | P15 GHCR build pipeline + Container Apps deploy: GitHub Actions matrix build → GHCR images; Key Vault provisioning + secret seeding; deploy-agents workflow (master first, then parallel); `trading-agents-kv` live; scanner boots + EHLO confirmed in log stream | **shipped** |
| [sprint-77](sprint-77-credential-naming.md) | Canonical credential-naming reconciliation: align `secret_map.py` output keys to match agent settings `env_prefix` (PROVIDER\_/EXECUTION\_/OPERATOR\_ prefixes); fix `.env` FNP\_ typo; version 0.16.1 (PATCH) | **shipped** |
| [sprint-78](sprint-78-provider-ingestor.md) | Provider as standalone graph-ingestor: replaced `idle_loop()` with real ingest loop; `build_graph_from_env()` kernel helper; provider writes market data to graph; version 0.17.00 (MINOR) | **shipped** |
| [sprint-79](sprint-79-agent-work-loops.md) | Agent work loops — vertical slice (DL-08b): provider persists full `MarketData` payload; scanner reads it from the graph (`poll.py` `find_pending`+`scan_market_node`, `SCANNED_BY` edge) instead of bus RPC; `kernel/work_loop.py` reusable loop; scanner drops idle_loop. analyst→reporter deferred to S80; version 0.18.00 (MINOR) | **shipped** |
| [sprint-80](sprint-80-analyst-graph-pull.md) | Analyst graph-pull (scanner→analyst): provider also persists full `RegimeContext`; scanner persists full `CandidateSet` on `ScanRun`; analyst reads CandidateSet+MarketData (DERIVED_FROM)+RegimeContext from graph (`agents/analyst/poll.py`), scoring core extracted to `agents/analyst/run.py` shared by bus+graph paths; analyst drops idle_loop. PM→reporter deferred to S81; version 0.19.00 (MINOR) | **shipped** |
| [sprint-81](sprint-81-pm-graph-pull.md) | PM graph-pull (analyst→PM): analyst persists full `RecommendationSet` on `AnalystRun`; PM reads it + MarketData (ANALYZED_BY→ScanRun→DERIVED_FROM) + same-day RegimeContext from graph (`agents/portfolio_manager/poll.py`), sizing/risk core extracted to `agents/portfolio_manager/run.py` shared by bus+graph paths; PM drops idle_loop, `EVALUATED_BY` edge. execution/monitor/reporter deferred to S82; version 0.20.00 (MINOR) | **shipped** |
| [sprint-82](sprint-82-execution-monitor-reporter-graph-pull.md) | Execution+monitor+reporter graph-pull (closes DL-08 end-to-end): PM persists full `OrderIntentSet` on `PMRun`; execution adds an `ExecutionRun` anchor + `poll.py`/`run.py` (`PMRun`→`EXECUTED_BY`); monitor reads close prices from graph `MarketData` instead of provider bus RPC + `poll.py` (`ExecutionRun`→`MONITORED_BY`); reporter gets a `poll.py` trigger over `build_snapshot` (`MonitorRun`→`REPORTED_BY`→`Snapshot`); all three drop idle_loop. Permanent graph store deferred; version 0.21.00 (MINOR) | **shipped** |
| [sprint-83](sprint-83-graph-pull-orchestration.md) | Graph-pull orchestration trigger: dispatcher writes one `RunRequest` node (`RUN_REQUEST_LABEL`); provider becomes graph-pull on it (`agents/provider/poll.py`, `INGESTED_BY` edge, drops timer `ingest_loop`); `orchestration/start.py` (pre-flight checklist + `place_run_request`); `orchestration/local_pipeline.py` `cascade_once`; first end-to-end graph-pull test + `scripts/run_local.py` demonstrator. Dispatcher cron deferred; version 0.22.00 (MINOR) | **shipped** |
| [sprint-84](sprint-84-platform-pack-grant-policy.md) | Grant policy out of the substrate (ADR-0012): `DEFAULT_GRANTS` → `orchestration/packs/trading_grants.json`, injected by path; DL-12 leak #1 closed; version 0.22.00→0.23.01 | **shipped** |
| [sprint-85](sprint-85-platform-pack-secret-map.md) | Secret map out of the substrate (ADR-0012): `AGENT_SECRETS` → `orchestration/packs/trading_secrets.json`, injected via `secret_map_path`; DL-12 leak #2 closed; version 0.23.01→0.23.02 | **shipped** |
| [sprint-86](sprint-86-deploy-pack-policy-wiring.md) | Deploy pack policy as config: feed `trading_grants.json` + `trading_secrets.json` to the master; image stays pack-agnostic; DL-12 complete; version 0.23.02→0.23.03 | **shipped** |
| [sprint-87](sprint-87-staleness-trading-sessions.md) | Staleness gate counts trading sessions, not calendar days (DL-10 resolved); version 0.23.03→0.23.04 | **shipped** |
| [sprint-88](sprint-88-filter-verdicts-collection.md) | Scanner per-ticker `FilterVerdict` + bypass mode (DL-09 collection side); version 0.23.04→0.24.00 | **shipped** |
| [sprint-89](sprint-89-filter-quality-scorecard.md) | Filter-quality scorecard — per-filter confusion matrix over dual labels (DL-09 measurement side); version 0.24.00→0.25.00 | **shipped** |
| [sprint-90](sprint-90-ci1-parameter-catalogue.md) | P16 CI-1: parameter catalogue on the graph (ADR-0013) | **deferred** (spec; etalon-first) |
| [sprint-91](sprint-91-ci2-run-metrics.md) | P16 CI-2: `RunMetrics` recorded on the graph | **deferred** (spec; etalon-first) |
| [sprint-92](sprint-92-ci3-parameter-set.md) | P16 CI-3: `ParameterSet` (configurable-not-settable) | **deferred** (spec; etalon-first) |
| [sprint-93](sprint-93-ci4-experiment-compare.md) | P16 CI-4: experiment + champion/challenger compare | **deferred** (spec; etalon-first) |
| [sprint-94](sprint-94-ci5-gate-promote.md) | P16 CI-5: quality gate + promote (absorbs ADR-0010) | **deferred** (spec; etalon-first) |
| [sprint-95](sprint-95-ci6-optimiser.md) | P16 CI-6: optimiser (parameter sweep; ingest target first) | **deferred** (spec; etalon-first) |
| [sprint-96](sprint-96-deliberation-understanding-veto.md) | Deliberation define-then-justify + scored parameter-understanding gate (Part A), then asymmetric challenger-veto in the loop (Part B: judge may block a PM-approved trade, never originate/resize) — DL-31; confidence by measurement, not eloquence | **shipped** (Part A 0.40.00 + Part B 0.41.00 + transcript 0.42.00) |
| [sprint-97](sprint-97-serve-loop-primitive.md) | Fleet Activation (DL-30/DL-35): kernel `serve_loop` + `RequestConsumer` protocol — the missing serve/consume primitive (twin of `work_loop`); in-process, CI-provable | **shipped** (0.43.00) |
| [sprint-98](sprint-98-control-plane-serve-supervisor-operator.md) | Control-plane served in-process (1/2): supervisor + operator over `serve_loop`; `idle_loop()` retired for both | **shipped** (0.44.00) |
| [sprint-99](sprint-99-control-plane-serve-forecaster-curator-researcher.md) | Control-plane served in-process (2/2): forecaster (RPC-triggered, `FORE-TRG-02`) + curator + researcher; **zero `idle_loop()` remains** | **shipped** (0.51.00) |
| [sprint-100](sprint-100-servicebus-receiver.md) | Azure Service Bus **receiver** behind the serve protocol + claim-check read + both-backends parity test (mirrors S67) — the distributed backend | **shipped** (0.61.00) |
| [sprint-101](sprint-101-permanent-graph-store.md) | Permanent graph store — **ABSORBED by S116–S118** (ADR-0014: the permanent spine is Neon Postgres; never executed as written) | **absorbed** |
| [sprint-102](sprint-102-fleet-run-through.md) | Distributed fleet run-through on the Postgres spine: env-selected serve transport (Part A) + 13 branch-tagged containers — 12-agent activation, distributed `ACCEPTANCE PASS`, five control-plane round-trips, DRIFT-016..019 fixed (Part B) | **shipped** (0.62.00) |
| [sprint-103](sprint-103-dispatcher-cron.md) | Dispatcher cron: Container Apps Job (22:30 UTC) places a day-keyed `RunRequest` (calendar-gated, merge-deduped); fleet stays deployed behind KEDA cron scale-to-zero windows — the platform goes self-driving in paper mode | **shipped** (0.63.00) |
| [sprint-104](sprint-104-credential-tested-activation.md) | Credential-tested activation (DL-36 A+B): master tests every credential before handover (cheap live + cache costly); a required failure refuses activation (fail-safe) + writes an `Escalation` with the one-shot counter | **shipped** (0.45.00) |
| [sprint-105](sprint-105-master-secret-cache.md) | Master Key Vault secret cache: caches fetched secrets for repeated references (TTL 3/5/10/0 min, 0=never); `CachingSecretStore` + `secret_cache_ttl_minutes`, wrapped in the master entrypoint | **shipped** (0.46.00) |
| [sprint-106](sprint-106-remediation-planner.md) | DL-36 Piece C: LLM remediation planner (bounded catalogue) — on an `Escalation`, the LLM selects a vetted remediation + justifies; records a `RemediationPlan` + `auto_eligible` (configurable `auto_remediation_scope`); plans+gates, never executes (D next) | **shipped** (0.47.00) |
| [sprint-107](sprint-107-remediation-execution.md) | DL-36 Piece D: eval-gated auto-remediation execution — DSPy behind the `PromptOptimizer` port gates the selector; safe executors run `test→execute→production→documentation` (one automatic shot, then human) | **shipped** (0.49.00) |
| [sprint-108](sprint-108-vault-seeder.md) | `.env` → Key Vault seeder, tested-before-insert: a secret enters the vault only after its live working-check passes; fail-closed, dry-run default | **shipped** (0.50.00) |
| [sprint-109](sprint-109-heterogeneous-deliberation-models.md) | Heterogeneous deliberation: GPT-5.5 debaters + separate Opus judge (`DELIBERATION_JUDGE_*`); veto debates a grounded proposition | **shipped** (0.52.00; live-Opus check deferred) |
| [sprint-110](sprint-110-signal-evaluation-battery.md) | Forecaster signal evaluation battery (qlib Q1b): rank IC, per-date IC series (mean/std/IR), quantile spread + monotonicity, multi-horizon decay, rank-autocorrelation stability; OOS-only evaluation CLI. Live Tiingo check (DL-37) | **shipped** (0.53.00) |
| [sprint-111](sprint-111-rolling-retrain.md) | Rolling retrain + IC-decay trigger (qlib Q1c): committed resumable Tiingo exporter, pure `retrain_policy` (decay verdict + champion-vs-challenger), retrain pipeline CLI — dry-run default, `--apply` swaps with archive-never-delete | **shipped** (0.54.00; merged) |
| [sprint-112](sprint-112-researcher-backtest-evidence.md) | Researcher backtest evidence (qlib Q3, self-built): pure no-lookahead walk-forward harness in researcher domain, `BacktestEvidence` optional contract field (0.2.0), bounded signal-catalogue evidence CLI — prospective Sharpe/IC alongside provenance on proposals | **shipped** (0.55.00) |
| [sprint-114](sprint-114-complete-deliberation-evidence.md) | 🔴 Complete deliberation evidence (DL-41): render every enforced gate as value + explicit pass/fail outcome; thread the PM risk gates (`max_sector_pct` concentration, sizing, held positions) that are computed but unrendered; PM emits gate outcomes (additive), split `veto_context.py`, completeness test. Priority — executed before S113 | **shipped** (0.56.00) |
| [sprint-113](sprint-113-governed-factor-proposal.md) | Governed factor proposal (qlib Q5, part A): bounded factor catalogue + LLM proposes an in-catalogue factor (enum-guarded, fail-open, LLM only in composition root) → S112 walk-forward scores it → `FactorProposal` + `BacktestEvidence` (contract 0.3.0) into the review queue. Researcher `external_io=()` intact; shadow→promote is S115 (part B) | **shipped** (0.57.00) |
| [sprint-115](sprint-115-factor-shadow-signal.md) | Factor shadow signal (qlib Q5, part B): approved factor → live forecaster shadow emitter under its own `model_id` (duplicated catalogue math + parity test, OFF by default), additive `forecast_factor` capability, generic scorecard coverage, operator promote/kill run-book — closes the governed factor-mining loop | **shipped** (0.58.00) |
| [sprint-116](sprint-116-postgres-graphstore.md) | PostgresGraphStore (DL-43 step 1): psycopg adapter + alembic schema + backend parity suite + dual selector; live check on Neon free (Sydney) — the spine's Postgres landing | **shipped** (0.59.00) |
| [sprint-117](sprint-117-postgres-fleet-swap.md) | Postgres fleet swap (DL-43 step 2): Key Vault seed (tested-before-insert), composition flip to `POSTGRES_DSN`, alembic deploy step, DEP-POSTGRES probe, ADR-0001 superseded + ADR-0008 amended — the system of record becomes Postgres | **shipped** (0.60.00) |
| [sprint-118](sprint-118-neo4j-ripout.md) | Neo4j runtime rip-out (DL-43 step 3): adapter/tests/driver dep deleted, `NEO4J_URI`-only env raises the ADR-0014 error, Aura scripts retired, workbench compose profile only — one store, one truth; Aura instance deleted by operator | **shipped** (0.60.01) |
| [sprint-119](sprint-119-dspy-deliberation-roles.md) | DSPy-compiled deliberation roles (DL-42): all three role prompts compiled behind the ADR-0010 `PromptOptimizer` port, artifact-file opt-in (byte-identical default), champion-vs-challenger report — judge artifact wins (94%/94%, stability 100%), challenger regresses, promotion operator-held | **shipped** (0.64.00) |
| [sprint-121](sprint-121-judge-promotion-challenger-recompile.md) | S119 resolution: promoted the compiled judge into `JUDGE_SYSTEM` (first ADR-0010 prompt promotion) + mandatory golden re-freeze; one bounded challenger recompile beat the promoted-judge champion and was adopted; defender untouched | **shipped** (0.65.01) |
| [sprint-120](sprint-120-broker-reconciliation.md) | Broker reconciliation (DL-44): broker = truth for holdings, graph = truth for lineage — execution-owned run-start `BrokerPositionSnapshot` + loud divergence Flag, monitor adopts broker truth, pending-Fill refresh, teardown discipline amended; live check repairs the CSCO double-buy divergence | **shipped** (0.65.00) |
| [sprint-122](sprint-122-dashboard-run-view.md) | Operations dashboard slice 1 (DL-47): `surfaces/dashboard/` FastAPI read-model + static frontend — run selector, Section III trading-run logical verdicts (observatory-sourced), acceptance banner + no-trade annotation, flags, graph-vs-broker positions, DL-36 recovery ladder; design spec `docs/design/dashboard-mockup.html` | **shipped** (0.66.00) |
| [sprint-123](sprint-123-dashboard-fleet-infra.md) | Operations dashboard slice 2 (DL-47): Sections I+II — Azure read-port (Container Apps state, job executions, Log Analytics per-container logs, Cost Management), fleet lifecycle projection, A$ hardware+LLM cost meters (`LLMCall` ledger x `llm_pricing.json`, CBA Send-IMT FX), `/api/vitals`, bundle gains real `logs`+`images` | **shipped** (0.67.00; merged `2ad656e`) |
| [sprint-124](sprint-124-dashboard-verdict.md) | Operations dashboard slice 3 (DL-47 redline r2, reqs 13–15): glance-first master verdict — binary RED/GREEN hero (animated, plain-sentence summary, warnings badge, explain-on-demand), first-class `NO_TRADE` acceptance verdict so the light never lies on a legitimate no-trade day, operator-language sweep + CI jargon-guard (no S-numbers/DL-ids in UI) | **shipped** (0.68.00; merged `b9ed20e`) |
| [sprint-125](sprint-125-operator-chat.md) | Operations dashboard slice 4 (DL-47 reqs 7+15): operator chat panel — `POST /api/chat` over the existing bounded surface dispatch (`command`/`status`/`runs`/`incidents`/`explain`, channel `dashboard`), run-scoped suggested asks, explicit confirm round-trip for gated intents, honest not-connected state, CommandAudit+LLMCall audit so `/audit-costs` prices the chat | **shipped** (0.69.00; merged `9420b78`) |
| [sprint-126](sprint-126-resume-and-tripwire.md) | Operations dashboard slice 5 (DL-47 req 8 + DL-46): bounded resume-from-stage — supersession via child RunRequest + `RESUMES` lineage, operator-gated with broker-consequence double-confirm; `DeployRecord` + deploy-currency judgement (current/behind/unverified); tri-state bus health (unverified ≠ unreachable) | **shipped** (0.70.00, merged `297354b`) |
| [sprint-127](sprint-127-fixpack.md) | Fixpack (backlog rows 4, 9, 10, 11, 12): flag ack/resolve from the dashboard through the audited operator `approve` command; `approve` interpret-routing fix + regression table; deploy currency judges the dispatcher by *template* image (execution stays as evidence); verdict warnings deep-link to their evidence panels; bus integration test skips without the azure extra | **shipped** (0.71.00, merged `32c73cc`) |
| [sprint-128](sprint-128-feed-resilience.md) | Feed resilience (DRIFT-021): per-request Finnhub pacing (55/min tunable budget), per-ticker fault attribution in all four enrichment feeds (DRIFT-014 pattern — one 429 costs one ticker, not the feed), durable attributed notes on the graph quality trace; live proof uses the real rate limit as fault injector | **SHIPPED** 0.71.01 — live check PASSED 2026-07-19 (paced 99/99 zero degraded notes in 7 min; unpaced per-ticker `:429` attribution, majority kept; Neon durability read-back); DRIFT-021 CORRECTED |
| [sprint-129](sprint-129-fixpack.md) | Fixpack (fixes-first directive 2026-07-19): S127 backlog row 3 — full `ScoreBreakdown.metrics` persisted into Recommendation + rendered to all three deliberation roles; DRIFT-023 follow-up egress reduction (TTL read cache + leaner polling, tunable); hardening D (dependency-review on PRs) + E (Trivy HIGH/CRITICAL image gate); hardening-backlog status reconciliation | **SHIPPED** 0.71.02 — merged `3be1ee8` 2026-07-19 (planning review re-ran the gate: 1597 passed / 100 %; CI + CodeQL green; Trivy gate red by design on 22 pre-existing base-image CVEs → row H / S130) |
| [sprint-130](sprint-130-base-image.md) | Chore (backlog row H via R005): drain the red Trivy gate with `ignore-unfixed: true`, then migrate all 14 Dockerfiles to a two-stage build on free near-zero-CVE `dhi.io/python:3.13` (Docker Hardened Images, Debian/glibc); `.trivyignore` stays empty; fleet retag stays operator-gated | **SHIPPED** 0.71.03 — merged `8aefe2a` 2026-07-19 (all 14 `s130-test` images built/pushed and Trivy-green in run `29681635979`; post-merge `build-images` on main GREEN; fleet deployed at `:s130` same day) |
| [sprint-131](sprint-131-blast-radius.md) | Blast radius (threat-model 2026-07-19; backlog rows I part 1 + J): 15 per-agent Neon Postgres roles (`ta_<agent>`, same grants, distinct identities — attribution + revocability), secret-backed per-app `POSTGRES_DSN` delivery, canary revocation proof; dispatcher image slimmed to its measured import closure; Service Bus SAS scoping deferred to row I part 2 | **HANDOFF** 0.71.04 — branch `sprint-131-blast-radius` pushed unmerged; role provisioning/flip/canary proven live, dispatcher image run `29714326960` Trivy/smoke green |
| [sprint-132](sprint-132-mutation-testing.md) | Mutation testing (backlog row G; trigger fired after 4 clean sprints): mutmut over the deterministic decision engines (analyst/PM/scanner/execution gates, acceptance, veto context) — kill or explain every survivor, fix any latent gate bug found; mutmut stays a manual periodic exercise, never a CI gate | **HANDOFF** 0.71.05 — branch `sprint-132-mutation-testing` pushed unmerged; scoped manual run improved from 5,282/6,731 killed (78.47%) to 5,376/6,731 killed (79.87%); remaining 1,355 actionable rows dispositioned in the [report](../reports/sprint-132-mutation-testing/README.md); no behavioral drift found |
| [sprint-133](sprint-133-servicebus-sas.md) | Blast radius part 2 (backlog row I): per-agent Service Bus SAS replacing the shared `RootManage` connection string — entity-level Send/Listen scoping (12-rule cap forces per-topic), secret-backed per-target delivery mirroring S131, least-authority + revocation proof; lower-severity than the Postgres half (bus = pointers+RPC, not data) | **HANDOFF** 0.71.07 — branch `sprint-133-servicebus-sas` pushed for PR per DL-52; 33 entity rules / 13 targets / 26 Key Vault secret names, scoped fleet secretRef flip, canary least-authority + revocation proof; report: [sprint-133-servicebus-sas](../reports/sprint-133-servicebus-sas/live-proof.md) |
| [sprint-134](sprint-134-assertion-hardening.md) | Assertion hardening (backlog row K — acting on the S132 findings): strengthened assertions for Alpaca pure helpers, analyst pure-math snapshots, and analyst/PM gate boundaries; recorded HTTPS transport, true wording, and individually justified equivalent survivors as permanent justified non-targets | **HANDOFF** 0.71.06 — branch `sprint-134-assertion-hardening` remains unmerged; S132 mutmut scope improved from 5,376/6,731 killed (79.87%) to 5,678/6,731 killed (84.36%), with Round 3 targeted analyst residuals 127 -> 20 and 0 untriaged via per-mutant dispositions; report: [sprint-134-assertion-hardening](../reports/sprint-134-assertion-hardening/README.md) |
| [sprint-135](sprint-135-unified-decision-run.md) | One run, one evidence set, both directions (ADR-0016): the analyst scores scanner survivors ∪ open held positions on the same snapshot/regime and emits buy/hold/sell; the PM sizes both into one `OrderIntentSet`; sells ride the existing buy rail, so DL-60's missing close-dispatch became moot rather than built | **SHIPPED** 0.74.00 — merged `1b858e7`, fleet `:s136`; **first sell order in the system's history** reached Alpaca (`ABT qty=98 accepted`, lifetime sell count 0→1) and the nightly re-buy accumulator (BAC 171→338→503) is stopped by `hold_recommendation` skips; `make ci` 1748 passed / 100.00% coverage |
| [sprint-136](sprint-136-realized-pnl-and-gate-integrity.md) | Realized PnL computed from the **fill** price with the entry basis resolved from the position actually sold (unresolvable → write nothing + record a Fault); reporter reads PnL from fills; plus hardening row L — `make ci` can finally fail on a CVE, with a `gate_selftest` case so it cannot regress | **SHIPPED** 0.75.00 — merged `b435183`, fleet `:s141`; realized PnL now computed from the fill price (ABT +5586c verified), reporter reads from fills, row L closed (local gate fails on a CVE) |
| [sprint-137](sprint-137-exit-authority.md) | Exit authority (ADR-0017) — alpha proposes, risk disposes: the analyst is the sole discretionary exit decider and a breached stop is forced onto the same rail regardless of confidence; the monitor stops deciding and raises a Fault on a breached-but-unsold stop; `target`/`time` retire into deferred strategy | **SHIPPED** 0.76.00 — merged `29a36f4`, fleet `:s141`; functionality check PROVEN — first-ever forced stop (`MRVL exit_trigger='stop'` at conf 0.637 > 0.5 floor overrode a hold thesis; monitor silent; sell reached broker); `make ci` 1786 passed / 100.00% |
| [sprint-138](sprint-138-broker-native-stops.md) | Broker-native stops (ADR-0015 §3, stop-only): a resting `gtc` sell stop at the broker for every held position — the durable home of the floor ADR-0017 §5 promised; the S137 forced-stop degrades to a gated fallback so the two never double-sell; the after-hours gtc-stop assumption probed and PROVEN 2026-07-25 | **handover** — packaged 2026-07-25 on `sprint-138-broker-native-stops` (design in DL-61) |
| [sprint-143](sprint-143-graph-vocabulary.md) | Graph vocabulary (R007 constraint half, DL-66): a closed set of labels, edge types and edge shapes enforced at write time by `GuardedGraphStore`, with the vocabulary as pack data derived from the live graph ∪ code ∪ law files. Inference refused permanently — a reasoner asserts facts nobody observed, and this is an append-only evidence store (LAW-02). Found `FORECAST_BY AnalystRun -> ForecasterRun` on first contact; ownership built but left off until the 8 law declarations are reconciled. | **SHIPPED** 0.79.00 — merged `798bfd4`, `make ci` 1841 passed / 100.00%; **not deployed** — the guard shipped unset, which S144 then found was also undeployable |
| [sprint-144](sprint-144-vocabulary-live.md) | Make the S143 guard deployable and prove its pack complete: `GRAPH_VOCABULARY_B64` (no image ships `orchestration/packs`, so the path interface was undeployable — S86/DL-12 precedent), plus static + dynamic superset checks. Caught `Fill -STOPS_WITH-> BrokerStopOrder` and `Position -PROTECTED_BY-> BrokerStopOrder` undeclared: enabling the guard would have thrown on the first real broker stop and killed the pending ADR-0015 §3 proof | **SHIPPED + ENABLED** 0.84.04 - guard armed on the fleet 2026-07-31 (`:s152`, `GRAPH_VOCABULARY_B64` set and read back byte-identical on all 14 targets). Three undeclared edge signatures were found and closed first; first guarded run `sched-2026-07-31` scored 8/8 with **zero** `VocabularyError`. Standing exposure: 46 `add_edge` sites the static scan cannot resolve. See [DL-68](../design-log.md) |
| [sprint-145](sprint-145-exit-replay-append-safe.md) | 🔴 **Execution is bricked in production.** The 0.74.01 position-keyed exit order key does two jobs — broker `client_order_id` (the oversell guard, which worked) and append-only `Fill` node key — and a replay carries a moved reference price, so `write_fills` raised `ValueError: property 'price_cents' cannot be overwritten` and crash-looped `sched-2026-07-27` at 4/7 stages. One attempt = one immutable node; a completed exit is never re-issued; a per-intent failure degrades to a per-intent `Fault` instead of costing three stages | **SHIPPED** 0.80.02 — merged `2c49f88`, `make ci` 1856 passed / 100.00%; remote CI + Security Findings green on the merge tip and image build green on `main`; AMD/ABT orphan Fill repair deliberately deferred to S146 |
| [sprint-146](sprint-146-unprotected-position.md) | 🟠 **A position with no floor.** ABT holds 96 shares (~$10k) and **no protective stop**: its last attempt was refused `HTTP Error 403: Forbidden`, no `BrokerStopOrder` node was written, and yet the 2026-07-28 run placed SCHW's missing stop and still skipped ABT — cause unestablished, most likely that a snapshot-adopted Position carries no `stop_pct`, which would make **every** broker-adopted position structurally unable to receive a floor. Plus lineage for 4 filled orders with no `Fill` node, the `/v2/v2/account` probe bug, and an audit that imports `contracts` predicates instead of re-deriving them. **Supersedes the first S146 packet** (orphan-fill lineage — its two orphans healed themselves on `:s145`, exactly as DL-72 predicted) | **SHIPPED** 0.80.03 — merged `7b06662`, `make ci` 1877 passed / 100.00%, remote CI + Security Findings green; fleet retagged `:s146`, `ACCEPTANCE PASS` on `sched-2026-07-28`. Root cause was **not** a missing `stop_pct`: Alpaca refuses ABT's stop as a wash trade against the buy orphaned by the 07-27 crash. Left open: **DRIFT-024** |
| [sprint-147](sprint-147-fresh-book-before-decision.md) | 🟠 **Every decision is made against a book at least one run old — DL-71 option B, the last open half.** Broker truth arrives at stage 5 (`reconcile_run_start`), is adopted at stage 6 (`reconcile_positions_from_latest_snapshot`), and is consumed by the analyst at stage 3 — *of the next run*. Across the 07-25/07-26 weekend that was three days, which is why the analyst authored `MRVL sell` for a position that had already sold and bricked the fleet. Ships a **head-of-run position sync**: execution refreshes the snapshot and the monitor reconciles the book *before* the analyst is pending — two agents in a new order, because `MON-IDN-02` reserves `Position` writes to the monitor and `EXEC-IDN-01` reserves the broker to execution. Carries a 20-test plan and the `BROKER_RESUME_STAGES` index trap | **SHIPPED** 0.81.00 — merged `2989acb`, `v0.81.00`; `make ci` 1909 passed / 100.00% verified independently on a clean worktree, CI `30423496421` + Security Findings `30423496418` green on the final tip. Found **DRIFT-025** law-first (execution writes `BrokerPositionSnapshot`, undeclared in `EXEC-IDN-02`) and a reporter defect before it existed. **Not deployed** — fleet still `:s146` |
| [sprint-148](sprint-148-decision-valid-one-session.md) | 🔴 **Fill it or drop it — implements [ADR-0018](../decisions/0018-decision-validity-same-session-or-dropped.md), the largest measured cost in the system (≈ −$2,850 across two exits).** The run fires at 22:30 UTC and submits *market* orders that cannot fill that session, so they execute at the next open ~15h later at a price nobody decided on. Ships a **bounded price tolerance** (a `tunable`, not a literal) and a **head-of-run drop sweep** that cancels what did not fill and records it as a visible `Fault`. The decided price already reaches the adapter and is discarded, and `Broker.cancel` already exists, so item 1 is surgical — **the risk is item 3: resting `gtc` stops are exempt and cancelling them would strip every position of its floor.** 23-test plan with stop-safety as its own group | **SHIPPED** 0.82.00 — merged `314fb8b`, `v0.82.00`; gates green on tip `73f0132` (CI `30445292405`, Security Findings `30445292170`), `make ci` re-verified independently at 1909→1929 passed / 100.00%. Opened **DRIFT-026**, the fourth consecutive sprint to find execution's LOCKED constitution behind its code. **Not deployed** — fleet still `:s147` |
| [sprint-149](sprint-149-volatility-scaled-tolerance.md) | 🟡 **A flat band is not a neutral parameter — it silently picks winners.** Measured against 60 sessions of real overnight gaps, S148's flat 50 bps refuses 25 % of SCHW buys but **52 % of AMD** and 48 % of MRVL: one median gap for SCHW, one *fifth* of one for MRVL. The same number encodes two different policies depending on which ticker it lands on. Ships a **volatility-scaled challenger** using the `atr_pct` the analyst already emits into `quant_metrics` (analyst unchanged; only PM→execution needs a field), **off by default** with the counterfactual tolerance recorded on every order so promotion is an operator config flip decided on evidence (ADR-0013), never a deploy. 22-test plan. Found alongside and deliberately not fixed: `suggested_stop_pct` is flat across the whole book too ([DL-76](../design-log.md)) | **SHIPPED** 0.83.00 — merged `eee68d1`, `v0.83.00`; gates green on tip `1b471c4` (CI `30512736642`, Security Findings `30512736663`), `make ci` re-verified independently at 1949 passed / 100.00% and again on merged `main` at 1950. Opened **DRIFT-027**. Found in review: it also extended the kernel vocabulary guard to enforce node *properties* for `Fill`, which has no static completeness proof yet. **Not deployed** — fleet stays `:s148` so S148's drop rate is measured against one variable |
| [sprint-150](sprint-150-volatility-scaled-stops.md) | 🟡 **The same 5 % stop is 2.4 ATRs for BAC and 0.6 for MRVL.** Every buy gets `regime.base_stop_loss_pct` - one global number for the whole book. Measured over ~65 sessions: a flat 5 % stop is touched on **0 % of days for BAC and 39 % for MRVL**, because on an 8.5 %-ATR name 5 % sits *inside* one day's normal range. A 2 x ATR stop equalises every name at 0-3 %. Ships a volatility-scaled challenger **off by default** with the counterfactual recorded (ADR-0013). 🚨 **The trap:** the reward-risk gate is `target_pct / stop_pct`, and the target is flat too - widen stops alone and AMD/MRVL/HPE silently stop passing the gate with no error at all. An honest check that MRVL's 07-27 forced stop was noise **failed**: it kept falling 16.6 % ([DL-77](../design-log.md)) | **SHIPPED** 0.84.00 — merged, `v0.84.00`; gates green on tip `3dd2c44` (CI `30520981441`, Security Findings `30520981516`), `make ci` re-verified independently at 1970 passed / 100.00%. The RR trap was handled provably — a test plants stop-only scaling and requires MRVL to be rejected. Opened **DRIFT-028** (the *analyst's* constitution this time). **Not deployed.** Measured after the fact: the 8 % risk cap, not the formula, now binds the volatile names ([DL-78](../design-log.md)) |
| [sprint-151](sprint-151-drop-sweep-append-safe.md) | 🔴 **The drop sweep stalled the fleet on its first night.** `sched-2026-07-30` reached **2/8 stages**: S148's `record_drop` writes Alpaca's raw `reason` (`"canceled"`) into `broker_status`, which reconciliation already filled with the normalised `BrokerStatus` (`"rejected"`) - two vocabularies in one property, refused by the append-only store. It raised *before* `reconcile_run_start` and *inside* its fault boundary, so the `BrokerPositionSnapshot` was never written, `position_sync` never completed, and stages 3-8 waited on a stage that could not finish - **5,762 identical faults over two hours**. Ships append-safe drop evidence (`drop_reason` + the `BrokerOrderStatus` node the sweep already writes correctly), per-order containment, and a sweep that can never cost the snapshot. 🚨 **The nine resting `gtc` stops survived the storm untouched** - re-prove that. 19-test plan ([DL-79](../design-log.md)) | **SHIPPED + DEPLOYED** 0.84.01 - merged `8f57f5f`, `v0.84.01`; gates green on tip `7e5c66b` (CI `30605541083`, Security Findings `30605541095`), `make ci` re-verified independently at 1983 passed / 100.00%. Fleet retagged `:s148` -> **`:s151`** (14/14) and the stalled `sched-2026-07-30` resumed **2/8 -> 8/8, `ACCEPTANCE PASS`**, with all nine resting `gtc` stops verified untouched and zero `cannot be overwritten` faults against 5,762 the night before. Planning review reverted an `EXEC-FAIL-03` over-claim; execution stays 30/49 |
| [sprint-152](sprint-152-law-amendment-cycle.md) | 🟠 **Five sprints, five law-gap drift rows, two agents - the gap is in how laws are maintained.** DRIFT-024/025/026/027 (execution) and DRIFT-028 (the analyst) each name a capability that an ADR decided and a sprint built, while the LOCKED constitution never declared it. 🚨 **The cause is a convention trap:** [conventions §4](../laws/conventions.md) forbids amending a law "to match whatever the code currently does", and every sprint doc tells its coding agent `laws.md` is read-only - so each sprint took the only sanctioned action, appended a register row, and moved on. Ships **one amendment cycle** for both agents plus the standing distinction that unblocks the next one: a capability *decided* in an ADR then built is a **lacking declaration** (amend), one that merely *appeared* is code drift (do not amend). Docs-only - no production source changes, and new clauses stay ⬜ | **SHIPPED** - merged `4524da5`; execution LOCKED **v1 -> v1.1** (+8 clauses, 49->57), analyst **v1 -> v1.1** (+3, 43->46); **DRIFT-024/025/026/027/028/029 all CORRECTED - zero open law-gap rows**. Greens stay 30 and 26: declaring is not proving. No production source changed; `make ci` 2001 passed / 100.00%; gates green on `737f779` and tip `fcbfb1c`, both verified by headSha. DRIFT-029 declared on a **weaker warrant** (sprint spec + DL-81, no ADR) and the row says so |
| [sprint-153](sprint-153-deliberator-agent.md) | 🔴 **The LLM veto has never run in production - not once.** Zero `DeliberationRun` nodes; all 25 `LLMCall`s are operator chat, newest 2026-07-15. `_drop_vetoed` is documented fail-open, so **every order this system has ever sent to the broker went unvetoed** ([DL-80](../design-log.md)). The debate itself is built and faithful - rounds, transcript, verdict, identical evidence - but the three roles were built as prompts inside a **kernel harness** called from `orchestration/veto.py`, and a harness only runs where something calls it: the sole caller is the *local* pipeline. Ships the operator's original design - a **`deliberator` agent, one image with the role distributed**, Manager on graph-pull over unvetoed `PMRun`s activating served Proponent and Opponent peers. 🚨 **Scope item 1 is the acceptance check that fails when a declared stage produces nothing** - without it the new agent rots exactly as the old one did, with the same green verdict. Narrative persisted as the point of exposure | **SHIPPED** 0.85.00 — merged `f32581a`, `v0.85.00`; deployed `:s155` (2026-08-02), three new apps (13 → 16). One image with the role distributed: graph-pull Manager over unvetoed `PMRun`s plus served Proponent/Opponent peers. New agent shipped **0 / 48** with a test-plan row for every clause — no green claimed. 🔴 **Did not close [DL-80](../design-log.md)**: the first production run wrote 23 `DeliberationRun`s that never reached a model (`llm unavailable (fail-open)`), which S158 then made loud |
| [sprint-155](sprint-155-bounded-retry-and-fault-containment.md) | 🟠 **The loop measures activity, not progress - third instance of one defect class in eight days.** `work_loop` sleeps only when it finds *nothing*, because `run_once` counts items **found** rather than items **advanced** - so an item that stays pending is re-found and re-processed with **no delay at all**. On 2026-07-30 that was ~1.3 s for two hours: **5,762 attempts, 5,762 identical faults, zero progress**. Live ledger today: **5,858 `Fault` nodes, 26 distinct messages** - 99.6 % duplicates, with a broker 403 and a breached stop buried under one defect fixed days ago. S151 and S154 each fixed this **on their own path** and left the loop. Ships progress-based backoff, visible poison quarantine, signature-collapsed faults that keep the recurrence signal, and a test for the `# pragma: no cover` half that has never been exercised. 🚨 **Fleet-wide blast radius** - one kernel change reaches eight agents plus the local pipeline, so the healthy path must be proven unchanged first | **SHIPPED** 0.85.01 - merged `4091d6c`, `v0.85.01`; gates green on tip `f0c237e` (CI `30706723232`, Security Findings `30706723236`) verified by `headSha`, `make ci` re-verified independently at **2054 passed / 100.00%**. **The `# pragma: no cover` is gone** - `work_loop.py` is at 100 % branch coverage, so the half whose failure mode caused the outage is finally under test. Planted failures observed pre-fix: **`attempts=25 sleeps=0`** on a no-progress loop, and 5 duplicate fault nodes for one signature. Collapse shape **2 (first + summary)** chosen and justified - preserves the first-fault timestamp/key contract; trade-off named. Opened **DRIFT-030** rather than amending a locked law |
| [chore-deploy-pack-off-the-command-line](chore-deploy-pack-off-the-command-line.md) | 🔴 **The `:s155` deploy failed on all 15 agents and the script said `Fleet deployed` and exited 0.** `az` is `az.cmd`, a cmd.exe wrapper, and `GRAPH_VOCABULARY_B64` alone is now **12,032 chars** — spliced into a `containerapp create` that also carries every secret, the GHCR PAT and the master key. The real error, `The command line is too long.`, was swallowed by `2>$null` and recovered only by re-running a copy with the redirect removed. Ships the two-step the same file already uses for `POSTGRES_DSN` (create without the pack, then a narrow `--set-env-vars`), plus a deploy that **fails loudly**: no success banner and a non-zero exit when any target fails. 🚨 **Minifying is not the fix** (8,128 chars leaves no headroom and the pack grew twice this week); `--yaml` is the durable answer and is deliberately deferred | **SHIPPED** 0.85.05 — merged `976d27e`; unblocked the `:s155` deploy. The first fix (`0.85.04`, a narrow `--set-env-vars`) **still failed**: `az` is `az.cmd`, so cmd's ~8,191-char ceiling applies to the whole invocation and the pack alone is **12,053** chars — measured, not inferred. The fix had to be leaving the wrapper: `python.exe -m azure.cli` goes through `CreateProcess` (32,767), proven on one app before adopting |
| [sprint-154](sprint-154-fill-refresh-terminal.md) | 🟠 **The green run was also writing 2,742 copies of the same settled fact.** `refresh_pending_fills` selects on `Fill.status`, which is written once at submit as `"pending"` and - on an append-only spine - **can never become `"filled"`**. So every fill the broker has already settled is re-selected every run: **39 fills carry a terminal `broker_status` and are still treated as pending work**, appending a `BrokerOrderStatus` node and a `REFRESHES` edge per pass, forever (2,742 nodes across 59 fills, **73 for a single fill**). The same loop re-emits the identical `UnresolvedEntryBasis` fault for one legacy ABT sell fill keyed in the pre-0.74.01 shape, which encodes no `position_ref` and can never resolve. Ships a terminal-status selector, a durable `pnl_unresolved_at` marker so a conclusion is recorded once instead of retried forever, and the vocabulary declaration that keeps the **now-armed** S144 guard from raising in production. 🚨 **Neither symptom is visible to the acceptance gate** - it scores what a run produced, not what it re-produced ([DL-57](../design-log.md)/[DL-59](../design-log.md) a third time). Carries a hard **remote-green** gate after four CI runs went red unattended | **SHIPPED** 0.84.05 - merged `676362a`, `v0.84.05`; gates green on tip `0649be0` (CI `30682575369`, Security Findings `30682575384`) and on impl SHA `ae9e106`, `make ci` re-verified independently at 2000 passed / 100.00%. **14 lines of source across two files.** Planted failures observed three ways - pre-fix, `partial` terminalized, and the pack declaration removed. Opened **DRIFT-029**. **Not deployed.** Review finding [DL-81](../design-log.md): the terminal skip precedes the marker write, so the one ABT fill that motivated the sprint is silently skipped rather than marked - a one-time marker write is recommended and awaits operator sign-off |
| [chore-exec-fail-03-coverage](chore-exec-fail-03-coverage.md) | 🟠 **A clause was flipped green by shrinking it to fit the test.** S151 proved one of `EXEC-FAIL-03`'s three conjuncts and **reworded the clause summary in `test-plan.md` toward that test's scenario**, so both documents agreed and nothing disclosed the gap; the review reverted it to ⬜ and execution to 30. Ships the missing coverage in the **submit** path — fault recorded, one stable idempotency key across a retry, and a repeated graph write that *appends* — plus the rule that stops the rewording recurring. 🚨 **The larger number is not automatically the right one:** reconciling execution's counter found `ledger.md`/`laws/INDEX.md` disagreeing with the test-plans for **8 of 14 agents**, filed as hardening row R rather than copied in | **SHIPPED** 0.85.06 — `EXEC-FAIL-03` 🟩, execution **30 → 31 / 57**. All three conjuncts observed failing on planted defects (DL-70), including a **measured two broker orders for one intent** when the key is made unstable. **One assertion was wrong and was replaced:** `PaperBroker.order_count` measured the fake's own key-addressed storage, so a broker that never de-dupes still passed. Settled by [ADR-0021](../decisions/0021-clause-summary-mirrors-the-law.md) + [conventions §7a](../laws/conventions.md) ([DL-87](../design-log.md)) |
| [sprint-156](sprint-156-law-coverage-is-checkable.md) | 🟠 **`conventions.md` §3 defines green as “a test cites the ID in its docstring”, and nothing checks it.** Measured on `main`: of **284 🟩 rows**, **10 cite a test function that exists nowhere** (several deleted with ADR-0017's retirement of `execute_close` and the monitor's exit authoring — the greens pointing at them stayed) and **19 cite a live test whose docstring never names the clause**. Separately, **101 of 653 clauses have no test-plan row at all** and one row (`RPT-OBS-03`) proves a clause that does not exist. Ships `scripts/check_law_coverage.py` as a tenth `make ci` step, a `gate_selftest` case so the new gate is observed failing, the 29 adjudications, and roll-ups derived rather than hand-maintained. 🚨 **Bias to ⬜: expect the book-wide green count to fall** — a ratio that gets worse because it got honest is the point. Row-per-clause ships **warn-only** (DL-82 precedent); S157 writes the 101 rows and flips it | **SHIPPED** 0.86.00 — merged `23ae35f`, `v0.86.00`; gates green on tip `a301ad9` by `headSha` (CI `30775862536`, Security Findings `30775862531`), `make ci` re-verified independently at **2085 passed / 100.00%**, `gate-selftest` **15/15** including `can-fail: law-coverage`. 29 adjudicated: **7 kept, 22 demoted**. 🟠 **Planning review caught the fix reintroducing its own defect** — the derived counter used *rows*, dropping the 101 row-less clauses out of the denominator while the header still said *Clauses green / total*; several agents' ratios *improved* in a sprint headlined "the green count fell honestly". Corrected to clauses: **252 / 653 (39%)**, master 56%→**26%**, provider 47%→**27%** |
| [sprint-158](sprint-158-fail-open-must-be-loud.md) | 🔴 **The LLM veto ran for the first time in production and never reached a model — and the gate went green.** On 2026-08-02 `DeliberationRun` went **0 → 23** on the `:s155` fleet, every one `verdict=uphold`, `turns=()`, `rationale='llm unavailable (fail-open)'`, while the `LLMCall` ledger stayed at **25 calls, newest 2026-07-15**. Cause: `connection_string_for_topic` **silently falls back to the primary** when a topic is missing from the S133 SAS bundle, so the manager addressed a peer topic with its own entity-scoped string (`EntityPath` mismatch). 🚨 **Worse than the original DL-80:** the stage now *produces an artifact*, so S153's declared-stage check **passes** — the project traded an invisible failure for a confidently green one, and the append-only store **permanently consumed all 23 `PMRun`s**. Ships a bundle miss that raises instead of falling back, a manager preflight that stays inert-and-loud rather than productive-and-wrong, fail-open as a queryable fact instead of a rationale substring, and an acceptance gate that calls it **FAIL**. Fail-open policy itself is deliberately unchanged (S147 item 2) | **SHIPPED** 0.86.01 — merged `8be1570`, `v0.86.01`; gates green on tip `c145e5e` by `headSha` (CI `30794608619`, Security Findings `30794608606`), `make ci` re-verified independently at **2097 passed / 100.00%**. Each shipped behaviour was watched rejecting a hand-planted violation: silent fallback restored (2 failed), acceptance checks removed (3), preflight disabled (2), `failed_open_count` falsified (3). Deliberator law coverage **0 → 4 / 48**. 🟠 **Planning review found ADR-0021's own shape on the first sprint after the ADR** — `DLIB-OUT-01`'s summary dropped the `DELIBERATED_BY` linkage conjunct and the cited test never asserted the edge; the S156 gate passed it, because a citation gate cannot see whether a test covers the clause *as written*. Fixed, not demoted — the edge is written, so the test now asserts it. **NOT DEPLOYED** |
| [sprint-159](sprint-159-tests-never-transact.md) | 🔴 **Every local `pytest` run has been publishing into the production Service Bus since at least 2026-08-02.** Hardening row T left one question open because it set the severity — does a message actually land? **It lands.** `deliberator-proponent.requests/agent` holds **40 dead-lettered messages** whose bodies carry `run_id: "turn-1"`, the literal fixture value, in pairs two per pytest run across 20 runs (08-02 10:47 → 08-04 02:12 UTC); the dead-letter reason `S100ReceiverFailure` is written by the **live consumer**, so production received them and retried to `max_delivery_count`. Cause is four correct facts composing: the root conftest loads `.env` into `os.environ`, `AzureServiceBusSettings` *also* reads `.env` directly, `publish()` takes the real branch whenever a string resolves, and the tests monkeypatch only the **receive** half. 🪤 **Removing `env_file` alone does not fix it** (os.environ still carries the creds) and **a worktree has no `.env`, so every proof there passes vacuously** — which is also why CI never saw it. Ships explicit offline settings, an autouse send-boundary guard observed rejecting a planted violation, and the clean-checkout half (`make install` is a bare `uv sync`; `azure` is an optional extra) | **SHIPPED** 0.86.03 — merged `56a157b`, residue closed in `a8cb973` / `0.86.04`. Closes hardening row T. 🚨 **The open question resolved to the worse answer**: the suite was not only publishing — it was performing **topic DDL on the production namespace**, creating, using and deleting two topics per local `make ci`. 40 production dead-letter messages were purged with operator approval. Both live Service Bus entry points are now **deleted rather than skipped** |
| [sprint-160](sprint-160-shadow-book.md) | 🟠 **Every `SKIP max_positions` is a discarded experiment, and we have been discarding them nightly.** The analyst produced a `buy` for MSFT on **two** consecutive nights (`sched-2026-08-04/05`); the PM rejected both on `max_positions` because the book is full at ten (**corrected 2026-08-06** — this row first said three nights; the shipped shadow book measured **2** `blocked_capacity` dispositions and `sched-2026-08-03` turned out to have produced zero buys) — so **there is no record anywhere of whether MSFT would have been a good pick**. Ships the shadow book: a decision-time reference price derived from the run’s *own* `MarketData` snapshot, one append-only outcome fact per (recommendation, horizon), a **disposition** (`taken` / `blocked_capacity` / `blocked_other` / `not_actionable`) read from `PMRun.order_intent_set.rejected[].reason`, a backfill over the 26 runs already on disk, and a scorecard whose 🎯 cut is **`taken` vs `blocked_capacity`** — if the picks we could not afford beat the ones we took, [DL-93](../design-log.md)’s resize decision is settled by evidence rather than argument. **Mostly a join, not instrumentation**: 163 `Recommendation`s and 26 price snapshots already exist, so the first scorecard needs no new runs. 🪤 `Recommendation` carries **no price and no `created_at`** (measured), and the graph is append-only at the property level, so the outcome is a **new fact**, never an edit. Applies `build-plan.md`’s own "advisory before binding" principle to the selection decision for the first time, and generalises [DL-09](../design-log.md) from filters to recommendations. **Observes only — no sizing, exit-policy or ADR-0017 change** | **SHIPPED** `2865c5e` / `0.87.00`; direction fix `c560549` / `0.87.01` — `hit_rate` was direction-agnostic, so **every sell scored inverted**. Deployed on `:s161`. ⚠️ **The 🎯 `taken` vs `blocked_capacity` cut is contaminated from 2026-08-06 onward**: once the S161 account gate went live, a buy can fail `max_positions` *and* `cash_available`, and the disposition reads only the first — so `blocked_capacity` now over-counts. The two measured dispositions (`sched-2026-08-04/05`) predate this and are correct. [S162](sprint-162-rejections-name-every-gate.md) records the caveat |

**S160 revision 2 (2026-08-06).** Revision 1 was stopped at the law-first gate and the stop was correct: no locked agent constitution owns a `RecommendationOutcome` label — `RPT-IDN-02` enumerates exactly `Snapshot`, `TradeNarrative`, `ReportSnapshotResult`, and `RPT-NEV-02` restricts the reporter to its own labels (both verified independently). **The spec was wrong, not the law.** Revision 1 told the implementer to *decide which agent owns the label* while assuming some agent should; none should, because the outcome should never have been persisted — every input is already an immutable fact, so the forward return is a computation, not an observation, and a stored copy can only drift from its own inputs (the mistake ADR-0015 §1 recorded when the monitor wrote `pnl_cents`). Revision 2 ships it as a **read-only derivation that writes nothing**, the pattern `accept.py`, `trace_run.py` and `observatory.py` already use — verified: **zero** `merge_node`/`add_edge` calls between them. Items 2 and 6 rewritten, tests B1/B3 dropped, B4 promoted to the sprint-defining assertion (*the derivation writes nothing*, watched failing on a planted write). **DRIFT-034 stays open on purpose** — routing around the gap does not close it, and the next artifact that genuinely must persist will hit the same wall.
| [sprint-161](sprint-161-pm-knows-what-it-paid.md) | 🔴 **The PM believes it has $100,000 of untouched cash at the start of every single run.** Measured on the live paper account: equity **$104,042**, cash **−$104,967**, market value **$209,009** — the book is on **~2× margin**. Cause is three individually harmless lines: `portfolio_from_graph` reads *positions* from the graph but sets `cash=Money(amount=starting_cash)`; `PortfolioState.value` returns that cash; and `starting_cash` is `tunable(100000.00, why="…before execution lands.")` — execution landed long ago. So `size_quantity = 100000 × max_position_pct / price`, a fixed ~$10k budget per order **forever**, and `reserved_cash` resets to 0 each run, so yesterday’s spend is invisible. **The asymmetry is the defect: the PM knows what it holds but not what it paid** — S147 taught it to read the position book from broker truth; nobody taught it to read the cash. 🪤 **Do not just copy Alpaca’s `cash` field (−$104,967)** — the gate would then reject everything and the pipeline dies quietly; the sizing base is a decision the sprint must make (recommended: **equity**). Ships account state on execution’s existing run-start snapshot (`EXEC-IDN-01` — the PM must not touch the broker), PM sizing against it, fail-visible on a stale account that **never blocks a sell**, and a multi-run test proving leverage is impossible by construction. **Expect zero new buys after the fix — that is it working.** Gates every DL-93 decision | **SHIPPED** `802b1c6` / `0.88.00`; **deployed** `:s161` via `up` (`f7c3cd6`) — 16/16 apps on tag, pack `dab0623b → b8d1a30f` moved with the image, verified independently. First scheduled run `sched-2026-08-06` (22:30 UTC): **8/8 stages, ACCEPTANCE PASS**, zero faults/flags/escalations. Of the three named success factors: `account_equity_cents` **proven** fresh (`$103,149.91`, `account_status="fresh"` — the factor separating a working fix from a silent degrade); zero new buy approvals **observed but not attributable**; sells-never-blocked **never exercised** (monitor `closes=0`). 🟠 **The sizing gate has still never been shown to fire** — MSFT was the only buy and `max_positions` is checked before `cash_available`, so the account gate's outcome was computed and discarded. Book unchanged at **2.02×** (equity `$103,149.91` vs deployed `$208,116.69`), by design — the fix stops adding, it does not deleverage |
| [sprint-162](sprint-162-rejections-name-every-gate.md) | 🟠 **We keep the full evidence when we say yes, and one word when we say no.** `OrderIntent` already carries `gate_report: tuple[GateOutcome, ...]`; `RejectedOrder` carries only `ticker` and `reason`. `position_outcomes` computes **all four** buy gates eagerly, then `position_rejection` returns on the **first** failure with a mapped reason and discards the rest — so on `sched-2026-08-06` MSFT recorded `max_positions` while the `cash_available` outcome S161 exists to produce was computed, failed, and thrown away. **Waiting does not fix it**: while the book sits at 10/10, `max_positions` shadows the sizing gate on every future run, and all ten holdings are rated `hold`. Ships the defaulted `gate_report` on `RejectedOrder`, an attach at every rejection site, the semantics decision (**report what was evaluated** — an absent gate is unknown, never passing), and both read-only renderers showing it. 🪤 **Five of the files it touches are 163–197 lines against the 200-line hard block** (`trading_observatory_views.py` has **3** left) — the module splits are the real work. 🪤 No vocabulary change: `PMRun` is in `labels` but **not** in `properties`, and the set is written as one JSON prop, so there is **no pack/image coupling** and the S148 stall cannot repeat. **Records nothing retroactively and moves no approval decision** | **SHIPPED** 0.89.00 — merged `ecde86d`, `v0.89.00`; deployed `:s162` (image-only, pack byte-identical). **Proven end-to-end on the deployed fleet**: 8/8 stages, `ACCEPTANCE PASS`, live trace `MSFT   SKIP  max_positions (also failed: cash_available)` carrying all four gate outcomes (`max_positions passed=False 11.0/10.0`, `cash_available passed=False 10000.7/-105407.246`). **No approval decision moved** — `approved=0 rejected=11`, identical reason strings, pre-gate rejections render with no suffix. 🟠 Three modules were trimmed to 198/198/193 rather than split, against a 200-line hard block |
| [sprint-163](sprint-163-an-exit-cancels-its-own-stop.md) | 🔴 **The book cannot be sold.** All ten positions showed `qty_available = 0`, each fully reserved by its own resting `gtc` stop, and `execute_pm_node` never cancelled the stop of a ticker it was about to sell. Ships cancel-before-submit, the decision for a failed exit after its cancel, and idempotency | **SHIPPED** 0.89.01 — merged `a46449e`, `v0.89.01`; deployed `:s164`. **Proven in production 2026-08-07**: `submitted=10 rejected=0`, and the broker confirmed 10 open `limit sell` orders and **zero** stop orders. [DL-95](../design-log.md)'s inferred half is now observed end to end |
| [sprint-164](sprint-164-a-cancelled-stop-can-be-replaced.md) | 🔴 **`EXEC-OBS-03` promised a retry the code could not perform.** `_place_stop` returned `None` whenever *any* `BrokerStopOrder` fact existed at `stop:{ref}:{ticker}` — including a **cancelled** one — so a still-held position whose stop was cancelled faulted truthfully every run and was never re-protected. Ships an attempt-chained stop key | **SHIPPED** 0.89.02 — merged `f3b2fcf`, `v0.89.02`; deployed `:s164`. Closes **DRIFT-038**. 🟠 **Proven by tests only** — no exit was rejected during the flatten, so the replacement path has never run in production |
| [sprint-165](sprint-165-score-every-name-not-five.md) | 🔴 **The system produced 1.67 selection decisions per run, and the throttle was a cap of 5 — not the filters.** 100 evaluated → 22 passed → **5** reached the analyst. A `FilterVerdict` was built for every evaluated ticker and thrown away, so [DL-09](../design-log.md) had nothing to score | **SHIPPED** 0.89.03 — merged `34ca6ae`, `v0.89.03`; deployed `:s165`. **Proven in production**: `filter_trace` with **100 verdicts for 100 evaluated**, `candidate_count=22` not 5. 🚨 **The law-first read produced a third option the spec had not considered** — a `FilterVerdict` label is unlawful *and* a derivation would have been wrong, so the fix was one line keeping what the `ScanRun` already decided, and the version dropped MINOR → PATCH |
| [chore-deploy-s158](chore-deploy-s158.md) | Retag the fleet `:s155` → `:s158` so the deliberation acceptance checks can see their own facts — **`up`, not an image-only retag**: S158 added three properties to a property-enforced label, so image and vocabulary pack had to move as one unit | **DONE** — executed 2026-08-04. 16/16 apps + job on tag, pack read back byte-identical `dab0623b…` on all 17 targets. 🚨 `up` printed `Deploy FAILED for 17 target(s)` and every word of it was wrong (hardening row Q, since closed) |
| [chore-flatten-and-resize](chore-flatten-and-resize.md) | Clear the deadlock: raise the analyst's `exit_confidence_floor` above the highest held confidence so all 10 positions exit through the **designed ADR-0017 rail**, then resize to small parcels across many slots. **Config-only — no code, no branch, no version bump** | **DONE** — executed 2026-08-07 on `:s164` after S163 + S164 unblocked it. All 10 emitted `sell`, `submitted=10 rejected=0`; `max_position_pct` 0.10 → **0.01**, `max_positions` 10 → **60**; floor verified back at 0.50. 🟠 **The fills are not proven** — `tif=day` pre-market, all ten queued for the 13:30 UTC open |
| [chore-teardown-leaves-no-orphans](chore-teardown-leaves-no-orphans.md) | 🔴 **A teardown that under-deletes exits 0.** `pg_teardown.py --run-id` reported `deleted_nodes=21` and left **41 rows** carrying the same run's stamps ([DL-94](../design-log.md)) | **SHIPPED** 0.89.04 — merged `9d6243c`, gate proven on `70426300…`. Tooling only, nothing deployed. 🚨 **DL-94's own prescription was measured insufficient**: widening the label list reaches **0** `PositionCheck` rows, whose sole edge is to `Position`. 🚨 The list was an undeclared traversal barrier — adding `BrokerStopOrder`, which reads exactly like a run artifact, would have deleted the 27 live `Position` rows |
| [chore-split-modules-before-the-block](chore-split-modules-before-the-block.md) | Split the three modules S162 trimmed to fit rather than split — `gate_report.py` 198, `trading_observatory_views.py` 198, `risk.py` 193 against a 200-line hard block | **SHIPPED** 0.89.05 — all three split into seven modules, every one under the 150-line warn line; no behaviour change (2180 → 2180 tests at the split). 🚨 A planted reorder of the sizing and reward-risk stages **passed all 86 PM tests** — the rejection precedence that names every `SKIP` reason was unpinned. [DL-96](../design-log.md); closed by three new tests, the reorder now fails 2 of 89 |
| [chore-one-sector-rejection](chore-one-sector-rejection.md) | Delete `SectorBook.rejection`, an exact duplicate of the sector gate-to-reason mapping called only by tests | **SHIPPED** 0.89.06 — the reason strings now exist in one place. 🚨 Dead production code at **100 % coverage**, kept alive by the single test that called it — a 100 % floor measures whether a line ran, not whether anything but a test made it run. Test re-pointed at the live composition; planted swap fails 5 tests. `concentration.py` 174 → 144. [DL-97](../design-log.md) |
| [sprint-166](sprint-166-the-veto-gates-buys.md) | 🚨 **The LLM veto has never blocked an order — because execution always won the race.** Measured: 18 orders reached the broker at 05:36:22 and the `DeliberationRun` saying *revise* landed 05:47:32. `_drop_vetoed` read an absent veto as *execute everything*, so *not finished yet* and *not deployed* were indistinguishable | **SHIPPED** — `v0.89.07`, 2026-08-08. A buy-carrying PMRun is held unconsumed for a bounded grace (`deliberation_grace_seconds`, default 900); **exits never wait** (S147 / ADR-0017). Fail-open survives but is loud: `ExecutionRun.deliberation_status` + a `DeliberationGraceExpired` fault. [ADR-0022](../decisions/0022-the-veto-gates-buys-never-exits.md); closes [DL-98](../design-log.md) |
| [sprint-167](sprint-167-a-fault-count-cannot-lie.md) | 🚨 **An audit reported “Faults today = 0” while 18 were being written.** `Fault` nodes stamp `occurred_at`; the query read `created_at`, which is `NULL` on every Fault node — measured both ways at the same moment: **18** vs **0**. Plus a fail-open asserting a cause the code never established (`llm unavailable` while 58 `LLMCall`s were logged) | **SHIPPED** — `v0.89.08`, 2026-08-08. Added `kernel.fault_query` for Fault-window reads that raise on a missing timestamp property and count `FaultSuppression` volume; `DeliberationRun.failed_open_reason` now records the captured exception cause and is declared in the property-enforced vocabulary pack. Fail-open policy and the S166 buy gate unchanged. 🟠 **Not deployed/live-proven:** pack moved `b8d1a30f…` → `13c0e3a0…`, so deploy must be full `up`, not image-only retag. Closes the code-shaped halves of [DL-99](../design-log.md) |
| [sprint-169](sprint-169-one-switch-and-a-deploy-that-keeps-it.md) | 🚨 **A provider switch that is four switches, and a deploy that silently unsets them.** Measured 2026-08-08: `DELIBERATOR_LLM_PROVIDER=openai` alone sends an *Anthropic* model name to OpenAI, because the three `*_model` tunables default to `claude-opus-5` and `entrypoint.py:76` passes them explicitly — and `role_models` would then record a vendor that never saw the order. Separately, a full `up` **replaces** each app's env set, wiping `SCANNER_CANDIDATE_CAP=25`, `MAX_POSITION_PCT=0.01`, `MAX_POSITIONS=60` and reverting the weekday-only dispatcher cron — all with a green `[OK]` | **SHIPPED `0.90.10`** (2026-08-14) — an unset model resolves its default from the provider and `role_models` records the **resolved** name; operator tunables and the dispatcher cron move into `orchestration/packs/trading_tunables.json`, and `up` refuses before its first create if it would drop a live env key, naming it (`-DropEnv` to drop one deliberately). `make ci` 2299 passed / 100.00 %. 🪤 **Not deployed** — the live half (a real `up`, tunables re-read off the app) is owed at the next full deploy. Closes [DL-100](../design-log.md) |
| [sprint-170](sprint-170-one-llm-adapter-in-the-plumbing.md) | 🚨 **The LLM layer is half in the substrate.** The port (`kernel/llm.py`) and the `LLMCall` ledger (`kernel/llm_ledger.py`) are kernel and both agents route through them — but the vendor adapters and provider selection are not, and are duplicated: two `AnthropicLLMClient` classes differing in **one method** (free-text vs tool-use). So S168's OpenAI fallback exists **only for the deliberator**; the operator can call Anthropic and nothing else while that key is usage-limited until 2026-09-01, and `surfaces/dashboard/chat_binding.py` imports the agent's adapter directly | **SPEC** — target `0.90.03` (retargeted 2026-08-11). One adapter set + factory in `kernel/`, the operator gains the same provider switch and an `openai-api-key` grant, the surface stops reaching into an agent. Opens [DL-101](../design-log.md) |
| [sprint-171](sprint-171-a-reply-must-answer-its-own-request.md) | 🚨 **The veto pairs a request with whatever reply is at the head of the queue.** Measured 2026-08-08 on `:s169`: the proponent made **18 real `gpt-5.5` completions**, yet the `DeliberationRun` read `real_debate_count=0, failed_open_count=18` with an *Anthropic* usage-limit reason — the manager drained **84** stale replies (oldest 5.5 h old) instead of reading its own. `peer_client.py:143` takes `messages[0]` with **no `request_id` correlation**. The error case fails open loudly; a stale **success** reply would be accepted as a verdict **about a different ticker**, with no fault and a green gate | **SHIPPED** 0.90.01 — merged `9a22102`, `v0.90.01`; `make ci` exit 0 unpiped, **2225 passed / 6 skipped / 100.00 %**; the correlation suite **30 passed / 1 skipped**, the stale reply dead-lettered `S171OrphanedReply` and surfaced as `OrphanedPeerReply`. **Proven in production 2026-08-08** on `:s171` from cold peers (`check-s171-cold-start`, image-only retag, vocabulary pack byte-identical): real_debate_count **18**, failed_open_count **0**, `role_models` all `gpt-5.5`, **zero** deliberator faults, reply subscription **0 active / 0 dead-letter** — the backlog no longer regenerates. 🚨 Fixing it exposed [DL-103](../design-log.md): with correlation correct the debate takes its true **943 s** against a 900 s grace, so it overran and all 18 orders proceeded unvetoed. Grace went 900 → 1800, then [DL-104](../design-log.md) read the verdicts for the first time and returned it to **900 deliberately**, leaving the veto advisory |
| [chore-openai-cutover](chore-openai-cutover.md) | **S168's provider switch cannot take effect yet.** Measured 2026-08-08: Anthropic **400** (limit to 2026-09-01), OpenAI **200**; but `trading_secrets.json` grants the three deliberators **Anthropic only** — `OPENAI_API_KEY` appears **0** times. Setting the provider today fails closed at startup: works on a laptop, dies on the fleet | **DONE** 2026-08-08 — merged `8b30624`, **no version bump** (the secret map is injected from the repo at deploy time and ships no package behaviour). `make ci` exit 0 unpiped, **2216 passed / 6 skipped / 100.00 %**; `make gate-ran` **GATE PROVEN**. Grant `openai-api-key → OPENAI_API_KEY` on all three deliberators with Anthropic **kept alongside**, so reverting the vendor is one env var with no redeploy; master fetched the key from Key Vault live (**HTTP 200**, 11:04:41), proven not inferred. 🚨 The vault and `.env` held **different** OpenAI keys — both authenticated, so nothing was broken, but the fleet would have billed a five-week-old key nobody tracked; synchronised and re-read (`sha12 5aacfe5c37db` both sides). 🚨 The full `up` **silently discarded operator configuration** — `SCANNER_CANDIDATE_CAP`, `MAX_POSITION_PCT`, `MAX_POSITIONS` wiped and the dispatcher cron reverted to daily, caught only by a hand snapshot → [DL-100](../design-log.md), which is what S169 fixes |
| [sprint-172](sprint-172-independent-debates-run-independently.md) | 🚨 **The debate is serial end to end — measured, not inferred.** Sum of per-call latency ÷ span = **0.90** on `sched-2026-08-10`: ninety per cent of the wall clock has exactly one of 90 calls in flight. Three serializations — `poll.py:64` is a plain synchronous `for` loop, rounds within an order are inherently sequential (and stay so), and all three deliberator apps run `maxReplicas=1`. At 100 orders that is **~4,740 s against a `le=3600` grace ceiling; no grace value fixes it**. Ships a bounded `debate_concurrency` tunable, deterministic result reassembly so a `DeliberationRun` stays reproducible, and peer replicas. 🪤 `max_rounds` 2 → 1 is a **tunable** worth ~40 % and is deliberately *not* in this sprint — it would contaminate the before/after measurement | **SPEC** — packaged 2026-08-11 from [DL-105](../design-log.md). Target next available MINOR |
| [sprint-173](sprint-173-a-verdict-must-be-reproducible.md) | 🚨 **The veto does not agree with itself — 56 % self-agreement on a binary verdict**, same model and prompt and tickers 3.5 h apart (9 of 16); cross-vendor agreement is *higher* at 12 of 17. Ships a **read-only** replay harness on the **Message Batches API** — the right substrate because measuring reproducibility means thousands of independent replays with no latency budget at all, at 50 % off — plus self/cross-vendor agreement metrics that exclude fail-opens (🪤 a fail-open is stored as `verdict: "uphold"`), and a warn-only quality gate. 🪤 Self-agreement is not accuracy: a veto reproducibly wrong still scores 100 % | **SPEC** — packaged 2026-08-11 from [DL-105](../design-log.md), closing [DL-104](../design-log.md) (e). Blocked on DL-104 (a)+(b) |
| [sprint-174](sprint-174-an-indicator-gets-the-history-it-declares.md) | 🚨 **The system has never once computed SMA-200 distance or the EMA crossover, and says it did on every recommendation.** The analyst declares `lookback_days=260` and sends it on the request path; production uses the graph-pull path, where `_today_window()` is called with no argument and `_DEFAULT_LOOKBACK_DAYS = 60` yields **41 bars** — measured on `sched-2026-08-11`, exactly 41 for all 99 tickers. Both indicators return `None` and vanish through `if x is not None`; `min_history_bars = 2` waves it through. Found by the LLM veto, which is the one veto class that checked out correct ([DL-104](../design-log.md) class 3). 🪤 The 60 is a **bare literal** where `kernel/config.py` requires a `tunable()` — nothing enforces that rule | **SPEC** — packaged 2026-08-12. Target next available PATCH at merge, deliberately unpinned |
| [sprint-175](sprint-175-the-veto-says-only-what-it-can-prove.md) | 🚨 **An unreviewed order is currently indistinguishable from an approved one.** `fail_open_review()` stores verdict `uphold`, `poll.py` builds the veto list as `verdict != "uphold"`, and execution's `drop_vetoed` reads only `vetoed_tickers` — so a failed debate is permission to trade. Proven live on `sched-2026-08-13`: 3 fail-opens, 3 of 8 orders submitted, **2 of them (AMD, DOW) never reviewed**. Bundled with the invented ATR fragment that DL-104 blamed for 6 of 15 objections — 🪤 S174 made it *worse*, flipping AMD `FAILED`→`PASSED` purely because the history window grew — and with the declared advisory/binding posture (DL-104 d). 🚨 Fail-open must **stay** permissive (S147): the defect is that it is invisible, not that it permits | **SPEC** — packaged 2026-08-13. Target next available PATCH at merge, deliberately unpinned |
| [sprint-176](sprint-176-a-partial-fill-must-be-able-to-finish.md) | 🚨 **A `partial` fill is stuck as `partial` forever, and so is the price realized PnL is computed from.** The write-once guard on `broker_status` is deliberate — the spine is append-only and `Fill` is protected (DL-94) — but `broker_price_cents` shares it, and `exit_price_cents` reads that stored value back, so a completed-after-partial fill prices its PnL at the *partial*. Dormant only because **zero of 188 production Fills have ever been partial** (measured 2026-08-13); a widened funnel with more limit orders at the open makes it likely. 🪤 The fix must narrow the guard, not delete it. Carries DRIFT-033 (`contracts/master.py:99` still declares a deleted Neo4j) because it needs the same CI cycle | **SPEC** — packaged 2026-08-13. Target next available PATCH at merge, deliberately unpinned |
| [sprint-177](sprint-177-every-number-names-its-unit.md) | 🚨 **The veto has now killed four real orders over values it could only misread.** None was a wrong number — each was correctly computed under a name carrying neither its unit nor its scope. Three are fixed (S175 ×2, DL-112). The fourth is open: `SectorBook.__init__` seeds `_names` from held positions but **never `_deployed`**, so `max_sector_pct` renders `deployed=0.00` meaning *nothing from this batch yet* while reading as *no portfolio exposure* — it overturned a real AVGO buy on `sched-2026-08-14` while `existing_sector_names=2` in the same gate report correctly saw AMD and NVDA. Sweeps all ~20 rendered value sites. 🪤 Label only — seeding `_deployed` changes which orders get approved and needs its own ADR | **SPEC** — packaged 2026-08-15. Target next available PATCH at merge, deliberately unpinned |
| [sprint-178](sprint-178-the-health-signal-must-be-able-to-go-green.md) | 🚨 **`healthy` has been `false` every day since 2026-07-08 and nothing can turn it green.** Measured 2026-08-15: 52 Flags, 7 resolutions, **45 critical unresolved**. The machinery works — 2 criticals *have* been resolved and all 7 join correctly. The backlog can only grow: `subject_ref` embeds `snapshot.key` (run id + ISO timestamp), so every run mints a unique flag and the dedupe guard never fires; and `resolve_flag` is reachable only via the human `approve` capability. 🪤 Severity is chosen at run **start** — on `sched-2026-08-14` the flag was stale 3 minutes later, describing normal successful adoption. 🟢 Reporting-only: acceptance reads neither field | **SPEC** — packaged 2026-08-15. Target next available PATCH at merge, deliberately unpinned |
| [sprint-179](sprint-179-a-fault-must-be-able-to-stop-being-an-incident.md) | 🚨 **S178 opened one of `healthy`'s two locks; the other cannot open at all.** Measured 2026-08-18: **6119** `Fault` nodes, **every one** `status=pending`, **0** `FaultResolution`. `health.py:32` counted faults whose status `!= "resolved"` and nothing ever wrote that value, so `open_incidents` only grew. 🪤 **94 % (5762) was one closed July incident**, not a live fire. 🪤 Fault recurrence is **deliberate** — `fault_node_key` is keyed by `occurred_at` on purpose. 🚨 **Two independent copies of the predicate** had to move together | **SHIPPED** `0.90.14`, 2026-08-18 — merged `4c8eeb0`, gate proven. `open_incidents` is now a live incident count scoped to the latest graph-run day, with append-only `FaultResolution` retirement; **`healthy` reached `true` on the spine for the first time since 2026-07-08**. `Fault` 6119 → 6119, all still `pending` — nothing mutated. Deployed `s179` 2026-08-18 (code + vocabulary pack together) |
| [sprint-180](sprint-180-a-deploy-record-must-name-the-commit-that-was-built.md) | **A `DeployRecord` can name a commit that was never built.** `record_deploy` validates only that tag/sha/actor are non-empty — no shape check, no cross-check. Measured 2026-08-18: the `s179` record captured `git rev-parse HEAD`, one **docs-only** commit past `4c8eeb0`, the commit the images came from. `projections_currency.py:61` compares that field to the newest main image build, so the dashboard would read the fleet **"behind" while current** — the DL-46 error the record exists to prevent. Caught only by reading the printed SHA. 🪤 40-hex validation would **not** have caught it. 🟢 Nothing in the trading path reads `DeployRecord` | **SPEC** — packaged 2026-08-18. Target next available PATCH at merge, deliberately unpinned |
| [sprint-181](sprint-181-an-untracked-order-is-reported-once.md) | 🚨 **`healthy` went green on 2026-08-18 and was red again at the next run, over one canceled test order from 2026-08-07.** The head-of-run drop sweep re-raises an identical `error` Fault every run for `stop:probe-s164:T#1` — a 1-share S164 probe with no `Fill` node. `_already_dropped` reads `fill.props["drop_reason"]`, so **the only memory the sweep has lives on a node that does not exist**; nothing on the path writes anything, and the next run repeats it exactly. 12 identical Faults since 2026-08-08, one per run. Measured: of 197 broker orders, 103 are terminal and pipeline-owned and exactly **1** has no Fill chain. 🪤 **The artifact cannot be torn down** — the order is already canceled, terminal and immutable at Alpaca; and 🪤 it self-heals once 500 newer orders push it out of the query window, which would flip health green on **order volume**. 🟢 Reporting-only | **SPEC** — packaged 2026-08-19 from the `sched-2026-08-18` diagnosis. Target next available PATCH at merge, deliberately unpinned |
| [sprint-182](sprint-182-a-new-position-is-protected-on-the-run-that-opens-it.md) | 🚨 **A position that fills between two runs stays unprotected until the run after the one that notices it.** `Position` nodes are created by the monitor at stage 7; stops are placed by execution at stage 6, so the producer runs after the consumer and protection lags a full run by construction. Measured 2026-08-19: 15½ hours and **$2,147.76** exposed. 🪤 `reconcile_run_start` never creates a `Position` — the natural wrong assumption. 🪤 Alpaca `403 potential wash trade` blocks a sell-stop while a buy rests on the same symbol. 🚨 Who may create a `Position` is a **law** question before a code one | **SPEC** — packaged 2026-08-20. Target next available PATCH at merge, deliberately unpinned |
| [sprint-183](sprint-183-a-gate-that-did-not-run-says-so.md) | 🚨 **A filter that never ran looks exactly like one that passed.** `if "days_to_earnings" in features:` skips the earnings gate in silence, and `_days_to_earnings` returns `None` for *unknown* and *already past* alike; `max_beta` is the same. **Measured: 10 of 98 tickers had an earnings date**, so ~88 per run pass unevaluated — and `quality.notes` was empty, so it reads as healthy. Second complaint carried: the **debate packet cannot see which mode produced the stop**, hiding S150's fully-built volatility-scaled stop. 🪤 **Corrected mid-build** (`d859746`) — an earlier version told Codex to register `stop_target_mode` as a `tunable()`; the locked analyst law declares it `NO (mode selector)` on purpose ([DL-120](../design-log.md)). 🪤 **Promotion of scaled stops is deliberately out of scope** — that is an experiment, not a fix. 🟢 Attestation is additive: the same tickers should survive | **SPEC** — packaged 2026-08-20 from [DL-119](../design-log.md). Target next available PATCH at merge, deliberately unpinned |
| [sprint-184](sprint-184-one-issuer-is-one-bet.md) | 🚨 **The PM's risk gates cannot see the whole book** — they count vendor industry labels, so GOOG passes while GOOGL is held and AMZN passes at 1 name in `Retail` beside a book of correlated mega-cap tech. **The deliberator rejected 19 of 26 PM-approved orders (73 %) on four consecutive nights** and `verify-2026-08-20-s182-a` approved four and traded **none**. Implements PM `laws.md` **v1.3**: `PM-NEV-07` issuer aggregation, `PM-NEV-08` a measured correlated-cluster cap, `PM-NEV-09` no silent pass. 🟢 **Measured: the input is free** — exactly 202 bars × 98 tickers sit in the run's `MarketData.snapshot`, and all 22 held names are in it. 🪤 `Bar` has **zero** nodes. 🚨 DRIFT-045 must land with it — fixing the name count un-masks a dollar cap that never sees held positions. Unlike S183 this **will** change which orders are approved | **SPEC** — packaged 2026-08-20 from [ADR-0023](../decisions/0023-concentration-is-issuer-and-correlation-not-a-vendor-label.md). Target next available MINOR at merge, deliberately unpinned |
| [sprint-185](sprint-185-the-veto-posture-is-declared-not-arithmetic.md) | 🚨 **Whether the LLM veto is advisory or binding is not declared anywhere — it is an accident of two numbers.** [DL-104](../design-log.md) set `deliberation_grace_seconds` to **900** *deliberately*, as the only no-code way to keep the veto advisory; [DL-116](../design-log.md) raised it to **1800** so debates could finish, and the veto became **binding by arithmetic**, with no declared mode anywhere. 🚨 **The outage made it top-ranked:** with no API credit until **2026-08-30** ([DL-125](../design-log.md)), every nightly run fails open, submits **unvetoed**, raises an `error` fault and turns acceptance **red for a non-defect** — ~6 sessions of exactly the *"trains the operator to read a real fault as noise"* failure DL-104 (d) named. **Measured: execution's `laws.md` contains ZERO clauses about deliberation**, and `deliberation_grace_seconds` has **no PARAM row** — both filed as drift, both are why the posture could be set by arithmetic. 🪤 **Not a softening of the veto** — DL-119 rejected that; declaring the posture is the opposite move. 🪤 **Exits never wait in either posture** (S147 / ADR-0017). 🪤 Every proof must be a fixture — no real debate is obtainable before 2026-08-30 | 🟢 **BUILT locally** `0.92.00`, 2026-08-22 — `deliberation_posture` is a declared `advisory | binding` mode selector recorded on `ExecutionRun`; advisory fail-open evidence is attributed warning/green, explicit binding blocks unreviewed buys, and exits never wait. Local `make ci` passed: **2378 passed / 4 skipped / 100.00 %**, pip-audit and detect-secrets clean. 🟠 Branch gate, merge, full `up`, and live scheduled-run proof remain pending |
| [sprint-186](sprint-186-a-headline-about-twenty-companies-is-not-news-about-one.md) | 🚨 **A headline the vendor filed under twenty tickers counts once, in full, for each of them.** Finnhub `/company-news?symbol=X` returns market-wide content, `_parse_news` applies no relevance test, and `score_sentiment` is an **unweighted mean** — so contamination moves the number the scanner ranks on. Measured on `sched-2026-08-21`: **50.9 %** of headline slots are filed under ≥2 tickers, **23.4 %** under ≥5, **14.8 %** under ≥10. ✅ **Shape already decided — [DL-127](../design-log.md) measured drop-at-N against down-weight through the real pipeline and chose `1/n`:** it silences **0** tickers where dropping silences **4**, discards **0 %** of slots where dropping discards 23.4 %, and its worst downstream confidence shift is **0.034 vs 0.065**. 🪤 **Not a safety fix** — DL-117's *"mis-attributed news is buying stocks"* was retracted; this is bidirectional noise, so ship it for accuracy. 🪤 The count belongs to the **batch**, not the headline — it cannot live in `_parse_news`. 🪤 DL-112 is the precedent for the metric-naming hazard | **SPEC** — packaged 2026-08-22 from work-queue item 26. Target next available PATCH at merge, deliberately unpinned |
| [sprint-187](sprint-187-a-parameter-is-declared-once.md) | 🚨 **The PARAM tables and the code have drifted apart in BOTH directions, across three agents, with nothing checking.** Verified 2026-08-22: `scanner.benchmark_ticker` is declared `YES` in the scanner law and left a bare default in code — while the **analyst carries the identical law row and honours it**, so one law row has two agents and only one obeys; `provider.alpaca_data_feed` and `ingest_ohlcv_only` appear in **no law at all** (provider `laws.md` is LOCKED v1, so post-lock additions → a law cycle, not a code edit); and `execution.deliberation_grace_seconds` is a registered `tunable()` with **no PARAM row**. 🚨 **`execution.stage` RETRACTED — it is already declared** (`laws.md:310`, `NO (config)`). 🪤 **`stop_target_mode` / `order_price_tolerance_mode` / `curator.predictor_strategy` are `NO (mode selector)` ON PURPOSE** — DL-120's headline called them defects and was retracted; registering any is a law violation. 🎯 **The durable half is a `make ci` check** reconciling every PARAM row against its settings field both ways — this is the second audit to find this class | **SPEC** — packaged 2026-08-22 from work-queue item 29. Target next available PATCH at merge, deliberately unpinned |
| [sprint-188](sprint-188-a-credential-is-tested-before-it-is-handed-over.md) | 🚨 **The master hands out credentials it has never tested, so activation succeeding proves nothing.** `MasterAgent` declares `credential_tests: tuple[CredentialTest, ...] = ()` (`agents/master/agent.py:57`) and `entrypoint.py:90-96` builds it **without that argument** — no pack supplies any, so DL-36 Piece A runs **zero** tests in the deployed fleet. 🚨 **The remediation chain is unreachable for the same reason**: `handle_activation_remediation` fires only when `failures` is non-empty, and `failures` can only be non-empty if there are tests — so DL-36's whole self-healing arc is dead code downstream of one empty tuple. This is why a dead LLM key went unnoticed for **six scheduled nights** ([DL-125](../design-log.md)), and why [DL-134](../design-log.md) could find no cheap probe of the master→Key Vault path. 🪤 **The obvious fix is the rejected one** — `orchestration/master_serve.py` already accepts the tests and 8 working probes already exist in the pack, but the master image copies only `kernel/ contracts/ agents/master/` and import-linter forbids `agents → orchestration`; the image stays pack-agnostic (S86 / DL-12). Tests reach master as **data**, exactly like the grant policy and secret map already do. 🪤 **The subtle decision is transport-vs-credential**: a probe that reads a DNS blip as a bad key halts the whole fleet — worse than the outage it exists to prevent | **SPEC** — packaged 2026-08-30 from the [DL-134](../design-log.md) finding. Target next available MINOR at merge, deliberately unpinned |
| [sprint-189](sprint-189-an-empty-answer-says-why-it-is-empty.md) | 🚨 **Neither LLM adapter inspects why the model stopped, so a truncated answer, a refusal and a genuinely empty answer are one recorded value.** `llm_openai.py:70-78` never reads `finish_reason` (its docstring says *"tolerating empties"*) and `llm_anthropic.py:54-60` has the **identical** defect on `stop_reason`. **Measured on the live spine: 135 of 1,037 `LLMCall` rows have an empty completion** — two agreeing signals, `response_hash == sha256("")` and `tokens_out == 0`. 🚨 **Work-queue item 35's framing is wrong and gets corrected at merge:** it filed this as OpenAI-fallback-only and "not currently biting", but **112 of the 135 are `claude-opus-5`, the primary**, and only 22 are `gpt-5.5`. 🪤 **98 of the 135 fall on 2026-08-08 alone** (a known outage day), so the honest steady-state figure is the **~3 %** on days with no known outage (3 of 100 across 08-17/08-18) — quoting 13 % would repeat DL-119's diluted-denominator mistake. 🚨 **The worst case has no fail-safe:** `debate_turn` returns `Turn(role, n, "")` with no guard, so a truncated argument enters the transcript as a hole and the judge rules on it, producing a verdict that looks legitimate; the arguing roles fail most (proponent **16.0 %**, opponent **13.5 %**). 🟢 The judge's case is fail-*safe* by accident — `_parse_verdict("")` → `revise` — but blames the parser, and a forced `revise` counts as a veto. 🪤 `tokens_out` **cannot** detect truncation: it is a word count of the *visible* answer, so "0 rows above 4096" means the metric is blind, not that truncation is absent | **SPEC** — packaged 2026-08-30 from work-queue item 35, after measuring it. Target next available MINOR at merge, deliberately unpinned |
