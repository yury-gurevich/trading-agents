<!-- Agent: planning | Role: fix-sprint backlog -->
# S127 fixpack — issue collection

Running list of small, confirmed defects and cleanups batched for the next fix sprint.
**How it works:** anyone appends a row while the issue is fresh; the planning agent packages
S127 from this list when it justifies a sprint. One row per issue; link the evidence. Genuine
law-vs-reality drift also gets a `DRIFT-NN` row in `docs/laws/drift-register.md` — this list
then just points at it (never two full records of one issue).

| # | Kind | Issue | Evidence / pointer | Reported |
| --- | --- | --- | --- | --- |
| 1 | ~~error~~ **FIXED (0.69.02)** — run-scoped via `?run=`; DRIFT-022 CORRECTED. | **Log drawer ignores the selected run** — always queries the latest fleet window, so run `sched-2026-07-08` shows 2026-07-13 log lines. Fix shape known: pass the selected run to `/api/containers/<name>/logs`, resolve its day from the RunRequest (`run_window` already exists; `/bundle` already does this), fall back to latest only when unscoped; label the shown window in the drawer. | **DRIFT-022**; `surfaces/dashboard/bundle_azure.py` (`container_logs` → `latest_window`) | operator, 2026-07-14 |
| 2 | ~~cleanup~~ **FIXED (0.69.03)** — shim deleted; hero renders server wording verbatim. | **Dead client-side summary shim** — `verdict.js compactSummary()` rewrites the old server wording, but since 0.68.03 the server already emits the compact sentence; the shim is dead code and a second wording source that can drift. Delete it once no cached pages depend on the old wording. | `surfaces/dashboard/static/verdict.js` | planning agent, 2026-07-14 |
| 3 | debt | **Quant evidence not fully persisted for deliberation** — all three roles receive the same rendered context, but the analyst `ScoreBreakdown.metrics` payload is not persisted in full into `Recommendation` / rendered by `veto_context`; full quant-signal availability unproven. Needs a typed bounded payload + a three-role capture test. May graduate to its own sprint at packaging. | S125 return notes (`docs/sprints/sprint-125-operator-chat.md`) | S125 handback, 2026-07-13 |
| 4 | enhancement | **Verdict warning rows should be links to their evidence** — clicking "2 pending flags" in the hero drill-down should jump to the Flags panel (Trading section); "4 degraded feeds" to the provider stage evidence. Each warning already carries a machine `code` (`pending_flags`, `degraded_feeds`, …), so the fix is a client-side code→anchor map: switch the target section, scroll to the panel, brief highlight. Do **after** S126 merges — it adds new warning codes in the same rendering path (map those too: deploy currency + bus health → Infrastructure). | `surfaces/dashboard/static/verdict.js` (`warning-rows` render); `index.html` section/panel ids | operator, 2026-07-14 |
| 5 | ~~error~~ **FIXED (0.69.01, merged `40e454b`)** — `allow_reuse_address=False` + loud taken-port exit; proven live (duplicate instance exits with the error). | **Dashboard binds a taken port silently — split-brain serving** — `wsgiref`'s default `allow_reuse_address` on Windows lets a second dashboard instance bind :8300 while an old one still owns it; the new process logs "operator chat connected" but the **stale** process answers the browser (observed 2026-07-14: a 07-12 pre-chat instance served "chat is not connected" for a day while the 0.69.00 instance sat idle). Fix shape: probe/fail loudly on a taken port (`allow_reuse_address = False` on `_ThreadingWSGIServer` + a clear startup error), so two instances can never share the socket. | `surfaces/dashboard/__main__.py`; diagnosis: GET `/api/chat` → `not found` from a server whose sibling logged "connected" | operator symptom + planning-agent diagnosis, 2026-07-14 |
| 6 | ~~error · priority~~ **FIXED (0.69.01, merged `40e454b`)** — all five alerts closed at source (statics allowlist dict; `public_message` attribute); CodeQL rescan on main shows 0 open error-level alerts; remaining step: make the gate a required check after its first green PR run. | **Security Findings gate red on every PR — five open error-level CodeQL alerts in the dashboard** — `py/path-injection` (high) at `surfaces/dashboard/app.py:166/167/171` (static-file + log route paths), `py/http-response-splitting` at `:170`, `py/stack-trace-exposure` at `:67`; none baselined, so the enforcing gate fails each PR (red since ≥2026-07-10; earlier 07-05 failures predate these alerts — confirm during fix). Double defect: (a) harden the code (normalize + allowlist static paths, sanitize the container param, no traceback bodies) rather than dismissing — the dashboard is 127.0.0.1-only today but is planned as a Container App; (b) the workflow is **not a required check**, so a red gate blocks nothing — make it required once the baseline is clean, or it stays a silenced alarm. | run 29301481423 (gate step exit 1); `gh api .../code-scanning/alerts?state=open` — alerts 90–93, 116 | operator ("last few CI jobs failed") + planning-agent diagnosis, 2026-07-14 |
| 7 | ~~enhancement~~ **FIXED (0.69.04)** — shared `ts()` renders `yyyy-MM-dd HH:mm UTC` (decision: stay UTC, labeled) in the containers table, job card, vitals next-fire, hero next-fire, and log drawer; positions snapshot line deferred to the S126-contested `app.js`. | **Human-readable timestamps across the dashboard** — raw ISO stamps (`2026-07-13T22:30:00+00:00`) render in the containers table (Last window), dispatcher job card (last/next), hero Next fire, and log drawer; operator asked for `yyyy-MM-dd HH:mm`. One shared client-side formatter; keep UTC and label it (ops times are UTC-anchored; decide UTC vs Melbourne at packaging — if local, label the offset). | `surfaces/dashboard/static/infra.js` (`containerRow`, job card), `verdict.js` (next fire), drawer subtitle | operator, 2026-07-14 |
| 8 | ~~enhancement~~ **FIXED (0.69.04)** — chip-styled external link, new tab + `noopener`, in the Positions panel header. | **Alpaca login link in the Positions panel** — a plain external link to the Alpaca dashboard login (paper account, `https://app.alpaca.markets/`) opening in a new tab (`target="_blank"` + `rel="noopener"`), placed in the "Positions — graph vs broker" panel header so broker-truth checks are one click from the graph view. Static link only — no credentials, no embedding. | `surfaces/dashboard/static/index.html` (positions panel), `app.js` (`renderPositions`) | operator, 2026-07-14 |
| 9 | error | **Live bus test fails instead of skipping without the azure extra** — `tests/test_bus_azure_receiver_integration.py` imports `azure.servicebus.management` inside the test body before any skip guard, so an env without the `azure` extra reads FAILED (`ModuleNotFoundError`), not SKIPPED (bit twice in fresh worktree venvs, 2026-07-14). Fix: `pytest.importorskip("azure.servicebus")` (or guard the import) so absence of an optional extra is a skip. | `tests/test_bus_azure_receiver_integration.py:97` | planning agent, 2026-07-14 |
| 10 | enhancement | **Flag lifecycle on the dashboard** — an ack/resolve affordance on each pending flag, routed through the operator's bounded `approve` command with the S125 confirm machinery ("6 flags pending" must be actionable, not just visible). Groundwork shipped 0.69.05: the vitals count and run panel are now FlagResolution-aware and a resolved flag renders `resolved` on its own run day. | operator approval 2026-07-14; `surfaces/dashboard/static/app.js` (flags panel) + `/api/chat` command path | operator, 2026-07-14 |
| 11 | error | **Operator interpret misroutes `approve <subject-ref>` to `status`** — both plain and steered phrasings produced a `status` intent for `approve broker-position-divergence:…` (same over-broad-status class as the S125 return note). Workaround used 2026-07-14: the CLI's direct-TypedIntent pattern (as `cmd_stage_promote`) dispatched `approve` through the supervisor gate successfully. Fix in the operator grammar/prompt + a routing regression test; check against Codex's in-flight S126 grammar changes first. | live interpret transcripts 2026-07-14; `agents/operator/domain/grammar.py` | planning agent, 2026-07-14 |
