<!-- Agent: planning | Role: archive — STATE history split out 2026-09-15 -->
# State archive 09 — the September proof run: item 3 closed, three gates that could not fail, and the stop flip

**Split out of [`../STATE.md`](../STATE.md) on 2026-09-15.** Nothing here is live. Chains from
[STATE-08.md](STATE-08.md).

Nine *Recent* entries moved, covering **S172, S173 Part A, S180, S191, S195, S196, S197, S198 and one
tooling repair — versions `0.94.03`–`0.97.00`, 2026-09-01 → 2026-09-05**. It is the stretch where the
deliberator finally ran its debates independently (S172, closing the longest-open queue item, **3**),
the veto's self-agreement became a number a script prints with its denominator (S173 Part A), three
gates that had never once fired were measured and made honest (S195's beta cap, S196's earnings
filter, S197's rendering), a deploy record stopped being able to name a commit that was never built
(S180), the acceptance view stopped flipping its verdict on a quiet night (S191), and the stop mode
flipped on an operator decision with the book set up to record whether it was right (S198). The
CodeQL entry belongs with them: the S172 merge raised an error-level alert on the enforcing Security
Findings gate the same evening, which is what the gate exists to do.

Split at **207 lines** — the S205 entry is what crossed it.

Entries are moved verbatim; the only edit is that relative links were re-rooted
`x.md` → `../x.md` for the new folder depth, as in the earlier archives.

---

🟩 **PROVEN RESULT — [S173](../sprints/sprint-173-a-verdict-must-be-reproducible.md) Part A MERGED `98cc45e` (`0.97.00`), 2026-09-05 — "does the veto agree with itself" is now a number a script prints, with its denominator, its exclusions and a confidence interval** ([DL-158](../design-log.md), [DL-159](../design-log.md), [DL-160](../design-log.md)). 🎯 **Validated against ground truth, which is the only reason to believe it:** the harness re-derives [DL-104](../design-log.md)'s hand-computed self-agreement **exactly** — `matched=9; compared=16; rate=56.25%`, through the same code path every other agreement number goes through. 🚨 **And the interval it adds changes what that number means: `ci95=[33.18%, 76.90%]` contains both 50 % and 75 %**, so the 56 % cited in three decisions (DL-105's sweep, the 2026-08-13 timeout incident, DL-140's rejected routes) can distinguish neither "chance" nor "acceptable". 🚨 **Measured before any code was written, and it changed the design:** a debate **cannot** be one batch of five requests — `render_debate_prompt` interpolates the transcript, so turn N+1's prompt contains turn N's answer, and the harness runs **five dependent rounds**, each one batch across every debate. 🚨 **The exclusion predicate is not a rounding correction: 83 of 329 approved orders on the spine are fail-opens — 25.2 %**, recorded as `verdict: "uphold"` and indistinguishable from a real ruling; every one of the other 246 has a real verdict, so the corpus splits exactly two ways. The recorded distribution is **61 % `revise` over 246**, not DL-104's 78 % over 58. 🪤 **The two graph stores return different Python types for one property** — in-memory freezes props (`dict` → `mappingproxy`, `list` → `tuple`), Postgres returns plain JSON, so an `isinstance(x, dict)` guard passes in production and fails in tests. Caught by a red test. 🟩 **DL-70 sweep: 10 mutations planted, and the first pass found two guards that did not guard** — `wilson_interval`'s clamps returned an in-range interval from a corrupted denominator (a clamp cannot correct a real Wilson bound, only hide a broken one; removed, and the interval is now pinned by value), and `no_counterpart` could be zeroed because its only covering test used a case where zero is correct. Re-run: **10 of 10 caught**. 🟠 **No clause moved, and that is the honest outcome:** `DLIB-IDM-02` was the candidate and measuring it produced **[DRIFT-056](../laws/drift-register.md)** instead — `prompt_hash` covers the *user* string only. 🟩 **Proven:** `make ci` exit 0 (2592 passed, 100.00 %), `GATE PROVEN` for the branch tip and re-proven on the merge commit incl. CodeQL and the image build, **0 open error-level alerts**. **No deploy — read-only offline tooling, decision 1 taken as NOT PERSISTED.** 🚨 **Part B ran one round and then stopped, and the reason is a 3.2× pricing error of our own making** ([DL-160](../design-log.md)). Round 1 — 2,961 requests, **2,957 succeeded** — cost **$58.05**, i.e. **$0.0196 per call** against DL-158's quoted **$0.0061**; round 2 was refused for **insufficient credit**. 🪰 **The estimate came off `LLMCall`'s word counts — work-queue item 43, ranked and not done — which exclude the system prompt, and the system prompts are wildly unequal: defender 434 chars, challenger 6,764, judge 6,341.** Round 1 is the only round that speaks with the small one, so calibrating on it understates everything after. Corrected: the funded design lands near **$331**. **DECISION (operator, 2026-09-05): stop, spend nothing more** — a choice made against a known price, not a deferral; the `effort`/`max_rounds` question stays deliberately unmeasured, and **item 9 is closed**. 🪤 **The $58 bought no verdicts** (a verdict needs all five rounds) but it bought the pricing correction, which the alternative discovery route priced at $331. Round 1's answers are on disk and would not be re-bought. 🟩 **Two fixes landed alongside:** `0.97.01` splits a round by **payload bytes** after round 2's single ~74 MB POST died with `APIConnectionError` (both documented Batch ceilings far away — a request *count* cannot work, because a judge request is several times a defender request); and **item 48 closed** — a duplicate key in a config pack is now refused rather than silently shadowed, all 11 packs parse clean, guard proven able to fail. 🪰 **My own process error, recorded:** the first crash reported `exit 0` because I ran the sweep through a `| grep` and read grep's code — the `make ci | tail` semantics CLAUDE.md documents, with the pipe mine rather than the Makefile's.

🟩 **PROVEN RESULT — [S197](../sprints/sprint-197-a-gate-says-what-it-examined.md) MERGED `dc79d05` (`0.95.03`), 2026-09-05 — the rendering defect [DL-154](../design-log.md) found is closed** ([DL-155](../design-log.md)). 🟩 **The measurement first:** over 120 bars to 09-03, **USB~BAC 0.7639** (correctly clustered), **USB~C 0.5650** and **USB~SCHW 0.3180** (correctly omitted), **AVGO~NVDA 0.4739**, **AVGO~INTC 0.4988** — all at **full 120-bar overlap**, so `min_correlation_bars` never bound and **a cluster of one was the correct answer**. 🚨 **The defect was that nothing could tell you that:** `cluster_issuers=AVGO` never reported how many issuers were examined, so *evaluated 8, none above threshold* and *evaluated nothing* were the same string — S183's skipped-vs-passed and [DL-152](../design-log.md)'s *producer knew something it never said*, in a **third** gate, this time costing a veto. 🟩 **Now fixed:** the outcome renders `examined_issuers`, `correlated_issuers` **with measured values**, the three strongest `below_threshold_top` near misses, `correlation_threshold` and `min_pair_overlap_bars` into the same `detail` the deliberator reads (`context_pm.py:95`, no truncation, no contract or vocabulary change — **image-only retag**). New clause **`PM-OBS-03`**, PM laws **v1.4**, rollup **29 / 48** derived by the gate, not by arithmetic. **Two guards planted, watched red, restored.** 🟩 **Proven:** `make ci` exit 0 (2508 passed, 6 skipped, 100.00 %), `GATE PROVEN` for `64764c73…` with the printed SHA checked against the branch worktree's `HEAD` and nothing committed above it. 🟩 **DEPLOYED `s198` 2026-09-05** (the full `up` above). 🪴 **This does not predict a trade** — objections (c) and (d) were CONFIRMED and are untouched; the next no-trade night will now name which one did it.

🟩 **Tooling repaired the same evening, after the S172 merge raised an error-level CodeQL alert.** `py/unused-loop-variable` **#178** (`_ticker`, not exempt where a bare `_` is) failed the enforcing Security Findings gate — **queue item 31's fourth instance**: `codeql.yml` runs only on `main`, so the branch was `GATE PROVEN` while the analysis that finds this class had never run against it, and CodeQL reporting `success` on the merge means *the analysis ran*, not that it found nothing. Fixed (`0.95.01`), alert `fixed` at 11:38:08Z, **0 open error-level alerts**. 🚨 **Then `make gate-ran` could not be satisfied at all:** it judged *every* run attached to a SHA, so a re-run left the superseded failure pinning the commit red forever — the commit that repairs a gate could never be proven. Now only the newest attempt per workflow decides (`0.95.02`, `scripts/gate_run_selection.py`), the printout shows what was judged, and `gate_selftest` **21/21** includes a case planting a *newer* failure so latest-wins cannot become ignore-failures. 🪤 **My first cut of that fix broke CI on clean infrastructure** — `from scripts.gate_run_selection import …` resolved locally off the editable install and raised `ModuleNotFoundError` in CI, taking all three `gate-ran-*` self-tests red at 18/21 and would have broken `make gate-ran` itself. Local green is not remote green; hardening row R, live.

🟩 **PROVEN RESULT — [S172](../sprints/sprint-172-independent-debates-run-independently.md) MERGED `3f29c1e` (`0.95.00`), 2026-09-04 — work-queue item 3 closed, 6b unblocked.** `debate_concurrency` is a bounded dial (`ge=1 le=25`) with deterministic reassembly, per-order fail-open isolation and a shared correlated reply inbox; `main` had no such setting at all, so K=1 is not the low end of the dial, it is the entire pre-merge world. 🚨 **It landed at K=1, not the branch's K=4.** The branch already carried `DELIBERATOR_DEBATE_CONCURRENCY: "4"` in `trading_tunables.json`, so the next full `up` would have deployed **the one configuration K>1 has never been proven in** — 1.78× of a possible 4× ([DL-145](../design-log.md)) and, on the earlier of two runs, **6 dead-lettered peer replies** with 2 of 15 orders failing open ([DL-140](../design-log.md)). The pack ships **1**; raising it is a deliberate pack edit plus an `up` ([DL-153](../design-log.md)). 🪤 **The speed case is gone anyway** — [DL-150](../design-log.md) re-measured 84 s/order, so serial fits the 1,800 s grace to 21 orders and recent nights approved 2, 2, 9, 7. **What merged is the option, not the behaviour.** 🟩 **Proven:** `make ci` exit 0 (2501 passed, 6 skipped, 100.00 %), `GATE PROVEN` for `1148091…` with the printed SHA checked against `HEAD`, nothing above it, security baseline untouched, MINOR bump correct. 🪤 **The catch-up merge is the cost of parking a branch:** forked 52 commits back at `2f4ff82`, five real conflicts, one semantic (S172's batch vs S194's `orphaned_reply_count` delta around the same loop — both kept), and **the `v1.2` law collision this file predicted did conflict** rather than merging silently: S194's v1.2 stands, S172's renumbered **v1.3**, rollup **15 / 52** — a number I wrote as 14 by arithmetic and the coverage check refused.

🟩 **PROVEN RESULT — [S196](../sprints/sprint-196-a-filter-that-can-answer-says-so.md) MERGED `41b9785` (`0.94.10`) **AND DEPLOYED `s196`**, 2026-09-04.** The provider now declares `MarketData.earnings_horizon_days` — a number means the earnings map is *complete* for that horizon, `None` means absence proves nothing — and the scanner reads absence as a **pass** only when that horizon strictly exceeds `earnings_exclusion_days`. It is withheld on an unrequested feed, on any `earnings_degraded` note, and when chunks disagree, because a degraded feed’s gaps look exactly like "no earnings due". 🟩 **Live-proven on `verify-2026-09-04-s196-earnings`: `skipped: NONE`** — all five scanner gates evaluated for **20 of 20** candidates, where the previous day `earnings_window` was skipped 20/20 and `max_beta` had never fired in 59 runs. 8/8 stages; `GATE PROVEN` for `41b9785`; `DeployRecord` `s196` written on the `.env` token alone. 🚨 **No trade followed, and that is the finding:** the deliberator vetoed `USB` and `AVGO` on the **substance** of the gates — `correlated_cluster_pct` rendering a **cluster of one**, the `{BAC,USB}` cluster omitting co-held `C`/`SCHW`, non-vol-adjusted sizing on a beta-2.152 name, and a stale-anchored `est_price` with no re-check at fill. Four checkable claims, filed as item **47**. 🪤 **Corroborated by the book:** AMD (−$3,614) and MRVL (−$1,330) are the two worst names ever held and together exceed the entire net realized gain — and AMD is exactly what the beta cap now drops.

🟩 **PROVEN RESULT — [S195](../sprints/sprint-195-the-beta-cap-gates-on-a-benchmark-it-actually-has.md) MERGED `92597aa` (`0.94.09`), 2026-09-04.** **The scanner’s `max_beta` gate has never fired in the fleet:** measured **0 evaluations across 59 stored `ScanRun` nodes** against **112 skips**. `compute_beta` was implemented and wired all along; the persisted snapshot carried `benchmark = ()` because `MARKET_FIELDS` never requested the field, and the graph-pull scanner reads that snapshot rather than fetching its own series — the in-process path does, which is why every unit test stayed green. 🚨 **It cost three nights of trading:** `sched-2026-09-01/-02/-03` each approved 2 orders and submitted **0**, the deliberator vetoing both names all three nights with byte-identical verdicts `{’AMD’: ‘revise’, ‘USB’: ‘revise’}` for want of a certified beta. AMD’s real beta over that window is **3.2877** against a **2.5** cap — it should have been dropped at the scanner. The `RunRequest` now declares the scanner’s benchmark ([DL-151](../design-log.md)); the provider ingests it once per batch. 🪤 **The first merge (`6b82092`, `0.94.08`) passed CI, CodeQL and Security Findings and then failed `build-images.yml`** — the slim dispatcher image lacked `agents/scanner/settings.py`; `92597aa` adds the `COPY` and replaces the hardcoded guard with a transitive read of the entrypoints’ imports. 🟩 **Proven:** local `make ci` exit 0 (2475 passed, 6 skipped, 100.00 %), and `make gate-ran` from `wt-main` printed `GATE PROVEN for 92597aa305180c2a047ccc54010cdb29c0a2f7c0` — CI, CodeQL, Security Findings **and the image build** all `success`, matching `git rev-parse HEAD`. 🟩 **DEPLOYED `s195`, 2026-09-04 14:55 AEST:** all **16** apps and `dispatcher-cron` on `:s195` (built from `89e879a`), **16/16 `Succeeded`**, KEDA preserved (`min=0 max=1`, one rule each), cron `30 22 * * 1-5` intact; vocabulary pack `d47e88b1…` identical to `s194`, so an image-only retag was the proven path. 🟩 **`DeployRecord` written and verified:** `deploy:2026-09-04T04:59:35…:s195:89e879a…` — S180’s tag-identity rule executing against the live API for the first time. It first refused with `GitHub build read failed (403)`: the `.env` `GITHUB_TOKEN` carries **`read:packages` only** (200 on `/user`, 4,899 of 5,000 remaining — **not expired, not rate-limited**; `/logs` returns `Must have admin rights to Repository.`). The `gh` CLI’s stored token (`repo, workflow`) reads that endpoint at 200/826 KB, and the record went through on it. 🪤 The two tokens are **different** — `gh auth token` echoes `GITHUB_TOKEN` back when it is set, which is how they first looked the same. 🟩 **Closed the same day (item 46):** `.env` now holds a fine-grained **Actions: Read-only** token, separate from the fleet’s `read:packages` pull credential — which the two had shared. Re-proven on `.env` alone: run logs `200`, `image_builds_for_tag("s195")` → `89e879a…`, and a wrong SHA refused. 🟩 **LIVE-PROVEN 2026-09-04 on `verify-2026-09-04-s195-beta`** (manual key, so tonight’s scheduled run is untouched): provider ingested **202 SPY bars**, `max_beta` went from **0 evaluations across 59 `ScanRun` nodes** to **20/20 evaluated**, and dropped **AMD at beta 3.186** and **INTC at 3.048** against the 2.5 cap — AMD being exactly the name that reached the PM and was vetoed three nights running. Run completed **8/8**. 🚨 **Trading still did not resume, and the cause is now singular:** the deliberator vetoed `USB` and `AVGO` **solely** because `earnings_window` was *skipped, not passed* (20/20) — work-queue item **45**, which S195 deliberately left. The beta objection is gone. 🪤 A deleted GitHub token revoked the fleet’s GHCR pull credential mid-procedure (`ImagePullBackOff`, measured); replaced across 17 targets and re-proven. KEDA windows restored.

🟩 **[S180](../sprints/sprint-180-a-deploy-record-must-name-the-commit-that-was-built.md) MERGED `fc2c799` (`0.94.07`), 2026-09-04** — `record_verified_deploy()` refuses a SHA that is not the head of a successful `build-images.yml` run **for the tag being recorded**; GitHub unreadable fails closed. Now exercised twice against the live API (`s195`, `s196`), accepting the built commit and refusing a wrong one. Detail in the sprint doc.

🟩 **PROVEN RESULT — [S198](../sprints/sprint-198-a-stop-we-never-measured.md) MERGED `b2fc22b` (`0.96.00`), 2026-09-05: the stop mode flips, and the book starts recording whether it was right** ([DL-156](../design-log.md), operator decision 2026-09-05). 🚨 **The finding that changed the decision:** ADR-0013 makes the S150 promotion an operator flip *on evidence*, and measured over **269 recorded recommendations** the evidence column was structurally empty — `known_outcomes` **0**, because `stop_target_observed_drawdown_pct` is declared in the vocabulary and read by `compare_stop_targets.py` and **no agent writes it** (item 49; [DL-152](../design-log.md)'s pattern in a fourth shape — a declared property with *no producer at all*, which reads exactly like “no outcomes yet”). 🟩 **The same measurement refuted [DL-77](../design-log.md)'s named trap:** RR pass rate **100 % in both modes** (flat **5.00 / 5.00 / 5.00**, scaled **3.00 / 5.45 / 8.00**), so widening stops does **not** silently stop AMD/MRVL/HPE being traded. 🟩 **Shipped together:** `ANALYST_STOP_TARGET_MODE=scaled` in the pack **and** `ANLZ-OBS-05` — the deepest close-to-low fall over a **declared horizon**, append-only, **absent** when unsettled and a **real zero** when the name did not fall. Analyst laws **v1.3**, rollup **25 / 48**; two guards planted, watched red, restored; `make ci` exit 0 (2524 passed, 100.00 %), `GATE PROVEN` for `9dba425…` and **re-proven on the merge commit** — CI, CodeQL, Security Findings **and the image build** all `success`, **0 open error-level alerts**. 🪴 **The flip is live, not vindicated** — the first settled windows will be short and few; state the denominator before acting on them. 🟩 **DEPLOYED `s198`, 2026-09-05 12:30 AEST, by full `up` — never a retag, because the vocabulary moved** (`d47e88b1…` → `2d1b2dbc…`, both hashes read before deciding; a retag would have met the S148/[DL-85](../design-log.md) fail-closed write guard mid-cascade). Carries **S197 + S198**. 🟩 **Verified:** **16/16** apps on `:s198`, **16/16 `Succeeded`**, **`ENV PRESERVATION` 16/16**, alembic OK, dispatcher on `:s198` with cron `30 22 * * 1-5` intact. 🎯 **The check that matters when the pack moves:** the *deployed* `GRAPH_VOCABULARY_B64` decodes **byte-identical** to the repo pack (`2d1b2dbc…`) and declares `stop_target_drawdown_horizon_days` — image and pack travelled together, so the fail-closed guard will accept S198's write. 🟩 **The flip is live:** `ANALYST_STOP_TARGET_MODE=scaled`, with `DELIBERATOR_DEBATE_CONCURRENCY=1` unchanged. `DeployRecord` written on the build run's own `70bf6662…`. 🎯 **Queue: empty by Friday 2026-09-18** (operator, 2026-09-05 — not 09-11). Next: **item 9 / S173**, the verdict-quality gate — the instrument for the `effort`/`max_rounds` cost question and the only way to read the **56 %** self-agreement. 🚨 **Verifying the deploy caught a real contradiction, and the scale diff is the only thing that would have** ([DL-157](../design-log.md)): both peer deliberators came up at `maxReplicas`/`desiredReplicas` **4** against a baseline of 1, because `deploy-agents.ps1` hardcoded `return 4` for them in S172's own infra commit — while the pack still declared `DELIBERATOR_DEBATE_CONCURRENCY=1`. **S172 withheld the fan-out in the code half and the infra took it anyway**, contradicting [DL-153](../design-log.md) and S172's own words (*“what merged is the option, not the behaviour”*). 🪴 **Same literal-default defect this file already fixed twice** — for env values (a 1 % position cap silently reverted to 10 %) and for the dispatcher cron — never carried to *scale shape*, so a new literal walked in beside the guard that exists to stop it. 🟩 **Fixed in the script, not the cluster** (`b0c1d4c`, no bump): `Resolve-DebateConcurrency` reads the pack, falling back to 1. Hand-setting the two apps back was **rejected** — it creates live state the script disagrees with, which is DL-100's own defect. Re-`up` re-run: scale now **byte-identical to the pre-deploy baseline, zero drift**, all three deliberators **1/1**. 🟠 **Nothing is proven live yet** — no run has executed under `s198`; the first is **Monday 2026-09-07 22:30 UTC**, and nothing ever ran against the 4-replica config.

🟩 **PROVEN RESULT — [S191](../sprints/sprint-191-a-quiet-night-gets-the-same-verdict-twice.md) MERGED `97e9fb5`
(`0.94.03`), 2026-09-01.** The acceptance view derives the approved-**buy** count from the linked
`PMRun.order_intent_set`, so quiet zero-order *and* sell-only `not_required` runs pass, while a `not_required`
beside an approved buy still breaches as `buy_veto_missing` and an unreadable PM payload fails closed.
🟩 **Verified independently:** claimed SHA **is** the tip, `GATE PROVEN` incl. post-merge **CodeQL**, PATCH correct.
🎯 **The real proof is live, not a fixture:** `sched-2026-08-31` — which returned `ACCEPTANCE FAIL` this morning —
now re-runs **`ACCEPTANCE PASS`**. 🟩 **DEPLOYED `s191`** by image-only retag: **16/16** on `s191`, **16/16**
`Succeeded`, scale **diffed identical**, cron intact, `DeployRecord` on the build run's `1f0b6a0…`. 🟩 **LIVE-PROVEN, AND NO CASCADE WAS NEEDED:** the view is imported only by `accept.py` and the
dashboard, and the fleet is **16 apps with no dashboard among them** — so it was proven over **all 55 runs on the spine**
rather than one run. Both race branches are covered by real runs; `sched-2026-08-31` **PASSes**.
