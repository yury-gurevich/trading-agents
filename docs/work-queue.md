<!-- Agent: planning | Role: the single ranked work queue — what to do next, in order, with provenance -->
# Work queue — the one ranked list

🚨 **DEADLINE, set by the operator 2026-09-05: this queue is to be empty by the **end of next week** — **Friday 2026-09-18**.** **15 items open** at `main` @ `0.98.05` *(recounted 2026-09-15: the "10" below was a miscount — 11 were open, and this review adds one)* — **four closed, two filed**, and the two filed are both *decisions* rather than defects. [S204](sprints/sprint-204-every-clause-declares-how-it-is-proven.md) closed **item 10**, the biggest carried row in the queue: every law clause now has a proof row and a rowless clause **fails the gate**. [S203](sprints/sprint-203-a-check-that-cannot-fail-says-so.md) closed **items 53, 56 and 41** — the last of the "a check that cannot fail" family except item 55, which S202 already made largely redundant. 🪰 **S203 also found the near-miss worth carrying forward:** the probe body its own spec prescribed returns **400 for a valid key** on a reasoning model, so copying a working probe between vendors is not safe without re-measuring the token budget ([DL-166](design-log.md)). New rows **57** (an inert pack field — decide whether it should be real) and **58** (the OpenAI probes can now fail and are still advisory) are both one-decision items, not builds. [S205](sprints/sprint-205-a-type-clause-names-the-fields-it-requires.md) then closed **item 30** on 2026-09-15 — the twelve contract-type clauses that were their own oracle now name the fields they require, and a deleted field turns a test red — taking the count **15 → 14**. [S208](sprints/sprint-208-a-risk-gate-says-what-it-can-reject.md) closed **item 61** and narrowed **item 60**, merged `d43280e` (`0.98.05`) on 2026-09-16 — leaving **15 open, recounted from the rows themselves**, which is also what the running prose above had drifted to claiming while 16 were open. It filed **no new queue row**: the two forced decisions S205 found (`CONTRACT.version` movement is ungated; five execution output clauses name fields the contract lacks) are **DRIFT-060** and **DRIFT-061** in the drift register, which already owns forced decisions of that shape. **2026-09-17:** [S210](sprints/sprint-210-a-concentration-gate-divides-by-the-book-it-measures.md) closed **item 65** (**15 → 14**), and reading `sched-2026-09-16` filed **items 66 and 67** — two small tooling defects, neither able to put a wrong order at the broker —  so **16 are open** — then **item 68** was filed the same morning for the correlation cutoff nobody owned, making **17**. **Later that day:** item **42** closed on live proof from `sched-2026-09-16`, and item **68** was measured ([EXP-008](research/experiments/EXP-008-correlation-cutoff-replay.md)) and re-sized to an ADR — **16 open**. Then item **31** was narrowed (its third mechanism closed, `6a67924`) and item **69** filed for line-ending drift found while fixing it — **17 open**. Then [EXP-009](research/experiments/EXP-009-volatility-sizing-replay.md) measured items 62/64 and found the regime classifier has never had a production input — filed as item **70**, **18 open**. Then item **71** was filed as **parked** by operator direction: the regime-probability track, for after the debt settles, so it does not count against the deadline — **18 open + 1 parked**. Then S211 merged (`7d3ff51`, `0.98.08`), closing item **60** — **17 open + 1 parked**. 🔁 **2026-09-18 — item 72 filed** (S211 deployed 17/17 but wrote no `DeployRecord`). 🪤 **The running "15 items open" has drifted and is not re-asserted here:** a marker recount of the live table finds **11 🟠 + 2 🔴 open, 27 🟩 done, 1 parked, and 7 rows whose cell starts with prose rather than a marker** — so the true figure needs one deliberate recount, not another guess. 🚨 **The deadline above is *today*.**

**Re-reviewed 2026-09-20 against `main` @ `880e825` (`0.99.00`), fleet on `s215`.** Counted from the rows themselves, which the running prose above had stopped doing: **14 open, 1 parked (71), 35 closed rows still in the table**. 🪤 **Corrected later the same day: 12 open, then 11 when item 31 closed that evening.** That count wrongly included **72**, which the row itself records as `CLOSED LIVE 2026-09-19` — read from the marker, not the cell — and **58** closed that afternoon when S219 merged and deployed. Counting from markers is what the header above had already drifted into; counting from the row text is the fix. 🔁 **Later 2026-09-20:** item **73** was **narrowed, not closed** - the repo now declares its markdown rules and every file passes, but 21 dead intra-document links were hand-repaired by the operator rather than by rewriting slugs wholesale, and the pre-commit hook now exists (pinned `markdownlint-cli2` v0.23.3). Item **74** was filed the same hour: `pip-audit` started failing on a newly published advisory with no dependency change ([DL-184](design-log.md)). 🟩 **Item 73 then CLOSED the same evening** — all three of its deliverables shipped (config, zero findings across 455 files, pinned pre-commit hook), merged `e3ebabe`. That leaves **11 open + 1 parked (71)**. 🟩 **Item 6b then closed too** — the posture flipped to `binding` ([DL-185](design-log.md)), leaving **10 open + 1 parked**; and **item 70's input half is proven live** (FMP `^VIX` 14.81 → `risk_on` through production code), with only the recording half owed to Monday's `sched-2026-09-21`. 🎯 **Then the whole table was re-counted mechanically, and the markers were the problem the header kept warning about:** **five** rows (**51, 58, 66, 67, 72**) still carried an open marker while their own text said closed — **58** among them, still claiming *“only S219 remains”* after S219 merged **and** deployed — and **four** more (**7, 22, 27, 33**) carried **no marker at all**. All nine fixed, so every row now states its own status and the count is mechanical rather than a reading. **The true figure is 10 open + 1 parked (71).** 💡 **Item 75 filed 2026-09-20 and deliberately does not move that count:** the defender/challenger grounding asymmetry ([DL-186](design-log.md)) is queued **after** the debt closes by operator direction, like item 71, so it is neither open work nor forgotten. 🚨 **The 2026-09-18 deadline above has passed and is not re-set here** — setting a new one without the operator would be inventing a commitment. **Four closed since the 2026-09-19 review:** **57** (S217 merged `f252dca`), **69** (S216 merged `9e39e40`), **11** (codex sandbox default), and **58 stopped being a decision** — it is now a three-sprint build (S217 done, S218 specced, S219 next) waiting on the operator's Telegram bot. **Item 73 is unblocked** by S216. 🪰 **Three of the fourteen are not work, they are a deploy:** **70** and **72** close and **64** unblocks when S217–S219 ship together — so the queue is shorter than it counts. 🪤 **Item 32 is a closed row still occupying a rank** (S190, 2026-08-31); it should be deleted at the next cleanup, not re-read.

**Recounted 2026-09-22 against `main` @ `b18014e`, fleet on the 2026-09-21 `up`: 8 open, 1 parked (71), 68 closed** — **79** closed this afternoon by [S223](sprints/sprint-223-an-envelope-says-what-it-checked.md) (`0.104.01`, `GATE PROVEN` for `b18014e`); the row total is unchanged at **77**, so this is that one row moving, not a re-derivation.
*(Prior recount, same day @ `663a89a`: 9 open, 1 parked, 67 closed — 8 after that recount, plus item **80**, filed the same day from the same run.)* Both counts are taken from the rows, not carried from the prose above. `sched-2026-09-21` closed **68** (the ramp's gate detail, live) and **70** (`vix_status=measured`, live). **31** was already closed in its own body on 2026-09-20 and only its marker lagged. **79** is now **shipped and closed** — it was built next exactly as ranked, and the `[WARN]` it would have baselined into never formed.

**Deploy, 2026-09-20 16:29 AEST — `s219`, S217+S218+S219 live.** This retires the "three of the fourteen are a deploy" note above: **58 closes**, **70**'s code is now on the fleet (its live `vix_status` proof arrives with `sched-2026-09-21`), and **64** unblocks on that same measurement. 🟠 **What the deploy did not prove:** the Telegram notice path has never fired in production. First exercise Mon 22:30 UTC = **Tue 22 Sep 08:30 Melbourne**.

**Re-reviewed 2026-09-15 10:45 AEST** against `main` @ `6e046e1` (`0.98.02`) and the fleet on **`s198`**,
triggered by reading `sched-2026-09-14`. Every open row was re-measured rather than carried. This review
**adds item 59 and ranks it first**, and it displaces the previous ordering for one reason: 🚨 **the pack
cannot satisfy the operator's etalon bar while item 59 stands, because the referee vetoes 100 % of what it
reviews and the only orders that have reached the broker since 2026-09-01 went through on nights the
referee was blind.** Items **33**, **11**, **7** and **31** re-measured **unchanged**; **22** re-measured
**worse** (12 status wordings, not three); **6b**'s blocking evidence has **arrived** and the row is now
gated on item 59 instead. 🪤 **One citation had drifted** — item 31's `assert_gate_ran.py:37` is now `:46` —
which is the S186 hazard the project already knows about, found here by re-reading rather than by luck.

**Re-reviewed 2026-09-01 11:56 AEST** against `main` @ `a9a447c` (`0.94.02`) and the fleet on **`s190`**.
This review **added items 38 and 39**, both discovered while deploying, and re-measured items 3, 6b and 36 —
all three of which were gated on `sched-2026-08-31`. That run has happened: it proved the deploy path and
**nothing about the LLM path**, because PM approved no orders so no debate was ever attempted.

**Re-reviewed 2026-08-31 16:40 AEST** against `main` @ `193e71b` (`0.94.02`) and the fleet on `s187`.
This review **closed item 34** (shipped the hour it was ranked) and then **item 32** — which had just
absorbed item 20 and item 12's Fill half — when S190 merged the same day. All three were re-measured
against the live spine first; item 12 survives only as the never-run `pg_teardown` delete path. Items 3, 6b and 36 are unchanged: all three are gated on `sched-2026-08-31`.

**Re-reviewed 2026-08-22 15:10 AEST** against `main` @ `12d15dc` (`0.91.01`) and the fleet on `s184`,
after `sched-2026-08-21` ([DL-125](design-log.md)). This review **promoted item 6 to first**, closed
item 18, corrected item 3's "unblocked" claim, and re-measured items 20 and 27.

**Reviewed 2026-08-19 20:40 AEST** against `main` @ `b84d221` (`0.90.15`) and the fleet on `s181`.
This review **deleted 11 closed rows**, folded **2 items into their parents**, re-measured every
carried number, and re-ranked.

> **This file is the only ranked queue.** The out-of-repo `..\debt.md` was **deleted 2026-08-19** —
> it was not a second opinion, it was a 2026-08-14 ancestor of this file, and everything in it was
> closed or superseded. That closes STATE's open item (ii) the way it was meant to close: by
> **removing a tracker, not reconciling one**. `docs/STATE.md` `## Next` is narrative around this
> ranking, never a second one. `docs/hardening-backlog.md` owns the dormant security rows and their
> unblock triggers; they appear here only so the ranking is complete.

**Reading it.** The table is **in rank order, best first**. The `#` is a **stable ID, not a rank** —
it never changes, because STATE and the design log cite these numbers. A gap in the sequence means
that item closed.

**Provenance per item:** **[measured 2026-08-19]** means checked against the live spine, the broker
or the code during this review. **[carried]** means the row exists but the claim has not been
re-measured — treat the number as suspect.

---

## The queue

| # | Work | Why it sits here | Size |
| --- | --- | --- | --- |
| 75 | 🅿️ **QUEUED, not parked — the judge has never been shown a correct `uphold`, and the defender has no examples at all** | *[measured 2026-09-20, [DL-186](design-log.md), operator hypothesis]* Champion system prompts are **defender 434 chars, challenger 6,764, judge 6,341**; only the challenger and judge carry the *“Preserve these distinctions”* list and the compiled examples, and the judge is additionally told *“if the Challenger catches a grounded implementation-specific flaw, do not uphold”*. 🎯 **So a meaningful part of the 78.9 % veto rate may be designed in rather than discovered** — a likelier driver than anything about the model. 🪰 **And the evidence chain has been circular at least once:** the challenger is told *“fixed-fraction sizing is not volatility-adjusted”*, it then argued exactly that on `sched-2026-09-15`, and ADR-0025 Decision B cited the veto as the referee being right. The flaw is real, but that was not a second witness. 💡 **The experiment the operator asked for (2026-09-20):** same `as_of`, same evidence, **defender-with-grounding as challenger against today's defender**, and measure whether the verdict distribution moves; a domain *persona* is the cheaper arm to test beside it. 🪤 **Persona is the weaker hypothesis and may backfire** — it raises the generic-finance-caution prior the challenger prompt spends words suppressing, so grounding and persona must be separate arms, not one change. 🚨 **Run it on recorded debates through the existing replay harness, not on live nights** — [S173](sprints/sprint-173-a-verdict-must-be-reproducible.md) Part A exists for this, and Part B was stopped at **$58.05 for one round** ([DL-160](design-log.md)), so the design must be the cheapest sufficient one. **Operator direction: schedule after the debt is closed** — it does not compete with the open rows. 🚨 **SHARPENED the same day — the few-shot examples are the stronger steer and they are one-sided.** Measured: **defender 0 examples; challenger 8, all objections; judge 8, of which `revise` × **8 — 100 %**, `uphold` **0**, `overturn` **0**.** Against the live spine, **353 rulings over 64 `DeliberationRun`s**: `revise` 168 (47.6 %), `uphold` 163 (46.2 %), `overturn` 22 (6.2 %) — a **7.6× revise/overturn ratio** reproducing the 7.5× ADR-0029 was written for. 🎯 **Traceable cause, not malice:** the examples were compiled from the **Class-1 library**, which [EXP-004](research/experiments/EXP-004-class1-cases-llm-judge.md) built *by construction* as a library of flaws — a flaw library contains no sound decisions, so examples drawn from it can only demonstrate objecting. 🪤 **This re-frames ADR-0029:** narrowing the block to `overturn` was right, but it treated `revise` over-production as a property of the referee when it may be an artifact of the only ruling ever demonstrated — which would make the *finding count* a weaker signal than it reads as. **So the arms are now ranked:** **(1) balance the judge's examples** (add worked `uphold` and `overturn` cases) — the strongest and cheapest lever; **(2) ground the defender**; **(3) domain persona**, weakest and may backfire. 🪰 **A false zero was caught measuring this:** `verdicts` is a `mappingproxy`, so `isinstance(x, dict)` is **False** and the first pass reported a confident **0 rulings** — DL-73's *audit on the predicate* lesson, third instance.| 1 experiment, after the queue empties |
| 74 | 🟠 **An unfixable advisory in a package no container installs is ignored by ID, and only the Makefile comment knows** | *[measured 2026-09-20, [DL-184](design-log.md)]* `pip-audit` began failing with no dependency change: **PYSEC-2026-2447** (CVE-2025-69872) against `diskcache` 5.6.3, which has **no fix release**. Measured reach: `diskcache` arrives only via `dspy` in the optional `optimizer` extra, and `optimizer` is installed by **zero** Dockerfiles (`runtime` 29, `azure` 13, `forecaster` 2, `llm` 2), so it reaches **no deployed container**; `dspy` also has **zero** import hits in the packages. Ignored by ID in `Makefile` and `ci.yml` rather than left red, because a permanently failing gate is one nobody reads. 🪤 **The ignore is bounded to one ID but has no expiry** - nothing re-checks whether a fix has shipped, which is the same cries-wolf-in-reverse shape as item 33's 57 baselined warnings. **Retire when** diskcache publishes a fixed release, or the `optimizer` extra is dropped. | small |
| 76 | 🟩 ~~**123 links between markdown files pointed at nothing, and no check in this repo looks at links between files**~~ | *[measured 2026-09-21, repaired the same hour]* A relative-link audit over all **459** tracked `.md` files found **123** broken targets — **112** of them in `docs/state-archive/`, where `STATE-0*.md` was moved down one directory and its `design-log.md` / `sprints/…` links were never re-prefixed — plus 4 dead same-file fragments in this file and 5 individually wrong slugs (ADR-0012, ADR-0023, S186, S197, S146, `LAW-02-proof.md`). 🎯 **The gap, not the breakage:** `make ci`'s 12 steps contain **no markdown step at all**, `make markdown` (markdownlint-cli2 v0.23.3, pinned) was **exit 0 with all 123 present**, and `MD051` only checks fragments **inside one document**. Nothing here has ever checked a link *between* files — item 73 closed the rules question, not this one. 🪤 **A mechanical `../` repair can produce a link that resolves and still lies:** `codeql/python-security/reports/INDEX.md:45` came out pointing at `codeql/README.md` under link text naming a file that does not exist; caught by comparing text against target, not by the resolver. **Retire when** a link check runs in the gate. Adding it edits a Python file, so it takes the **full cycle**, not the light path. | 🟩 **DONE — [S221](sprints/sprint-221-a-rule-the-repo-states-is-a-rule-the-gate-enforces.md), merged `863574e` (`0.103.01`, tag `v0.103.01`), 2026-09-21.** `GATE PROVEN` for `39c28c7` on CI, CodeQL and Security Findings. Both checks run in the `Makefile`, in `ci.yml` and in `gate_selftest_cases.py`; the self-test is **23/23** with each new guard rejecting its planted violation. |
| 77 | 🟩 ~~**The version scheme is a HARD RULE and no gate step reads the version at all**~~ | *[measured 2026-09-21, on the S220 handback]* [S220](sprints/sprint-220-a-correlated-issuer-counts-in-proportion.md) came back with `version = "0.103.0"` in `pyproject.toml`. The scheme is `MAJOR.MMM.PP` and the last group is **two** digits, so the correct value is `0.103.00`; `CLAUDE.md` calls breaking it *“a blocker — do not merge”*. 🎯 **`make ci` was exit 0 over it** — `grep` over the `Makefile` and `ci.yml` finds **zero** steps that read the version. It was caught by a human reading the diff, which is the work the gate exists to remove. 🪰 **A naive check would fail on a correct repo:** `uv` normalises trailing zeros, so with `0.102.00` in `pyproject.toml` the lock file records `0.102.0` — measured on `main` today. Read `pyproject.toml`, never `uv.lock`. **Retire when** a format check runs in the gate. Same family as item 76, and folded into the same sprint. | 🟩 **DONE — [S221](sprints/sprint-221-a-rule-the-repo-states-is-a-rule-the-gate-enforces.md), merged `863574e` (`0.103.01`, tag `v0.103.01`), 2026-09-21.** `GATE PROVEN` for `39c28c7` on CI, CodeQL and Security Findings. Both checks run in the `Makefile`, in `ci.yml` and in `gate_selftest_cases.py`; the self-test is **23/23** with each new guard rejecting its planted violation. |
| 78 | 🟠 **The 200-line module block does not look at `scripts/`, and 15 files there are already over it** | *[measured 2026-09-21, while merging S221]* `check_module_size.py` runs on `$(PKGS) tests` — `kernel contracts agents orchestration surfaces tests` — so **`scripts/` is outside it**, although `check_module_header.py` *does* cover `scripts/`. Measured: **15** of 83 files under `scripts/` exceed the 200-line hard block (largest `gate_selftest_cases.py` at **411**, then `compare_deliberation_prompts.py` **363**), and 6 more sit between 150 and 200. 🎯 **Found by the sprint that closed items 76 and 77** — S221's own `check_markdown_links.py` came in at **220** lines and nothing objected, which is the same shape as the two rules it had just taught the gate. **Not an S221 defect:** 15 files predate it. **Retire when** `scripts/` is inside the size check — which needs those files split first, so it is a chore then a one-line path change, in that order. | small — 1 chore, after a split |
| 79 | 🟩 ~~**The first envelope warning contradicts an ADR that is four days old, and two envelopes name values their own rail rejects**~~ **DONE** — [S223](sprints/sprint-223-an-envelope-says-what-it-checked.md) (`0.104.01`, merged `b18014e`, `GATE PROVEN` for `b18014e` on CI + CodeQL + Security Findings — fast-forward, so the merged SHA **is** the gated SHA) | *[measured 2026-09-21 on the S207 merge, [DL-195](design-log.md)]* S207's check produced exactly **three** breaches on its first run. 🪤 **Two are code defaults the fleet overrides** — `max_position_pct` warns at the default `0.10` while the fleet runs **`0.01`**, `max_positions` warns at `10` while the fleet runs **`60`** — because `envelope_warnings` reads `info.get_default()`, not the live value. The spec's scope said *live value*; the build **disclosed** the narrowing in `STATE.md`, so this is a stated deviation, not a silent one, and CI genuinely cannot see live values (it has no `.env`, which is the property the gate is trusted for). **What is left unowned: the two values the fleet actually trades on are checked by nothing.** 🚨 **The third breach is live and deliberate:** `correlation_threshold=0.50` is out of band against `envelope=(0.6, 0.8)`, and [ADR-0030](decisions/0030-the-correlation-gate-is-a-ramp-not-a-cliff.md) set that 0.50 **four days ago** — the parameter changed from a cluster *cutoff* to a ramp *floor*, and its evidence (*“r = 0.7 is a strong correlation”*) did not follow the change of meaning. Either the band is re-derived for a ramp floor or 0.50 is defended against the cited one. 🪰 **And two envelopes extend past their own rails:** `correlation_lookback_days` `(60, 252)` against `le=250`, `min_correlation_bars` `(13, 60)` against `ge=20` — bands that name values Pydantic rejects. 🪤 **Retire before it baselines:** an unanswered breach becomes a permanent `[WARN]`, which is item 33's 57-warning shape reproducing in the instrument built to prevent it. | ✅ **CLOSED 2026-09-22 — [S223](sprints/sprint-223-an-envelope-says-what-it-checked.md)** shipped at `0.104.01`. **Proven, not assumed:** `make ci` exit 0 re-run independently at merge review (2,997 passed, 6 skipped, 100.00 % coverage), and `GATE PROVEN` for `b18014e` on all three remote workflows. The checker now emits `declared_default=` so a warning cannot be misread as a fleet value; `kernel.tunable()` refuses an out-of-rails band at declaration (`kernel/tunable_envelope.py`, covering every rail the primitive has — there is no `lt`); both PM bands corrected **without moving a rail or a default**; `correlation_threshold` re-sourced to ADR-0030 / EXP-008 at `(0.50, 0.70)`. 🎯 **Envelope breaches 3 → 2 as designed** — the two honest out-of-band defaults survive; zero would have meant a default moved. 🪰 **The spec's `16` was wrong and stamped as measured**: there are **13** declared envelopes, pinned since S207 by `test_exactly_thirteen_risk_settings_declare_evidence_envelopes`. The build measured it, refused to invent three envelopes to fit, and filed the disagreement (`DL-196`) instead — the evidence discipline catching its own spec. Original spec note follows. 📦 S, PATCH, no deploy. 🎯 **Re-measured while speccing, and it sharpens the row:** both “false alarm” live values are *inside* their own bands (`max_position_pct` **0.01** ∈ (0.01, 0.05); `max_positions` **60** ∈ (30, 60), read off the live `portfolio-manager`), so the check is **2/3 noise** today. The rails sweep measured **2 of 13** declared envelopes outside their own `ge`/`le` (the spec said 16; corrected at build — 13 has been pinned since S207). The spec names the scope on every warning line, makes `tunable()` refuse an out-of-rails band at declaration, and re-derives the floor's band from ADR-0030 / EXP-008 — **no ADR needed**, because ADR-0030 already decided 0.50; only the envelope failed to follow. 🚨 **The live-value half is explicitly NOT in S223** — aligning the two PM defaults to the fleet is an operator decision about what an unconfigured deployment risks, and a live-reading check needs a runner with `.env`, which CI deliberately is not. |
| 80 | 🟠 **The operator trace tells a rejected order it also failed the gate it failed** | *[measured 2026-09-22 on `sched-2026-09-21`]* The run's PM block rendered `GOOGL  SKIP  sizing (also failed: sizing)`. 🎯 **Cause, and it is a second copy, not a typo:** `agents/portfolio_manager/domain/position_gates.py:110-114` is the authoritative gate→reason map and includes `"sizing": "sizing"`, while `orchestration/pm_rejections.py:16-27` keeps its own inverted copy, `_REASON_GATE`, which **omits `sizing`**. With no entry, `primary_gate` is `None`, so the failing `sizing` gate is not filtered out of the secondary list and is echoed back as its own “also failed”. 🪰 **Display only** — no order, gate or value is affected; the defect is in what the operator is told, which is the surface S212 was built to make trustworthy. 🪰 **`sizing` is the only reason that mismatches today**, checked against every `RejectedOrder` reason in the PM domain; the `risk.py` reasons (`hold_recommendation` and friends) carry no `gate_report`, so they render no suffix at all. 🚨 **The fix is not the missing key.** Adding `"sizing": "sizing"` silences this instance and leaves two copies of one map to drift again — the shape S222 just finished removing for the LLM adapters. Derive the display map from the PM's own, or move the mapping somewhere both read. | small — 1 chore; worth pairing with whichever sprint next touches `orchestration/` |
| 59 | 🟩 ~~**The referee blocks 100 % of the orders it actually reviews, and the ground it leads with is a comparison no gate performs — one the stop mode we shipped on 2026-09-05 makes impossible to pass**~~ **CLOSED 2026-09-16 by decomposition, not by a sprint** — root cause shipped in S206 + S208; the residue is items **60**, **62**, **63** and **65** | *[measured 2026-09-15 against the live spine, walking all 48 scheduled runs; [DL-167](design-log.md)]* 🚨 **Every order that has reached the broker since 2026-09-01 did so because the referee was broken — 9 of 9.** Split by `deliberation_status`: the five runs where it genuinely ran (`applied`, `real_debate_count=2`) submitted **0** orders each — `sched-2026-09-01, -02, -03, -04, -14`, reviewed 2 and vetoed 2 every time; the four that *did* reach the broker were all `applied_failed_open` — `sched-2026-09-08, -09, -10, -11` (2, 1, 1, 1 orders) — which is item 55's blind-night signature. **When the referee works, nothing trades; when it breaks, everything does.** 🎯 **All-time the rate has risen, not fallen:** 28 real-debate runs, **220 reviewed, 174 vetoed = 79.1 %** (`revise` 152, `uphold` 46, `overturn` 22) against [DL-119](design-log.md)'s 73 % — so [ADR-0023](decisions/0023-concentration-is-issuer-and-correlation-not-a-vendor-label.md)'s prediction that S184's issuer/correlation dimension would lower it is **measured false**. 🪤 **The lead ground is manufactured.** `_stop_regime_line` (`agents/deliberator/context_pm.py:132`) renders `target_pct >= base_take_profit_pct -> FAILED`, but under `mode=scaled` `_scaled_target = flat_target * (scaled_stop / flat_stop)` (`agents/analyst/domain/stop_target.py:86`), so `scaled_stop < flat_stop` ⟹ `scaled_target < flat_target` **always** — the target leg **cannot** pass whenever ATR tightens the bracket, and the stop leg **cannot** fail. Measured on `sched-2026-09-14`: WFC stop `4.14 % ≤ 5.00 %` PASSED, target `8.29 % < 10.00 %` **FAILED**; MDLZ `3.83 %` / `7.66 %`. Both carry `reward_risk = 2.0` exactly — the ratio the **real** gate checks — and it passed. 🪤 **No PM gate performs the failing comparison at all:** both approved orders carry 8 `gate_report` entries (`sizing`, `min_order_quantity`, `max_positions`, `cash_available`, `reward_risk`, `max_sector_pct`, `max_names_per_sector`, `correlated_cluster_pct`), **all `passed`**, and none named `stop_vs_regime_volatility`. 🚨 **This is [DL-104](design-log.md)'s defect class surviving in the leg [S175](sprints/sprint-175-the-veto-says-only-what-it-can-prove.md) left behind** — S175 deleted the invented `stop_pct vs ATR%` half of this same rendered line; the `target_pct` half stayed, and [S198](sprints/sprint-198-a-stop-we-never-measured.md)'s `ANALYST_STOP_TARGET_MODE=scaled` flip (2026-09-05, deployed `s198`) turned it from usually-true into **always-false**. 🪰 **Why nine days passed unseen:** the only runs between the flip and 2026-09-14 were the four blind nights, where the referee never read the line. **Fix direction:** render the comparison against the applied mode's own baseline, or render the counterfactual beside it — `StopTargetEvidence` already carries `mode`, `flat_*`, `scaled_*` and `counterfactual_*`, so the evidence exists and is simply not shown. 🪤 **Do not just delete the line** (the same argument would delete a real check), and **do not conclude the referee is merely harsh** until this is corrected — its other recurring grounds (non-vol-adjusted fixed-fraction sizing; `correlated_cluster_pct` omitting co-held names) are separate, repeated on every night measured, and not assessed here  📦 **SPECCED as [S206](sprints/sprint-206-a-rendered-verdict-names-the-check-that-produced-it.md)**, 2026-09-15 — scoped to the *truthfulness* of the rendered context only. 🪤 **The spec deliberately does NOT make "the referee stops vetoing" a success factor**: two to three further grounds per order are untouched, so whether the veto changes is a measurement the sprint reports, not a claim it makes. 🟩 **NARROWED, not closed — [S206](sprints/sprint-206-a-rendered-verdict-names-the-check-that-produced-it.md) MERGED `8c6f74a` (`0.98.04`), 2026-09-15.** The manufactured ground is gone: the context renders `stop_target_regime basis:` with mode, applied/flat/scaled values and the preserved ratio, and no verdict; `DLIB-NEV-08` forbids the class; `DRIFT-062` records why S175 left it. 🎯 **The measurement, reported either way as the spec required:** the WFC-only replay changed `revise` → `uphold` (5 calls, one order). 🚨 **What is still open is the item's actual subject — the 79.1 % veto rate.** One order flipping on a replay is not a rate, the other recurring grounds (non-vol-adjusted fixed-fraction sizing; `correlated_cluster_pct` omitting co-held names) are untouched and unassessed, 🟩 **DEPLOYED `s206` 2026-09-15 by image-only retag** (16/16 apps + dispatcher, all three injected packs byte-identical to `s203`, env and KEDA diffed unchanged), so the corrected context reaches the fleet from **`sched-2026-09-15`, 22:30 UTC** onward — and the required Anthropic activation probe was re-run live (**200**) so the referee will actually debate rather than halt. **Re-measure after that run and the next few**; item **6b** un-gates on the measurement, not on the deploy. 🟩 **RE-MEASURED 2026-09-16 on the live spine, as this row asked — the headline claim is now FALSE and the root cause is already fixed.** `sched-2026-09-15` ran the referee genuinely (`applied`, `real_debate_count=3`, `failed_open_count=0`) and it **upheld WFC**: 3 reviewed, 2 vetoed, **1 order to the broker on a night the referee was awake** — the first ever, against “0 orders each” on all five prior `applied` runs. 🎯 **The causal evidence is far stronger than the rate:** WFC was **vetoed 09-14 and upheld 09-15**, same ticker, adjacent nights. The 09-14 rationale *led with* “*the `stop_vs_regime_volatility` target leg is rendered FAILED on the record and the `reward_risk` ratio is a shape test that cannot discharge it*” — both halves being exactly what [S206](sprints/sprint-206-a-rendered-verdict-names-the-check-that-produced-it.md) and [S208](sprints/sprint-208-a-risk-gate-says-what-it-can-reject.md) removed; on 09-15 that ground is **absent** from the debate and the challenger fell back to a staleness argument the judge found ungrounded. 📊 All-time reproduces this row exactly, +1 run: **29 real-debate runs, 223 reviewed, 176 vetoed = 78.9 %** (revise 154, uphold 47, overturn 22) vs the 28 / 220 / 174 / 79.1 % recorded here — the rate barely moves because one night in 29 cannot move it. 🪰 **What is left of this row is not a build.** The two surviving 09-15 vetoes argue on ground already filed: AMZN is item **62** verbatim (“*sizing is a fixed-fraction notional cap, not volatility-adjusted*”) plus the item **60**/**63** confidence-floor complaint, and MDLZ surfaced a defect nothing had filed — now item **65**. **Recommendation: close 59 into 60/62/63 + 65 rather than sprint it**, or hold ~5 more runs if the rate itself must be re-proven. 🟩 **OPERATOR DECISION 2026-09-16: close 59 into 60/62/63 + 65 rather than sprint it.** The manufactured lead ground this row identified is gone (measured: absent from the 09-15 debate, present in every prior real-debate run), and every surviving veto argues on ground already filed. **The veto rate itself is deliberately left un-re-proven** — 78.9 % all-time stands as the last measurement, and if it matters later it is a measurement, not a sprint. Nothing carried forward from this row except the four successor items. | **closed — decomposed** |
| 65 | 🟩 ~~Three PM ratio gates cannot reject anything at the book's current deployment — not even if every holding were one perfectly correlated cluster~~ **DONE** — [S210](sprints/sprint-210-a-concentration-gate-divides-by-the-book-it-measures.md) (`0.98.07`, merged `1d515d8`, deployed `s210`) | *[measured 2026-09-16 against the live spine and the broker; surfaced by the referee itself on `sched-2026-09-15`]* 🎯 **`correlated_cluster_pct`: 38 recordings across every PMRun, 38 `passed`, zero rejections.** Highest value ever observed **0.0199** against threshold **0.25** — **8 % of the threshold**. 🚨 **It is not merely unexercised, it is structurally unable to bind today:** the gate divides cluster value by *total equity*, but the book runs at **$21,711.95 of $102,203.26 = 21.24 % deployed**, so **the whole book as a single cluster still reads 0.2124 and PASSES**. It can only start binding above **25 %** deployment. 🪰 **Two compounding causes, and the second is the subtle one:** the denominator is total equity rather than deployed capital, *and* the 0.70 correlation cutoff is binary — so MDLZ's genuine staples co-movers **KO 0.6402 / KHC 0.6107 / MO 0.5787** all register as `correlated_issuers=none`. 👁️ **Found by the referee, on the record, unprompted** — the MDLZ veto of 2026-09-15 called it “*a vacuous PASS, not evidence of independence*”, which is the second half of the operator's etalon bar working. 🪰 **Unlike item 61's `reward_risk`, this one *varies*** (34 distinct values across 38 recordings), so it looks alive on inspection and only the denominator arithmetic exposes it. Same family as item **61** ([S208](sprints/sprint-208-a-risk-gate-says-what-it-can-reject.md)), which closed the identical shape for a different gate. 🟩 **RULED 2026-09-16 — [ADR-0025](decisions/0025-concentration-measures-the-book-position-risk-measures-the-capital-base.md) (operator: “yes”).** Concentration divides by **deployed capital**; per-position risk keeps **equity**. Thresholds 0.30 / 0.25 unchanged. 🚨 **`max_sector_pct` folded in during the investigation — a second gate inert for the identical reason** (cap 0.30 vs max-possible 0.2124), found by re-measuring rather than filed as a new row. 🎯 **The migration was checked, not asserted: re-computed against `sched-2026-09-15`, every approval and rejection is unchanged** (Banking 0.0383 → 0.1803, Media 0.0274 → 0.1289, Food 0.0196 → 0.0922, all under 0.30) — the gates become *capable* of binding with no retroactive shock. 🪰 `portfolio.deployed_value` already exists (`portfolio.py:37`) and is already disclosed (`position_gates.py:97`); this is a denominator swap, not new machinery. 🪰 Same change should fix `portfolio.value` (`portfolio.py:32`), documented “equity-backed” but returning `self.cash.amount`. 🟩 **BOOTSTRAP RISK ACCEPTED 2026-09-16 — [ADR-0026](decisions/0026-a-small-book-is-allowed-to-be-concentrated.md) (operator: “Yes - accept the risk”).** Found while writing the sprint, not while taking the decision: under a deployed denominator a **flat book deadlocks forever** (allowance = 0.30 × 0 = 0), and **14 % of all 58 recorded runs had a book under 4 positions**, smallest ever **1**, with a deliberate flatten on 2026-08-07. Resolution: whole-gate `NOT_EVALUATED` below a floor **derived** from existing tunables (`names_cap × max_position_pct ÷ cap` = 10 % / 12 % of equity) — no new tunable, and today's 21.24 % book is above both. 📝 **Specced as [S210](sprints/sprint-210-a-concentration-gate-divides-by-the-book-it-measures.md).** 🟩 **PROVEN LIVE 2026-09-16 on `verify-2026-09-16-s210-a`, on the fleet.** `max_sector_pct` emits `denominator=deployed_capital` and discloses both denominators; Banking with WFC in-batch read **0.185255** against the 0.30 cap — hand-checked `(1915.80+986.86+1011.24)/21127.19` — versus **0.0383** under the old equity denominator. `sizing` still divides by equity (AMZN `0.009720`) and still rejects (GOOGL `0.016869`). Recorded in [`laws/functionality-checks.md`](laws/functionality-checks.md). 🟠 **The below-floor `NOT_EVALUATED` path is unit-proven only** — the book is 21.24 % deployed, above both derived floors, so it did not run live and will not until the book is small. | **closed** — built, deployed and proven live; one residue noted (below-floor path unit-proven only) |
| 60 | 🟩 ~~**`reward_risk` now discloses its fixed ratio — independent target derivation remains a policy decision**~~ **DONE — [S211](sprints/sprint-211-reward-risk-compares-two-measured-quantities.md) implements ADR-0027 Correction 2 / EXP-011 as disclosure-only by default** | *[built 2026-09-17 in S211]* Analyst targets use measured trailing favourable excursion and PM records the built target/stop ratio in every `reward_risk` gate report, but `min_reward_risk_ratio=0` means the default cannot reject on that ratio. EXP-011 found the ratio does not predict returns over 47,485 historical decisions and that the returned positive floor would reject a median 49 % of names per night, so rejection now requires an explicitly configured positive floor and new evidence. `ANLZ-OBS-06` and `PM-OBS-05` are green; `StopTargetEvidence` carries target estimate/window/sample evidence; vocabulary moved, so deploy is full `up`. See the sprint closeout for exact gate output. | **closed — built in S211; deploy still owed** |
| 61 | 🟩 ~~**One thinly-overlapping holding disables the correlation gate for the whole candidate — it has never once failed, and it was evaluated on 31 of 181 orders**~~ **CLOSED by S208 — unusable pairs are skipped pairwise, and all-unusable evidence remains `NOT_EVALUATED`** | *[measured 2026-09-15 across all 48 scheduled runs]* `_unevaluated_pair` (`agents/portfolio_manager/domain/correlation.py:98`) walks held issuers and returns `NOT_EVALUATED` for the **entire** gate on the **first** issuer whose overlap is under `min_correlation_bars = 60`. So a single short-history holding switches off correlation checking for every candidate that night. Census: **31 passed, 0 failed, and absent from the other ~150 order evaluations**. 🟩 **`NOT_EVALUATED` is the honest state** and [DL-73](design-log.md)/PM-NEV-09 already forbid reading it as a pass — the defect is the **scope of the disable**, not the honesty. 🪰 The referee's recurring objection is the same one from the other side: it reported `cluster_issuers=AVGO` alone and `{BAC,USB}` while C and SCHW were held, and called the `0.70` cutoff thin on an **81-bar** overlap. **Fix direction:** evaluate pairwise and skip only the unusable pair, rather than abandoning the gate. 🚨 **RE-MEASURED 2026-09-15 — the carried census was an artefact, and the defect is latent, not live** ([DL-169](design-log.md)). *"31 of 181"* counted a population that mostly predates outcome recording: only **35** `OrderIntent` nodes carry outcomes at all (from 2026-08-20), and the correlation gate is present on **35 of 35** — **all passed, 0 failed, 0 not-evaluated**. Observed `min_pair_overlap_bars` is **81–82** against the threshold of **60**, so **the abort path has never once fired in production**. The code defect is real; its live cost so far is **zero**. 🟩 **S197's census is working and already discloses the near-misses** the referee complains about (`below_threshold_top=C:0.6650…` beside `correlated_issuers=USB:0.7054`), which makes the `0.70` cutoff a `/tuner` question rather than a defect. 🟩 **CLOSED by [S208](sprints/sprint-208-a-risk-gate-says-what-it-can-reject.md):** usable pairs are evaluated even when one pair is below `min_correlation_bars`, skipped pairs are attributed with issuer and overlap, and the whole gate still reports `NOT_EVALUATED` only when no usable pair remains. | closed |
| 62 | 🟩 **Position sizing caps dollars, not risk — the referee has said so on every night it has run, and it is textbook** | *[measured 2026-09-15]* `sizing` tests `issuer_value <= max_position_pct x portfolio_value` with **`MAX_POSITION_PCT = 0.01` live** (the code default is `0.10`; the fleet runs 10x tighter). It is a **fixed fraction of portfolio dollars** and takes no account of how far the stop sits. Measured consequence: a 1 % position in AMD (ATR 4.00 %, stop 7.995 %) risks **~0.08 %** of the book, while 1 % in MDLZ (ATR 1.87 %, stop 3.74 %) risks **~0.037 %** — the same dollar cap carries **2.1x** the loss exposure. 🪰 **The referee names this every single night** (*"fixed-fraction 0.01 sizing caps dollars not volatility-adjusted risk"*), and it is the standard risk-parity critique, not a model quirk. 🪤 **This is a policy decision, not a defect** — position sizing by `risk_budget / stop_distance` is a different sizing philosophy with its own consequences (it buys *more* of a calm stock), and ADR-0012's wall makes it a trading-pack decision. Rank it behind item 59, whose fix is free of policy 🟩 **RULED IN DIRECTION 2026-09-16 — [ADR-0025](decisions/0025-concentration-measures-the-book-position-risk-measures-the-capital-base.md) Decision B (operator: “yes”): yes, volatility should scale position risk, and a regime label that moves no risk number is not a regime input.** **Items 62 and 64 are one question (sizing half)** and are answered together. 🚨 **No implementation is authorised by that ruling** — it changes every position size on every run and the stop/target bracket is *already* ATR-scaled (`applied_mode=scaled`), so two volatility adjustments could compound invisibly. Next step is a champion–challenger measurement on one `as_of`, not a sprint. 🧪 **Measured 2026-09-17 ([EXP-009](research/experiments/EXP-009-volatility-sizing-replay.md)):** over 61 approved buys (2026-08-17 → 09-16) iso-risk sizing cut per-position risk dispersion **4.2× → 1.9×** and the worst loss by 30 %, but lost **$445 more** than today's sizing (AMD alone −$255; calm names fell). No compounding at book level (total planned risk held). Whole-share rounding and the 8 % stop cap blunt any adoption. **Don't adopt on one window**; re-measure over a window with a down-leg before an ADR. 🧪 **RE-MEASURED 2026-09-18 — [EXP-012](research/experiments/EXP-012-volatility-sizing-ten-year-replay.md): the precondition is met and the answer is NO.** 47,549 decisions on EXP-011's cached SIP bars (2017-2026, no network/LLM, zero cost). Iso-risk delivers its dispersion claim (**6.40x → 2.00x**, CV 0.36 → 0.09) and still loses **$11,619**; ticker-cluster bootstrap **[-$19,210, -$4,186], 100 % of resamples negative** — EXP-009's one month (-$445, 98 %) was not a period effect but an understatement. 🎯 **Down-legs confirm the direction and refute the magnitude:** +$3,006 in the 2018 Q4 leg and +$1,112 in the 2022 bear, but **-$2,161 in the COVID crash** (a gap-driven fall punishes share count, not stop width), netting +$1,957 against -$11,619. 🪤 **Mechanism, and the reason no retune helps:** mean 10-session return rises with stop width — **D2 +0.092 % → D9 +0.532 %**, volatile names returning ~5x calm ones — so iso-risk buys *down* the return gradient by construction. No variant rescues it: the 2 % notional cap **never binds** (as in EXP-009), a 50/50 blend is -$7,972, and capping iso-risk at 1 % notional is **-$24,541** (it only ever cuts). 🚨 **This contradicts [ADR-0025](decisions/0025-concentration-measures-the-book-position-risk-measures-the-capital-base.md) Decision B** (*"yes, volatility should scale position risk"*, operator 2026-09-16) **on returns** — confirming or amending that ruling is the operator's, and nothing shipped on it. 🪰 **Same shape as EXP-008:** the referee's nightly *"fixed-fraction, not volatility-adjusted"* ground is **true and costly to act on**. Caveats that bound it: population is all grid decisions (not PM-approved buys), **survivorship** (today's 98 names back-projected) bounds the decile result specifically, and overlapping positions make the dollar totals an arm-vs-arm comparison, not a forecast. 🟩 **CLOSED 2026-09-18 — [ADR-0025](decisions/0025-concentration-measures-the-book-position-risk-measures-the-capital-base.md) *Correction* withdraws Decision B's directional acceptance on EXP-012's evidence.** Fixed-fraction 1 % notional **stands as champion**; nothing shipped on Decision B, so nothing is unwound. The withdrawal is **scoped, not universal** — volatility-scaled sizing stays standard practice and is measured negative only for *this* configuration (10-session horizon, 2 × ATR stop clamped 2.5–8 %); re-open if any of those three change. The 62/64 bundle is dissolved: the regime half went to [ADR-0028](decisions/0028-the-regime-reads-vix-from-fmp-and-says-when-it-cannot.md), the sizing half ends here. | **CLOSED — ADR-0025 Correction (no code change; null-check test only)** |
| 63 | 🟩 ~~**Twelve of thirteen risk bounds restate the type rather than bounding risk, and none records why it is there**~~ **DONE** — [S207](sprints/sprint-207-a-bound-cites-the-evidence-that-set-it.md) (`0.104.00`, merged `d0b4cee`, `GATE PROVEN` for `c484aea` on CI + CodeQL + Security Findings) | *[measured 2026-09-15 via `describe()`]* `max_position_pct` declares `le=1.0` — **it permits the entire book in one name**; `max_sector_pct` and `max_correlated_cluster_pct` likewise; `max_positions` allows `ge=1` (a one-name portfolio); `base_min_confidence` allows `ge=0.0` (accept everything). **Only `base_stop_loss_pct` (`le=0.08`) would stop anything**, and it is the one whose `why` names a risk cap. 🎯 **LAW-05 `DD-04`** requires a decision to cite the evidence backing it *so it can be re-evaluated when the evidence changes* — `tunable(why=)` does that for the **default** and nothing does it for the **bound**, which LAW-05 calls a defect rather than a detail. 🟩 **The anchors now exist**: [risk-parameter-bounds](research/risk-parameter-bounds/INDEX.md) records UCITS 5/10/40, the diversification literature, the swing ATR band and breakeven win-rate math, with sources. 📦 **SPECCED as [S207](sprints/sprint-207-a-bound-cites-the-evidence-that-set-it.md)** — which adds the `envelope=`/`source=` declaration and a **warning** check, and deliberately **changes no bound and no default**: tightening a rail is capital-safety and therefore ADR-only. 🪤 The follow-on is an **ADR**, not a sprint. 🟩 **Spec re-verified 2026-09-21 against `774cfbe` and handed back ready:** every measured number still holds (`kernel/config.py` 119 lines, baseline exactly 57, bounds unmoved, zero `default=` call sites), and four pieces of drift were corrected *in* the spec — **`DRIFT-063` was already taken** (the register runs to `DRIFT-070`, so the spec now files `DRIFT-071`, which is the [DL-148](design-log.md) duplicate-number harm avoided rather than repeated), **“12 CI steps” → 14** (S221 added two), **228 → 232** `tunable()` call sites, and the stale *“S206 is in flight”* note. The sprint **number stays 207** — a stable ID, not an execution order | **closed — the instrument shipped; the ADR that tightens a rail is still owed, and item 79 is its first input** |
| 64 | 🟠 **The regime label is computed, stored and carried — and never changes a single risk number** | *[measured 2026-09-15, [DL-169](design-log.md)]* `agents/provider/agent.py:155-161` builds `RegimeContext` with `label` and `vix` alongside `base_min_confidence`, `base_stop_loss_pct`, `base_take_profit_pct` and `base_max_holding_days` — but **every one of those four comes straight from settings**, unmodulated by the label it ships next to. So `calm`, `neutral` and `stressed` produce **identical** risk parameters, and "regime-aware risk" is nominal. 🎯 **This is what makes item 60 unfixable by threshold:** `base_take_profit_pct / base_stop_loss_pct` is a pair of global constants, so `reward_risk` is 2.0 in every regime. 🪤 **Not a defect to patch — a policy question.** Whether regime should widen stops, lower the confidence floor or shorten the holding window is a capital-risk decision owed an **ADR**, and it interacts with items 60 and 62. 💡 **The cheap first move is measurement, not policy:** the spine already holds 78 `Regime` rows, so *how often has the label actually changed?* If it is one value on every run, the question is moot until the classifier earns it 🟩 **RULED IN DIRECTION 2026-09-16 — [ADR-0025](decisions/0025-concentration-measures-the-book-position-risk-measures-the-capital-base.md) Decision B (operator: “yes”): yes, volatility should scale position risk, and a regime label that moves no risk number is not a regime input.** **Items 62 and 64 are one question (regime half)** and are answered together. 🚨 **No implementation is authorised by that ruling** — it changes every position size on every run and the stop/target bracket is *already* ATR-scaled (`applied_mode=scaled`), so two volatility adjustments could compound invisibly. Next step is a champion–challenger measurement on one `as_of`, not a sprint. 🚨 **Re-framed 2026-09-17 ([EXP-009](research/experiments/EXP-009-volatility-sizing-replay.md)):** the label has **never had a production input**. 50/50 scheduled runs were `neutral` because no live source supplies VIX. That's item **70** (specced as S213). Until it ships, "should regime scale risk" has nothing to scale by. 🟩 **SHAPE DECIDED 2026-09-20 — [ADR-0031](decisions/0031-regime-scales-the-risk-budget-atr-keeps-the-stop.md): the regime scales the **risk budget** and nothing else; **ATR keeps the stop distance**; `max_position_pct` stays a flat ceiling.** One volatility measure per quantity, so the two **compose instead of compounding** — which is precisely the interaction risk ADR-0025 Decision B refused to take. 🎯 **It is decidable now because both halves were read rather than assumed:** `size_quantity` (`agents/portfolio_manager/domain/sizing.py:25`) is `(portfolio_value * max_position_pct) // est_price` — a **pure notional fraction with no stop distance in it** — while the bracket is separately ATR-scaled, so volatility today touches **one** quantity and sizing touches **none**. Rejected: widening stops (ATR's quantity, and undetectable in the artefacts), moving `min_confidence` (conflates volatility with conviction), shortening the holding window (unmeasured), and naming multipliers in an ADR (that is an experiment's output). 🪰 **Also measured:** the fleet runs `max_position_pct=`**`0.01`** with `MAX_POSITIONS=60`, not the code defaults 0.10/10 — up to 60 names at 1 % each. 🟠 **Remaining: the multiplier**, by champion–challenger on one `as_of` under a **non-neutral** regime — obtainable for the first time now that `s219a` carries S213; item **70** is still the dependency. No sizing change is authorised until then.| **narrowed — shape decided by ADR-0031; the multiplier is an experiment, still gated on item 70** |
| 68 | 🟩 ~~**The correlation gate is a cliff at 0.70 — a 0.68 co-mover counts as zero, a 0.72 one counts in full, and the referee now leads with it**~~ **DONE** | *[measured 2026-09-17 on `sched-2026-09-16`, all 31 real-debate `DeliberationRun`s]* `PORTFOLIO_MANAGER_CORRELATION_THRESHOLD = 0.70` (`agents/portfolio_manager/settings.py:105`, pack `trading_tunables.json:16`) is a **binary inclusion test**: an issuer at or above it joins the cluster at full value, one below it contributes **nothing**. Measured on last night's four approvals: **WFC** clusters USB at `0.7172` but ignores C at `0.6788`; **AMZN** clusters alphabet at `0.7031` and ignores MSFT `0.4607`; **MDLZ** reads `correlated_issuers=none` beside KO `0.6397`, KHC `0.6097`, MO `0.5811` — the whole staples complex, invisible. 🎯 **Nobody owns this.** [S210](sprints/sprint-210-a-concentration-gate-divides-by-the-book-it-measures.md) closed item 65, whose row named the binary cutoff as the *second* compounding cause — but ADR-0025 ruled on the **denominator** only, and S210 changed nothing else; item 61 called the cutoff *“a `/tuner` question rather than a defect”* and closed without filing it. 🚨 **It is now the referee's lead ground:** narratives citing the cutoff appear in **5 of 31** real-debate runs all-time, but in **every** real-debate run since 2026-09-14 (MDLZ 09-14, AMZN 09-15, AMZN on both 09-16 runs) — last night's AMZN `revise` opened with *“a binary >0.70 inclusion test that discards MSFT 0.4607/DIS/NFLX entirely rather than haircutting co-movement”*. S197's census already discloses the near-misses (`below_threshold_top`), so the evidence is on the record and simply not used. 🪤 **Two different questions — do not merge them:** (a) *where* the cutoff sits is a `/tuner` measurement on one `as_of`; (b) *binary vs weighted* (cluster value scaled by correlation) changes the gate's meaning and is a capital-risk policy call owed an ADR, alongside [ADR-0025](decisions/0025-concentration-measures-the-book-position-risk-measures-the-capital-base.md). 🪤 **Do not just lower the threshold** — at 0.60 MDLZ's cluster jumps to four names and the gate starts binding where it has never bound, which is exactly the retroactive shock ADR-0025 checked for before moving the denominator. **Measure first:** re-compute the last N PMRuns under 0.60 / 0.65 and under a weighted cluster, and report how many approvals flip. 🟩 **MEASURED 2026-09-17 — [EXP-008](research/experiments/EXP-008-correlation-cutoff-replay.md).** Replay of **17 scheduled runs / 38 approvals**, validated **38/38** against the recorded cluster membership and value. 🎯 **No binary cutoff from 0.70 down to 0.50 rejects a single approval** (worst share 0.206 against the 0.25 cap); a 0.5→0.9 ramp rejects none; counting every positive ρ rejects **MDLZ ×5** and turns the check into book-wide co-movement. 🪤 **Near the line the verdict is sampling noise:** on 16 pairs with 0.50 ≤ ρ < 0.90 the 95 % interval spans 0.70 on **12**, the two halves of the window disagree on **7** — and dropping the five largest market days moves mean ρ by **+0.001** (flips 2), so it is **not** outlier days. **The referee's ground is true but inert:** no retune lifts a veto. What remains is an ADR on the gate's *meaning* (cliff, ramp or weighted), not urgent. 🟩 **DECIDED 2026-09-20 — [ADR-0030](decisions/0030-the-correlation-gate-is-a-ramp-not-a-cliff.md): the cluster becomes a ramp, 0 at ρ=0.50 rising linearly to 1 at ρ=0.90.** Chosen on EXP-008's own numbers: the ramp rejects **0 of 38** replayed approvals — the same no-retroactive-shock bar ADR-0025 applied to the denominator — and its **worst share is 0.098 against the cliff's 0.102**, so it is *not* a loosening. 🎯 **The decisive reason is epistemic:** the 95 % interval spans 0.70 on **12 of 16** near-line pairs, so a binary test asserts a distinction the estimate cannot resolve. **Weighted ρ⁺ was rejected on measurement**, not taste — it rejects MDLZ on all 5 nights it was approved, worst share **0.300 above the 0.25 cap**, and turns a cluster check into a book-wide co-movement check. 🪤 **Newly measured while deciding:** the threshold is **pack-injected** (`trading_tunables.json:16`, confirmed live at `0.70` on the `portfolio-manager` app), so adding a ceiling key **moves a pack and the implementing deploy is a full `up`, not a retag** — the S202 trap. 🟠 **Remaining: build the ramp**, with a fresh EXP-008 replay on the book as it stands then; 0/38 was measured on a 17-run history, not promised for the future.| 🟩 **BUILT AND MERGED — [S220](sprints/sprint-220-a-correlated-issuer-counts-in-proportion.md), `f3993d6` (`0.103.00`, tag `v0.103.00`), 2026-09-21.** `GATE PROVEN` for `01af4c9` on CI, CodeQL and Security Findings. 🟩 **ADR-0030's pre-merge replay met on a larger book than the ADR used** — 22 runs / 45 approvals, membership identical 45/45, ramp rejects **0 of 45** at worst share **0.098** against binary 0.70's **0.106** (the bar was `0.102`; the cliff drifted past it and the ramp did not). 🟠 **DEPLOYED 2026-09-21** at `7d5528e` — full `up`, all 15 agents + master verified at that image, `Succeeded`/`Running`. **Live pack read-back:** `CORRELATION_THRESHOLD` **0.70 -> 0.50**, `CORRELATION_CEILING` **absent -> 0.90**. 🟩 **Replay against production (read-only):** 24 recorded censuses / 10 `PMRun`s, **19 change**; the 2026-09-14 MDLZ case goes `none` -> **0.8380**. 🪰 **39 near-miss issuers still count zero below the 0.50 floor, incl. MSFT 0.4607 (AMZN)** — ADR-0030 as designed. 🟩 **CLOSED — the run exists** *[measured 2026-09-22 on `sched-2026-09-21`, the first scheduled run after the 2026-09-21 full `up`]*: both approvals' `correlated_cluster_pct` detail carries `correlation_ramp=0.5000..0.9000`, and it is **load-bearing, not cosmetic** — **DOW** reads `correlated_issuers=XOM:0.6235:w0.3087`, an issuer the 0.70 cliff counted as **zero**, now contributing `cluster_weight_total=0.3087` of `cluster_value_usd=1307.12` against `deployed_portfolio_usd=21919.46`. **META** reads `correlated_issuers=none` with `below_threshold_top=alphabet:0.4208,AMZN:0.3744,KHC:0.1836` — the ADR-0030 floor as designed, not a miss. `trace_run.py` **8/8**, **0** faults in the window. *Originally: narrowed — decided by ADR-0030; a small PM sprint + law cycle remains; specced 2026-09-21 — ramp floor `0.50`, ceiling `0.90`; law cycle amends `PM-NEV-08` + `PM-OBS-03` (laws v1.7 → v1.8); the tunables pack moves, so the deploy is a **full `up`**, not a retag. 🪤 **The EXP-008 re-replay ADR-0030 requires is planner-side, not the builder's** — a worktree has no `.env`, so the spec carries a unit-scale stand-in and names the live replay as a pre-merge gate.* |
| 69 | 🟩 ~~**55 tracked files have CRLF or mixed line endings and nothing stops more — including 11 Python modules** | *[measured 2026-09-17, `git ls-files --eol`]* **39 `i/crlf` + 15 `i/mixed`** of 1,712 tracked files (`i/lf` 1,640), **11 of them `.py`** — e.g. `agents/analyst/agent.py`, `agents/scanner/agent.py`, `agents/reporter/agent.py`, `codeql/scripts/codeql_errors.py` — plus `.secrets.baseline`, `.github/codeql-config.yml` and, until `3144a74`, `.github/workflows/codeql.yml`. **The repo has no `.gitattributes`**; the system git config sets `core.autocrlf=true` and the user's global overrides it to `false`, so what gets committed depends on which tool wrote the file. 🚨 **It is live, not historic:** on 2026-09-17 S211's in-flight worktree had flipped **all of `docs/work-queue.md`** to CRLF while changing one row, and a file-edit tool here wrote CRLF into `codeql.yml`. A flip turns a one-line change into a whole-file diff, hides the real change in review, and makes a 3-way merge against any other edit to the file conflict on every line — the IDE-autoformat hazard already recorded in memory, with a second cause. **Fix direction:** one chore — add `.gitattributes` with `* text=auto eol=lf` (explicit `-text` for binaries; keep `*.ps1` LF unless a Windows-only consumer needs CRLF — check first), then `git add --renormalize .` as its own content-free commit verified by an empty `git diff --ignore-cr-at-eol`. 🪤 **Sequence it after S211 merges** — renormalising while a branch is open converts that branch's merge into a line-ending conflict. Touches Python files, so full cycle; no package behaviour changes, so no bump. | 1 chore — 📦 **SPECCED as [S216](sprints/sprint-216-every-text-file-is-lf.md)** 2026-09-19, for GitHub Copilot. Re-measured: **52** files now (38 `i/crlf`, 14 `i/mixed`); dry run of the exact change is content-free and leaves 0 CRLF 🟩 **DONE 2026-09-19** — [S216](sprints/sprint-216-every-text-file-is-lf.md) merged `9e39e40` (built by GitHub Copilot), `GATE PROVEN` for the merge (CI, CodeQL, Security Findings). 52 files renormalized with an **empty** CR-ignoring diff; index now 1,727 `i/lf`, 15 `i/-text`, 3 `i/none`, **0** CRLF/mixed; `.gitattributes` enforces LF at every `git add`, and `tests/test_line_endings.py` fails the gate if either stops being true ([DL-178](design-log.md)). Item 73 is now unblocked. |
| 73 | 🟩 ~~**The IDE rewrites markdown in the checkout, and nothing says which markdown rules this repo keeps**~~ **DONE — all three deliverables shipped, 2026-09-20** | *[measured 2026-09-19]* The operator's VS Code uses `DavidAnson.vscode-markdownlint` as its markdown formatter (`[markdown]` → `editor.defaultFormatter` in the user settings, with `files.autoSave: afterDelay`), and the repo has **no** markdownlint config, so every default rule applies. On 2026-09-19 a markdownlint pass rewrote `docs/design-log.md` in the main checkout while Copilot was working (trigger not pinned down): trailing periods stripped from 4 headings (MD026), blank lines added around lists (MD032), and text damaged, `save **$0.41` → `save**$0.41` (MD037). Table realignment is MD060, present from markdownlint 0.39. **Defaults over the 452 tracked `.md` files: 36,312 findings in 441 files, 35,030 of them MD013 line length** (long lines are house style). A tuned config (MD013, MD026, MD029, MD033, MD036, MD037, MD038, MD041 off; MD024 `siblings_only`; MD010 `code_blocks: false`) leaves **243 in 59 files** under markdownlint 0.41.1: about 149 blank-line fixes (MD032 62, MD022 39, MD031 33, MD012 8, MD058 7), MD049 24, MD060 12, MD040 12, MD004 6, MD028 5, MD034 3, MD024 1, MD003 1, and **30 real defects: 18 broken link fragments (MD051), 9 tables with the wrong column count (MD056), 3 empty links (MD042)**. 🎯 **Operator's proposal, 2026-09-19: a markdown linter as a local-only check.** Planner's recommendation: (1) commit `.markdownlint.jsonc`, which the VS Code extension also reads, so the IDE stops applying the disabled rules (MD060 off: never realign tables); (2) fix the 30 real defects by hand and the whitespace with `--fix`; (3) enforce with a **pre-commit hook** (`markdownlint-cli2` on staged `.md`) plus `make lint-md`, **not** a `make ci` step: docs commits take the light path and never run `make ci`, and `make ci` mirrors the 12 remote steps that `make gate-ran` proves. Not added to remote CI, as the operator asked. 🎯 **MEASURED 2026-09-20 on `docs/design-log.md` + `docs/laws/drift-register.md`, and the measurement *is* this row's argument: three tools give three answers for the same two files.** The operator's IDE reports **96**; `markdownlint-cli2` at its defaults reports **5,660**; excluding `MD013/line-length` (5,611 of them, at the default 80 chars this repo has never written to) the real figure is **49** — 47 in `design-log.md`, 2 in `drift-register.md`. **Without a committed config the count is whatever the tool defaults to, which is what makes it unfixable rather than merely untidy.** 🎯 **20 of the 49 are genuine navigation defects, not formatting:** 18 `MD051` dead link fragments inside the decision record itself (`[DL-134](design-log.md)`, `[DL-152](design-log.md)`, `[DL-104](design-log.md)` and 15 more) plus 2 `MD042` empty links (`[DL-80](design-log.md)`, `[DL-96](design-log.md)`) — cross-references in the log that resolve nowhere. 🪰 **10 are `MD024` duplicate headings**, which is the same surface the IDE silently renames, so the linter and the autoformatter are fighting over the same lines. **Sequencing (operator, 2026-09-20): fix after S218 merges**, not during — both touch `.md` files. 🟩 **NARROWED 2026-09-20 — the row's actual complaint is answered; merged `b15aeae` (chore `2b51c51`, `GATE PROVEN`).** `.markdownlint.jsonc` now states which rules this repo keeps, with the measured reason beside every rule turned off, and the IDE reads it — so a reformat can finally be judged a fix or damage. Blank-line normalisation across **38 files**, content proven untouched by comparing every non-blank line. 🎯 **The check earned itself within minutes: markdownlint's own `--fix` corrupts text.** MD037's fixer deleted the real word space before bold on lines carrying dollar amounts — `still loses **$11,619**` became `still loses**$11,619**`, rendering as `loses$11,619` — in `work-queue.md`, `design-log.md`, `functionality-checks.md` and a sprint doc. 🪰 **`git diff -w` shows none of it**, because the change is inside a line; only a non-blank content comparison catches it. Same shape as S216's "content-free" commit lesson. 🟠 **STILL OPEN, and it is a decision not a typo: 45 findings remain, 21 of them dead intra-document links** (`[DL-134](design-log.md)`, whose heading carries a title so the real slug is `#dl-134---<title>`). The durable fix is a choice — rewrite every slug, or add stable `<a id="dl-134">` anchors. **Rewriting is measured brittle:** two links already written in long form have broken again since. The rest are 12 `MD040` unlabelled fences, 10 `MD056` table column-count errors (real rendering bugs), 3 `MD042` empty links and 2 one-offs. **No pre-commit hook until the repo passes** — a hook that cannot fail is what this repo bans. 🟩 **CLOSED 2026-09-20 — merged `e3ebabe` (chore `903bc1d`, `GATE PROVEN` for CI, CodeQL and Security Findings).** All three of this row's deliverables are met. **(1)** `.markdownlint.jsonc` committed. **(2)** The remaining 45 findings are **0 across 455 files**: the operator repaired the 21 dead link fragments by hand — the *rewrite-the-slug* option, chosen over stable `<a id>` anchors — and the rest were fixed here. 🎯 **Three were real rendering bugs, not lint noise:** `sprint-26`'s outer ```` ```markdown ```` fence wrapped a document containing its own ```` ```bash ```` fence, so CommonMark closed it early and **the rest of that file was being read as code**; `sprint-211` had a `---` directly under a paragraph, which is a **setext H2 underline** silently turning that sentence into a heading; and six `s127-fixpack-backlog.md` rows had **lost their Issue column entirely** — marking a row FIXED had appended the fix note to Kind and deleted the problem statement, **all six recovered from git history** rather than reworded. **(3)** Pre-commit hook added, **pinned to `markdownlint-cli2` v0.23.3** rather than `npx`, which downloads an unpinned build on demand and fails offline; **proven to fail** on a planted bad fragment, so it is not a check that cannot fail. 🪤 **Named deviation:** the target is `make markdown`, not the `make lint-md` this row proposed — same thing, different name. 🪰 **A false green was caught on the way:** a hand-built file list exceeded the argument limit, so a run linted nothing, printed nothing, and `grep -c error` read **0**, indistinguishable from a clean repo — which is why the scope now lives in `.markdownlint-cli2.jsonc` as one invocation rather than in a remembered command line.| **closed — config, defects and hook, all three proven** |
| 70 | 🟩 ~~**The regime classifier has never received an input in production — every night is `neutral` by default, and nothing says so**~~ **DONE** | *[measured 2026-09-17, [EXP-009](research/experiments/EXP-009-volatility-sizing-replay.md)]* **50 / 50** scheduled `RegimeContext` rows (`sched-2026-07-07` → `sched-2026-09-16`) are `neutral`, **0** label changes, `vix` absent on **all 50**. 🎯 **Cause, in the code:** `classify_regime` returns `"neutral"` when `inputs.vix is None` (`agents/provider/domain/regime.py:20-21`); the composite source delegates regime inputs to the price source (`composite.py:44-46`); and **every production source hard-codes `vix=None`** — `alpaca_data.py:54`, `tiingo.py:46`, `fmp.py:46`, `stooq.py:42`, `fundamentals.py:73`. Only `FakeDataSource` (`sources.py:122`, tests and `run_local.py`) ever returns a value. 🚨 **Silent:** `_get_regime` marks `regime_source_degraded` only when the fetch *raises* (`agents/provider/agent.py:146`), so a source that returns nothing yields a clean, confident `neutral`. `PROV-OUT-02` is 🟩 on tests that hand the classifier a VIX value production never supplies — **DRIFT-058's shape (a check that cannot fail), in the provider**, and `PROV-OUT-03` promises a total output space that includes DEGRADED. 🪤 **This re-frames item 64:** *"should regime scale risk"* has nothing to scale by until the label is computed from data, so the fix outranks the policy question. **Two halves, separable:** (a) **honesty, small** — a missing VIX must mark the regime degraded (incident ref + fault) rather than emit a clean `neutral`; a law question for `PROV-OUT-02/03`, so a law cycle; (b) **a real input** — a VIX (or VIXY/realised-volatility proxy) source; which vendor is an ADR-0006 data-feed question, and credentials are DL-36. 🪤 **Do not fix (b) by computing realised volatility inside the classifier without an ADR** — that silently changes what `risk_on`/`risk_off` mean against thresholds calibrated for VIX points. 🟩 **DECIDED 2026-09-17 — [ADR-0028](decisions/0028-the-regime-reads-vix-from-fmp-and-says-when-it-cannot.md)** (operator delegated: *"make your best decision"*). Probed live: **FMP `^VIX`** ✅ (already granted and credential-tested for the provider), **FRED `VIXCLS`** ✅ but always one session behind at run time, **Cboe CSV** ✅ undocumented; Finnhub, Alpha Vantage, Tiingo ❌. The three working sources agree **to the cent on 74 days**. With real VIX the label would have been non-neutral on **11 of 49** nights (10 `risk_on`, 1 `risk_off`; 12 changes vs 0). **FMP is the source; a prior-session bar is tolerated with a warning; anything staler or missing is DEGRADED, never a clean `neutral`.** No risk number changes — only the label, the deliberator's `vix_index` and the trace. 🚨 **ADR-0028 CORRECTED the same day, before any build:** the analyst (`run.py:56`) and PM (`run.py:123`) halt the whole run, exits included, on any regime incident ref, so a VIX shortfall is recorded on the regime and as a warning fault, never as an incident ref, and a vendor failure never raises. 🟩 **THE INPUT HALF IS NOW PROVEN LIVE — 2026-09-20, measured from this machine through the production code path, not a mock.** `FMPVixSource.fetch_regime_inputs` (`agents/provider/fmp_vix.py`) against live FMP with the real `PROVIDER_FMP_API_KEY` returns **`vix=14.81`, `vix_status='measured'`, `vix_as_of=2026-09-18`**, and feeding that to the untouched `classify_regime` yields **`risk_on`** — not the `neutral` that all 50 scheduled rows carry. So the vendor, the key, the parser, the freshness classifier and the thresholds all work end to end. 🪤 **Probed for Monday too:** `as_of=2026-09-21` returns `vix_status='prior_session'` **today**, because Monday's bar does not exist yet — an artifact of probing on a Sunday, not a defect; ADR-0028 tolerates a prior-session bar with a warning either way. 🟠 **What is still owed is only the recording half:** the deployed fleet writing `vix_status` on a run. Re-measured on the spine 2026-09-20: the newest scheduled `RegimeContext` is **`sched-2026-09-18`** (Friday, *before* the `s215` deploy), `vix=None`, `label='neutral'`, and **`vix_status` is not a key on any of the 71 rows**. So no run has yet executed S213's code. 🟢 **The fleet now carries S213 — deployed `s219a` 2026-09-20** (16/16 apps + `dispatcher-cron`, `DeployRecord` written). Every prerequisite for the recording half is therefore in place; the owed measurement is **`sched-2026-09-21`**, Monday 22:30 UTC.| 🟩 **DONE — code-fixed, deployed, and measured live** — [S213](sprints/sprint-213-a-regime-says-what-vix-it-measured.md) (`0.98.11`, merge `028382d`, [ADR-0028](decisions/0028-the-regime-reads-vix-from-fmp-and-says-when-it-cannot.md)). 🟢 **DEPLOYED 2026-09-19 as `s215`** (17/17). 🟩 **Live proof landed** *[measured 2026-09-22]*: `regime-context:sched-2026-09-21` reads `vix=14.8`, `vix_status=measured`, `vix_as_of=2026-09-21`, `label=risk_on` — the first scheduled `RegimeContext` in production carrying a VIX at all, and the first whose label is not `neutral`. That same deploy closes item 72 and unblocks item 64. *Queue row reconciled 2026-09-19.* |
| note | 🟩 **S194 shipped the missing orphan-count instrument** ([S194](sprints/sprint-194-a-number-nobody-records-is-a-number-nobody-has.md), `e0a144f`, `0.94.05`, 2026-09-02) | Item 3 is no longer blocked on “the number nobody records”: future `DeliberationRun` rows carry `orphaned_reply_count` as a per-run delta, and old rows render as `n/a`. S192 can now diagnose late/lost replies from measured data after the next live deliberation run. 🟩 **That run has happened:** `sched-2026-09-02` wrote the first value, **`orphaned_reply_count=0`** on `pm-run-c31f4806…` (`real_debate_count=2`, `failed_open_count=0`) — a quiet-night baseline, not yet a late/lost-reply sample. | queue note |
| note | 🟠 **Two design-log entries are both numbered `DL-148`** *(found 2026-09-03)* | *"Orphaned peer replies are a per-run fact"* and *"S194 verified; two hazards found in the verifying"*, both 2026-09-02. Both are cited by `STATE.md` and this queue, so renumbering silently would break live citations — which is why it was **left alone** rather than fixed in passing. Needs a deliberate renumber-plus-update-citations pass, or a documented decision to tolerate it. | doc hygiene, small |
| 46 | 🟩 ~~**The `.env` `GITHUB_TOKEN` carries `read:packages` only, so the deploy verifier cannot read the evidence it verifies against**~~ **DONE** (credential, no bump, 2026-09-04) | *[measured 2026-09-04, first real use of `record_deploy.py`]* The `s195` record was refused with `GitHub build read failed (403)` — `Must have admin rights to Repository.` Measured: **not expired, not rate-limited** (200 on `/user`, 4,899 of 5,000 remaining); the token was `trading-agents-ghcr-pull`, whose one scope is `read:packages`, and the workflow-runs *list* returned 200 only because the repo is public. 🪤 **The fleet pulls its images with that same token** (`ghcrio-yury-gurevich` on all 16 apps, fingerprint-matched to `.env`), so widening it would have handed sixteen containers full repo write. 🟩 **Fixed by separation, not by widening:** a fine-grained token scoped to this repo with **Actions: Read-only** now holds `GITHUB_TOKEN` in `.env`; the pull token is untouched. Proven on `.env` alone — run logs `200 / 826 KB`, `image_builds_for_tag("s195")` → run `33837562094` / `89e879a…`, and a negative control refused `92597aa` for that tag | small |
| 49 | 🟩 ~~**The one number that would settle the S150 stop promotion on our own book can never be computed — nothing writes it**~~ **DONE** ([S198](sprints/sprint-198-a-stop-we-never-measured.md), `0.96.00`, 2026-09-05) | *[measured 2026-09-05 while preparing the item 47 (c) decision]* `scripts/compare_stop_targets.py` reports `would_touch_rate_pct` from `stop_target_observed_drawdown_pct`, which **is declared** at `trading_graph_vocabulary.json:483` and read by the script — and a repo-wide search found **no producer**, only two tests. Measured over **269 recorded recommendations**: flat **5.00 / 5.00 / 5.00**, scaled **3.00 / 5.45 / 8.00**, RR pass rate **100.00 % in both modes** ([DL-77](design-log.md)'s named trap does not bite), `known_outcomes` **0**, `would_touch_rate_pct` **n/a**. 🟩 **Fixed by building the producer** under new clause `ANLZ-OBS-05`: the analyst records the deepest close-to-low fall over `stop_target_drawdown_horizon_days` sessions **and the horizon beside it**, append-only, absent when unsettled and a **real zero when the name did not fall**. 🪴 **Fourth instance of [DL-152](design-log.md)'s pattern in a new shape** — a declared property with no producer at all, which reads from the report exactly like "no outcomes yet" | small |
| 48 | 🟩 ~~**A duplicate key in a config pack is silently shadowed, and nothing checks for it**~~ **DONE** (`tests/test_pack_files.py`, no bump, 2026-09-05) | *[measured 2026-09-04 while merging S172]* Adding a row to `orchestration/packs/trading_tunables.json` produced **two** `DELIBERATOR_DEBATE_CONCURRENCY` keys in one object; `json.load` keeps the **last**, so the visible first entry was inert and all 61 tests passed because every one of them iterates the *parsed* dict where the loser no longer exists. 🟩 **Closed at file level, not in a loader** — the pack is read by `entrypoint.py` (Python) and `deploy-agents.ps1` (PowerShell, whose `ConvertFrom-Json` discards the same way), so guarding the file guards every reader including ones not written yet. All **11 packs** parse clean under an `object_pairs_hook` that refuses a repeat at any depth, and a vacuous-glob guard stops the check quietly passing on zero files. 🟩 **Proven able to fail:** planting a second `ANALYST_STOP_TARGET_MODE` in the live pack turned it red naming the key, then restored. | small |
| 50 | 🟩 ~~**The deliberator's credential probe cannot detect a drained account, so a dead veto starts a run**~~ **DONE** ([S202](sprints/sprint-202-a-probe-that-cannot-fail-is-not-an-entry-condition.md), `0.98.00`; [DL-163](design-log.md) decision, [DL-164](design-log.md) implementation, **DRIFT-058**) | *[measured 2026-09-13, live API + live spine]* **Both halves shipped.** The four **required** Anthropic probes now `POST /v1/messages` with `max_tokens: 1`, so they test *spend capability* rather than key validity — proven falsifiable live: funded key → `passed`, unusable key → `credential_failure http_401`, while `GET /v1/models` returned **200** for the same key in the same run. 🪤 **DL-163's one-line pack edit would have taken the fleet down:** `HttpProbeRequest` had no body, and a bodyless POST returns **400 for a valid key** — all four required probes would have failed at once. Probe bodies are therefore a code change, which is why this is a MINOR. 🎯 **The gate half shipped with it, not after it**, and was **corrected the same day by `/audit-costs` before merge**: keying only on `proceeded_unvetoed` produced a rule that fires on **0** of 69 runs while the operator's condition sat in the ledger **4** times as `applied_failed_open` with `failed_open_count == reviewed`, `real_debate_count = 0` and a `400 … credit balance is too low` reason. **The line is partial-vs-total, not present-vs-absent**; a genuinely partial fail-open has occurred **0** times. 🪤 I had cited [DL-125](design-log.md) as forbidding this — it does not; it argued against nightly red for a *declared, accepted, external* outage. **The outage is nine nights, not four.** **Still open, deliberately:** credit exhausted *mid-run*; and **item 53** | **closed 2026-09-13** |
| 51 | 🟩 ~~**A debate cannot be tied to its own cost rows — `llm_call_keys` has been empty on every `DeliberationRun` ever written**~~ **RETRACTED 2026-09-13, same day it was filed — my query was the defect, not the code** | *[re-measured 2026-09-13]* `llm_call_keys` is **not a property**: it is a parameter of `write_deliberation_run` that creates `PRODUCED_BY` **edges** via `_link_llm_call`, and it is correctly absent from the vocabulary's 14 `DeliberationRun` properties. I read `props.get('llm_call_keys')`, got `None`, and called it empty. The relationship it denies exists **944 times**: `DeliberationRun -PRODUCED_BY-> LLMCall` across **27** runs, and of the 69 runs on the spine **27 of 27** with `real_debate_count > 0` have their edges while all **42** zero-debate runs correctly have none — **0 defects**. 🚨 **This is [DL-73](design-log.md) repeated exactly**, against a warning written because DL-73 cost a false red-severity defect: *never audit the graph on raw props*. Kept struck through rather than deleted, because a retraction that leaves no trace is how the same mistake gets made a third time. Recorded as hardening row **T** | **retracted — no work, and none was needed** |
| 52 | 🟩 ~~**`pg_teardown --run-id` exits 0 with orphans left, because its self-check is blind in exactly the deleter's blind spot**~~ **DONE** ([S201](sprints/sprint-201-a-verifier-is-not-blind-where-its-subject-is.md), `0.97.05`, 2026-09-13) | *[measured 2026-09-13; fixed the same day]* Root cause located in code, not inferred: `_owned_patterns` built `<container key>:%` — **prefix-anchored** — so `broker-position-snapshot:<pm-run id>:<ts>` *contained* its owner without *starting* with it; and `survivors()` calls the same function, so the verifier could not see what the deleter could not reach. One-line fix (`contains=False` → `True`), safe because every query consuming these patterns is scoped to `RUN_ARTIFACT_LABELS`, which protected labels are not in — asserted, not assumed. 🚨 **The blast radius was larger than the one row that exposed it:** a read-only sweep of all 6,259 disposable rows found **2 more orphans** from teardowns on **2026-07-23** and **2026-09-01**, so this had been leaving debris for ~2 months; all three removed, **0 remain**. Three tests were watched red first, including the survivor check that made the defect *silent* rather than merely present | **closed — 1 line + 5 tests** |
| 47 | 🟩 ~~**Two promotion decisions on already-built code**~~ **DONE — all four objections resolved** ([DL-165](design-log.md), operator 2026-09-13) | *[measured 2026-09-13]* **(a)/(b) REFUTED** and the rendering defect they exposed closed by [S197](sprints/sprint-197-a-gate-says-what-it-examined.md). **(c) DECIDED AND SHIPPED** as [S198](sprints/sprint-198-a-stop-we-never-measured.md)'s scaled stops. **(d) DECIDED 2026-09-13: promote the S149 volatility-scaled tolerance**, `EXECUTION_ORDER_PRICE_TOLERANCE_MODE` flat->scaled. 🪤 **Promoted on DL-76's *external* measurement, not own-book evidence, and the reason matters:** own-book evidence was structurally uncollectable while the narrower band was applied — `compare_order_tolerances` reported flat and scaled identical at **0.00 %** drop over **170** orders, *correctly*, because scaled is wider on **170 of 170** (75-250 bps vs a fixed 50), so every order flat accepted scaled also accepts, and the orders flat **refused** never produce a `Fill` row to be counted. 🎯 **The flip is what makes the comparison informative:** with the wider band applied, an order filling above the flat limit is exactly an order flat would have refused — DL-76's 35 %-of-buys refusal rate, measured on our own book for the first time. Decision and instrument arrive together | **closed 2026-09-13** |
| 56 | 🟩 ~~**A comparison that cannot discriminate reports a confident `0.00`, and says nothing about why**~~ **DONE** ([S203](sprints/sprint-203-a-check-that-cannot-fail-says-so.md), `0.98.02`, 2026-09-14) | *[measured 2026-09-13, [DL-165](design-log.md)]* `scripts/compare_order_tolerances.py` printed `flat 0.00` and `scaled 0.00` over 170 orders. **The computation is right** — `_would_fill` compares the actual execution price against each mode's limit, which is the correct construction — but with the **narrower** band applied the answer could not have been anything else, and the report does not say so. A reader concludes *"the two modes make no difference"*; the truth is *"this comparison cannot differ in this direction"*. 🪰 **[DL-152](design-log.md)'s shape for the sixth time** — a producer that knew something it never said (S183 skipped-vs-passed, S197 `cluster_issuers`, S198's absent `known_outcomes`, S199's word counts, S202's free probe). Closes when the report labels the structurally-uninformative case instead of printing a zero that reads as a result. 🟩 Cheap: the discriminating condition is *is the applied band the wider one on every row?* 📦 **SPECCED as [S203](sprints/sprint-203-a-check-that-cannot-fail-says-so.md)** 🟩 **CLOSED:** every report now ends with a `comparison:` line reading `FORCED`, `INFORMATIVE` or `NO DATA`, computed from the `order_tolerance_mode` the `Fill` actually records. The arithmetic is untouched and pinned (`test_would_fill_is_unchanged_by_this_sprint`). 🎯 **The label is also a deploy check:** under the promoted `scaled` band new rows must read `INFORMATIVE`, so a `FORCED` label after a week of trading means the promotion never reached the fleet. | **small** |
| 53 | 🟩 ~~**Four more credential probes cannot fail either — the same defect as item 50, left in on purpose**~~ **DONE** ([S203](sprints/sprint-203-a-check-that-cannot-fail-says-so.md), `0.98.02`, 2026-09-14) | *[measured 2026-09-13]* The three `openai` entries in `orchestration/packs/trading_credential_tests.json` call `GET https://api.openai.com/v1/models`, and `probe_openai` / `probe_anthropic` in `orchestration/packs/trading_vault_probes.py` do the same for both vendors. All five are free metadata: a key that cannot spend passes. 🟩 **Why this was ranked below 50 rather than done with it:** the OpenAI entries are `required: false`, so their verdict halts nothing, and the vault probes are reached only by `scripts/seed_key_vault.py` and `scripts/seed_key_vault_live_check.py` — operator tooling that gates no run. Neither can cause the four-night outage item 50 caused. 🪤 **It is filed rather than dropped because an undocumented blind probe is exactly how item 50 survived four nights.** The machinery now exists (`json_body`), so this is a pack edit plus two small functions. Closes when every probe that claims to test a credential has a result that would falsify it 📦 **SPECCED as [S203](sprints/sprint-203-a-check-that-cannot-fail-says-so.md)** (2026-09-14, with items 56 and 41) 🟩 **CLOSED:** the three pack `openai` entries and both `trading_vault_probes.py` LLM probes now POST a real completion; measured live in both arms (valid key → 200/`passed`, bogus key → 401/`credential_failure`). 🪰 **The spec's suggested body would have failed every one of them for a valid key** — `gpt-5.5` is a reasoning model and `max_completion_tokens: 1` returns **400**, measured before the pack was edited ([DL-166](design-log.md)). Cost: ~US$0.0028 per full fleet activation, from the vendor's `usage` block. | **small** |
| 57 | 🟩 **DONE — [S217](sprints/sprint-217-the-master-checks-the-whole-fleet.md) merged `f252dca` (`0.99.00`), not yet deployed** · 🟢 **DECIDED 2026-09-19 — the field counts: a listed status is an UNRECOVERABLE failure** ~~A pack field that reads like a control has never been consulted~~ | *[measured 2026-09-14 while building [S203](sprints/sprint-203-a-check-that-cannot-fail-says-so.md), [DL-166](design-log.md)]* `credential_probes.py` had **two consecutive 4xx branches with identical bodies**, so `credential_failure_statuses` has never widened or narrowed anything: every non-expected status below 500 fails closed whether the pack lists it or not. 🟩 **Not a live defect** — fail-closed is the right posture, and S203 left the behaviour untouched, collapsed the duplicate branch and pinned the fact (`test_an_unlisted_4xx_still_fails_the_probe`). The open question is whether the field *should* become authoritative, so an unlisted 4xx is a **transport** fault rather than a credential one. 🪰 That direction makes a probe *stop* blocking activation for statuses nobody predicted — which is why it is a decision, not a cleanup. | **decided** ([DL-179](design-log.md), operator) — built together with item 58 📦 **SPECCED as [S217](sprints/sprint-217-the-master-checks-the-whole-fleet.md)**. |
| 58 | 🟩 **DONE 2026-09-20 — DECIDED 2026-09-19 — the fleet does not start with any subsystem unfunded or inaccessible: retry +1 h, +1 h, then a human decides** ~~The OpenAI probes can now fail, and still are not allowed to~~ | *[decided 2026-09-14, [S203](sprints/sprint-203-a-check-that-cannot-fail-says-so.md) out-of-scope note]* All three `openai` pack entries stay `required: false`, so a drained OpenAI key is now *detectable* and still does not halt activation. That was correct for a defect-fix sprint — `DELIBERATOR_LLM_PROVIDER=anthropic`, so promoting them would halt three deliberators on a vendor we do not currently use. 🎯 **The question this row owns:** does the fallback provider ([S168](sprints/sprint-170-one-llm-adapter-in-the-plumbing.md)'s OpenAI path) count as load-bearing? If it does, `required: true` is right and the posture change should be deliberate; if it does not, say so and the probes are advisory forever. 🟩 **S218 MERGED 2026-09-20** — `69f3601`, `0.100.00`, tagged `v0.100.00`; `GATE PROVEN` for the handback tip `12bb962` and for the merge. The dispatcher places a run only after a fresh passing `FleetPreflight`; otherwise it writes an append-only `RunHold` and places nothing, and the dashboard goes RED naming what failed. S217's fault-per-probe defect is fixed here too. 🎯 **Returned once on a planner amendment (R1) that found two defects the 15-row test table missed** — a re-hold after a release reused the released node, leaving a stale re-hold **held with the dashboard GREEN**, and a held run with no check read `0 check(s) failing`. Both re-verified independently against the spec's committed reproduction. 🟠 **Only S219 (Telegram + answer buttons) remains**, and it waits on the operator creating the bot. Nothing deploys until it lands, and that deploy is a full `up` because `RunHold` moved the vocabulary. 🟩 **CLOSED 2026-09-20 — all three shipped and deployed.** S219 merged `25bd053`; S217+S218+S219 deployed together as **`s219`** by full `up` (the vocabulary pack had moved twice, so a retag was never an option), and the Telegram channel was proven end to end before any code depended on it. 🟠 **The live exercise is still owed** — the notice path has never fired in production; first run `sched-2026-09-21`.| **closed — all three sprints merged and deployed `s219`** |
| 55 | 🟠 **A total LLM outage has a free, unambiguous signature in the ledger and nothing watches it** | *[measured 2026-09-13 by `/audit-costs`]* On all **nine** blind nights — `2026-08-21, -24, -25, -26, -28`, `2026-09-08, -09, -10, -11` — **every** `LLMCall` recorded `response_hash = e3b0c44298fc…`, the **SHA-256 of the empty string**, with `stop_reason='unknown'` and a mean latency of **~300 ms** against a working night's **~15,000 ms** (min 233 ms; the non-empty population averages 15,610 ms over 1,042 calls). 🎯 **Nine days where 100 % of calls returned nothing, and the first was 2026-08-21 — nineteen days before item 50 was filed.** 🟠 **Re-scoped hours after filing: S202 already watches most of this.** The corrected S202 gate turns a total-outage night **red** on the acceptance run, so the nine nights would no longer pass silently and the row's original *“nothing watches it”* framing is now false. What is left is genuinely smaller: (a) detection **without** waiting for an acceptance run, (b) naming the *cause* cheaply across runs from one indexed query, and (c) coverage of `LLMCall` rows **outside** deliberation — the operator agent’s calls are not behind that gate at all. Ranked below item 53 on that basis. 🪰 Note `tokens_out = 0` alone is **not** the signature — 140 of 1,182 rows have it, including partial-failure days — the discriminator is the empty-string response hash plus sub-second latency. Closes when a total-outage night is detectable without reading `failed_open_reason` by hand | **small** |
| 66 | 🟩 **A night the referee vetoes every buy reads as an execution that did nothing — the CLI trace never says a veto happened** | *[measured 2026-09-17 reading `sched-2026-09-16`]* PM approved **4** buys (USB, WFC, AMZN, MDLZ); the trace printed `[execution] submitted=0 rejected=0` and acceptance printed `PASS`, with **no reason anywhere on either screen**. The reason exists only on the `DeliberationRun`: `verdicts` all `revise`, `vetoed_tickers=('USB','WFC','AMZN','MDLZ')`. 🎯 **The `ExecutionRun` cannot say it either:** `drop_vetoed` removes the tickers *before* the counters are taken (`agents/execution/pm_execution.py:58`), and `skipped` counts only `result.skipped + filtered.blocked_count` (`:83`), where `blocked_count` is the *posture* filter — so the record reads `skipped=0 deliberation_blocked_count=0` while four approved orders were withheld. **Approved 4 ≠ submitted 0 + rejected 0 + skipped 0**, and nothing on the record reconciles the gap. 🪰 **The rendering already exists — in the wrong surface:** `orchestration/packs/trading_deliberation_view.py:42` renders `reviewed=… vetoed=…` for the observatory, and `orchestration/batch_trace.py` never calls it, so the terminal answer to *“why no trades?”* needs a hand-written graph query. Cost measured today: one `/diagnose-run` spent four extra queries finding a cause the fleet had already written down. 🪤 **Not a capital defect** — the veto bound exactly as ADR-0022 intends (`advisory` governs only the no-`DeliberationRun` case; DL-134). **Fix direction, cheapest first:** render the deliberation line in the trace (orchestration only, no law); optionally record a `deliberation_vetoed_count` on `ExecutionRun` so the counts reconcile on the record itself — that half adds an execution output guarantee and owes an `EXEC-*` law cycle. 🔁 **Recurred, second independent case** *[measured 2026-09-18 reading `sched-2026-09-17`]*: PM approved **3** (USB, AMZN, WFC), `DeliberationRun pm-run-87942b0d882644eda8ad95a618a06cc3` returned `verdicts` all `revise` with `vetoed_tickers=('USB','AMZN','WFC')`, and the `ExecutionRun` again read `submitted=0 rejected=0 skipped=0 deliberation_blocked_count=0` under `deliberation_status=applied` — the identical unreconciled shape, one night later, on `:s211`. **Approved 3 ≠ 0 + 0 + 0.** Cost measured again: the same hand-written graph queries were needed to answer *"how did we go"*. This is now **four consecutive sessions with zero fills** (09-14, 09-16 ×2, 09-17) and **20 of the last 26 reviewed buys vetoed (77 %)**, so the block S212 adds would have explained every one of them. | 🟩 **DONE** — [S212](sprints/sprint-212-a-trace-says-why-nothing-was-submitted.md) merged, `GATE PROVEN` for `b275d87` (trace half only; the `ExecutionRun` half is recorded there as the road not taken). *Queue row reconciled 2026-09-19.* |
| 72 | 🟩 **A correctly deployed fleet has no `DeployRecord` — the recorder only accepts builds dispatched on `main`** | *[measured 2026-09-17, S211 deploy]* `deploy-agents.ps1 up -Tag s211` exited 0 and readback confirmed **17/17 apps on `:s211`, all `Succeeded`**, but `scripts/record_deploy.py` refused with *"GitHub build evidence is required"*: its reader `_successful_main_image_runs` (`surfaces/dashboard/github_builds.py`) accepts only image-build runs whose ref is `main`, and build `35217747046` was dispatched on the tag ref `v0.98.08` — **the same SHA as the merge commit `7d3ff51`**. 🎯 **The refusal is right, the outcome is wrong:** the fleet moved and the graph's deploy ledger cannot say so. Latest `DeployRecord` is still `s210` (`deploy:2026-09-16T12:19:57…`), so any check that asks *"what is deployed?"* from the graph now answers one tag stale, while `az` says `s211`. 🪤 **The obvious workaround is the worse option:** rebuilding from `main` would stamp the later docs commit `c035592` as the deployed SHA — a *false* record beats no record only if you never read it. 🪰 **Related, not the same:** item 21 ([S180](sprints/sprint-180-a-deploy-record-must-name-the-commit-that-was-built.md)) made the recorder distrust a handed-in SHA; this is the mirror — it now distrusts a *truthful* tag-ref build. **Fix direction:** accept a build whose ref resolves to a commit contained in `main` (not only ref == `main`), which keeps S180's guarantee while admitting tag dispatch; or make the deploy script dispatch on `main` while `main` still equals the merge commit, and fail loudly if it does not. Prefer the first — the second is a procedure humans forget. 🧪 **Workaround proven on `s214`, 2026-09-18** — dispatching `build-images.yml` on `main` while `main` still equalled the merge commit `1a6f342` let `record_deploy.py` accept the evidence, and `DeployRecord deploy:2026-09-18T11:04:47…:s214:1a6f342…` was written. **The defect is unchanged:** the reader still accepts only `main`-ref builds, so this depends on a human remembering the order — which is why the row's own fix direction prefers accepting any ref that resolves to a commit contained in `main`. | 🟩 **code-fixed and merged** — [S215](sprints/sprint-215-a-build-is-evidence-for-its-commit-and-its-tag.md) (`0.98.12`, merge `5ef4a4c`, 2026-09-19; built by GitHub Copilot). Live read-only check, old reader beside new: `s211`/`7d3ff51` `[]` → `[35217747046]` (the tag-dispatched build, item 72); `smoke-test`/`576ee57` `[]` → `[]` (never merged, still refused); `s21` and `s2` on `1a6f342` **false accept** `[35336964246]` → `[]`; `s176`/`74bdd7c` **false accept** of the `s176a` build `[31854512795]` → `[]`; controls `s214` and `s176a` accepted by both. 🟠 **The row closes** when S213's deploy, dispatched on its tag, gets a `DeployRecord` — the first tag-dispatched build the recorder accepts 🟩 **CLOSED LIVE 2026-09-19** — `s215` built from tag `v0.98.12` (run `35422844229`, 15/15 images), fleet retagged 17/17, and `record_deploy.py --tag s215 --git-sha 5ef4a4c…` **exited 0** and wrote `deploy:2026-09-19T05:10:21Z:s215:5ef4a4c…`: the first tag-dispatched build with a `DeployRecord`. |
| 67 | 🟩 **`trace_run.py` exits 1 on a complete run — it still expects seven stages** | *[measured 2026-09-17]* `scripts/trace_run.py:33` is `sys.exit(0 if complete == 7 else 1)`, but `print_trace` counts `_COMPLETE_KEYS` = `position_sync` + the chain (`orchestration/batch_trace.py:22`, `:184-187`), which is **8**. Measured both ways: `sched-2026-09-16` and `sched-2026-09-15` each print `RESULT  8/8 stages complete  OK batch processed` and exit **1**. The docstring at `batch_trace.py:26` still says *“max 7”*. 🪤 **A truthful exit code is the only machine-readable verdict the script has** — anything that wraps it (a skill, a loop, a `&&` chain) reads every healthy night as a failure, which is DL-125's cries-wolf shape. **Fix:** return the total alongside the count and compare against it, rather than moving the literal to 8 — a hard-coded stage count is what drifted when `position_sync` was added. | 🟩 **DONE, proven live** — [S212](sprints/sprint-212-a-trace-says-why-nothing-was-submitted.md) merged; `sched-2026-09-18` ran 8/8 stages and `trace_run.py` exited **0**. *Queue row reconciled 2026-09-19.* |
| 45 | 🟩 ~~**A filter that can answer certainly still records SKIP**~~ **DONE** ([S196](sprints/sprint-196-a-filter-that-can-answer-says-so.md), `0.94.10`, merged `41b9785`, deployed `s196`, 2026-09-04) | *[measured 2026-09-04, [DL-152](design-log.md)]* The provider queries a **30-day** earnings horizon; the scanner excludes within **5** — so absence proved no earnings due, yet `evaluate_filters` filed **20 of 20** absences under `skipped`. `MarketData.earnings_horizon_days` now declares the map’s scope and is withheld on any degraded or unrequested fetch. 🟩 **Live-proven on `verify-2026-09-04-s196-earnings`: `skipped: NONE`** — all five gates evaluated 20/20. 🪤 **It did not produce a trade:** the deliberator still vetoed, now on the substance of the gates (self-referential `correlated_cluster_pct`, cluster lists omitting co-held names, non-vol-adjusted sizing, stale `est_price`) — filed as item 47 | small |
| 38 | 🟩 ~~**The advisory acceptance gate is non-deterministic on a quiet day — a race decides red or green**~~ **DONE** ([S191](sprints/sprint-191-a-quiet-night-gets-the-same-verdict-twice.md), `97e9fb5`, `0.94.03`, 2026-09-01) | *[measured 2026-09-01, `sched-2026-08-31`]* The run completed **8/8** with zero orders (28 analyst holds; GOOGL, the lone buy, failed the PM sizing gate) and returned `ACCEPTANCE FAIL  deliberation.advisory_attribution: missing`. 🟩 **The `ExecutionRun` is correct:** `deliberation_posture=advisory`, `deliberation_status=not_required`, and the `DeliberationRun` narrates *"No PM-approved orders required deliberation."* `not_required` is a **declared** status (`agents/execution/deliberation_gate.py:38`, [ADR-0022](decisions/0022-the-veto-gates-buys-never-exits.md), execution laws v1.4) that `ADVISORY_STATUSES` omits (`orchestration/packs/trading_deliberation_view.py:20`), so `_advisory_attribution` returns `missing` and the gate reds. 🚨 **Second instance of the same policy gap** — the first is the `analyst.scored: 0 < floor` no-trade signature from 2026-07-09, still unfixed. 🪤 **Do not simply widen the vocabulary:** `not_required` alongside `submitted > 0` would be a genuine defect, so the check must condition on there being **no orders**, not on the status alone. 🚨 **RE-MEASURED 2026-09-01 AND IT IS WORSE THAN A NO-TRADE RULE — IT IS A RACE.** `verify-2026-09-01-s190-stops` ran the **same zero-order inputs** and returned `ACCEPTANCE PASS`. The difference is purely ordering: on `sched-2026-08-31` the deliberator wrote its (empty) `DeliberationRun` **after** execution polled, so `linked_deliberation` was `None`, `has_buy` was false, and the status was `not_required` → **FAIL**; on the verify run it landed **before**, so the status was `applied` → **PASS**. 🪤 **So the gate's verdict on an identical quiet day is decided by a scheduling race**, which is harder to read than a consistent red and cannot be reproduced on demand. 🚨 **Ranked first because it blocks reading any quiet night** — a red meaning *nothing to trade* is indistinguishable at a glance from a red meaning *the fleet broke*, which is DL-125's cries-wolf shape and fails the second half of the operator's etalon bar. 🪤 **The fix must not simply add `not_required` to the set**, and must not key on `submitted == 0` either: `not_required` also covers **sells-only** runs (`has_buy` false with orders submitted), which [ADR-0022](decisions/0022-the-veto-gates-buys-never-exits.md) requires to pass — so the condition is *no **buy** intent*, read from the approved set's actions, not `approved_count` | small, but it is a policy decision |
| 44 | 🟩 ~~**Nothing records which code version produced an `LLMCall`, so whether a debate can be replayed is unknowable until you try**~~ **DONE** ([S200](sprints/sprint-200-a-prompt-says-which-code-rendered-it.md), 2026-09-13) — closed with a **content digest, not a version string**: a version would still leave you checking whether the renderer moved between builds. Re-measured on the way in: **62 of 285 = 21.75 %**, not the carried 57/280. Also **CORRECTS DRIFT-056** by hashing the system prompt, which promotes `DLIB-IDM-02` ⬜ → 🟩 | *[re-measured 2026-09-05 via the committed `scripts/deliberation_reproducibility.py`]* **57 of 280 = 20.36 %** of recorded `defender:r1` turns replay to their stored `prompt_hash` across the whole corpus, and **10 of 10** on the five most recent debated runs (2026-09-01 → 09-04). The split is **not** recency, it is **the renderer the run was recorded on**. 🟩 **The harness is no longer the suspect** — 100 % on current-code runs proves the replay is correct, so the 20 % is entirely historical drift. 🚨 **The cost is now demonstrated rather than predicted:** DL-104's 56 % had to be re-derived from *stored verdicts* rather than replayed prompts, because its own runs replay 0 of 18. | **closed — one `up` with S199, same pack** |
| 43 | 🟩 ~~**Every LLM cost this project has reported is understated — the ledger records word counts, not tokens**~~ **DONE** ([S199](sprints/sprint-199-a-token-count-comes-from-the-vendor.md), 2026-09-13) | *[measured 2026-09-03, [DL-150](design-log.md) correction]* `kernel/llm_ledger.py:114` writes `tokens_in`/`tokens_out` as `_rough_tokens(value) = max(1, len(value.split()))` — **a word count** — and `agents/deliberator/agent.py:139` passes `prompt=user`, so the count also **excludes the system prompt entirely**. 🚨 **The blast radius is every cost number we have:** the `/audit-costs` skill prices LLM spend from these same `LLMCall` rows, and DL-150's own per-order figures were quoted as tokens and are not. 🪰 **The fix is not a 1.3× multiplier** — the honest source is the API's own `usage` (`input_tokens`, `output_tokens`, and the cache fields), which both adapters currently discard. 🟠 Also narrows **DLIB-IDM-02**: its “bounded by prompt hashes” cannot see a system-prompt change, so two calls with the same `prompt_hash` may have had different system prompts — now filed as **[DRIFT-056](laws/drift-register.md)** with its forced decision named. 🟩 **Not urgent for safety** — nothing trades on these numbers; it is urgent for *decisions*, because the effort/cost sweep in S173 Part B is one of them. 🚨 **It has now cost money, not just accuracy** ([DL-160](design-log.md)): S173's sweep was priced off these counts and was **3.2x low**, discovered by spending **$58** on one round. 🟩 **Same trip, second win — prompt caching is unclaimed:** `cache_read` was **0** across all 2,957 calls, and the challenger/judge system prompts (6,764 / 6,341 chars) are byte-identical on every request of a round, so `cache_control` on the system block cuts them to 10 % of the input rate — measured **~20 %** off a sweep, and it applies to **every nightly debate** too, where all orders in a run share those prompts inside the cache TTL. | **closed — both halves, plus the pricer** |
| 42 | 🟩 **CLOSED 2026-09-17 — live closure proven on `sched-2026-09-16`** ~~The head-of-run sweep reads stop liveness before the run refreshes it, so every stop that fired since the last run raises one spurious warning~~ — the code, law cycle and deploy are proven; the row stays **open** only until a scheduled run in which a protective stop actually fired shows **zero** same-date `BrokerStopIdentityMismatch` faults (LAW-02: a no-stop-fired run proves nothing) | *[re-measured 2026-09-02, [DL-147](design-log.md) — **this row previously said something else and was wrong**]* 🚨 **The first filing claimed a stale graph stop the broker had never heard of, to be retired. Measured properly, all of that is false** and acting on it would have **deleted live, correct data**. 🟩 **S190's predicate is right and working:** `active_broker_stop_orders` returns **26** — exactly the live stops — and neither warned stop is in it; both broker orders exist and are `filled`. 🪤 **The discriminator I used selected the entire population** — all **47** `BrokerStopOrder` siblings carry `Fill.status='pending'`, because that is every stop's normal state, exactly the [DL-73](design-log.md) trap; the real prop is **`broker_status`** (`None` 26 / `filled` 11 / `rejected` 10). 🎯 **What actually happens, to the second:** the sweep warned at **22:30:42.42** (AMD) and **22:30:47.08** (USB); the `BrokerOrderStatus` refresh wrote `filled` for both at **22:30:58.6** — **16 s and 11 s later, inside the same run**. So the graph really was stale for those seconds, the sweep read it correctly, **exempted** the order, and the run then fixed it. It is **transient and self-correcting**, fires **once per stop that fired since the previous run** (which is why `s187` saw 17 on a heavy fill day and 2 last night), and **will not recur for AMD or USB**. 🪤 **The defect that is left is item 20's original wording** — *the check compares a type against a lifecycle*. S190 answered it by comparing **live-to-live**, and comparing two liveness views is what produces this, because they refresh at different times. **Fix direction is the opposite of the first filing:** compare **identity to identity** and handle liveness separately, rather than reordering the head of the run. Cost of leaving it: one permanent `Fault` row per fired stop | **closed** — 🟩 **MERGED and DEPLOYED as [S209](sprints/sprint-209-a-stop-that-fired-is-not-a-mismatch.md), 2026-09-16 (`0.98.06`, deploy SHA `032cd8e`, tag `s209`).** The sweep warning now compares broker stop identity (`is_broker_stop_order`) to graph stop identity (`broker_stop_orders`) rather than two liveness views; `EXEC-OBS-05` is amended to v1.6, the rollup stays **35 / 61**, and `DRIFT-064` records the 2026-08-31→S209 stale clause. 🟩 **Proven:** local `make ci` green (**2768 passed, 4 skipped, 100.00 %**); deploy-SHA `make gate-ran` on `032cd8e` green for CI, CodeQL, Security Findings and image build; image-only retag kept vocabulary `58769995…`, credential tests `f8f03950…`, issuer map `2ed1f41c…`; **17/17 targets on `:s209`**, all `Succeeded`, dispatcher cron intact, scale/env/cron drift **0**; `DeployRecord deploy:2026-09-16T08:45:36…:s209:032cd8e…`. 🟠 **Live closure still owed:** the only production proof that can close this row is a future scheduled run where a protective stop actually fired and zero `BrokerStopIdentityMismatch` faults appear for that date. A no-stop-fired run proves nothing. 🟩 **LIVE CLOSURE MET 2026-09-17, measured on the spine and the broker.** `sched-2026-09-16` was the first scheduled run on `s209` **and** a night on which protective stops fired since the previous run: Alpaca filled CMCSA `stop:6a3376f977fc6cfb` (40 @ $23.91, 18:35 UTC) and USB `stop:db20c46b900716c4` (16 @ $59.09, 19:20 UTC). 🎯 **The exact S209 window occurred:** the sweep runs first inside `sync_run_request` (`agents/execution/poll.py:124`), before `reconcile_run_start` wrote `BrokerOrderStatus` `filled` for both stops at **22:30:47** — so the sweep read both stops while the graph still said `pending`, which is precisely what raised a warning per fired stop before S209. **Same-date `BrokerStopIdentityMismatch` faults: 0**, against **3** on 09-14 and **1** on 09-15 (pre-`s209`). Reconciliation then adopted broker truth — graph and broker **21 = 21**, zero divergences; the CMCSA `extra_graph_position` Flag is that reconciliation working. Recorded in [`laws/functionality-checks.md`](laws/functionality-checks.md). |
| 3 | 🟩 ~~**Deliberator concurrency — S172 built but unmerged**~~ **DONE** ([S172](sprints/sprint-172-independent-debates-run-independently.md), `0.95.00`, merged `3f29c1e`, 2026-09-04) | *[merged 2026-09-04 after a 52-commit catch-up merge]* The branch forked at `2f4ff82`, before S191/S193/S194/S195/S196, and `strict` status checks meant it could not land until it was rebuilt on current `main` and green **on that union**. Five real conflicts: `poll.py` (S172's `review_approved_orders` batch vs S194's `orphaned_reply_count` delta around the same loop — resolved to keep both, baseline read before the batch), the **`v1.2` law collision STATE predicted** (S194's stays, S172's renumbered **v1.3**), and a rollup I wrote as 14/52 that the coverage check **derived as 15/52** and refused. `make ci` exit 0 — 2501 passed, 100.00 %; `GATE PROVEN` for `1148091…` with the printed SHA checked against `HEAD`, nothing above it, security baseline untouched, MINOR bump correct. 🚨 **The merge landed at K=1, not the branch's K=4.** The branch already carried `DELIBERATOR_DEBATE_CONCURRENCY: "4"` in `trading_tunables.json`, so the next full `up` would have deployed **the one configuration K>1 has never been proven in** — 1.78x of a possible 4x, and 6 dead-lettered peer replies with 2 of 15 orders failing open on one of two runs. The pack ships **1**, which is the status quo behaviourally; the reason is recorded in the pack's own `_measured` note. **What merged is the dial, not the fan-out.** 🪤 A branch test pinned the value (`== "4"`), which made every operator change a code edit and defeats the tunables mechanism; it now validates through `DeliberatorSettings` instead, proven by planting `0`. 🟠 **Raising K remains unproven work** — the intermittent fail-open is not disproven, and S194's `orphaned_reply_count` is now the instrument for counting it across nights | **closed — the dial merged; K>1 still owed** |
| 40 | 🟩 ~~**A back-compat shim that has never once run in production — the acceptance gate cannot read 69 % of its own history**~~ **DONE** ([S193](sprints/sprint-193-a-shim-that-never-runs-is-not-a-shim.md), `0.94.04`, 2026-09-02) | S193 changed the historical `GateOutcome` shim from `dict` to `Mapping` and added a regression that round-trips legacy `passed` gate payloads through a real `GraphStore`, so the `mappingproxy` nesting that broke production is exercised directly. Red proof on `main`: four missing-`outcome` validation errors, including nested `rejected.0.gate_report.0` and the PM stage view. Green proof after: six new graph-roundtrip tests plus the PM contract suite pass, and the live Postgres sweep returns **0 `ERROR`s**. The sweep denominator moved from 55 to **56** because `sched-2026-09-01` now exists; current tally is **54 `FAIL`, 2 `PASS`**, with `sched-2026-08-31` and `sched-2026-09-01` passing. Remaining failures are old verdicts, not reader crashes. No deploy for this alone; it rides the next fleet move. | **closed** |
| 41 | 🟩 ~~**Flipping `remediation_mode` to `automatic` would change nothing in the fleet, and say nothing**~~ **DONE** ([S203](sprints/sprint-203-a-check-that-cannot-fail-says-so.md), `0.98.02`, 2026-09-14) | *[measured 2026-09-01, [DL-144](design-log.md)]* 🟩 **Not a live defect — filed as a latent trap, and the distinction is the point.** `remediation_mode` defaults to `"manual"` (`agents/master/settings.py:91`) and its own `why` says auto-remediation is *"a later piece (C/D)"*, so today's zero automatic attempts are **correct, configured behaviour** — an earlier reading of mine that called the arc dead was wrong and is retracted in DL-144. 🚨 **The trap is the switch.** `agents/master/entrypoint.py:105` builds `MasterAgent` without `remediation_llm`, `remediation_catalogue`, `remediation_system_prompt` or `remediation_executors` — all four at empty defaults, for the S86/DL-12 reason that the master image copies only `kernel/ contracts/ agents/master/` and import-linter forbids `agents → orchestration`, where `build_trading_master` lives. So the day the documented switch is thrown, `plan_and_try_remediation` returns `None` at `if llm is None or not catalogue` (`agents/master/remediation_execution.py:141`) — **silently**: no fault, no flag, no marker on the `Escalation`. 🪤 **The absence would be indistinguishable from remediation having run and found nothing to do**, which is DL-125's cries-wolf shape inverted — a switch that reports success by saying nothing at all. 🪤 **The fix is not to wire the catalogue now** (Pieces C/D are deliberately later): it is to make the unwired `automatic` case **refuse to be silent** — raise at construction, or stamp the `Escalation` with why no attempt was made 📦 **SPECCED as [S203](sprints/sprint-203-a-check-that-cannot-fail-says-so.md)** 🟩 **CLOSED as noise, not as wiring:** `agents/master/remediation_posture.py` refuses to start a master whose remediation mode it cannot honour, naming the setting that fixes it. DL-144's Pieces C/D stay unbuilt on purpose. 🚨 **No `MST-*` clause was written** — all 44 master clauses are silent on remediation, recorded as [DRIFT-059](laws/drift-register.md) with the forced decision named rather than answered prematurely. | small, and it protects a later sprint |
| 39 | 🟩 ~~**Dependabot auto-merges reach `main` ungated — the branch-is-the-gate rule has a hole**~~ **DONE** (settings-only, no bump, 2026-09-01) | *[measured 2026-09-01]* PRs **#81** and **#82** merged 2026-08-31 20:58 / 21:00 UTC. Both PR *heads* were fully green (`CI`, `CodeQL`, `Security Findings`), but **neither merge commit on `main` has a CI or Security Findings run at all**: `dependabot-auto-merge.yml` merges via `GITHUB_TOKEN`, and a `GITHUB_TOKEN` push does not trigger further workflows by design. 🚨 **The content gap is real, not theoretical.** #81's merge tree matched its gated head exactly, but **#82's merge commit differs from its gated head by 475 lines of `uv.lock`** — it branched before #81 landed, so `main` carries a union of two lockfiles that was **never gated as a unit**, and that union is what the `s190` images were built from. Closed by hand this once: `ci.yml` and `security-findings.yml` were dispatched on `main` and `GATE PROVEN` re-read for `b0185e76…` *before* the fleet moved. 🪤 **Same family as S131/S132/S134** — work reaching `main` without the gate — which the push-triggered gate was believed to have closed. 🪤 **Related, pointing the other way:** PR **#83** (`openai` 2.49.0 → **3.5.0**) is open with CI **passing on its branch and failing merged-with-main**; auto-merge correctly held it, but that is the same never-gated-as-a-union asymmetry 🟩 **CLOSED 2026-09-01 by prevention, not detection ([DL-142](design-log.md)).** The hole was never a skipped check — *the branch-is-the-gate rule gates **heads**, and a merge creates a tree no head ever had*. Two repo settings measured and flipped: `required_status_checks.strict` on `main` **`false` → `true`**, and repo `allow_update_branch` **`false` → `true`**. `strict` refuses a merge whose head is behind `main`, so the gated head *is* the merged tree — #82's 475-line lockfile union could not have landed without first being rebuilt on post-#81 `main` and passing CI on that union. `allow_update_branch` is the other half: without it a behind PR merely stalls, and a silently parked security update is its own failure mode. 🟩 **Proven, not assumed:** an independent re-read of the protection object showed **exactly one field changed** (contexts, `enforce_admins`, force-push and deletion settings all identical), and both open PRs (#80, #83) flipped to `mergeStateStatus=BEHIND` where they had been mergeable. 🪤 **It binds Dependabot without touching the operator:** `enforce_admins` is `false`, so local `git merge` + `git push origin main` is unaffected, while `GITHUB_TOKEN` is not an admin and *is* bound. 🪤 **Merge queue was the road not taken** — the right answer at scale, rejected as heavier than a one-developer repo on a monthly grouped Dependabot schedule needs; reopen if `strict` ever churns. 🟠 **#83 is still open and still correctly held** (an `openai` major, human review by design) | **closed — settings only** |
| 6b | 🟩 ~~**UNBLOCKED 2026-09-04 — the posture stays `advisory`, but the thing it waited on has merged**~~ **DONE — flipped to `binding` 2026-09-20 (operator)** | *[decided 2026-08-30, [DL-134](design-log.md)]* 🪤 **This row's original framing was wrong and is superseded.** Read in the code: `drop_vetoed` runs **before** `apply_deliberation_posture`, so an **arrived veto binds identically under both postures**. `binding` bites only on `proceeded_unvetoed` — *no `DeliberationRun` at all* after the grace — and on acceptance, where it adds `debate_coverage >= 1.0` and `failed_open_count <= 0`. Every degraded night of the outage was `applied_failed_open`, so **`binding` would have blocked zero orders**; it would only have turned each night red. So this was never "the veto is non-binding by default" — DL-116's grace already binds it, and DL-119's 73 % is a rate over arrived verdicts that this switch does not move. 🟠 **FLIP CONDITION MET 1 OF 3 — measured 2026-09-01.** `deliberation_posture=advisory` landed ✅; `real_debate_count` was **0**; and `failed_open_count == 0` only **vacuously**, because no debate was ever attempted. PM approved nothing, so the run **spent itself as the instrument and measured nothing** about the master/Key Vault path, which is *still* untested since 2026-08-19. 🚨 **The posture stays `advisory` — now for want of evidence, not by choice.** 🟩 **THE CREDENTIAL HALF IS NOW ANSWERED DIRECTLY, 2026-09-01.** DL-134 said clause 3 existed *"to prove the master/Key Vault path"*; S188's in-fleet credential tests show **all three deliberators passing `anthropic` through master's handover**, which proves that path without needing a debate to happen. 🪤 What remains is only **debate mechanics** (messages API, effort, timeouts, reassembly), and the instrument for that is **item 3's K=4 run**, not another scheduled night — a quiet market can withhold a real debate indefinitely 🟩 **EVIDENCE ARRIVED — re-measured 2026-09-15.** Clauses 2 and 3 are now met **non-vacuously**: six runs since 2026-09-02 carry `real_debate_count=2` with `failed_open_count=0` **and** `orphaned_reply_count=0` (`2026-09-02, -03, -04`×3, `-14`), so the debate mechanics this row was waiting on — messages API, reassembly, timeouts — are proven **without** needing item 3's K=4 run. 🚨 **But the posture question is now moot in a direction nobody predicted:** `advisory` already blocks **100 %** of reviewed orders (item 59), so flipping to `binding` would change nothing about what trades and only change what turns red. **Settle item 59 first; this row cannot be decided before it.** 🟩 **DECIDED AND BUILT 2026-09-20 — the posture is now `binding` ([DL-185](design-log.md)).** The stated blocker cleared: item **59** closed 2026-09-16 into 60/62/63 + 65, all since closed, so the question was decidable for the first time. 🎯 **Measured on the spine before deciding, not argued from the row:** `proceeded_unvetoed` — the *only* state in which binding blocks a buy — has occurred **3 times ever** (08-08, 08-10, 08-19) and **not once in the 32 days since**, so the last five runs are unaffected; while `failed_open` last fired **2026-09-11**, ending a four-night cluster (09-08→09-11) with `real_debate_count=0` in which **5 orders reached the broker with no referee review and nothing on the board said so**. Under binding those nights read **error**, not warning. 🚨 **The blast radius was larger than the spine showed, and only the gate found it:** flipping the default turned **7 tests red across 5 files** — six were fixtures silently relying on `advisory`, but the seventh (`test_no_llm_means_no_veto_stage`) encodes a policy binding deliberately inverts, so **any path with no deliberator wired now buys nothing**. That is binding working as designed and a sharper claim than “3 occurrences” implies; the fleet shows only 3 because its deliberators are present and credential-tested (S188). Each test now **pins the posture it means** instead of inheriting an unstated default, and the two fail-open tests collapse into one parametrized test proving warning under `advisory` and error under `binding`. `make ci` exit 0 — **2939 passed, 100.00 %**. 🟢 **DEPLOYED `s219a` 2026-09-20 20:31 AEST — image-only retag, path proven not assumed.** All **three** injected packs were hashed against the deployed `s219` commit (`25bd053`) first and are **byte-identical** — vocabulary `579a40a3…`, credential tests `97f083bf…`, issuer map `2ed1f41c…` — so a retag was correct and the S202 inert-pack trap does not apply; all three were then **decoded back off the running fleet** and hash-matched to the repo. Built from the gate-proven merge `ffa7467` via tag `v0.102.00` (run `35504842360`, **15/15** images), never from `latest`, which a push build publishes and which is mutable. **16/16 apps on `:s219a`**, all `Succeeded`, each with exactly **one** active revision, Healthy; `dispatcher-cron` on `:s219a` with cron `*/10 22-23 * * 1-5` intact. Scale, KEDA rule names and per-app env-var counts diffed **byte-identical** to the pre-deploy baseline on all 16 — master kept `daily-master-window`, the other 15 `daily-agent-window`. `DeployRecord deploy:2026-09-20T10:31:14…:s219a:ffa7467…`. 🪰 **Second instance of the S206 transient-revision read:** `deliberator-opponent` reported **0 active revisions** mid-sweep and re-read as one Healthy revision on `:s219a`; a one-shot read would have called this a partial deploy. 🟠 **Still owed:** no run has yet executed under `binding` — `sched-2026-09-21` is the first.| **closed — decided, built, gate-green and deployed `s219a`** |
| 36 | 🟩 ~~**Master hands over credentials it has never tested — DL-36 Piece A runs zero tests in the fleet**~~ **DONE** (both halves proven, 2026-09-01) | *[measured 2026-08-30, [DL-134](design-log.md)]* `MasterAgent` declares `credential_tests: tuple[CredentialTest, ...] = ()` (`agents/master/agent.py:57`) and `entrypoint.py:90-96` constructs it **without that argument**; no pack supplies any. So activation succeeding proves nothing about any credential it hands over. 🚨 **The remediation chain is unreachable for the same reason** — `handle_activation_remediation` fires only inside `if failures:`, and `failures` can only be non-empty when tests exist, so DL-36's entire bounded self-healing arc is dead code downstream of one empty tuple. **This is the check that would have caught the 2026-08-19 outage at activation instead of six nights later**, and its absence is why item 6b's flip has to be decided by a whole scheduled run. 🪤 **The obvious fix is the rejected one:** `orchestration/master_serve.py` already accepts the tests and 8 working probes already exist in the pack, but the master image copies only `kernel/ contracts/ agents/master/`, `CMD` is `agents.master.entrypoint`, and import-linter forbids `agents → orchestration` — the image stays pack-agnostic (S86 / DL-12), so tests must arrive as **data**, like the grant policy and secret map already do. 🟩 **SHIPPED — [S188](sprints/sprint-188-a-credential-is-tested-before-it-is-handed-over.md) merged `108475c` (`0.93.00`), 2026-08-30.** `GATE PROVEN` for the merged SHA, `make ci` re-run by me: **2410 passed / 100.00 %**; post-merge **CodeQL success, 0 open error-level alerts**. Master laws **v1.2** (5 new `MST-*` clauses). 🚨 **A merge-review correction was needed:** the sprint required the *fallback* OHLCV credential and left the *primary* optional, on the strength of a sentence ADR-0006's own amendment supersedes — flipped, and pinned by a test ([DL-136](design-log.md) amendment). 🟢 **DEPLOYED `s190` 2026-09-01** by full `up` (the vocabulary pack had moved, so an image retag was never an option). Every `required=True` probe in `orchestration/packs/trading_credential_tests.json` — `alpaca-data`, `alpaca-broker` and `anthropic` ×4 — was verified green from this machine first. 🟩 **PROVEN IN THE FLEET 2026-09-01** — the fleet was scaled up outside the KEDA window and **15/15 agents activated `state=active` with the tests actually running**: provider 4/4 (`tiingo`, `finnhub`, `fmp`, `alpaca-data`), execution 1/1 (`alpaca-broker`), operator 1/1 and each deliberator 2/2 (`anthropic`, `openai`); **0 failed, 0 transport-failed, 0 `Escalation`, 0 `RemediationPlan`**. That is the empty-tuple defect closed where it mattered — in the fleet, not just in code. 🟠 **Only the refusal half is still owed**, and 🪤 no green-credential night can supply it: it needs an activation that actually refuses, so it wants a deliberately broken credential in a disposable run 🟩 **THE REFUSAL HALF IS NOW PROVEN TOO — 2026-09-01, [DL-144](design-log.md).** A deliberately broken credential, with a **control arm**, on real master code, real pack data, a real HTTPS probe to Alpaca and the live spine. `execution` was the only single-variable target in the pack (two secrets, one probe, `required=True`). **Broken arm:** `ActivationRefused — credential test(s) failed for 'execution': ['alpaca-broker']`, `Escalation` **0 → 1** carrying `failed_credentials=('alpaca-broker',)`, and **`AgentInstance` did not move (2981 → 2981)** — the refused agent gets no instance, which is the property the rest of the fleet reads. **Control arm**, identical wiring and the real credential: **ACTIVATED**, `AgentInstance` 2981 → 2982. One variable, opposite outcomes. 🪤 **Named seam:** not inside a fleet container — proving it there means corrupting a live Key Vault secret whose value **cannot be read back to restore it**, so it was refused as unsafe rather than quietly skipped. 🟩 Torn down by explicit label+key (4 nodes), all five watched labels asserted back to recorded pre-test values | **closed — both halves proven** |
| 27 | 🟠 ~~**A position filled between runs is unprotected until the run after**~~ **DONE + DEPLOYED** (`S182`, `0.90.16`, `s182`, 2026-08-20) — [DL-118](design-log.md). 🪤 **Live proof still owed — and 2026-08-21 was checked and did NOT supply it.** The run looked like the case: INTC/NEE/XOM filled between runs, raised `missing_graph_position` divergence flags at run start, and execution placed three protective stops on them at 22:40. **But each stop carries `stop_pct_source=position`**, i.e. it came from a `Position` that `position_sync` had already created at 22:31:52 — **eight minutes before execution ran** — so the Fill+OrderIntent fallback never fired. 🚨 **Do not re-check it this way.** Broker reconciliation now adopts holdings at run start, which means it *closes the window* S182 was built for; the proof needs a fill that lands **after** `position_sync` in the same run, or the execution log naming the derivation path | At 22:30 on 2026-08-19 the fleet raised three `error` faults — *"unprotected held position CSCO qty=9: no active graph position"* (also MO, NFLX) — yet by the end of that same run all three **were** active `Position` nodes. So `place_broker_stops` runs **before** reconciliation has adopted the run's newly-filled positions, and the stop can only be placed on the *following* run. **Measured consequence right now: 22 positions, 19 stops, $2,147.76 unprotected.** 🪤 **A second, independent blocker was stacked on it** — Alpaca rejected two stop submissions with `403 potential wash trade detected... opposite side order exists`, because open buy limits from a test run existed on the same symbols. **Test orders can block protective stops.** That half is cleared (orders cancelled 2026-08-20). 🚨 Same class as S146's unprotected-ABT incident. **Cause found on packaging and it is structural, not an ordering slip:** `Position` is created by the **monitor (stage 7)**, stops are placed by **execution (stage 6)**. 🪤 `reconcile_run_start` never creates a `Position` — I assumed it adopted holdings and it does not. 🚨 Ownership is a **law question**: the monitor's `laws.md` declares `Position` in `labels_owned` | 1 sprint + possible law cycle |
| 33 | 🟠 **57 PARAM/settings divergences are accepted debt, and only the drift register knows** | *[measured 2026-08-30 by S187, [DL-133](design-log.md) decision 3, [DRIFT-052](laws/drift-register.md)]* S187's first all-agent reconciliation measured **60** name-presence divergences between PARAM tables and settings fields — **20× the four instances the sprint was scoped around**, spread over nine agents (analyst 22, provider 14, master 8, forecaster 7, researcher 3, execution 3, scanner 1, deliberator 1, PM 1). 3 were fixed; **57 are baselined warning-only** in `scripts/param_law_sync_baseline.py`, printed as `[WARN]` with `file:line` on every `make ci`, with unbaselined drift failing the gate. 🟢 **The ratchet was the right call** — a naive hard fail would have turned a small sprint into a nine-agent law cleanup. 🪤 **But the retire trigger points at nothing ranked**, which is why this row exists: DRIFT-052 is visible only to someone reading the drift register. 🚨 Clearing it means putting secret *names* into PARAM tables as `NO (secret)` — that is the contract the check enforces, and it is a decision, not a typing exercise. 🪤 57 `[WARN]` lines every run is the same cries-wolf shape as item 20's 228 faults, bounded and decreasing by design 🪰 **Re-measured 2026-09-15: `LEGACY_BASELINE` is still exactly 57 — unchanged, neither growing nor shrinking.** | 1 chore per 2–3 agents |
| 12 | 🟩 ~~**`pg_teardown`'s delete path has never run on live data**~~ **DONE — exercised four times on the live spine, 2026-09-13** (the row's size cell already said *closed*; the title had not been struck through — drift fixed here) | *[measured 2026-09-13]* `--run-id verify-2026-09-13-s200` deleted **171 edges / 146 nodes** with **26 protected rows kept**, then `--prefix` deleted **1 node each** on three separate orphans. First live use was actually S190 on 2026-09-01 (`--run-id`, recorded in `functionality-checks.md`), so this row outlived its own claim by twelve days. 🚨 **And running it for real is what found item 52** — the path works, but its *self-check* did not, which no amount of not-running-it would ever have revealed. *Item 12's Fill half moved into item 32 and closed with S190* | **closed — proven by use, not by inspection** |
| 21 | 🟩 ~~**`record_deploy.py` trusts the SHA it is handed**~~ **DONE** ([S180](sprints/sprint-180-a-deploy-record-must-name-the-commit-that-was-built.md), `0.94.07`, merged `fc2c799`, 2026-09-04) | *[measured 2026-08-18, closed 2026-09-04]* Recording `s179` with `--git-sha $(git rev-parse HEAD)` wrote a commit that was never built. `projections_currency.py:61` compares that field to the newest main image build, so the dashboard read the fleet **"behind" while current**. 🚨 **Hand-avoided twice on 2026-08-19** — both the `s181` deploy and its record required the build’s own head SHA to be pinned by hand. That workaround is retired: `record_verified_deploy()` refuses any SHA that is not the head of a successful `build-images.yml` run **for the tag being recorded**, and fails closed when GitHub is unreadable. 🪤 **Tag identity, not SHA existence, was the real bar** — the first cut (`0.94.06`) proved only *“this SHA was built”*, so recording `s193` against the commit that built `s194` still passed | small |
| 9 | 🟩 ~~**Verdict-quality gate**~~ **CLOSED — Part A shipped, Part B declined on price** ([S173](sprints/sprint-173-a-verdict-must-be-reproducible.md), `0.97.00`) | *[measured 2026-09-05]* **Part A merged and proven:** the harness replays recorded debates as five dependent batch rounds, the metrics carry their denominators and a Wilson interval, and the warn-only gate runs. 🟩 **Validated against ground truth** — it re-derives DL-104's hand-computed **9 of 16** exactly, and the interval it adds, **`ci95=[33.18%, 76.90%]`**, contains both 50 % and 75 %, so the 56 % cited in three decisions distinguishes nothing. 🚨 **Part B ran one round and stopped:** round 1 (2,957 succeeded) cost **$58.05** against a quoted **$0.0061/call**, measured at **$0.0196** — the price was 3.2x low because it came off the ledger's word counts (**item 43**), which exclude the system prompt, and the system prompts are 434 / 6,764 / 6,341 chars for defender / challenger / judge. **Operator decision 2026-09-05: stop, spend nothing more** ([DL-160](design-log.md)) — a decision against a known price, not a deferral. The `effort`/`max_rounds` question stays deliberately unmeasured. | **closed** |
| 7 | 🟩 **DONE — S170 / DL-101 — one LLM adapter in the kernel** | *[carried]* **The only dated item: 2026-09-01.** The port and `LLMCall` ledger are kernel; the vendor adapters and factory are not, and are duplicated, so S168's OpenAI fallback reached only the deliberator. 🪤 **Would not have prevented the 2026-08-19 outage, nor the 2026-08-20 one** — a provider switch only helps if some provider has credit, and **both OpenAI and Anthropic ran dry inside three days** ([DL-125](design-log.md)). Do not re-justify this item on either outage. 🪤 Its 2026-09-01 date is about the Anthropic *usage cap*; the *credit* outage is a separate, earlier constraint clearing 2026-08-30. Also stops `surfaces/dashboard/chat_binding.py` reaching into an agent's adapter 🪰 **Re-measured 2026-09-15: unchanged** — two adapter copies still exist (`agents/deliberator/llm_anthropic.py`, `agents/operator/llm_anthropic.py`) and `surfaces/dashboard/chat_binding.py:13` still imports the operator's adapter directly. | 🟩 **DONE — BUILT AND MERGED as [S222](sprints/sprint-222-one-llm-adapter-lives-in-the-kernel.md)**, merged `7d5528e` (`0.103.02`, tag `v0.103.02`), 2026-09-21. `GATE PROVEN` for `7cea9de` on CI, CodeQL and Security Findings, read from the worktree at that SHA. The adapters and factory are in `kernel/`, the four agent-side files are deleted, and `surfaces/dashboard/chat_binding.py` imports from `kernel`. **Zero stale references remain in any `.py`, `.toml`, `.yml`, `.json`, Dockerfile or `.ps1`.** 🎯 **Three gray clauses were proven on the way through** — `DLIB-SEC-02`, `OPR-SEC-01`, `OPR-DEP-01` — because the sprint had its hands on the only code that could prove them; rollups moved to deliberator **23/56** and operator **18/50**. 🔴 **Two planner findings, both fixed before merge:** the key-containment test folded `caplog.text` into its evidence but this repo sets no pytest `log_level`, so a bare caplog sees WARNING and above only — a key logged at DEBUG passed it; and the handback claimed no `.env`, which its own `4 skipped` (against S220 and S221's `6`) disproved ([DL-193](design-log.md)). *Originally specced 2026-09-21 — a **refactor**, not a capability: the adapters and factory move to `kernel/`, the four agent-side files are deleted, and `surfaces/dashboard/chat_binding.py` stops importing an agent's internals. 🪰 **Re-measured, and the row's framing was wrong:** the two “duplicate” adapters are **not** duplicates — the deliberator's (**129** lines) adapts free-text completions with S189 stop-reason handling, the operator's (**103**) adapts **tool-use** responses and parses JSON, so deleting one breaks the operator. Giving the operator a provider choice is held out of scope: `OPR-DEP-01` names Anthropic as its sole external call, so it owes a law cycle.* |
| 22 | 🟠 **A sprint's built/unbuilt state is not machine-checkable** | *[carried]* Closeout placeholders use at least three wordings, so nothing answers "is this spec built?". Cost a wasted handover when I recommended S171 after it had already shipped. **Fix:** one required line (`**Status:** SPEC \| BUILT`) plus a check reconciling it against the INDEX. 🟢 Docs-only; the cost is wasted handovers and wrong ranking 🪰 **Re-measured 2026-09-15 and it is worse than "three wordings":** **12 distinct `**Status:**` forms** across the spec corpus (16 `ready for handover`, 16 `BUILT`, 15 `shipped (…)`, 10 `planned`, 8 `shipped`, 7 `MERGED`, 5 `queued · Phase …`, 2 `SPEC`, …) and **8 specs carry no `**Status:**` line at all**. 🚨 **RE-MEASURED 2026-09-21 and it is worse again — the numbers above count leading tokens, not lines.** The corpus is **215** sprint/chore docs; the full `**Status:**` lines take roughly **150 distinct forms**, and **19** docs carry no such line. 🎯 **The risk that re-ranks this row:** most of those lines *carry evidence* — e.g. ``MERGED `7d3ff51` (`0.98.08`) · DEPLOYED `s211` · LIVE CHECK DISCHARGED``. Collapsing them to a token **destroys** it, and deriving a token from freeform prose risks writing a **false** status, which is the exact harm this row exists to prevent. So the migration must preserve the prose verbatim beside the token and refuse to guess, and ambiguous docs must be flagged rather than mapped. **No longer a chore:** M/L, with a silent-corruption failure mode. | M/L — a data migration over 215 docs, not a chore |
| 10 | 🟩 ~~**S157 / hardening row O — 101 law clauses with no test-plan row**~~ **DONE** ([S204](sprints/sprint-204-every-clause-declares-how-it-is-proven.md), `0.98.01`, merged `24d61f0`, 2026-09-14) | *[carried]* 101 law clauses with no test-plan row; assertion E in `check_law_coverage.py` is warn-only and cannot be promoted until the rows exist 📦 **SPECCED as [S204](sprints/sprint-204-every-clause-declares-how-it-is-proven.md)** (2026-09-14) — re-measured: **101 confirmed**, but **5 families are 62 %** and it is about **rows, not tests**, which is what makes it tractable 🟩 **CLOSED:** every target clause has a proof row, the 🧱/📜 row kinds are documented and checked, and assertion E in `check_law_coverage.py` is **promoted from warn-only to enforcing** — watched pass as warn-only on a planted `RPT-PERF-99`, then fail after promotion on the same plant. A rowless clause is now a gate failure. | 1 sprint |
| 30 | 🟩 ~~**15 law clauses say "matches the contract file exactly" — which is unfalsifiable**~~ **DONE** ([S205](sprints/sprint-205-a-type-clause-names-the-fields-it-requires.md), `0.98.03`, merged `af75079`, 2026-09-15) | *[measured 2026-08-20, [DL-121](design-log.md)]* The `laws → test-plan → tests` loop is CI-enforced; **contracts are outside it**. Zero of the 24 files in `contracts/` cite a clause ID, and the only link is 16 clauses asserting *"X matches `contracts/<agent>.py` exactly"* — so the file is both the claim and the oracle, and a contract can change with every test still green. 🚨 **Not theoretical:** `PM-NEV-09` turned out to be unexpressible in `GateOutcome` (`passed: bool`, two states), and the scanner's `FilterVerdict` collapses *"did not run"* and *"passed"* into the same bytes — **that is the S183 bug**, in a second agent, which neither law book prevented. `PM-TYP-03` is rewritten (v1.3); **15 remain**. Each rewrite enumerates the required fields and will demote greens — the conventions §7a trade, accepted knowingly 🪤 **Considered for [S204](sprints/sprint-204-every-clause-declares-how-it-is-proven.md) and deliberately excluded** (2026-09-14): each of the 15 rewrites is a **full law cycle** that demotes greens under conventions §7a, and 101 rows plus 15 law cycles is an XL sprint whose predictable failure is a *rushed* law cycle. Own sprint, after S204 📦 **SPECCED as [S205](sprints/sprint-205-a-type-clause-names-the-fields-it-requires.md)**, 2026-09-14, and the population was **re-measured rather than carried**: **12** clauses name a `contracts/` file as their own oracle (11 *"match … exactly"*, `MST-TYP-01` *"are declared in"*) plus **3** of a second, partly-falsifiable shape — which reconciles this row's "15", with `PM-TYP-03` the 16th, already rewritten by S184 and now the pattern to copy. 🚨 **4 of the 12 are green today on `*_is_deserializable` round-trip tests**, which stay green when a field is deleted from both sides, so the sprint expects **demotions** and says so up front. 🟩 **CLOSED:** the twelve file-as-oracle `TYP` clauses now **enumerate the payload fields they require**, each traceable to the clause that needs it, and the two guard modules `tests/test_contract_required_fields.py` + `tests/test_contract_required_payload_fields.py` go **red when a required field is deleted** — proven on three agents: Scanner `skipped_filters`, Master `capability_declaration`, Researcher `backtest`. The three serialization-shape clauses stayed in their family, as specced. Zero `contracts/` edits (`git diff --stat -- contracts` empty). [DRIFT-047](laws/drift-register.md) is **CORRECTED**. 🪤 **Two forced decisions surfaced and are owned by the drift register rather than by new queue rows:** **DRIFT-060** — nothing requires `contracts.<agent>.CONTRACT.version` to move when required fields change; **DRIFT-061** — five execution output clauses name fields `contracts.execution` does not carry. | 1 chore per 3–4 agents |
| 31 | 🟩 ~~**A green gate does not mean CodeQL-clean — three mechanisms now, and the newest is self-inflicted**~~ **DONE** | *[measured 2026-08-20, [DL-123](design-log.md)]* `codeql.yml` runs **only on `main`**, so a branch can be `GATE PROVEN` on CI + Security Findings while the analysis that finds a whole defect class has not run. 🚨 **Twice in four days:** `py/mismatched-multiple-assignment` #177 (S181-era, [DL-110](design-log.md)) and **#187** raised by the S184 merge — same rule, same package, same one-line shape (a fixed-arity unpack of a call that can return `()`). Each cost a red gate on the *next* branch and a merge-then-verify. **A trap that fires twice is a missing check.** 🚨 **THIRD MECHANISM, measured 2026-08-30: on `main`, a commit's CodeQL run is **cancelled by the next push**, so a code merge followed by any docs commit is never analysed at its own SHA.** `codeql.yml` sets `concurrency: group: codeql-${{ github.ref }}` with `cancel-in-progress: true`, and every `main` push shares that ref, so only the **last** commit of a burst is analysed. **Measured twice in one afternoon, both self-inflicted by my own docs pushes:** S187's merge `7d36771` → cancelled by `bbbab8e`, and `5629b18` → cancelled by `f892209`. 🪤 **Nothing catches it.** `make gate-ran` requires only `("CI", "Security Findings")` (`scripts/assert_gate_ran.py:46`), and `cancelled` is neither `success` nor `failure`, so a glance at `gh run list` reads as “not red”. 🟢 Both merges today were *confirmed* clean by analysing the following commit, which contains the same code — but that was me checking by hand, which is the whole complaint. **Two candidate fixes, both cheap:** drop `cancel-in-progress` from `codeql.yml` (costs redundant scan minutes, nothing else), or add a main-only post-merge assertion that `HEAD` has a *completed success* CodeQL — it cannot go in `REQUIRED_WORKFLOWS` wholesale, because CodeQL never runs on branches and every branch proof would then fail. Options: run CodeQL on branches (obvious, cost unmeasured — scan minutes plus the branch-vs-main alert-state semantics DL-110 already found confusing), or a cheap local guard in `make ci` for this shape with a `gate_selftest` case 🪰 **Re-measured 2026-09-15: unchanged** — `codeql.yml` still triggers on `push: branches: [main]` only with `cancel-in-progress: true`, and `REQUIRED_WORKFLOWS` is still `("CI", "Security Findings")`. 🟩 **THIRD MECHANISM CLOSED 2026-09-17 — merged `6a67924`, gate proven for `c24bcfd`.** Re-measured first: **15 of the last 200** `main` CodeQL runs were `cancelled`, including code merges `dc79d05`, `71bc69f` and `7d36771`; median scan **153 s** (n=28), free on a public repo. `codeql.yml` now groups on `${{ github.ref }}-${{ github.sha }}`. 🪤 **`cancel-in-progress: false` alone would not have fixed it:** GitHub keeps one *pending* run per group and drops the rest, so the middle push of a burst would still go unscanned. The file's committed endings were also `i/mixed` and were normalised to LF in a separate, content-free commit (`3144a74`). 🟠 **Still open — the first mechanism:** CodeQL still does not run on branch pushes and `REQUIRED_WORKFLOWS` still omits it, so a branch can be `GATE PROVEN` without CodeQL. Deferred deliberately while S211 is in flight: enabling branch scans feeds branch alerts into the Security Findings gate Codex's push will be judged by, and enforcing it edits `scripts/assert_gate_ran.py` (Python, a version bump that would collide with S211's). ➕ **Same defect in `ci.yml` (2026-09-17):** it keeps `group: ci-${{ github.ref }}` with `cancel-in-progress: true`, so a docs push to `main` cancels CI on the commit before it (6 of the last 100 CI runs were cancelled). A cancelled run on a merge commit leaves `make gate-ran` with nothing to prove. Same one-line fix as `codeql.yml` (`6a67924`). S211 has now merged, so this and the branch-scan half are unblocked. 🎯 **MEASURED 2026-09-20 — the cancel group fired on a real merge, first recorded instance.** S218's merge commit `69f3601` has four runs: `Security Findings` success, `CodeQL` success, `Build and push agent images` success, and **`CI` `cancelled`** — superseded by the docs push `049ba55` about two minutes later. Consequence: **a merge SHA can sit on `main` permanently un-CI'd**, and `make gate-ran` run from `main` afterwards proves only whatever the tip has since become. Caught here only because the gate watcher printed a SHA that differed from the merge it was started for; the STATE row had already claimed the merge was proven. 🪰 **This makes the row's cost concrete:** the hazard is no longer only "a branch can be green while CodeQL never ran", it is also "a commit on `main` can have no CI result at all". 🟩 **CLOSED 2026-09-20 — both remaining halves, merged `4cbf468` (chore `4fbefee`, `GATE PROVEN`).** 🎯 **The `ci.yml` cancel group was the same defect as `codeql.yml`'s, left in the sibling file:** measured **17 of the last 200 `main` CI runs cancelled**, **four of them merges** — `69f3601`, `7d3ff51`, `dc79d05` and `6a67924`, and that last one is *the merge of the chore that fixed this in `codeql.yml`*. It bit for real the same morning: S218's merge was claimed `GATE PROVEN` here and its CI was `cancelled`. Fixed with the per-SHA group the sibling already proved. 🟩 **Branch scanning shipped, and the cost objection this row carried since August was never true:** the repo is **public**, so Actions minutes are free and a scan is ~153 s. Proven by observation, not by reading YAML — `4fbefee` is the first branch gate in this repo whose proof reads `CI: success  CodeQL: success  Security Findings: success`. 🪰 **Carried forward, measured while proving it:** `Security Findings` concluded `success` while `CodeQL` was still `in_progress`, so it does **not** wait for the branch scan. That race is why **CodeQL is deliberately not in `REQUIRED_WORKFLOWS`** — requiring its presence would not fix the ordering, and would couple the gate to branch-scoped alert semantics DL-110 found confusing. The reasoning is recorded at the constant in `assert_gate_ran.py`. Revisit after a few branches have been observed. | 🟩 **CLOSED — all three mechanisms.** Marker reconciled 2026-09-22: the row's own body recorded both remaining halves merged (`4cbf468`) on 2026-09-20 while the marker and this column still read open — work-queue item **22**'s complaint, again. Re-measured here rather than taken from the row: `REQUIRED_WORKFLOWS = ("CI", "Security Findings")` at `assert_gate_ran.py:46`, with CodeQL deliberately not *required* but still **judged if present** — confirmed live on `663a89a`, whose `GATE PROVEN` output lists `CodeQL: success` beside the two required ones. The carried-forward note (Security Findings does not wait for the branch scan) is a recorded design decision at that constant, not open work. |
| 11 | 🟩 ~~**Hardening row N — delegated-agent sandbox default**~~ **DONE 2026-09-19** | *[carried]* `~/.codex/config.toml` carries `approval_policy = "never"` and `sandbox_mode = "danger-full-access"`. Dormant: every delegated run has overridden it by hand, which is why it has never bitten. **The default is the defect, not the practice** 🪰 **Re-measured 2026-09-15: unchanged** — `~/.codex/config.toml` still carries `approval_policy = "never"` and `sandbox_mode = "danger-full-access"`. | 🟩 **done** — `~/.codex/config.toml` now `approval_policy = "on-request"`, `sandbox_mode = "workspace-write"` (re-read and TOML-parsed after the edit; backup `config.toml.bak-2026-09-19`). Operator: *"do what needs to be done"* 🪤 **Measured the same day with `codex sandbox windows` (codex-cli 0.130, `[windows] sandbox = "unelevated"`), in a throwaway worktree:** write inside the workspace ✅; write outside it **denied**; `git commit` from a worktree ✅, though a pre-commit `sh` crashed with Win32 error 5 on the way, so **hooks may not run in the sandbox** and the local gate cannot be trusted from inside it; **network blocked**, even with `-c sandbox_workspace_write.network_access=true`. So a sandboxed Codex **cannot push, run `make gate-ran`, pass `pip-audit`, or touch Azure/Alpaca**. Evidence still travels as committed files in the worktree, but every networked step needs an approved escalation (`on-request` asks) or runs on the planner side. (The CLI also rejects the desktop-written `service_tier = "default"`; override with `-c service_tier="flex"`.) |
| 71 | 🅿️ **PARKED — the regime-probability track: derive the quantity, then compare it with probability** | *[measured 2026-09-17, [EXP-011](research/experiments/EXP-011-regime-markov-and-barrier-calibration.md); operator: "leave it queued, for after the debt settles and we are ready to implement"]* The idea: model regime transitions as a Markov chain and give every trade a probability of hitting its stop first, its target first, or neither, then keep checking those probabilities against what actually happened. **EXP-011 already measured where to start:** (1) **a ledger of declared vs realised probabilities**, beginning with a claim the system already makes: S211's median target should be reached within 10 sessions about half the time (historically **50.2 %**). (2) **A sticky regime label** (switch only after 3 consecutive closes in the new band). The raw daily label is not Markov (37 switches a year, G-test p≈10⁻¹³⁰); the sticky one is (p = 0.30). A provider label-design ADR, after S213 gives the regime a real VIX. (3) **Barrier simulation only if a decision needs it.** A regime-weighted bootstrap added ~1 % Brier skill over base rates, its P(stop first) was over-dispersed, and regime weighting pointed the wrong way after VIX spikes; a model that keeps volatility clustering (block bootstrap or GARCH) would be the next candidate. Home: the forecaster (advisory until its scorecard earns promotion). Origin of the design: a pre-trade forward-simulation layer with calibration measurement, carried in the operator's earlier architecture notes. | **parked** until the debt settles — then an ADR, then (1) as the first sprint |

---

## Closed in this review

Removed from the table rather than left struck through. Detail lives in each sprint doc and the
design log; nothing here needs tracking any more.

| # | What | Closed by |
| --- | --- | --- |
| 32 | One liveness question, asked four ways — with items **20** and **12's Fill half** folded in | **Closed 2026-08-31 — [S190](sprints/sprint-190-one-liveness-question-one-answer.md) merged `193e71b` (`0.94.02`).** `contracts/broker_lifecycle.py` is the single place execution broker-fact liveness is asked: a `BrokerStopOrder` whose sibling `Fill` is terminal is no longer returned by `active_broker_stop_orders`, so **a fired stop stops counting as protection**; a resting-stop `Fill` is not an open order; and the sweep compares **live broker stop to live graph stop** rather than type-to-lifecycle. Six status vocabularies collapse to one module, `BrokerStopIdentityMismatch` carries structured context, and 🪤 `partial` stayed non-terminal so S176 is intact. Execution laws **v1.4** (`EXEC-OBS-05`), rollup **35 / 61**, `DRIFT-055` **CORRECTED**, `DL-139` amended. 🟩 **Gate re-proven by the reviewer** for `c68290734239c55fcb9ab974fea4eab0bcc0b6f9` from a worktree at that SHA; branch had one commit, nothing above it, based on `main`'s tip. 🚨 **LIVE PROOF ANSWERED 2026-09-02, AND IT IS NOT MET** ([DL-146](design-log.md)). The first scheduled run after the deploy, `sched-2026-09-01`, raised **2** `BrokerStopIdentityMismatch` faults, not zero — recorded as **verified failing** rather than dropped. 🪤 **The direction flipped, which is the useful part:** item 20's 304 faults were all `broker_stop=True graph_stop=False`; these two are the **mirror** — `broker_stop=False graph_stop=True`, a graph stop with no broker counterpart. The sweep read it correctly and **exempted** the order, so nothing was wrongly cancelled and the book is right (audit: graph and broker agree on **26 tickers**, `failures=1` and that one the known `probe-s164` residue). 🟩 S190 remains a large real improvement — **17 → 2**, and the 304-fault direction is gone — but it did not eliminate the class. Residue carried to **item 42**. 🟩 **CORRECTED, THEN MET — 2026-09-03** ([DL-147](design-log.md) corrects [DL-146](design-log.md)): those 2 faults were **transient, not defects** — the head-of-run sweep warned at `22:30:42` and the *same* run's refresh wrote `filled` at `22:30:58`, **16 s later**, so the warning fires once per stop that fired since the last run and self-corrects. The success factor as written is **met on the very next run**: `sched-2026-09-02` raised **zero** new faults of any kind (newest on the spine remain the two at `2026-09-01T22:30:42/47`), because no stop fired.<br>**Residue only, see 42.** |
| 34 | Deploy preflight did not check that its own steps can import their dependencies | **Closed 2026-08-31, merged `6b4463c` (`0.94.01`).** `Test-ServiceBusImports` runs the exact import `scripts/servicebus_prepare_routes.py` runs — measured both ways, real import → exit 0, missing module → exit 1 — and needs no credentials, so it reports a corrupt local environment even when every config row is red. Observed in place: `[OK] Service Bus route-prep imports (azure extra)`. `tests/test_deploy_script_invariants.py` pins **both sides**: if route prep changes what it imports, the test fails and preflight has to follow. `make ci` exit 0 (2419 passed, 100.00 %), `GATE PROVEN` for `a234c28641386de4c9667a989d7ad571cc878dc3`, post-merge CodeQL success |
| 20 | Stop-identity mismatch — the check compares a type against a lifecycle | **Folded into item 32** — it is one of the three faces of a single pattern, not a separate defect, and DL-131 already ruled that fixing them one at a time is the wrong move. Its numbers were re-measured on the way in: **304 faults, 17 distinct keys** (not 228 / 13), all still `broker_stop=True graph_stop=False`, and **0 of 304 carry any context** — the order key exists only inside the message string |
| 37 | Three CodeQL alerts temporarily baselined, with the un-baseline recorded only in prose | **Closed 2026-08-31, the hour it was filed.** Post-merge CodeQL on `934ffb5` completed **success** and marked `#189`/`#190`/`#191` **`fixed`** at `2026-08-31T02:25:05Z`; open error-level alerts are **0**. `security/findings-baseline.json` pruned **4 keys → 1**, back to the single accepted `github-dependabot` entry, with `fail_on_code_scanning_error` still `true`. 🪤 Pruned **after** verifying closure, never on assumption — [DL-138](design-log.md)'s stated cleanup, done rather than carried |
| 35 | An LLM answer that was cut off or declined was recorded as an empty answer | **S189, `0.94.00`, merged `934ffb5` 2026-08-31.** Adapters now raise `LLMCompletionStoppedError` instead of returning `""` — OpenAI on `finish_reason=length`, Anthropic on `max_tokens` and on `refusal` with its `stop_details.category`; `LLMCall.stop_reason` is recorded, `max_tokens`' ceiling went 4096 → **8192** (it was both default and ceiling), and a stopped judge returns `Verdict("revise", ...)` naming the provider cause instead of blaming the parser. 🟩 A `S189_PROMPT_SENTINEL` test proves no prompt or completion text reaches the exception, the fault, `DeliberationRun` or `LLMCall`. DLIB laws **v1.1** |
| 1 | A deploy must not discard operator config | S169, `0.90.10`, deployed `s176a` — the guard *refused* and named all three keys |
| 2 | The 42-bar history gap | S174, `0.90.07` — 202 bars live, SMA-200 computed in production for the first time |
| 4 | Veto context correctness (DL-104 a+b) | S175, `0.90.08`, deployed `s176a` |
| 5 | A partial fill can never upgrade | S176, `0.90.09`, deployed `s176a`. 🪤 No live proof possible — 0 of 188 Fills have ever been partial |
| 18 | The PM has no issuer or correlation dimension | **S184, `0.91.00`, deployed `s184`.** 🟩 **PM half proven live 2026-08-21, unattended**: GOOG and GOOGL hit the same batch, GOOG's approved $1025.19 counted against GOOGL as `issuer=alphabet`, and GOOGL's sizing gate read **1.67 % > 1 %, `outcome=failed`** — pre-S184 both would have passed at 1.00 % and 0.67 % and opened two positions in one company ([DL-122](design-log.md) amendment). 🚨 **The reason this item was promoted — ADR-0023's prediction that the 73 % veto rate falls — is NOT proven and cannot be measured until 2026-08-30** ([DL-125](design-log.md)). Tracked there, not here: the *work* is done |
| 28 | A scanner gate that never ran is indistinguishable from one that passed | **S183, `0.91.02`, merged `4187770` 2026-08-22**, `GATE PROVEN` for `948f1bd`. `Candidate` and `FilterVerdict` carry `skipped_filters`; no-earnings-date is attested, a known **past** date is an evaluated pass, thin beta history is attested, and the debate packet renders the stop's basis. 🪤 **The mid-build correction held** — `stop_target_mode` stayed a bare default, as the locked analyst law requires. 🚨 **My spec omitted the law cycle** although the sprint changed `contracts/scanner.py` under a LOCKED law book; closed at merge rather than filed — `SCAN-OUT-06`/`SCAN-OUT-07`, scanner laws **v1.1**, rollup **18 / 41**, and `DRIFT-047` for the `SCAN-TYP-01` clause the change slipped under (item 30's class). The omission is now a required section in [`_TEMPLATE.md`](sprints/_TEMPLATE.md) |
| 6 | A real advisory/binding switch (DL-104 d) | **S185, `0.92.00`, merged `5bea06d` 2026-08-22**, `GATE PROVEN` for `f9ac6b0`. `deliberation_posture` is a declared **mode selector** recorded on every `ExecutionRun`; fault severity and the acceptance check follow it. Law cycle done — three `EXEC-*` clauses, laws **v1.2**, rollup **33 / 60**, DRIFT-048/049 filed. 🟩 `advisory` did not become a free pass. 🪤 The operational half is now **item 6b** |
| 8 | The SCAN-OBS-01 shape sweep | 2026-08-14, DL-111 — 13 rows not 17; 5 demoted, 2 false in code (DRIFT-039/040) |
| 13 | Doc drift | `bf928ca` |
| 14 | Sentiment counts read as article counts | DL-112, `0.90.11`, deployed `s176b` |
| 15 | Broker↔graph divergence | **Not a defect** — graph active == broker 19/19; my claim was retracted |
| 16 | Debate context unit/scope sweep | S177, `0.90.12`, deployed `s177`, DL-113 |
| 17 | 45 unresolved critical Flags pin `healthy=false` | S178, `0.90.13` — severity follows persistence; **53 Flags now have 53 resolutions** |
| 19 | `open_incidents` is the only lock on `healthy` | S179, `0.90.14`, deployed `s179` |
| 23 | One canceled test order red-lights the fleet nightly | **S181, `0.90.15`, deployed `s181`, proven live** — sweep #1 wrote the ack and one fault, sweep #2 wrote neither |
| 24 | Nine orders reached the broker unvetoed | **Folded into item 3** — it is the cost of the serialization, not a separate defect. Mitigated 2026-08-19 by grace 900 → 1800; the fix is still S172 |
| 25 | The deliberator has no working LLM provider | **Folded into the standing note below** — it was an outage, not a work item. Operator restored credits 2026-08-19 |

---

## Standing operational notes

Not work items — conditions to know about.

**🚨 THE DELIBERATOR IS DOWN UNTIL 2026-08-30 — operator-stated, not estimated (2026-08-22).** The
Anthropic **credit balance** is exhausted (`HTTP 400 "Your credit balance is too low"`), first seen on
`sched-2026-08-20` and again on `sched-2026-08-21`, and it cannot be topped up before **2026-08-30**.
🪤 **This is a different constraint from the 2026-09-01 date** that appears elsewhere in this file —
that one is the Anthropic *usage cap*; this is *credit*, and it clears earlier. **Both providers have
run dry inside three days**: OpenAI `HTTP 429 "no credits remaining"` on 2026-08-19, Anthropic
`HTTP 400` on 2026-08-20. *[Superseded: "the operator restored credits; roughly ten runs of balance"
— measured 2026-08-19, false by the next evening. A 9-order run costs **$0.46**, so a balance
expressed in runs is not a durable claim; state the date instead.]*

**Consequences while it lasts, all of which are expected and none of which are defects.** Every
scheduled run fails acceptance on `debate_coverage: 0.0 < 1.0` and `failed_open_count > 0`; every
run submits its PM-approved orders **unvetoed**; `compute_health` returns `healthy=false` on the
three `deliberator-manager` error incidents each run raises. 🚨 **Do not diagnose these as new
faults, and do not fire test runs to investigate them** — they will reproduce exactly.
🪤 **An exhausted account presents as `no deliberator peer reply received`**, which reads exactly
like a timeout — it cost me a wrong diagnosis and a wasted run (DL-116 amendment). **Read
`DeliberationRun.failed_open_reason` before the latency metrics.**

**🪤 `healthy=false` is NOT the Flag backlog — that premise is stale.** Re-measured 2026-08-22 with
the real predicate (`agents/supervisor/domain/health.py::compute_health`) rather than raw props:
`pending_human_flags` = **0**, because all **60** Flags carry a `FlagResolution` (items 17 and 19
closed this via S178/S179). `Flag.status` stays `pending` **by design** — the resolution is a separate
node — exactly like `Position.status` staying `open`. What actually pins `healthy=false` is
`open_incidents`, today **5**: 3 deliberator billing faults + 2 execution (`cancel_stop` HTTP 422, and
the `DeliberationFailedOpenSubmit` fault). 🚨 **Anything that still ranks a critical-Flag sweep on
"it pins healthy" is citing a dead premise** — audit on the contract predicate, never on raw props.

**Pre-production, so test runs are allowed.** Widen the KEDA window or force `minReplicas 1`, fire a
run, restore. 🪤 **The drop sweep runs *first*, before anything else** — firing a run before the
13:30 UTC open cancels any pending order from a previous run. 🪤 A manual dispatch of
`sched-YYYY-MM-DD` **consumes that night's scheduled run** (DL-110); use a non-scheduled run id if
the schedule must survive.

---

## Two notes on the ordering

**Edited 2026-08-30, not re-reviewed.** Items **26** and **29** were deleted — S186 and S187 both
shipped, so the rows were closed (the sequence gap is the convention). Item **6b** was decided
([DL-134](design-log.md)) and item **3** unblocked, and **36** and **35** are new. Every other row's
numbers are still carried from the 2026-08-22 review and should be treated as suspect.

🎯 **The order, top to bottom: 3 → 36 → 35.** Item **3** (S172's K=4 measurement) is first — built,
gate-proven, and the only thing between it and merge is a measurement that is finally obtainable;
it is scheduled **2026-09-01**, after Monday's run. Item **36** (S188, credential tests) is second and
is the deeper fix: it is what removes a scheduled run as the only instrument for answering *"does the
fleet have working credentials"*, which is currently blocking item 6b as well. Item **35** follows it
by operator direction, and they pair naturally — both are *a value that looks like data but means
nothing happened*, one in the LLM adapters, one in the activation path. 🚨 **35 was re-measured on
2026-08-30 and is bigger than it was filed as** — the defect is in the *primary* provider's adapter,
not only the fallback's; packaged as [S189](sprints/sprint-189-an-empty-answer-says-why-it-is-empty.md).

🪤 **Item 6b sits second only because it is waiting on a date, not on work.** Its decision is made;
`sched-2026-08-31` either meets the flip condition or names the credential path as the finding.

**Item 18 is closed.** S184 shipped and its PM behaviour is proven live; what remains is a
*prediction* about the veto rate, which is evidence owed rather than work queued, and lives in
[DL-119](design-log.md)/[DL-125](design-log.md).

**Item 28 is closed, and it left a standing fix behind it.** S183 shipped, but its spec never asked
for the law cycle a `contracts/` change owes — caught only at merge. That is now a mandatory,
answer-before-you-code section in [`_TEMPLATE.md`](sprints/_TEMPLATE.md), which every new sprint is
copied from. 🪤 **Item 22 (a sprint's built/unbuilt state is not machine-checkable) is partly
addressed there too** — the template pins one `**Status:** SPEC | BUILT | MERGED` line — but the
reconciling check the row asks for is still unbuilt, so the row stays open.

**Ranked below the fixes on purpose:** items 9, 7, 22, 10 and 11 are all real, and none of them can
put a wrong order at the broker. Item 7 is the only one with a date (**2026-09-01**), which is why
it sits above the docs and law-coverage work rather than at the bottom. 🪤 **Item 9 (S173) is also
provider-blocked until 2026-08-30** — it needs the Batch API. **Items 28, 29, 26, 12, 20, 21, 22, 30,
31 and 11 are not**, and they are what the next six sessions can actually ship.
