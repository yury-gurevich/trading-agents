# Copy to `infra/neo4j/.env` (gitignored) — used only to interpolate the compose file.
# This must match NEO4J_PASSWORD in the project-root .env (the app's connection).
NEO4J_LOCAL_PASSWORD=change-me-local-dev
